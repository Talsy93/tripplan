# MyTrip — נקודת המשך לשיחה חדשה

> **איך משתמשים בקובץ הזה:** בתחילת שיחה חדשה, לבקש מ-Claude לקרוא את
> `PROJECT_PLAN.md`, `ARCHITECTURE.md` ואת הקובץ הזה. `PROJECT_PLAN.md` הוא מקור
> האמת; הקובץ הזה הוא תמונת מצב מתגלגלת ומתעדכן בסוף כל פאזה.

## מצב נכון ל-2026-08-29 (K8 — האייקון השחור)

**⚠️ האייקון באייפון לא יתעדכן לבד.** iOS קורא את האייקון בזמן ההתקנה. צריך
**להסיר את ה-PWA ולהתקין מחדש** — וזה מוחק את מנוי הפוש, אז אחרי ההתקנה מחדש
צריך להפעיל שוב ״תזכורות למכשיר״.

**מקור האייקון עבר ל-`assets/logo-source.png`.** כל הקבצים ב-`public/` נגזרים
ממנו על ידי `npm run icons`. **לא לערוך אותם ביד** — בדיוק זה מה שיצר את הבאג.

### מה היה

`public/apple-touch-icon.png` הוחלף ידנית בלוגו (commit `e635b12`), ונמדד:
1024×1024, 525KB, **65.4% מהפיקסלים לא אטומים**, פינות ב-`alpha=0`.

**iOS מרכיב אייקון שקוף על שחור.** זה כל הסיפור של הרקע השחור.

ובנוסף הלוגו כבר היה מעוגל, ו-iOS מחיל מסכה משלו — עיגול כפול, שמתועד בקובץ הזה
מפאזה J ונשכח.

**וכי רק הקובץ הזה הוחלף, היו שלושה לוגואים שונים באפליקציה בו-זמנית:** הלוגו
החדש במסך הבית של iOS, המטוס הישן ב-Android וב-PWA, וברירת המחדל של
`create-next-app` בלשונית הדפדפן (`src/app/favicon.ico`, מ-14 ביולי, מעולם לא
הוחלפה).

### הכלל לכל אייקון עתידי

| קובץ | חייב להיות | למה |
| ---- | ---------- | --- |
| `apple-touch-icon.png` | **אטום לחלוטין, בלי עיגול עצמי** | iOS ממלא שקיפות בשחור ומעגל בעצמו |
| `icon-maskable-512.png` | אטום, איור ב-80% המרכזיים | Android חותך לצורת המשגר |
| `icon-192/512.png` | מעוגל, שקיפות מחוץ לפינה — תקין | הדפדפן מציג as-is |
| `favicon-32.png` | אטום | לשונית יכולה להיות כהה |

**האימות הוא מדידה ולא עין:** ספירת פיקסלים לא-אטומים ודגימת RGB בארבע הפינות.
בשלב אחד נדמה היה לי שנשארו פינות כהות; המדידה הראתה `#2292d1` בכל הארבע והפיקסל
הכהה ביותר בתמונה כולה הוא `#02619c` — כחול מהאיור, לא שחור.

**גדלים:** `apple-touch-icon` ירד מ-525KB ל-15.5KB (הוא גם היה 1024×1024 בזמן
ש-`layout.tsx` הצהיר 180×180, כלומר כל מכשיר הוריד חצי מגהבייט וכיווץ אותו).

**ה-SVG-ים נמחקו.** `icon.svg` היה ראשון ברשימה ב-`layout.tsx`, ולכן הוא זה
שהדפדפן בחר — הוא צייר את המטוס הישן. המקור עכשיו ראסטר, ואין מה לשים ב-SVG
שיהיה נכון.

## מצב נכון ל-2026-08-28 (K3 — פרטיות ואישור מייל)

**⚠️ שתי הגדרות ידניות ב-Supabase. בלעדיהן חלק ממה שנבנה לא עובד, ואחת מהן היא
בדיוק מה שהמשתמש ביקש:**

1. **Authentication → URL Configuration → Redirect URLs** — להוסיף
   `https://tripplan-ten.vercel.app/reset/confirm` ו-
   `http://localhost:3000/reset/confirm`. בלעדיהן מייל האיפוס מוריד את המשתמש
   ב-Site URL בלי סשן. **(עדיין פתוח מ-K2.)**
2. **Authentication → Providers → Email → Confirm email = ON** — זה מה שהופך
   הרשמה לדורשת אישור במייל. **הקוד לא קובע את זה**, הוא מטפל בשני המצבים, ולכן
   הפעלה או כיבוי לא דורשים דיפלוי.

### מה השתנה במודל הסיסמאות

**הסיסמה משתנה עכשיו רק דרך הקישור במייל.** אין מסך שינוי סיסמה בתוך
האפליקציה, ואין דרך להוסיף סיסמה לחשבון Google — שניהם הוסרו לבקשת המשתמש.
התוצאה טובה מבחינת אבטחה, וכדאי לא לבטל אותה בטעות: **מי שהשתלט על סשן פעיל
לא יכול לשנות את הסיסמה ולנעול את הבעלים בחוץ.**

חשבון Google שיגיע בכל זאת לטופס הסיסמה נחסם ב-`setNewPassword` — כלומר **אחרי**
שהוכיח שליטה בתיבה, ולא בטופס הבקשה. **זו הכרעה ולא נוחות:** בקשה שמדווחת ״אין
סיסמה לחשבון הזה״ היא אורקל — אפשר להקליד כל כתובת ולגלות אם לאדם יש כאן חשבון
ובאיזו שיטה הוא נכנס. אל תזיזו את הבדיקה הזו קדימה.

### דף הפרטיות נכתב מהקוד, ולכן הוא מתיישן

כל צד שלישי שמופיע ב-`/privacy` מופה מהמקור: Supabase, Vercel, Gemini,
Nominatim/Overpass/אריחי OSM, Open-Meteo. **מדיניות שמונה שירות שלא בשימוש, או
משמיטה כזה שכן, גרועה מכלום** — היא טענה ספציפית שקורה שהיא לא נכונה.
**כל שינוי בצדדים השלישיים ב-`src/lib/ai`, `src/app/api` או
`src/features/*/infrastructure` מחייב עדכון של הדף באותו commit.**

האישור נאכף בצד השרת דרך `z.literal(true)`. `required` ב-HTML הוא נוחות למי
שממלא, לא בקרה — Server Action הוא endpoint של HTTP.

### מה שלא אומת בריצה

- **הרשמה עם אישור מייל מקצה לקצה** — דורשת את ההגדרה למעלה ומייל אמיתי.
- **איפוס סיסמה מקצה לקצה** — דורש את רשומת ה-Redirect URL.
- **חסימת חשבון Google בטופס הסיסמה** — דורשת חשבון Google שמגיע לשם.
- נבדק: 12 בדיקות סכמה עוברות (כולל דחיית `"on"` ו-`1` כאישור, שהם truthy),
  `/privacy` ציבורי ו-200, הצ׳קבוקס מרונדר עם `required`, ההרשמה מקשרת למדיניות
  ודף ההתחברות לא מציג צ׳קבוקס. `type-check`, `lint` ו-`build` עוברים; 33 routes.

## מצב נכון ל-2026-08-28 (K2 — תיקוני פרודקשן ושחזור סיסמה)

**פאזה K מוזגה ל-`main` ב-PR #47 והיא בפרודקשן.** שני באגים שהיא הכניסה תוקנו,
ונוסף מסלול שחזור סיסמה שלא היה קיים קודם.

**⚠️ שתי פעולות ידניות, שבלעדיהן חלק מזה לא עובד:**

1. **`migration 0019` — הורץ כבר, ואומת.** שמונה טבלאות חזרו מ-`42501` ל-`[]`.
2. **`/reset/confirm` חייב להיכנס לרשימת ה-Redirect URLs ב-Supabase.**
   Authentication → URL Configuration → Redirect URLs, רשומה לכל origin:
   `https://tripplan-ten.vercel.app/reset/confirm` ו-
   `http://localhost:3000/reset/confirm`. **בלעדיו מייל השחזור מוריד את המשתמש
   ב-Site URL בלי סשן**, והטופס יסרב להיטען — נכון, אבל חסר תועלת.

### הכלל שהבאג הראשון מלמד, והוא כללי

**כל פונקציה שמדיניות RLS מפנה אליה חייבת להיות ניתנת להרצה על ידי כל תפקיד
שמגיע לטבלה — כולל `anon` ו-`service_role`.** מדיניות RLS מוערכת בתפקיד שמריץ
את השאילתה, ולכן `anon` שבוחר מ-`trips` מריץ את גוף המדיניות. בלי EXECUTE
PostgreSQL לא מחזיר `false` אלא זורק `42501` ומפיל את השאילתה.
**הגבלת EXECUTE על פונקציה כזו לא מצמצמת גישה — היא מהפכת ״אין שורות״ ל״נכשל״.**
זה נראה כמו הקשחה, וזו הסיבה שזה עבר.

### הכלל שהבאג השני מלמד

**Supabase מאמת את `redirectTo` מול רשימת ה-Redirect URLs, וכתובת עם query
string לא מתאימה לרשומה בלי wildcard.** כשזה קורה Supabase לא נכשל בקול — הוא
נופל חזרה ל-Site URL, שלא מחליף את הקוד לסשן, והמשתמש חוזר מנותק. **כל מה
שנוסף ל-`redirectTo` הוא שינוי מסוכן.** לכן היעד שאחרי ההתחברות נוסע בקוקי
httpOnly ולא בכתובת.

**חריג:** בשחזור סיסמה קוקי לא עובד — מייל נפתח לרוב במכשיר אחר מזה שביקש אותו.
לכן `/reset/confirm` הוא route נפרד שמחליף את הקוד בעצמו, ולכן הוא זה שצריך
להיות ברשימה.

### תקלה באבחון ששווה לזכור יותר מהבאגים

הצהרתי בביטחון ש״הפרודקשן מריץ בילד ישן״ על בסיס grep שחזר ריק. **הבדיקה עצמה
הייתה שבורה:** שרשרת `&&` שבה `echo` הצליח והטעה, ומחרוזת (`overflow-x:clip`)
שלא שרדה את המינימייזר. בדיקה חוזרת עם ארבעה סמנים הראתה שפאזה K כן בפרודקשן.
**הכלל: בדיקה שמחזירה ״לא נמצא״ חייבת להוכיח שהיא מסוגלת למצוא משהו לפני
שמסיקים ממנה.** זה עלה לי מסקנה שגויה שאמרתי למשתמש.

### מה שלא אומת בריצה

- **זרימת השחזור מקצה לקצה** — דורשת את הגדרת ה-Redirect URLs ומייל אמיתי.
  נבדק שהמסלולים נטענים (`/reset` 200, `/reset/confirm` 200 ומציג ״הקישור לא
  תקף״ בלי קוד) ושדף ההתחברות מקשר אליהם.
- **״קביעת סיסמה״ לחשבון Google** — הזרימה שהמשתמש שאל עליה במפורש.
  אחרי קביעה, לוודא ש**גם** Google וגם אימייל+סיסמה עובדים לאותו חשבון.
- כל מסך מאחורי התחברות. `type-check`, `lint` ו-`build` עוברים; 32 routes.

## מצב נכון ל-2026-08-27 (פאזה K)

**פאזה G מוזגה ל-`main` ב-PR #46** — `origin/main..HEAD` היה ריק בתחילת הפאזה.
כל מה שכתוב בהמשך הקובץ על ״פאזה G ממתינה ל-PR״ הוא היסטוריה.

**⚠️ שני migrations חדשים חייבים לרוץ, בסדר הזה, לפני שמשהו מפאזה K עובד:**

- **`0017_trip_gear.sql`** — טבלת הציוד. אידמפוטנטי, בלי תלויות. בלעדיו מסך
  ״עוד → ציוד״ יראה רשימה ריקה ולא יוכל להוסיף.
- **`0018_trip_members.sql`** — **זה ה-migration המשמעותי בפרויקט מאז 0002.**
  הוא **משכתב את מדיניות ה-RLS של כל שמונה טבלאות הטיול**. עד עכשיו כל מדיניות
  אמרה `trips.user_id = auth.uid()`; מעכשיו כולן עוברות דרך שלוש פונקציות
  `SECURITY DEFINER`. **להריץ אותו במלואו בבת אחת** — הרצה חלקית תשאיר טבלאות עם
  מדיניות ישנה וטבלאות עם חדשה, ואז חבר יראה חלק מהטיול.
  בלעדיו: מסך השיתוף נטען, ההזמנות לא נוצרות, וכל קישור `/invite/...` מחזיר 404.

**מה שנבנה:** רשימת ציוד ידנית · ״איך זה עובד״ במסך הבית ובדיאלוג טיול חדש ·
תיקון החריגה האופקית וסריקה רוחבית · שיתוף בכותרת של כל מסך טיול ובמסך משלו ·
גישה אמיתית ממכשיר אחר עם צפייה או עריכה. הפירוט המלא ב-PROJECT_PLAN.md,
פרק ״פאזה K״.

### שלוש עובדות שכדאי לא לגלות מחדש

**1. `break-words` לא מקטין min-content — רק `wrap-anywhere` מקטין.**
`overflow-wrap: break-word` מרשה שבירה בתוך מילה כדי למנוע חריגה, אבל תרומת
האלמנט למינימום נשארת רוחב המילה הארוכה ביותר. זה השורש השני של באג ״הכרטיס
מגדיל את העמוד״, והתיקון הראשון שלי נכשל בגלל שלא הכרתי את ההבחנה. השורש הראשון:
**ל-`<li>` בגריד יש `min-width: auto`**, ומסלול `1fr` הוא `minmax(auto, 1fr)` —
כלומר רצוף על min-content של הפריט. **צריך את שניהם.**

**2. `overflow-x: clip` ולא `hidden` ברשת ביטחון.** `hidden` הופך את האלמנט
ל-scroll container, וכזה בין `position: sticky` ל-scrollport שלו הורג את ה-sticky
בשקט. האפליקציה מסתמכת על sticky בשלושה מקומות לפחות (אינדקס הימים, שם היעד
בחיפוש האטרקציות, ה-header).

**3. RLS על שתי טבלאות שמצביעות אחת על השנייה = רקורסיה, והיא מתפוצצת בזמן
שאילתה ולא בזמן ה-migration.** מדיניות על `trips` שמתייעצת עם `trip_members`,
ומדיניות על `trip_members` שמתייעצת עם `trips` — `infinite recursion detected in
policy for relation "trips"`. `SECURITY DEFINER` לא נכנס שוב ל-RLS ולכן חותך את
המעגל. **וחובה `set search_path`** על כל פונקציה כזו, אחרת היא פותרת שמות לא
משויכים לפי ה-search_path של הקורא.

### חור אבטחה שכדאי להכיר כדפוס, לא רק כתיקון

`WITH CHECK` נבדק מול השורה **החדשה**, אבל פונקציה בתוכו שקוראת מאותה טבלה רואה
את השורה **הישנה**. לכן `using (can_edit_trip(id)) with check (can_edit_trip(id))`
על `trips` אִפשר ל-editor לשלוח `PATCH /trips?id=eq.<id> {"user_id": "<שלו>"}`
ישר ל-REST API של Supabase ולהפוך לבעלים. **שום דבר באפליקציה לא עושה את זה, ובדיוק
לכן זה לא נתפס** — ה-REST API נגיש עם anon key וסשן, לא רק דרך הקוד הזה.
תוקן בטריגר `BEFORE UPDATE`. **הכלל:** מדיניות RLS שמגנה על עמודה מסוימת מפני
שינוי — לבדוק אם ה-`WITH CHECK` בכלל יכול לראות את השינוי. לרוב לא.

### מה שלא אומת בריצה — לקרוא לפני שמניחים שמשהו עובד

כל מסך מאחורי התחברות נבנה, עבר type-check/lint/build, ולא הופעל. **הזרימה
היחידה שהיא חדשה במלואה ולכן הכי שווה לבדוק ראשונה — מקצה לקצה:**

1. להריץ 0017 ו-0018 ב-Supabase.
2. **קודם כל לוודא שהטיולים הקיימים עוד נטענים.** 0018 נגע במדיניות של כל
   הטבלאות; אם משהו בו לא עבר, זה ייראה כמו טיול ריק ולא כשגיאה.
3. ״עוד → ציוד״: להוסיף פריט ידנית, ללחוץ הצעת התחלה, לסמן, ״איפוס הסימונים״.
4. ״עוד → שיתוף״ (או כפתור השיתוף בכותרת): להזמין **כתובת אימייל שנייה שלכם**,
   להעתיק את הקישור, לפתוח בחלון פרטי, להירשם, ולראות שהטיול מופיע.
5. לוודא שחבר `viewer` **לא** מצליח לערוך, ושחבר `editor` **כן**.
6. לוודא שקישור הזמנה שנפתח בחשבון לא נכון אומר זאת במקום להיכשל בשקט.

**עדיין פתוח מפאזה D:** D7 — אימות ויזואלי של המסכים המוגנים.

**⚠️ עניין הגיט לא נסגר.** המשתמש ביקש להפסיק למזג דרך GitHub. עמוד העריכה של
ה-ruleset הוא `/settings/rules/<id>` — הכתובת `/rules/<id>` היא תצוגת שקיפות
לקריאה בלבד, בלי checkboxes, וזה מה שבלבל. **הקובץ הזה ו-PROJECT_PLAN.md
(שורות 88, 156, 352) עדיין אומרים שיש חובת PR** — לעדכן כשיתברר מה נקבע.

## מצב נכון ל-2026-08-24 (פאזות G + H + I + J)

**אייקון האפליקציה הוחלף (J).** תכלת `#0071c2` + המטוס של lucide (אותו סמל
שב-`AppHeader`) + קו מסלול מקווקו, **בלי כיתוב**. הקודם היה סיכת מפה בפלטת
המוקה הישנה, כלומר לא תאם לא ללוגו ולא לצבעים מאז פאזה D.

**אין כיתוב בכוונה:** iOS מדפיס את שם האפליקציה מתחת לאייקון בעצמו, וב-60px
האותיות נמרחות — השם הופיע פעמיים ואחת מהשתיים לא נקראה. גרסה עם כיתוב נבנתה,
הוצגה בגודל האמיתי, והמשתמש הפך את ההחלטה אחרי שראה אותה.

**⚠️ ה-SVG-ים ב-`public/` הם פלט של `scripts/generate-icons.mjs`, לא מקור.**
לשנות שם ולהריץ `npm run icons`. עריכה ידנית מוציאה אותם מסינכרון עם ה-PNG-ים,
וה-PNG-ים הם מה שהמערכות באמת קוראות.

שלוש עובדות שקבעו את המימוש, ושווה לא לגלות אותן מחדש:
- **`apple-touch-icon.png` חייב להיות ריבוע מלא בלי עיגול עצמי** — iOS מחיל
  מסכה משלו, ומקור מעוגל מקבל עיגול כפול עם פינות כהות.
- **iOS מתעלם מ-SVG כ-apple-touch-icon** ושם צילום מסך של הדף במקומו. PNG חובה.
- **טקסט באייקון חייב להיות outlines ולא `<text>`** — sharp מרסטר דרך librsvg,
  שפותר פונטים דרך fontconfig ו**משמיט טקסט בשקט** כשהמשפחה חסרה. רלוונטי לכל
  תמונה מיוצרת בעתיד, גם אם באייקון עצמו אין כיתוב.

**האייקון באייפון לא יתעדכן לבד** — צריך להסיר את ה-PWA ולהתקין מחדש, וזה
מוחק את מנוי הפוש (ראו למטה).

## מצב נכון ל-2026-08-24 (פאזות G + H + I)

**⚠️ migration 0016 חייב לרוץ, ויש לו סדר פעולות.** הוא מזיז את שעות ההזמנות
הקיימות (ראו פאזה I), ומזיז את **כל** השורות — כולל כאלה שהקוד החדש יכתוב
נכון. **להריץ אותו ולמזג בסמיכות, ולא להוסיף או לערוך הזמנה בין לבין.** הוא
שומר על עצמו מפני הרצה כפולה דרך טבלת `applied_migrations` חדשה.

**באג אזור הזמן (I):** שדה `datetime-local` מייצר קריאת שעון בלי אזור, והעמודה
היא `timestamptz`. הכתיבה לא המירה, אז Postgres קרא את המחרוזת כ-UTC — 22:20
נשמר כ-`22:20+00` והוצג בירושלים כ-00:20 למחרת. **וזה לא היה רק תצוגה:** אותה
הזזה שייכה טיסות ליום הלא נכון בכל מקום שמדלה לפי תאריך, כולל כל המנגנון
הדטרמיניסטי של פאזה E. ההמרה מרוכזת עכשיו ב-`src/lib/datetime.ts`, שהוא הגבול
היחיד בין שעה מוקלדת לרגע בזמן.

**הכלל:** `datetime-local` ו-`timestamptz` הם שני סוגי נתון. כל מעבר ביניהם
חייב להמיר במפורש ולציין באיזה אזור — ולעולם לא `toLocaleString` חשוף לתצוגה,
שנותן תשובה שונה לכל קורא.

## מצב נכון ל-2026-08-24 (פאזות G + H)

**⚠️ באג אובדן נתונים שתוקן ב-H1 — כדאי להכיר את הדפוס, לא רק את התיקון.**

"רענון הצעות" במדריך עיר מחק **כל** שורות ה-AI של אותה עיר, כולל אלה עם
`selected = true`. כלומר הוא רוקן את הטיול מכל מה שהמשתמש הוסיף באותה עיר, וגם
מחק את שורת ה-`overview` שמחזיקה את ה-cache של הקואורדינטות והמדינה — כך שהוא
היה **גם אחד הגורמים לדיווח על המפה**, במיוחד אחרי שפאזה G גוזרת את מיקום העיר
מהמקומות שבתוכה.

**הכלל שנגזר מזה, ושווה לאכוף על כל מחיקה חדשה:** פעולה שנקראת "רענון" או
"בנייה מחדש" מחליפה **הצעות או סידור** — לעולם לא בחירות של המשתמש. מחיקה
שנוגעת בטבלה שמכילה גם בחירות חייבת לחסום `selected = true` במפורש.
ובנוסף: `update`/`delete` ב-Supabase שאמור לפגוע בשורה מסוימת — לבדוק `count`,
כי Postgres מדווח על התאמה לאפס שורות כהצלחה.

**מדריך "איך זה עובד"** נוסף ב-`/trips/<id>/more/guide` — שבעה שלבים,
כל אחד עם קישור למסך שבו הוא קורה. השלבים הם נתון ב-`domain/workflow.ts`, לא
JSX, כדי שמצבים ריקים יוכלו להצביע לאותו נוסח בלי לשכפל אותו.

## מצב נכון ל-2026-08-24 (פאזה G)

**פאזות E ו-F מוזגו ל-`main` דרך PR #41 — הן בפרודקשן.**
**פאזה G נבנתה על ענף `feature/phase-g-search-map-flights` וממתינה ל-PR.**

| מה | קובץ מרכזי |
| -- | ---------- |
| חיפוש אזור בתוך יעד (Omotesando) — OSM + AI | `route-service.ts`, `place-search.tsx` |
| מיקום עיר נגזר מהמקומות בפועל במקום מגאוקודינג | `route-service.ts` |
| סיכה לכל מקום על המפה, לא רק לערים | `route-map-canvas.tsx` |
| טיסות ורכבות על ציר הלו״ז | `domain/timeline.ts`, `day-timeline.tsx` |

**⚠️ שני באגים אמיתיים במפה שנמצאו בקריאת הקוד — כדאי להכיר את שניהם:**

1. **`cacheCoordinates` היה `update` חשוף, ולערים בלי שורת `overview` הוא לא
   כתב כלום.** שורת overview נוצרת רק ב-`saveCityGuide`, כלומר רק כשנפתח מדריך
   העיר. עיר שנכנסה דרך חיפוש האטרקציות לא הייתה לה שורה כזו, ו-Postgres מדווח
   על update שלא התאים לשום שורה כהצלחה — כך שהקואורדינטות גואוקדו מחדש בכל
   רינדור, **ותיקון הסיכה של פאזה F כתב לאוויר.** זו הסיבה שסיכה שתוקנה חזרה
   להיות שגויה. תוקן ל-`upsert`.
2. **המפה זרקה את הנתון המדויק ביותר שיש.** מקום מחיפוש האטרקציות נושא את
   הקואורדינטות של OSM עצמו — השורות היחידות שלא עברו גאוקודינג ולכן היחידות
   שלא יכולות להיות שגויות. עכשיו מיקום העיר נגזר מהחציון שלהן, ונופל
   לגאוקודינג רק אם אין אף מקום כזה.

**הכלל שנלמד:** `update` ב-Supabase שאמור לפגוע בשורה אחת — לבדוק `count`, או
להשתמש ב-`upsert`. כשל שקט בכתיבה נראה בדיוק כמו באג בקריאה.

## מצב נכון ל-2026-08-24

**שם האפליקציה הוא MyTrip** (שונה מ-TripPlan ב-2026-08-24). כל מה שמשתמש רואה
עודכן. **מה שלא שונה ודורש פעולה שלך:** שם הריפו ב-GitHub, שם פרויקט ה-Vercel,
והכתובת `tripplan-ten.vercel.app`. מזהים פנימיים (`aiTripPlanSchema`, ה-tag
`tripplan-reminder`, `tripplan-dev` ב-launch.json) נשארו בכוונה — ראו את יומן
ההחלטות.

**⚠️ ה-PWA המותקן באייפון לא יתעדכן לבד.** `manifest.name` השתנה, אבל iOS
קורא את המניפסט בזמן ההתקנה ולא אחריה — האייקון במסך הבית ימשיך לומר
"TripPlan" עד שתסיר ותתקין מחדש. **והסרה מוחקת את מנוי הפוש** (C9), כך שאחרי
התקנה מחדש צריך להפעיל שוב "תזכורות למכשיר" ב-״עוד״ → פרטי הטיול.


**פאזות E ו-F נבנו על ענף `feature/phase-e-trip-fixes`, ממתינות ל-PR.**
פאזה C הושלמה (C1–C10). פאזה D הושלמה (D1–D11) ומוזגה כולה ל-`main` דרך
PRs #33–#37. המשתמש העלה לפרודקשן והריץ את `migration 0013` ב-Supabase.

- **Migration 0014** (עלות הזמנה) — ✅ רץ (אושר על ידי המשתמש 2026-08-20).
- **Migration 0015** (מדינה לעיר + שיתוף פומבי) — ⏳ **עדיין לא רץ.** בלעדיו:
  אין קיבוץ לפי מדינות, אין תיקון אוטומטי לסיכות שגויות, ועמוד השיתוף מחזיר
  404 תמיד. הקוד מטפל בהיעדר העמודה בחן (`isSchemaOutOfDate`), אז המפה
  ממשיכה להיצייר — היא פשוט לא יודעת מדינות.
- `main` מסונכרן מול origin. ענף פתוח יחיד: `feature/phase-e-trip-fixes`.
- **⚠️ ה-PR של פאזה E מעולם לא נפתח**, ולכן שום דבר מ-E או מ-F אינו בפרודקשן.
  זו הסיבה שהמשתמש דיווח "אני עדיין לא רואה את השינוי". פאזה F נבנתה על אותו
  ענף במכוון — שני PRs שאחד חוסם את השני אינם שווים את הפיצול.
- עבודה חדשה מתחילה מענף חדש מעל `main` (יש branch protection: חובה PR +
  status check `Type-check & Lint`).

## מה שנבנה בפאזה F

תשעה דיווחים נוספים. הפירוט המלא ב-PROJECT_PLAN.md, פרק "פאזה F".

| מה | קובץ מרכזי |
| -- | ---------- |
| עמוד נחיתה: נקודות המכירה כבר לא נראות כפתורים | `app/page.tsx` |
| מטבע ₪/$/€ עם ברירת מחדל שקל, וסימנים במקום קודים | `domain/expenses.ts` |
| סינון ההוצאות לפי יעד | `components/expense-summary.tsx` |
| חלוקת התחנות לפי מדינות | `domain/route.ts`, migration 0015 |
| קישור בין טיסות קונקשן + זמן המתנה | `domain/booking.ts` |
| שיתוף פומבי לצפייה בלבד | `domain/share.ts`, `app/share/[token]` |
| מסלול לפי קרבה גאוגרפית | `lib/geo.ts` |
| כל הימים בלו״ז + הצעות AI ליום ריק | `components/day-suggestions-dialog.tsx` |
| תיקון סיכות שנחתו במדינה הלא נכונה | `infrastructure/route-service.ts` |

**התובנה המרכזית: שלושה מהפריטים היו אותה בעיה.** מדינות, מפה שבורה וקרבה
גאוגרפית — לשלושתם חסר אותו נתון. גאוקודינג של שם עברי הוא ניחוש; **גאוקודינג
הפוך** (קואורדינטות → מדינה) הוא ודאי. מרגע שיש מדינה לכל עיר, סיכה שנחתה מחוץ
למדינת הרוב בולטת מיד ומתוקנת אוטומטית.

**⚠️ עמוד השיתוף לא נבדק בריצה אמיתית** — הוא קורא דרך `createAdminClient`,
ו-`SUPABASE_SERVICE_ROLE_KEY` קיים רק ב-Vercel ולא ב-`.env.local`. מה שכן אומת
מקומית: `/share/<טוקן שלא קיים>` ו-`/share/<מעוות>` שניהם 404 (זהים במכוון),
ו-`/trips/x` עדיין 307 ל-login. **הדבר הראשון לבדוק בפרודקשן אחרי המיזוג:**
ליצור קישור שיתוף ולפתוח אותו בחלון פרטי.

## מה שנבנה בפאזה E

תשעה דיווחי משתמש מתוך שימוש אמיתי בטיול, כולם בענף אחד. הפירוט המלא
(כולל הכרעות והתקלה שנתפסה בבנייה) ב-PROJECT_PLAN.md, פרק "פאזה E". בקצרה:

| מה | קובץ מרכזי |
| -- | ---------- |
| שכונה (שיבויה) לא יוצרת עיר כפולה — AI guard + מיזוג לפי מרחק | `guide-service.ts`, `ai-suggestion.ts` |
| טוסט פינתי אחרי הוספת/עדכון הזמנה | `components/ui/toast.tsx` |
| שם היעד דביק בגלילת תוצאות | `place-search.tsx` |
| "עוד תוצאות" + "חיפוש ליד כאן" באטרקציות | `place-search.tsx`, `overpass.ts` |
| עלות הזמנה + מחשבון הוצאות (בלי המרת מטבעות) | `domain/expenses.ts`, migration 0014 |
| עריכת הזמנה קיימת | `booking-form.tsx`, `booking-list.tsx` |
| לו"ז: בלוק ימים דטרמיניסטי לפי עיר + מודע לזמן נחיתה | `trip-days.ts`, `itinerary-plan.ts` |

**התיקון המהותי ביותר:** בניית הלו"ז עברה מ"בקש מהמודל לכבד חלוקת ימים בטקסט
חופשי" ל"תן לו עובדה קבועה, ותקן בקוד כל דבר שהוא בכל זאת מפר" —
`buildDayCityPlan` בונה מפה יום→עיר מתאריכי הזמנות אמיתיים (לינה + זמן נחיתה
של הזמנת התחבורה המוקדמת ביותר), ו-`reconcileItineraryWithDayPlan` מתקן כל
פריט שחזר מהמודל ביום שסותר את זה — בלי תלות בציות המודל. פועל רק לטיול עם
תאריך התחלה+סיום קבועים; בלעדיהם חוזר בדיוק להתנהגות הקודמת.

**⚠️ תקלה שכדאי לזכור: circular import שהפיל את ה-build בלי type-check/lint
לתפוס אותו.** `ai-suggestion.ts` ↔ `place.ts` ייבאו זה מזה; `next build`
(Turbopack) נכשל עם `ReferenceError` בקובץ שלישי לא-קשור. `distanceKm` עבר
ל-`lib/geo.ts` ניטרלי. הלקח: type-check ולינט לא בודקים סדר הערכה של מודולים —
`next build` הוא הבודק היחיד, ולכן הוא חובה לפני שקוראים לשינוי גמור.

## מה שנבנה בפאזה D

עיצוב בכיוון **Booking.com**, שנבחר על ידי המשתמש אחרי שראה השוואה חזותית של
המצב הקיים מול שלושה כיוונים.

| # | מה | סטטוס |
| - | -- | ---- |
| D1 | טוקנים v3: קנבס `#eceef2` (1.16:1 מול לבן, נמדד), שלשות תפקיד בסגנון BUI, סולם 12/14/16/20/24/32/48, `line-height: 1.7` לעברית, סקרימים כטוקנים | ✅ |
| D2 | פרימיטיבים: `SectionHeading`/`ListRow`/`Banner`/`Dialog`/`Select`/`Field`/`Surface`/`ToneDot`; `Card` עם padding; 5 שיבוטים נמחקו | ✅ |
| D3 | `AppShell` עם sidebar בנייבי מ-1024px; `AppHeader` חולץ; ניווט = `NavItem[]` אחד בשלוש מצגות | ✅ |
| D4 | שתי חלוניות: `/map` (מפה + לוח נגלל בנפרד), `/days` (אינדקס ימים sticky) | ✅ |
| D5 | כל המסכים לפי הדקדוק החדש; "טיול חדש" עבר לדיאלוג | ✅ |
| D6 | `not-found`/`error`/`global-error`, כותרות דפדפן לכל route, יישור skeletons | ✅ |
| D8 | מחיקת טיול, עם אישור שמפרט מה נמחק | ✅ |
| D9 | ימים בכל עיר: נגזר מהלינה, נדרס ידנית, נכנס ל-prompt כאילוץ | ✅ |
| D10 | עריכת פריט בלוח: שעות, יום, הערה, "איך מגיעים" + זמן הגעה | ✅ |
| D11 | סימון יום דל והפניה להוספת פעילויות | ✅ |
| D7 | **אימות ויזואלי של המסכים המוגנים** | ⏳ **הפריט היחיד שנשאר** |

## מה שלא אומת בריצה — קרא את זה לפני שאתה מניח שמשהו עובד

Claude לא יכול להיכנס לאפליקציה: ה-proxy מפנה כל route שאינו `/`, `/login`
ו-`/signup` אל `/login`, ואסור לו להזין סיסמה. לכן **כל מה שמאחורי התחברות נבנה,
עבר type-check/lint/build, ולא הופעל אף פעם**:

- מחיקת טיול (בלתי הפיכה — כדאי לנסות על טיול שלא חבל עליו).
- עורך "ימים בכל עיר" ודיאלוג עריכת הפריט.
- הזרימה של בניית לו"ז עם האילוץ החדש.

**וכל תשעת פריטי פאזה E** (הטוסט, ה-sticky, "עוד תוצאות"/"חיפוש ליד כאן",
עריכת הזמנה, מחשבון ההוצאות, ומיזוג היעדים) — הפונקציות הטהורות שמאחורי בניית
הלו"ז נבדקו מול מקרי קצה דרך route זמני שנמחק (ראו PROJECT_PLAN.md), אבל אף
מסך לא נפתח בדפדפן מאומת. מיזוג היעדים (E1) דורש בנוסף קריאת Gemini אמיתית,
שלא הורצה — ראו את הכלל למטה על 20 הבקשות לדקה.

מה שכן נמדד בדפדפן על שרת ה-dev: קנבס `rgb(236,238,242)`, כרטיס לבן ברדיוס
12px, גובה כותרת 56px (ה-offset שה-sidebar מסתמך עליו), `letter-spacing: normal`,
`line-height` של טקסט גוף **1.70 בדיוק**, אפס שגיאות קונסול, ו-`next build` עובר
עם 16 עמודים.

**מה שכדאי לפתוח קודם, לפי סדר החשיבות:** `/trips/<id>/map` ב-1440 (שם
שתי-החלוניות, והמקום הסביר ביותר שמשהו ייראה לא נכון) · `/days` ב-1440 (אינדקס
הימים + עורך הימים בעיר) · `/profile` · חמשת הטאבים ב-375.

## מגבלות ותקלות שכבר שילמנו עליהן

**`git push` עובד.** ההנחה הקודמת שהכלי חוסם אותו הייתה שגויה — יש credential
helper (`manager`). מה שכן נכשל היה
`GH007: Your push would publish a private email address`. **תוקן:** `user.email`
בריפו הזה הוא `304832351+Talsy93@users.noreply.github.com`. אין צורך לבקש
מהמשתמש לדחוף. ל-`main` עדיין נכנסים רק דרך PR.

**Gemini free tier = 20 בקשות לדקה, לכל הפרויקט, משותף.** 429 מ-Google מוחזר גם
למגבלה לדקה וגם למכסה יומית, והגוף האמיתי (נתפס 2026-08-18) **לא** מכיל
`retryDelay` ולא `quotaId` שמזכיר חלון — ה-delay בפרוזה: `Please retry in
15.659996788s.`. `src/lib/ai/quota.ts` מסווג לפי זה, והגוף האמיתי הוא מקרה בדיקה.
**אין להריץ קריאות אמיתיות ל-Gemini בלי לומר למשתמש קודם** — זה גוזל מאותן 20
ויכול להפיל לו את האפליקציה תוך כדי שהוא בודק.

**`@theme inline` לא פולט את המשתנה ל-`:root`.** זו המשמעות של `inline`. קוד
שקורא טוקן דרך `var()` מחוץ ל-utilities של Tailwind — למשל מחרוזת ה-CSS שבונה את
הסיכה של Leaflet — לא יראה אותו. לכן הצללים ב-`:root` נקראים `--elevation-*`
ומופים ל-`--shadow-*`; השם **חייב** להיות שונה, כי
`--shadow-soft: var(--shadow-soft)` היא הגדרה מעגלית.

**`import { Map } from "lucide-react"` שובר `new Map()`** באותו קובץ, והשגיאה
("'new' expression whose target lacks a construct signature") לא מרמזת על השורש.
הכלל: alias לכל אייקון שמתנגש עם גלובל — `Map as MapIcon`.

**פס-צד בעובי 4px הוא מילוי, לא אקסנט.** זה היה השורש של עומס הצבע. `ListRow`
החליף אותו בנקודה בעשרה מקומות. החריג היחיד הוא `day-timeline`, ובמכוון — שם
הקצה רץ לאורך הבלוק ולכן מקודד את משך הפריט.

**`npm run build` ו-`next dev` חולקים את `.next`.** אחרי build למחוק את התיקייה
לפני `npm run dev`, אחרת מתקבל מפל `Module not found` שנראה כמו התקנה שבורה.

**פורט 3000 תפוס על ידי פרויקט אחר (PriceDrop).** `.claude/launch.json` עם
`"autoPort": true`, כך ש-`preview_start` מקבל פורט פנוי. OAuth של Google צריך
callback ל-3000 — התחברות באימייל+סיסמה עובדת בכל פורט.

## אופן העבודה שהמשתמש ביקש

**בחתיכות גדולות, בריצה אחת, בלי אישור לכל פריט.** זה גובר על
"צעד-צעד באישור" שב-`CLAUDE.md`, שנכתב לפני הבקשה הזו. את **הכיוון** כן מחליטים
יחד — ועדיף להראות אפשרויות חזותית מלהסביר אותן בטקסט, כי זה מה ששחרר את הבחירה
בפאזה D. לשאול רק כשהתשובה משנה מהותית את התוצאה ואי אפשר לתקן בזול.

## דפוסי עבודה שכדאי להמשיך איתם

1. **פונקציה טהורה נבדקת מול מקרי קצה לפני חיבור למסך.** זה החזיר את ההשקעה
   פעמיים בפאזה D: 22 מקרים ב-`city-days` (ואחד גילה שהציפייה שלי שגויה, לא
   הקוד), ו-16 ב-`quota` (שם התיקון הראשון נכתב מול צורה **משוערת** של גוף
   השגיאה ולכן לא עבד — עד שנתפס הגוף האמיתי).
2. **לא לנחש צורה של נתון חיצוני — למדוד אותה.** ראה למעלה.
3. **בדיקה חזותית ב-375 / 768 / 1024 / 1440 / 1920.**
4. **ניגודיות נמדדת ולא מוערכת** (ב-B5 נמצאו שני גוונים מתחת לסף AA).
5. **route זמני / probe נמחק תמיד לפני commit.**
6. עדכון `PROJECT_PLAN.md` כולל יומן ההחלטות אחרי כל צעד.

## מצב נכון ל-2026-08-30 (K10 — רמוני התצוגה)

**`/preview` — כל מסך שמאחורי התחברות, בלי התחברות.** פיתוח בלבד: הדפים מחזירים
`notFound()` מחוץ ל-development.

זו התשובה לשורה שחזרה כאן שוב ושוב — ״כל מסך שדורש התחברות נבנה ולא הופעל אף
פעם״. 29 סצנות, כל אחת רכיב אמיתי במצב אחד, עם נתונים קבועים ב-`fixtures.ts`.

**החלוקה היא לפי מצב ולא לפי רכיב, וזה העיקר:** המצבים ששוברים פריסה הם דווקא
אלה שקשה להגיע אליהם בחשבון אמיתי — טיול ריק, יום בלי פעילויות, עיר שהגאוקודר
לא מצליח למקם, שם בלי נקודת שבירה, רשימת חברים כפי שהיא נראית ל-`viewer`.

**כפתור ״הרצה״ בעמוד `/preview`** מריץ את כל הסצנות ב-320/375/414 בתוך iframe
ומדווח על חריגות ועל שדות מתחת ל-16px.

### מה הרמוני **לא** בודק

**את מסלול הנתונים.** אין סשן, ולכן RLS, Server Actions שכותבות באמת, וכל מה
שתלוי בהרשאות — בלתי נראים כאן. סצנה מרונדרת; זה לא מוכיח שהכפתור עובד.
לזה עדיין צריך חשבון אמיתי.

### שני דברים שהרמוני מצא מיד

1. **`Banner` דחף את מסך ״היום״ ל-510px בתוך טלפון של 375.** ה-children שלו
   יושבים ב-`min-w-0` אבל בלי יכולת לשבור מילה, וכמעט כל מה שבאנר אומר מצטט
   טקסט של משתמש או של ספק. תוקן ב-`wrap-anywhere`. הוא בשימוש ב-15 מקומות.
2. **`SegmentedControl` חרג ב-18px ב-320.** מספר הלשוניות הוא נתון — מסנן
   ההזמנות מגדל לשונית לכל סוג הזמנה שקיים, אז טיול עם טיסה, רכבת ולינה מקבל
   ארבע. **הסריקה הקודמת שלי פספסה את זה כי הפיקסצ׳רים הישנים הכילו שני סוגים
   בלבד, והפקד מרונדר רק מעל שניים.** עכשיו הוא גולל במקום לדחוף.

### מלכודת בבדיקה עצמה, ששווה יותר מהבאגים

הפילטר שמתעלם מאלמנטים חתוכים טיפס עד `<html>` — ול-`<html>` יש את
`overflow-x: clip` שהוספנו כרשת ביטחון. כלומר **כל אלמנט נחשב חתוך, והבדיקה
השביתה את עצמה בשקט ודיווחה ״נקי״.** הקנרית תפסה את זה. הטיפוס נעצר עכשיו
בשורש הסצנה, ו**באנר ההצלחה מרונדר רק כשהקנרית עברה** — אי אפשר לקבל ״נקי״ בלי
הוכחה שהגלאי חי.

