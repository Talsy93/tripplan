# TripPlan — נקודת המשך לשיחה חדשה

> **איך להשתמש בקובץ הזה:** בתחילת שיחה חדשה, תגיד ל-Claude לקרוא את
> `PROJECT_PLAN.md`, `ARCHITECTURE.md`, ואת הקובץ הזה. אפשר למחוק את הקובץ הזה
> אחרי שהשיחה החדשה מתחילה — הוא סיכום זמני, לא מקור אמת (`PROJECT_PLAN.md` הוא).

## מצב נכון ל-2026-08-18

**פאזה C הושלמה במלואה (C1–C10). פאזה D (עיצוב) נבנתה — D1 עד D6.**

הענף הפעיל: **`feature/phase-d-design`** — ארבעה קומיטים (D1–D3, D5 חלק א,
D4–D6, D8), **נדחף ל-origin**. נשאר PR + מיזוג דרך ה-UI של GitHub (אין `gh` CLI
במכונה): https://github.com/Talsy93/tripplan/pull/new/feature/phase-d-design

## מה נשאר לעשות בפאזה D

**D7 — אימות ויזואלי של המסכים המוגנים.** זה הפריט היחיד שנשאר, והוא חסום:
ה-proxy מפנה כל route שאינו `/`, `/login` ו-`/signup` אל `/login`, ול-Claude אין
סיסמה ואסור לו להזין אחת. מה שכן אומת בדפדפן על שרת ה-dev:

- הקנבס יוצא `rgb(236, 238, 242)`, כרטיס לבן ברדיוס 12px עם גבול `#dcdfe6`.
- גובה הכותרת 56px — בדיוק ה-offset שה-sidebar ב-`AppShell` מסתמך עליו.
- `line-height` של טקסט גוף: **1.70 בדיוק**. `letter-spacing: normal`.
- כפתור ראשי: כחול `#0071c2`, רדיוס 8px.
- אפס שגיאות בקונסול, וכותרת דפדפן נפרדת לכל route.
- `npm run build` עובר: 16 עמודים, `_not-found` נוצר.

**מה שכדאי לפתוח ולבדוק, לפי סדר החשיבות:**

1. `/trips/<id>/map` ב-1440px — זו שתי-החלוניות האמיתית (מפה מימין, לוח נגלל
   בנפרד משמאל). אם משהו בפאזה נראה לא נכון, סביר שזה כאן.
2. `/trips/<id>/days` ב-1440px — אינדקס ימים sticky שמופיע רק מ-1024px.
3. `/profile` — "טיול חדש" הוא עכשיו כפתור בכותרת שפותח מודאל, לא טופס פתוח.
4. כל חמשת הטאבים ב-375px — שהפס התחתון לא מסתיר תוכן.
5. `/trips/<id>/more/chat` בטלפון — ה-composer אמור להיות sticky מעל הפס.

## מגבלות חוזרות שחשוב לדעת עליהן

**`git push` כן עובד — ההנחה הקודמת הייתה שגויה.** ב-2026-08-18 התברר שיש
credential helper מוגדר (`manager`), ושהדחיפה מצליחה להתחבר. מה שנכשל בפועל היה
משהו אחר לגמרי, ושווה לזכור אותו:

**GitHub דחה את הדחיפה ב-`GH007: Your push would publish a private email
address`.** ההגנה הזו הופעלה בחשבון בשלב מסוים, בזמן ש-109 הקומיטים בהיסטוריה
כולם נושאים `appsab11@gmail.com` — כלומר היא חסמה כתובת שכבר גלויה בריפו ציבורי.
**התיקון:** `user.email` בריפו הזה הוגדר ל-`304832351+Talsy93@users.noreply.github.com`
(כתובת ה-noreply של GitHub), וארבעת הקומיטים של פאזה D עברו re-author. מכאן והלאה
כל קומיט חדש בריפו הזה נושא את כתובת ה-noreply, ולכן לא ייתקל בהגנה שוב.

**מה שזה אומר לשיחות הבאות:** אין צורך לבקש מהמשתמש להריץ `git push` — אפשר
לדחוף ישירות (ענפי feature בלבד; ל-`main` יש branch protection שדורשת PR).

**פורט 3000 תפוס במכונה על ידי פרויקט אחר (PriceDrop).** `.claude/launch.json`
קיבל `"autoPort": true` בפאזה D, כך ש-`preview_start` מקבל פורט פנוי. אם OAuth של
Google צריך callback ל-3000, זה יעבוד רק על השרת שרץ שם — התחברות עם
אימייל+סיסמה עובדת בכל פורט.

**`npm run build` ו-`next dev` חולקים את `.next`.** אחרי build צריך למחוק את
התיקייה לפני `npm run dev`, אחרת מתקבל מפל של `Module not found` שנראה בדיוק
כמו התקנה שבורה. `.next` נמחקה בסוף פאזה D, כך שהמצב נקי.

## שלושה דברים מפאזה D ששווה לזכור

1. **`@theme inline` לא פולט את המשתנה ל-`:root`.** זו המשמעות של `inline`.
   קוד שקורא טוקן דרך `var()` מחוץ ל-utilities — למשל מחרוזת ה-CSS שבונה את
   הסיכה של Leaflet — לא יראה אותו. לכן הצללים יושבים ב-`:root` בשם
   `--elevation-*` ומופים ל-`--shadow-*`; השם חייב להיות שונה, כי
   `--shadow-soft: var(--shadow-soft)` הוא הגדרה מעגלית.
2. **`import { Map } from "lucide-react"` שובר `new Map()`** באותו קובץ. השגיאה
   שמתקבלת ("'new' expression whose target lacks a construct signature") לא
   מרמזת על השורש. הכלל: alias לכל אייקון שמתנגש עם גלובל — `Map as MapIcon`.
3. **פס-צד בעובי 4px הוא מילוי, לא אקסנט.** זה היה השורש של "יותר מדי צבע":
   `ListRow` החליף אותו בנקודה בעשרה מקומות. החריג היחיד שנשאר הוא
   `day-timeline`, ובמכוון — שם הקצה רץ לאורך הבלוק ולכן מקודד את משך הפריט.

## דפוסי עבודה שכדאי להמשיך איתם

1. **בדיקה חזותית ב-375 / 768 / 1024 / 1440 / 1920.** שני הרוחבים האחרונים
   נוספו בפאזה D, כי היא זו שהוסיפה משמעות מעל 1024.
2. **ניגודיות נמדדת ולא מוערכת.** ב-B5 נמצאו rose ו-peach ב-4.35 ו-3.89 מול סף
   AA; בפאזה D הקנבס נמדד מול לבן (1.16:1) לפני שנבחר.
3. **פונקציות טהורות נבדקות מול מקרי קצה** לפני חיבור למסך.
4. **route זמני / probe נמחק תמיד לפני commit.**
5. Claude בונה קומיטים על ענפי feature, לא ישירות על `main` (הגנת ענף).
6. עדכון `PROJECT_PLAN.md` (כולל יומן ההחלטות) אחרי כל צעד.
