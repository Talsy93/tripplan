# TripPlan — נקודת המשך לשיחה חדשה

> **איך להשתמש בקובץ הזה:** בתחילת שיחה חדשה, תגיד ל-Claude לקרוא את
> `PROJECT_PLAN.md`, `ARCHITECTURE.md`, ואת הקובץ הזה. אפשר למחוק את הקובץ הזה
> אחרי שהשיחה החדשה מתחילה — הוא סיכום זמני, לא מקור אמת (`PROJECT_PLAN.md` הוא).

## מצב נכון ל-2026-08-18

**פאזה C הושלמה במלואה — C1 עד C10.** אין עבודת פיתוח פתוחה בפרויקט כרגע.

- `main` מעודכן, נקי, `npm audit` = 0.
- ענף אחרון שנפתח: `docs/c9-verified-c10-recap` (עדכון תיעוד בלבד) — **טרם נדחף
  ונמזג**. צריך `git push -u origin docs/c9-verified-c10-recap` ואז PR + מיזוג
  דרך ה-UI של GitHub (אין `gh` CLI במכונה).
- יש ענף לא-מזוהה בשם `kuku` בריפו המקומי, מצביע על אותו קומיט כמו `main` בעבר —
  לא נגעתי בו, כדאי לבדוק מה הוא ואם אפשר למחוק.

## מגבלה חוזרת שחשוב לדעת עליה

**Claude לא יכול להריץ `git push` בעצמו** — הכלי חוסם פרומפט אינטראקטיבי
לסיסמה/טוקן. בכל שיחה, המשתמש הוא זה שמריץ `git push` בטרמינל שלו, ו-Claude
בונה את הענף/קומיט ונותן את הפקודה המדויקת להרצה.

## מה שנבנה בפאזה C (תמצית — הפירוט המלא ב-PROJECT_PLAN.md)

| # | מה | סטטוס |
| - | -- | ---- |
| C1 | שגיאות AI אמיתיות: מכסה מול תקלה + לוג בשרת | ✅ |
| C2 | הוספת מקום ידנית | ✅ |
| C3 | "איפה אני ישן" לכל יום (לילות נגזרים מהזמנות לינה) | ✅ |
| C4 | "איך מגיעים" — קישורי Google Maps directions מהלינה | ✅ |
| C5 | "הצג עוד יעדים" בתכנון (append, לא replace) | ✅ |
| C6 | סימון ברור בסרגל הניווט (pill מלא בטאב הפעיל) | ✅ |
| C7 | AI עמוס (503) כמצב שלישי, נפרד ממכסה ומתקלה | ✅ |
| C8 | ניהול הזמנות: דאבל-בוקינג, ביטול חינם, התראות מראש | ✅ |
| C9 | התראות פוש אמיתיות (Web Push, Vercel Cron, חינמי) | ✅ **אומת מקצה לקצה** — דחיפה אמיתית הגיעה לאייפון ב-2026-08-18 |
| C10 | תוקן שורש "שירות ה-AI עמוס" — לא Gemini, אלא ה-alias | ✅ |

### C9 — נקודות שכדאי לזכור אם עולה שאלה עליו
- **אין צורך ב-cron נוסף ב-GitHub Actions.** Vercel Cron כבר רץ ועובד. מניעת
  שליחה כפולה היא ברמת הנתונים (`cancel_notified_at`/`book_by_notified_at`),
  לא ברמת התזמון — אז גם cron שני לא היה מזיק בפועל, אבל גם לא נחוץ.
- באייפון פוש עובד **רק** כשהאתר מותקן למסך הבית (מגבלת iOS 16.4+), לא בטאב
  Safari רגיל. באנדרואיד עובד גם בטאב רגיל.
- 4 משתני סביבה נדרשים ב-Vercel: `NEXT_PUBLIC_VAPID_PUBLIC_KEY`,
  `VAPID_PRIVATE_KEY`, `CRON_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`. כולם כבר
  מוגדרים ועובדים.
- `NEXT_PUBLIC_*` נצרב בזמן build — הוספת משתנה כזה דורשת build חדש (לא
  מספיק "Redeploy" עם Build Cache).

### C10 — ממצא שכדאי לזכור
`gemini-flash-latest` (ה-alias) נכשל ב-503 בכ-2 מתוך 3 קריאות בגלל ניתוב
לרמת preview עמוסה. `MODEL` ב-`src/lib/ai/provider.ts` קבוע עכשיו על
`gemini-3.6-flash`. **כשגוגל יפרוש גם את השם הזה, יהיה צריך לעדכן ידנית** —
זו החלטה מודעת (ראו את ההערה בקוד).

## דפוסי עבודה שכדאי להמשיך איתם

1. **אימות בזמן ריצה, לא רק build.** בכל שינוי — מוחקים `.next` לפני `npm run
   dev` אחרי `npm run build` (הם חולקים תיקייה וזה שובר את שרת הפיתוח).
2. **פונקציות טהורות נבדקות מול מקרי קצה** (סקריפט זמני עם `node
   --experimental-strip-types`) **לפני** חיבור למסך.
3. **בדיקה חזותית ב-375/768/1440** על כל שינוי שנוגע ל-UI.
4. **route זמני / קובץ probe זמני נמחק תמיד בסוף** — לפני commit.
5. Claude בונה קומיטים על ענפי feature, לא ישירות על `main` (הגנת ענף).
6. עדכון `PROJECT_PLAN.md` (כולל יומן ההחלטות) אחרי כל צעד.

## מה שאין עדיין ברשימת "לעשות"

אין פריט פתוח בפאזה C. אם המשתמש מגיע עם בקשה חדשה, זו תהיה תחילתה של פאזה D
(או המשך פאזה C עם מספור חדש, כמו ש-C7/C8/C9/C10 נוספו תוך כדי עבודה ולא
מתכנון מראש — התבנית הזו כבר מבוססת בפרויקט).
