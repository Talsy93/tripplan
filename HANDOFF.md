# MyTrip — נקודת המשך לשיחה חדשה

> **איך משתמשים בקובץ הזה:** בתחילת שיחה חדשה, לבקש מ-Claude לקרוא את
> `PROJECT_PLAN.md`, `ARCHITECTURE.md` ואת הקובץ הזה. `PROJECT_PLAN.md` הוא מקור
> האמת; הקובץ הזה הוא תמונת מצב מתגלגלת ומתעדכן בסוף כל פאזה.

## מצב נכון ל-2026-08-24 (פאזה G)

**פאזות E ו-F מוזגו ל-`main` דרך PR #41 — הן בפרודקשן.**
**פאזה G נבנתה על ענף `feature/phase-g-search-map-flights` וממתינה ל-PR.**

| מה | קובץ מרכזי |
| -- | ---------- |
| חיפוש אזור בתוך יעד (Omotesando) — OSM + AI | `route-service.ts`, `place-search.tsx` |
| מיקום עיר נגזר מהמקומות בפועל במקום מגאוקודינג | `route-service.ts` |
| סיכה לכל מקום על המפה, לא רק לערים | `route-map-canvas.tsx` |
| טיסות ורכבות על ציר הלו״ז | `domain/timeline.ts`, `day-timeline.tsx` |

**⚠️ שני באגים אמיתיים במפה שנמצאו בקריאת הקוד — כדאי להכיר את שניהם:**

1. **`cacheCoordinates` היה `update` חשוף, ולערים בלי שורת `overview` הוא לא
   כתב כלום.** שורת overview נוצרת רק ב-`saveCityGuide`, כלומר רק כשנפתח מדריך
   העיר. עיר שנכנסה דרך חיפוש האטרקציות לא הייתה לה שורה כזו, ו-Postgres מדווח
   על update שלא התאים לשום שורה כהצלחה — כך שהקואורדינטות גואוקדו מחדש בכל
   רינדור, **ותיקון הסיכה של פאזה F כתב לאוויר.** זו הסיבה שסיכה שתוקנה חזרה
   להיות שגויה. תוקן ל-`upsert`.
2. **המפה זרקה את הנתון המדויק ביותר שיש.** מקום מחיפוש האטרקציות נושא את
   הקואורדינטות של OSM עצמו — השורות היחידות שלא עברו גאוקודינג ולכן היחידות
   שלא יכולות להיות שגויות. עכשיו מיקום העיר נגזר מהחציון שלהן, ונופל
   לגאוקודינג רק אם אין אף מקום כזה.

**הכלל שנלמד:** `update` ב-Supabase שאמור לפגוע בשורה אחת — לבדוק `count`, או
להשתמש ב-`upsert`. כשל שקט בכתיבה נראה בדיוק כמו באג בקריאה.

## מצב נכון ל-2026-08-24

**שם האפליקציה הוא MyTrip** (שונה מ-TripPlan ב-2026-08-24). כל מה שמשתמש רואה
עודכן. **מה שלא שונה ודורש פעולה שלך:** שם הריפו ב-GitHub, שם פרויקט ה-Vercel,
והכתובת `tripplan-ten.vercel.app`. מזהים פנימיים (`aiTripPlanSchema`, ה-tag
`tripplan-reminder`, `tripplan-dev` ב-launch.json) נשארו בכוונה — ראו את יומן
ההחלטות.

**⚠️ ה-PWA המותקן באייפון לא יתעדכן לבד.** `manifest.name` השתנה, אבל iOS
קורא את המניפסט בזמן ההתקנה ולא אחריה — האייקון במסך הבית ימשיך לומר
"TripPlan" עד שתסיר ותתקין מחדש. **והסרה מוחקת את מנוי הפוש** (C9), כך שאחרי
התקנה מחדש צריך להפעיל שוב "תזכורות למכשיר" ב-״עוד״ → פרטי הטיול.


**פאזות E ו-F נבנו על ענף `feature/phase-e-trip-fixes`, ממתינות ל-PR.**
פאזה C הושלמה (C1–C10). פאזה D הושלמה (D1–D11) ומוזגה כולה ל-`main` דרך
PRs #33–#37. המשתמש העלה לפרודקשן והריץ את `migration 0013` ב-Supabase.

- **Migration 0014** (עלות הזמנה) — ✅ רץ (אושר על ידי המשתמש 2026-08-20).
- **Migration 0015** (מדינה לעיר + שיתוף פומבי) — ⏳ **עדיין לא רץ.** בלעדיו:
  אין קיבוץ לפי מדינות, אין תיקון אוטומטי לסיכות שגויות, ועמוד השיתוף מחזיר
  404 תמיד. הקוד מטפל בהיעדר העמודה בחן (`isSchemaOutOfDate`), אז המפה
  ממשיכה להיצייר — היא פשוט לא יודעת מדינות.
- `main` מסונכרן מול origin. ענף פתוח יחיד: `feature/phase-e-trip-fixes`.
- **⚠️ ה-PR של פאזה E מעולם לא נפתח**, ולכן שום דבר מ-E או מ-F אינו בפרודקשן.
  זו הסיבה שהמשתמש דיווח "אני עדיין לא רואה את השינוי". פאזה F נבנתה על אותו
  ענף במכוון — שני PRs שאחד חוסם את השני אינם שווים את הפיצול.
- עבודה חדשה מתחילה מענף חדש מעל `main` (יש branch protection: חובה PR +
  status check `Type-check & Lint`).

## מה שנבנה בפאזה F

תשעה דיווחים נוספים. הפירוט המלא ב-PROJECT_PLAN.md, פרק "פאזה F".

| מה | קובץ מרכזי |
| -- | ---------- |
| עמוד נחיתה: נקודות המכירה כבר לא נראות כפתורים | `app/page.tsx` |
| מטבע ₪/$/€ עם ברירת מחדל שקל, וסימנים במקום קודים | `domain/expenses.ts` |
| סינון ההוצאות לפי יעד | `components/expense-summary.tsx` |
| חלוקת התחנות לפי מדינות | `domain/route.ts`, migration 0015 |
| קישור בין טיסות קונקשן + זמן המתנה | `domain/booking.ts` |
| שיתוף פומבי לצפייה בלבד | `domain/share.ts`, `app/share/[token]` |
| מסלול לפי קרבה גאוגרפית | `lib/geo.ts` |
| כל הימים בלו״ז + הצעות AI ליום ריק | `components/day-suggestions-dialog.tsx` |
| תיקון סיכות שנחתו במדינה הלא נכונה | `infrastructure/route-service.ts` |

**התובנה המרכזית: שלושה מהפריטים היו אותה בעיה.** מדינות, מפה שבורה וקרבה
גאוגרפית — לשלושתם חסר אותו נתון. גאוקודינג של שם עברי הוא ניחוש; **גאוקודינג
הפוך** (קואורדינטות → מדינה) הוא ודאי. מרגע שיש מדינה לכל עיר, סיכה שנחתה מחוץ
למדינת הרוב בולטת מיד ומתוקנת אוטומטית.

**⚠️ עמוד השיתוף לא נבדק בריצה אמיתית** — הוא קורא דרך `createAdminClient`,
ו-`SUPABASE_SERVICE_ROLE_KEY` קיים רק ב-Vercel ולא ב-`.env.local`. מה שכן אומת
מקומית: `/share/<טוקן שלא קיים>` ו-`/share/<מעוות>` שניהם 404 (זהים במכוון),
ו-`/trips/x` עדיין 307 ל-login. **הדבר הראשון לבדוק בפרודקשן אחרי המיזוג:**
ליצור קישור שיתוף ולפתוח אותו בחלון פרטי.

## מה שנבנה בפאזה E

תשעה דיווחי משתמש מתוך שימוש אמיתי בטיול, כולם בענף אחד. הפירוט המלא
(כולל הכרעות והתקלה שנתפסה בבנייה) ב-PROJECT_PLAN.md, פרק "פאזה E". בקצרה:

| מה | קובץ מרכזי |
| -- | ---------- |
| שכונה (שיבויה) לא יוצרת עיר כפולה — AI guard + מיזוג לפי מרחק | `guide-service.ts`, `ai-suggestion.ts` |
| טוסט פינתי אחרי הוספת/עדכון הזמנה | `components/ui/toast.tsx` |
| שם היעד דביק בגלילת תוצאות | `place-search.tsx` |
| "עוד תוצאות" + "חיפוש ליד כאן" באטרקציות | `place-search.tsx`, `overpass.ts` |
| עלות הזמנה + מחשבון הוצאות (בלי המרת מטבעות) | `domain/expenses.ts`, migration 0014 |
| עריכת הזמנה קיימת | `booking-form.tsx`, `booking-list.tsx` |
| לו"ז: בלוק ימים דטרמיניסטי לפי עיר + מודע לזמן נחיתה | `trip-days.ts`, `itinerary-plan.ts` |

**התיקון המהותי ביותר:** בניית הלו"ז עברה מ"בקש מהמודל לכבד חלוקת ימים בטקסט
חופשי" ל"תן לו עובדה קבועה, ותקן בקוד כל דבר שהוא בכל זאת מפר" —
`buildDayCityPlan` בונה מפה יום→עיר מתאריכי הזמנות אמיתיים (לינה + זמן נחיתה
של הזמנת התחבורה המוקדמת ביותר), ו-`reconcileItineraryWithDayPlan` מתקן כל
פריט שחזר מהמודל ביום שסותר את זה — בלי תלות בציות המודל. פועל רק לטיול עם
תאריך התחלה+סיום קבועים; בלעדיהם חוזר בדיוק להתנהגות הקודמת.

**⚠️ תקלה שכדאי לזכור: circular import שהפיל את ה-build בלי type-check/lint
לתפוס אותו.** `ai-suggestion.ts` ↔ `place.ts` ייבאו זה מזה; `next build`
(Turbopack) נכשל עם `ReferenceError` בקובץ שלישי לא-קשור. `distanceKm` עבר
ל-`lib/geo.ts` ניטרלי. הלקח: type-check ולינט לא בודקים סדר הערכה של מודולים —
`next build` הוא הבודק היחיד, ולכן הוא חובה לפני שקוראים לשינוי גמור.

## מה שנבנה בפאזה D

עיצוב בכיוון **Booking.com**, שנבחר על ידי המשתמש אחרי שראה השוואה חזותית של
המצב הקיים מול שלושה כיוונים.

| # | מה | סטטוס |
| - | -- | ---- |
| D1 | טוקנים v3: קנבס `#eceef2` (1.16:1 מול לבן, נמדד), שלשות תפקיד בסגנון BUI, סולם 12/14/16/20/24/32/48, `line-height: 1.7` לעברית, סקרימים כטוקנים | ✅ |
| D2 | פרימיטיבים: `SectionHeading`/`ListRow`/`Banner`/`Dialog`/`Select`/`Field`/`Surface`/`ToneDot`; `Card` עם padding; 5 שיבוטים נמחקו | ✅ |
| D3 | `AppShell` עם sidebar בנייבי מ-1024px; `AppHeader` חולץ; ניווט = `NavItem[]` אחד בשלוש מצגות | ✅ |
| D4 | שתי חלוניות: `/map` (מפה + לוח נגלל בנפרד), `/days` (אינדקס ימים sticky) | ✅ |
| D5 | כל המסכים לפי הדקדוק החדש; "טיול חדש" עבר לדיאלוג | ✅ |
| D6 | `not-found`/`error`/`global-error`, כותרות דפדפן לכל route, יישור skeletons | ✅ |
| D8 | מחיקת טיול, עם אישור שמפרט מה נמחק | ✅ |
| D9 | ימים בכל עיר: נגזר מהלינה, נדרס ידנית, נכנס ל-prompt כאילוץ | ✅ |
| D10 | עריכת פריט בלוח: שעות, יום, הערה, "איך מגיעים" + זמן הגעה | ✅ |
| D11 | סימון יום דל והפניה להוספת פעילויות | ✅ |
| D7 | **אימות ויזואלי של המסכים המוגנים** | ⏳ **הפריט היחיד שנשאר** |

## מה שלא אומת בריצה — קרא את זה לפני שאתה מניח שמשהו עובד

Claude לא יכול להיכנס לאפליקציה: ה-proxy מפנה כל route שאינו `/`, `/login`
ו-`/signup` אל `/login`, ואסור לו להזין סיסמה. לכן **כל מה שמאחורי התחברות נבנה,
עבר type-check/lint/build, ולא הופעל אף פעם**:

- מחיקת טיול (בלתי הפיכה — כדאי לנסות על טיול שלא חבל עליו).
- עורך "ימים בכל עיר" ודיאלוג עריכת הפריט.
- הזרימה של בניית לו"ז עם האילוץ החדש.

**וכל תשעת פריטי פאזה E** (הטוסט, ה-sticky, "עוד תוצאות"/"חיפוש ליד כאן",
עריכת הזמנה, מחשבון ההוצאות, ומיזוג היעדים) — הפונקציות הטהורות שמאחורי בניית
הלו"ז נבדקו מול מקרי קצה דרך route זמני שנמחק (ראו PROJECT_PLAN.md), אבל אף
מסך לא נפתח בדפדפן מאומת. מיזוג היעדים (E1) דורש בנוסף קריאת Gemini אמיתית,
שלא הורצה — ראו את הכלל למטה על 20 הבקשות לדקה.

מה שכן נמדד בדפדפן על שרת ה-dev: קנבס `rgb(236,238,242)`, כרטיס לבן ברדיוס
12px, גובה כותרת 56px (ה-offset שה-sidebar מסתמך עליו), `letter-spacing: normal`,
`line-height` של טקסט גוף **1.70 בדיוק**, אפס שגיאות קונסול, ו-`next build` עובר
עם 16 עמודים.

**מה שכדאי לפתוח קודם, לפי סדר החשיבות:** `/trips/<id>/map` ב-1440 (שם
שתי-החלוניות, והמקום הסביר ביותר שמשהו ייראה לא נכון) · `/days` ב-1440 (אינדקס
הימים + עורך הימים בעיר) · `/profile` · חמשת הטאבים ב-375.

## מגבלות ותקלות שכבר שילמנו עליהן

**`git push` עובד.** ההנחה הקודמת שהכלי חוסם אותו הייתה שגויה — יש credential
helper (`manager`). מה שכן נכשל היה
`GH007: Your push would publish a private email address`. **תוקן:** `user.email`
בריפו הזה הוא `304832351+Talsy93@users.noreply.github.com`. אין צורך לבקש
מהמשתמש לדחוף. ל-`main` עדיין נכנסים רק דרך PR.

**Gemini free tier = 20 בקשות לדקה, לכל הפרויקט, משותף.** 429 מ-Google מוחזר גם
למגבלה לדקה וגם למכסה יומית, והגוף האמיתי (נתפס 2026-08-18) **לא** מכיל
`retryDelay` ולא `quotaId` שמזכיר חלון — ה-delay בפרוזה: `Please retry in
15.659996788s.`. `src/lib/ai/quota.ts` מסווג לפי זה, והגוף האמיתי הוא מקרה בדיקה.
**אין להריץ קריאות אמיתיות ל-Gemini בלי לומר למשתמש קודם** — זה גוזל מאותן 20
ויכול להפיל לו את האפליקציה תוך כדי שהוא בודק.

**`@theme inline` לא פולט את המשתנה ל-`:root`.** זו המשמעות של `inline`. קוד
שקורא טוקן דרך `var()` מחוץ ל-utilities של Tailwind — למשל מחרוזת ה-CSS שבונה את
הסיכה של Leaflet — לא יראה אותו. לכן הצללים ב-`:root` נקראים `--elevation-*`
ומופים ל-`--shadow-*`; השם **חייב** להיות שונה, כי
`--shadow-soft: var(--shadow-soft)` היא הגדרה מעגלית.

**`import { Map } from "lucide-react"` שובר `new Map()`** באותו קובץ, והשגיאה
("'new' expression whose target lacks a construct signature") לא מרמזת על השורש.
הכלל: alias לכל אייקון שמתנגש עם גלובל — `Map as MapIcon`.

**פס-צד בעובי 4px הוא מילוי, לא אקסנט.** זה היה השורש של עומס הצבע. `ListRow`
החליף אותו בנקודה בעשרה מקומות. החריג היחיד הוא `day-timeline`, ובמכוון — שם
הקצה רץ לאורך הבלוק ולכן מקודד את משך הפריט.

**`npm run build` ו-`next dev` חולקים את `.next`.** אחרי build למחוק את התיקייה
לפני `npm run dev`, אחרת מתקבל מפל `Module not found` שנראה כמו התקנה שבורה.

**פורט 3000 תפוס על ידי פרויקט אחר (PriceDrop).** `.claude/launch.json` עם
`"autoPort": true`, כך ש-`preview_start` מקבל פורט פנוי. OAuth של Google צריך
callback ל-3000 — התחברות באימייל+סיסמה עובדת בכל פורט.

## אופן העבודה שהמשתמש ביקש

**בחתיכות גדולות, בריצה אחת, בלי אישור לכל פריט.** זה גובר על
"צעד-צעד באישור" שב-`CLAUDE.md`, שנכתב לפני הבקשה הזו. את **הכיוון** כן מחליטים
יחד — ועדיף להראות אפשרויות חזותית מלהסביר אותן בטקסט, כי זה מה ששחרר את הבחירה
בפאזה D. לשאול רק כשהתשובה משנה מהותית את התוצאה ואי אפשר לתקן בזול.

## דפוסי עבודה שכדאי להמשיך איתם

1. **פונקציה טהורה נבדקת מול מקרי קצה לפני חיבור למסך.** זה החזיר את ההשקעה
   פעמיים בפאזה D: 22 מקרים ב-`city-days` (ואחד גילה שהציפייה שלי שגויה, לא
   הקוד), ו-16 ב-`quota` (שם התיקון הראשון נכתב מול צורה **משוערת** של גוף
   השגיאה ולכן לא עבד — עד שנתפס הגוף האמיתי).
2. **לא לנחש צורה של נתון חיצוני — למדוד אותה.** ראה למעלה.
3. **בדיקה חזותית ב-375 / 768 / 1024 / 1440 / 1920.**
4. **ניגודיות נמדדת ולא מוערכת** (ב-B5 נמצאו שני גוונים מתחת לסף AA).
5. **route זמני / probe נמחק תמיד לפני commit.**
6. עדכון `PROJECT_PLAN.md` כולל יומן ההחלטות אחרי כל צעד.
