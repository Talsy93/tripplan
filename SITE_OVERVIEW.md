# MyTrip — מבנה האתר והשימוש בו

> סקירה למי שרוצה להבין את האתר בעשר דקות: מה הוא עושה, מאיפה נכנסים, איך בנוי כל מסך, ואיך משתמש עובר מ״יש לי רעיון לטיול״ ל״אני עומד עכשיו בשוק בטוקיו״. מקורות האמת המפורטים הם `PROJECT_PLAN.md` (מצב הפרויקט ויומן ההחלטות) ו-`ARCHITECTURE.md` (כללי הקוד). עודכן ל-2026-09-11, אחרי המעבר לעיצוב ״מפה חיה״.

---

## 1. מה האתר עושה

אפליקציית ווב בעברית (RTL) לתכנון טיולים וללוויה בזמן הטיול.

- **תכנון:** אומרים לאן ולכמה זמן, מקבלים הצעות ליעדים ולמקומות (גם מ-AI), בוחרים, מזינים טיסות ולינה, והלו״ז נבנה לימים ולשעות.
- **בטיול:** מסך ״היום״ יודע איפה אתם עכשיו, מה הבא בתור, מה מזג האוויר ואיפה ישנים הלילה.
- **יחד:** טיול אפשר לשתף לצפייה בקישור, או להזמין חברים לעריכה.

טיול הוא מכונת מצבים: `planning → executing → completed`. המסכים מגיבים למצב הזה (לפני / בזמן / אחרי) ולא לדגלים.

**סטאק בקצרה:** Next.js (App Router) + TypeScript strict · Supabase (PostgreSQL, Auth, RLS) · Tailwind CSS v4 · Zod · Gemini דרך שכבת הפשטה · Leaflet + OpenStreetMap · Open-Meteo · Vercel. אין שירותים בתשלום.

---

## 2. מפת המסלולים

### ציבורי (בלי חשבון)

| מסלול | מה יש שם |
| --- | --- |
| `/` | דף נחיתה: הצעת הערך, כפתור התחברות/הרשמה, ״איך זה עובד״. |
| `/login`, `/signup` | התחברות והרשמה (Supabase Auth). |
| `/reset`, `/reset/confirm` | איפוס סיסמה. |
| `/privacy` | מדיניות פרטיות. |
| `/share/[token]` | **טיול משותף לצפייה** — לו״ז קריא בלי חשבון, לפי קישור שהבעלים יצר. |
| `/invite/[token]` | קבלת הזמנה להצטרף לטיול (דורש התחברות בסוף). |

### מחובר

| מסלול | מה יש שם |
| --- | --- |
| `/profile` | **הבית** — כל הטיולים על מפה אחת, ופאנל עם הטיול הקרוב ו״הצעד הבא״. |
| `/trips/[id]` | נקודת כניסה לטיול; מפנה לטאב ההתחלתי (״היום״ אם יש תאריך יציאה, אחרת ״יעדים״). |
| `/trips/[id]/today` | **היום** — ״עכשיו״, הבא בתור, מזג אוויר, לינה. לפני היציאה: ספירה לאחור ומה פתוח. |
| `/trips/[id]/days` | **ימים** — הלו״ז המלא, יום־יום, עם בנייה מחדש ועריכה. |
| `/trips/[id]/explore` | **יעדים** — חיפוש מקומות לפי קטגוריה, הצעות AI לערים, ״נבחרו לטיול״, הוספה ידנית. |
| `/trips/[id]/more` | **עוד** — תפריט לכל מה שלא יומיומי (טבלה למטה). |
| `/trips/[id]/map` | מפנה ל״ימים״. המפה כבר לא טאב — היא פתוחה לצד כל טאב. |
| `/trips/[id]/city/[city]` | **מדריך עיר** — אזורי לינה, מסעדות, אטרקציות וחוויות לעיר אחת, עם הוספה לטיול. |

#### תת-מסכי ״עוד״

| מסלול | תוכן |
| --- | --- |
| `more/trip` | **פרטי הטיול** — שם, תאריכים, טיסות, רכבות, מלונות (ההזמנות הן מה שקובע איזה יום שייך לאיזו עיר). מחיקת הטיול בתחתית. |
| `more/guides` | רשימת מדריכי הערים של הטיול. |
| `more/guide` | ״איך זה עובד״ — שמונת שלבי העבודה עם קישור לכל מסך. |
| `more/phrases` | שיחון בשפת היעד, עם תעתיק והשמעה. |
| `more/gear` | רשימת ציוד ואריזה (הדבר היחיד שממלאים לגמרי ביד). |
| `more/chat` | צ׳אט תכנון חופשי עם ה-AI, שיודע להפוך שיחה לתוכנית. |
| `more/members` | מי בא איתנו — הזמנות לפי אימייל, צפייה או עריכה. |
| `more/share` | קישור שיתוף פומבי לצפייה. |

### API (צד שרת בלבד)

| מסלול | תפקיד |
| --- | --- |
| `/api/ai/suggest` | הצעות ערים לפי תיאור חופשי. |
| `/api/ai/recommendations` | מקומות מומלצים לעיר/קטגוריה. |
| `/api/ai/city-guide` | מדריך עיר מובנה. |
| `/api/ai/itinerary` | בניית לו״ז לימים ולשעות. |
| `/api/ai/chat`, `/api/ai/plan-from-chat` | הצ׳אט, והמרת שיחה לתוכנית. |
| `/api/ai/phrasebook` | השיחון. |
| `/api/places/search` | חיפוש מקומות (OpenStreetMap / Overpass). |
| `/api/notifications/run` | הרצת התזכורות (Web Push). |
| `/auth/callback` | חזרה מ-Supabase Auth. |

כל תשובת AI היא Structured Output שעובר ולידציית Zod לפני שנוגע בדאטהבייס. המפתחות חיים במשתני סביבה בלבד.

---

## 3. איך נראה מסך (העיצוב ״מפה חיה״)

### הבית (`/profile`)

- **המפה** היא הרקע: כל טיול שיש לו קואורדינטות הוא נקודה. הטיול הקרוב כחול, טיול שרץ עכשיו ענבר, השאר אפורים.
- **הפאנל** צף מעל המפה (בדסקטופ) או עולה מלמטה כגליל (בטלפון). בו: כותרת ו״טיול חדש״; **כרטיס הטיול הקרוב** עם ספירה לאחור, הערים, כמה דברים נשארו לסגור, וכפתור **״הבא: …״** שמוביל ישר לפריט הפתוח הראשון; ושאר הטיולים כשורות ממוספרות (אותו מספר כמו על המפה).

### סדנת הטיול (`/trips/[id]/*`)

```
┌──────┬────────────────────────────────────┬──────────────────┐
│ רַיל │  כותרת: שם הטיול · תאריכים · מצב  │                  │
│ 64px ├────────────────────────────────────┤   המפה           │
│      │  היום · ימים · יעדים · עוד         │   (עמודה קבועה,  │
│ בית  │                                    │   352–480px,     │
│ טיול │  תוכן הטאב                         │   מתקפלת לפס)    │
│ מדרי │  (עד 66rem, שתי חלוניות            │                  │
│ צ׳אט │   כשיש מקום)                       │   סיכות ממוספרות │
│ ציוד │                                    │   סיכה חיה בענבר │
└──────┴────────────────────────────────────┴──────────────────┘
```

- **הרַיל** (דסקטופ): בית · הטיול · מדריכי הערים · הצ׳אט · ציוד, אייקונים בלבד, האות של המשתמש למטה.
- **הכותרת:** שם הטיול, תגית תאריכים, תגית מצב (״בעוד 13 ימים״ / ״יום 3 בטיול״), שיתוף.
- **הפאנל** הוא העמודה הראשית. שורת הטאבים יושבת בראשו. התוכן מגיב לרוחב הפאנל (container queries), לא לרוחב החלון.
- **המפה** נטענת פעם אחת בלייאאוט ונשארת בין טאבים. הסיכות כחולות וממוספרות לפי סדר התחנות; העיר של היום הנוכחי מסומנת בענבר עם הילה. כפתור בפינה מקפל אותה לפס, והבחירה נשמרת בדפדפן.
- **בטלפון:** המפה היא המסך, והפאנל הוא **גליל תחתון** (Bottom Sheet) עם שלוש נקודות עצירה (30 / 58 / 90% מהמסך). גוררים בידית או לוחצים עליה כדי לדלג. נפתח ב-90%, כך שהתוכן ראשון והמפה מציצה מלמעלה.

### שפת העיצוב

- **צבעים:** קנבס אפור-קריר, משטחים לבנים עם גבול של פיקסל, **כחול ניווט** אחד לכל פעולה, **ענבר** רק ל״עכשיו״ (סיכה חיה, הפריט שרץ, תזכורת), ירוק להצלחה, אדום לסכנה. אין צללים במנוחה — רק למה שמרחף מעל המפה.
- **טיפוגרפיה:** Rubik לכותרות ולמספרים (ספירה, מספרי סיכות), Noto Sans Hebrew לגוף. ספרות טבלאיות, בלי letter-spacing בעברית.
- **צורה:** רדיוס 10–14px, פינות 20px רק לגליל ולדיאלוגים. צ׳יפ נבחר הוא שחור (מצב), לא כחול (כחול לוחצים).
- **אייקונים:** `lucide-react` בלבד, גם לדומיין.

---

## 4. זרימת השימוש — מטיול ריק ללו״ז

אלה שמונת השלבים שמופיעים ב״עוד → איך זה עובד״, ולכל אחד יש מסך:

1. **תאריכים** (`more/trip`) — מספר הימים, הספירה לאחור והתחזית נגזרים מהם.
2. **יעדים** (`explore`) — מתארים מה בא לכם, מקבלים ערים מ-AI, או מוסיפים עיר בעצמכם.
3. **מה עושים בכל יעד** (`explore` → מדריכי הערים) — קטגוריות, חיפוש מקומות עם קואורדינטות אמיתיות, ״נבחרו לטיול״.
4. **טיסות ולינה** (`more/trip`) — לא רק תיעוד: הלו״ז מסתמך על הלינה כדי לדעת באיזו עיר אתם כל יום, ועל הטיסות כדי לדעת מתי מגיעים. טיסה עם קונקשן היא כרטיס אחד; חדר בסטנד-ביי לא נספר כלינה.
5. **בניית הלו״ז** (`days`) — ״בנה לו״ז״ מסדר את מה שנבחר לפי לינה, זמני טיסה וקרבה גאוגרפית. אפשר לקבוע ידנית ימים לכל עיר, לערוך פריטים, ולקבל רעיונות ליום ריק.
6. **אימות על המפה** — המפה פתוחה לצד כל טאב; תחנות לפי הסדר, סיכה לכל מקום. ״רענון המיקומים״ אם סיכה נחתה במקום הלא נכון.
7. **אריזה** (`more/gear`) — רשימות התחלה בלחיצה, סימון שלא מזיז פריטים, איפוס בסוף הטיול.
8. **בזמן הטיול** (`today`) — ״עכשיו״ עם ניווט, הבא בתור, מזג אוויר, לינה של הלילה, ותזכורות Push (באייפון רק אחרי התקנה למסך הבית).

**מה שמניע את המשתמש:** הבית מחשב לכל טיול ״פריטים פתוחים״ (אין תאריכים, אין יעדים, הלו״ז לא נבנה, ימים ריקים, אין טיסות…) ומציג את הראשון כ״הבא: …״. כך אף פעם לא צריך לזכור באיזה שלב עוצרים.

---

## 5. נתונים

| ישות | מה היא |
| --- | --- |
| `profiles` | המשתמש, מקושר ל-Auth של Supabase. |
| `trips` | טיול: שם, תאריכים, סטטוס, בעלים. |
| `suggested_destinations` | יעד/מקום מוצע לטיול: עיר, קטגוריה, קואורדינטות, מקור (AI / ידני / חיפוש), נבחר או לא. |
| `itinerary_items` | פריט בלו״ז: יום, שעת התחלה וסיום, סדר. |
| הזמנות, לינות, חברים, הזמנות הצטרפות, טוקני שיתוף, ציוד, מדריכי ערים שמורים, שיחון, קאש מיקומים | טבלאות תומכות; לכל אחת סכמת Zod אחת ב-`features/trips/domain`. |

- **הרשאות:** RLS בדאטהבייס. בעלים עורך; חבר עם הרשאת עריכה עורך; חבר צופה רואה; קישור שיתוף רואה בלי חשבון.
- **מיקומים:** ערים מגוקדות פעם אחת ונשמרות בקאש; מקומות מחיפוש מגיעים עם קואורדינטות מ-OpenStreetMap ולכן מדויקים תמיד.
- **מיגרציות:** `src/db/migrations`, ממוספרות 0001–0020; `src/db/check_migrations.sql` בודק מה מותקן.

---

## 6. מבנה הקוד

```
src/
├── app/                      # מסלולים בלבד (העמודים דקים, הלוגיקה בפיצ׳רים)
│   ├── profile/              # הבית
│   ├── trips/[id]/(tabs)/    # layout.tsx = סדנת הטיול; today · days · explore · more/*
│   ├── trips/[id]/city/      # מדריך עיר
│   ├── share/, invite/       # מסכים ציבוריים לפי טוקן
│   ├── api/                  # AI, מקומות, תזכורות — צד שרת
│   └── preview/              # הרמס פריוויו: כל רכיב בכל מצב, עם נתוני fixture (dev בלבד)
├── components/
│   ├── ui/                   # אטומים טיפשים: Button, Card, Chip, Badge, Dialog, Toast…
│   └── layout/               # AppHeader, IconRail, BottomSheet, SidePane, TwoPane, AppShell
├── features/
│   ├── auth/                 # התחברות, משתמש נוכחי
│   ├── profile/
│   └── trips/                # הפיצ׳ר הגדול
│       ├── domain/           # סכמות Zod, מכונת המצבים, לו״ז, מסלול, פריטים פתוחים…
│       ├── application/      # Server Actions
│       ├── infrastructure/   # Supabase, גיאוקודינג, מזג אוויר, AI
│       ├── components/       # TripWorkspace, WorkspaceMap, HomePanel, DayPager, NowCard…
│       └── index.ts          # נקודת הכניסה היחידה
├── lib/                      # ai/ (הפשטת Gemini), supabase, cn, maps
└── db/                       # סכמה ומיגרציות
```

**כללי הברזל** (הפירוט ב-`ARCHITECTURE.md`): פיצ׳ר מייבא מפיצ׳ר אחר רק דרך `index.ts`; קומפוננטה לא פונה ל-Supabase; סכמת Zod אחת לכל ישות; סודות רק בשרת; רוחב לפני עומק; `components/ui` לא מכיר ״טיול״.

**איך בודקים בלי חשבון:** `npm run dev` ואז `/preview` — אינדקס של עשרות סצנות (`workspace-v5`, `home-v5`, `explore-tab`, `days-tab`, `today-during`…) שמרנדרות את המסכים האמיתיים עם נתונים מזויפים, כולל המצבים שקשה להגיע אליהם (טיול ריק, עשרים הזמנות, עיר שלא נמצאה). בפרודקשן המסלול לא קיים.

**CI:** GitHub Actions מריץ `type-check` ו-`lint` על כל push; Vercel מפרסם מ-`main`.

---

## 7. מה בדרך

העיצוב מחדש (פאזה R ב-`PROJECT_PLAN.md`) בארבע פרוסות:

1. ✅ תשתית וקליפה — טוקנים, רַיל, סדנה, גליל, הבית כמפה.
2. ⬜ **סיכות משותפות** — ריחוף על שורה מדגיש את הסיכה ולהפך, סיכות לפריטי היום שנבחר, כרטיס ריחוף.
3. ⬜ **גרירה ליום** — מגש ״נבחרו ולא שובצו״, גרירה לתוך יום עם שעה לפי מרחק, פס ״כמה מהיום מלא״.
4. ⬜ **מצב לילה, דף נחיתה, ניקוי** — ערכת לילה לזמן הטיול, דף נחיתה שמראה את הסדנה, ומחיקת קוד העיצוב הקודם.
