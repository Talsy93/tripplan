# TripPlan — מסמך תוכנית ומעקב פרויקט

> **איך משתמשים בקובץ הזה:** זהו מקור האמת של הפרויקט. בתחילת כל שיחה חדשה עם Claude — מעלים את הקובץ הזה, ו-Claude יודע בדיוק איפה עצרנו.
> מסמנים `[x]` על כל משימה שהושלמה. מעדכנים את "השלב הנוכחי" בכל פעם שמתקדמים.
> הקובץ יושב בשורש הריפו ומתעדכן יחד עם הקוד.

---

## 🎯 מהות הפרויקט

אפליקציית ווב לתכנון טיולים. המשתמש יוצר פרופיל, יוצר טיולים, ולכל טיול שני מצבים: **תכנון** (קבלת הצעות ליעדים לפי דרישות וזמן, ובחירה מתוכן — כולל הצעות מ-AI) ו**ביצוע** (לו"ז מסודר של היעדים שנבחרו לפי זמנים). בפרופיל רואים את כל הטיולים, כולל תצוגה עתידית בגוגל מפות.

**מטרה כפולה:** אפליקציה שימושית + פרויקט ברמה גבוהה לתיק עבודות (Clean Architecture, TypeScript מלא, CI/CD).

---

## 🏗️ החלטות ארכיטקטורה (סגורות)

| תחום            | החלטה                                   | נימוק                                              |
| --------------- | --------------------------------------- | -------------------------------------------------- |
| פריימוורק       | Next.js + TypeScript (strict)           | Frontend + Backend בפרויקט אחד, סטנדרט תעשייה      |
| Database + Auth | Supabase (PostgreSQL)                   | חינמי, Auth מובנה, RLS להרשאות, SQL אמיתי          |
| אחסון ודיפלוי   | Vercel                                  | חיבור ישיר ל-GitHub, דיפלוי אוטומטי, URL חינמי     |
| CI              | GitHub Actions                          | lint + type-check על כל push                       |
| State מהשרת     | TanStack Query                          | ניהול data fetching, cache                         |
| State מקומי     | Zustand                                 | state של UI בלבד                                   |
| ולידציה         | Zod                                     | סכמה אחת = מקור אמת לטיפוסים, טפסים ו-API          |
| AI              | Gemini (דרך שכבת הפשטה)                 | Free tier נדיב; ההפשטה מאפשרת החלפת ספק בקובץ אחד  |
| פורמט תשובות AI | Structured Output (JSON + ולידציית Zod) | לא טקסט חופשי — נכנס ישר לדאטהבייס                 |
| מבנה ריפו       | ריפו יחיד (לא Monorepo)                 | אפליקציה אחת; מונורפו = מורכבות מיותרת (YAGNI)     |
| ארגון קוד       | Feature-Based                           | קוד שמשתנה יחד גר יחד; הסטנדרט המקובל              |
| קומפוננטות UI   | שתי רמות: ui גנרי / פיצ'ר               | הרעיון מ-Atomic Design בלי הטקס; shadcn/ui לאטומים |

### מבנה תיקיות מחייב

```
src/
├── app/              # Routes ו-UI (כולל app/api — צד שרת)
├── components/
│   ├── ui/           # אטומים גנריים (לא מכירים את הדומיין)
│   └── layout/       # header, navigation
├── features/         # כל פיצ'ר = קופסה סגורה
│   └── <feature>/
│       ├── domain/         # טיפוסים, entities, סכמות Zod
│       ├── application/    # use-cases, hooks
│       ├── infrastructure/ # גישה ל-Supabase / ספקים חיצוניים
│       ├── components/     # קומפוננטות שמכירות את הדומיין
│       └── index.ts        # נקודת הכניסה היחידה של הפיצ'ר
├── lib/              # clients, utils משותפים
└── db/               # סכמת DB, migrations
```

### כללי ברזל (מונעי סלט)

הפירוט המלא בקובץ `ARCHITECTURE.md`. תמצית:

1. פיצ'ר לא מייבא מהקרביים של פיצ'ר אחר — רק מ-index.ts שלו.
2. אף קומפוננטה לא פונה ל-Supabase ישירות — הכול דרך infrastructure.
3. כל ישות = סכמת Zod אחת שממנה נגזר הכול.
4. מפתחות וסודות — רק במשתני סביבה בצד השרת. לעולם לא בקוד.
5. רוחב לפני עומק — פרוסה דקה מקצה לקצה קודם.
6. טיול הוא מכונת מצבים: `planning → executing → completed`.
7. components/ui נשאר "טיפש" — לא מכיר את הדומיין.

### מודל נתונים ראשוני (ישויות)

- **profiles** — נתוני משתמש (מקושר ל-auth של Supabase)
- **trips** — שייך למשתמש; שדות: שם, תאריכים, סטטוס
- **suggested_destinations** — יעד מוצע לטיול; שדות: שם, קואורדינטות, משך משוער, מקור (AI/ידני), נבחר/לא
- **itinerary_items** — יעד שנבחר בלו"ז; שדות: זמן התחלה, זמן סיום, סדר

---

## 📋 שלבי הפרויקט

**השלב הנוכחי: פאזה C (ראו את הטבלה בסוף המסמך). C1–C5 ו-C7 הושלמו.**
**C8 בעבודה — הדומיין נבנה ונבדק, ה-UI פתוח. נותרו גם C6 ו-C9.**
**⚠️ `migration 0011` נכתב ו*טרם הורץ* — צריך להריץ אותו לפני שה-UI של C8 יעבוד.**

> **↪️ המשך בשיחה חדשה:** קרא קובץ זה + `ARCHITECTURE.md`. אופן העבודה (עברית, הסבר לפני ביצוע, צעד-צעד באישור, פרוסות דקות, PR לכל פיצ'ר, **אין שירותים בתשלום**) מוגדר ב-`CLAUDE.md` ובזיכרון. אחרי כל צעד: לעדכן קובץ זה.
>
> **✅ מצב הענפים (2026-08-13): הכול ממוזג.** שלושת הענפים שהמתינו כאן —
> `feature/design-phase-b`, `chore/security-updates` ו-`feature/phase-c` —
> מוזגו ל-`main` (PRs #20–#22). אין ענפים פתוחים.
>
> אין `gh` CLI במכונה — פתיחת PR ומיזוג דרך ה-UI של GitHub. יש branch protection
> על `main`: חובה PR + status check `Type-check & Lint`.
>
> **✅ `0010_trip_chat.sql` הורץ** (אושר על ידי המשתמש 2026-08-13). migrations 0001–0010 כולם בתוקף.
>
> _תוקן 2026-08-12: שתי אזהרות שהיו כאן היו שגויות. **שלב 17 כן מוזג** (PRs #17 ו-#18, `git log main --grep=trip-chat`), וכל קבצי הצ'אט כולל ה-migration נמצאים ב-main. הענף `feature/maps-links` כבר לא קיים._
>
> **מה קיים היום (אחרי פאזה B).** עמוד הטיול הוא חמישה מסלולים: `/trips/<id>/today` (ספירה לאחור לפני הטיול, לו"ז היום הנוכחי בזמנו) · `/days` (הלו"ז המלא עם תאריכים) · `/explore` (רשת קטגוריות, חיפוש OSM, נבחרים, גילוי יעדים) · `/map` (Leaflet + תחנות) · `/more` (תפריט → `/more/trip`, `/more/phrases`, `/more/chat`). `/trips/<id>` הוא redirect. בנוסף: `/profile` (hero + רשימת טיולים), `/trips/<id>/city/<city>` (מדריך עיר).
>
> **ספקים חיצוניים — כולם חינמיים ובלי מפתח, למעט Gemini:** Gemini (AI), OpenStreetMap Overpass (מקומות), Nominatim + ויקיפדיה (גאוקודינג ותמונות), Open-Meteo (מזג אוויר), אריחי OSM (מפה).
>
> **חוב ופריטים פתוחים:**
> - ~~**פגיעויות npm**~~ — **נסגרו 2026-08-12** (ענף `chore/security-updates`). `npm audit fix` סגר את `brace-expansion`, `js-yaml` ו-`nanoid`; שדרוג Next ל-16.3.0 סגר את `postcss` ו-`sharp`. `npm audit` מחזיר **0**
>
> ⚠️ **מלכודת סביבה שעלתה תוך כדי, שווה לדעת:** `npm run build` ו-`next dev` חולקים את `.next`, ואחרי build תיקיית פרודקשן נשארת שם. הפעלת dev עליה מייצרת מפל של `Module not found` על הפנימיים של Next עצמו — שנראה בדיוק כמו התקנה שבורה. **אבחנתי את זה לא נכון בהתחלה** והאשמתי את שדרוג Next, ביטלתי אותו, ורק אז גיליתי שהאפליקציה נשברה גם בלעדיו. הכלל: אחרי `npm run build`, לעצור שרתים ולמחוק `.next` לפני `npm run dev`. וגם — הקונסול של הדפדפן בכלי הפיתוח צובר שגיאות מכל הסשן ולא מתנקה בין הפעלות שרת, אז לאימות עדיף `fetch` שבודק את התשובה עצמה
> - **ביקורות למקומות** — ל-OSM אין דירוגים. חלופה: Foursquare free tier (התוכנית מתירה free tier של שירותים מסחריים), דורש שהמשתמש ייצור API key. לא נבחר
> - **תמונות לכרטיסי מקום** — ראו את ההערה על B6 בסוף המסמך
> - פריטים שנדחו במפורש מסומנים `[ ]` בשלבים 8 ו-9
> - ~~**העיצוב מעולם לא עבר סבב ייעודי**~~ — נסגר בפאזה B
>
> **מגבלות מוצהרות שכדאי להכיר לפני שמתכננים מחדש:**
> - **תחבורה בין נקודות** — מרחק מחושב רק כששני הקצוות מגיעים מחיפוש OSM; פריטי מדריך AI הם שם בלבד. ניתוב תחבורה ציבורית לא קיים בחינם **כ-API**, ולכן C4 מעביר את השאלה ל-Google Maps בקישור directions מהלינה לכל יעד — האפליקציה עצמה עדיין לא מחשבת מסלול
> - **מזג אוויר** — 16 יום קדימה, תקרה קשיחה של Open-Meteo
> - **גאוקודינג ערים** — ויקיפדיה עברית; לעיר בלי ערך ייעודי הסיכה עלולה ליפול על אתר מוכר בעיר ולא על מרכזה
> - **שיוך פריט↔עיר בלו"ז** — לפי שם. אם ה-AI ינסח שם מחדש, הפריט יישאר בלי עיר

### שלב 0 — הרשמות והכנת סביבה ✅ הושלם

- [x] התקנת Node.js (גרסת LTS)
- [x] התקנת Git
- [x] חשבון GitHub
- [x] חשבון Vercel (הרשמה דרך GitHub)
- [x] חשבון Supabase (הרשמה דרך GitHub)
- [x] מפתח API של Gemini (דרך Google AI Studio)
- [x] שמירת כל המפתחות במקום בטוח (לא בקוד!)

### שלב 1 — שלד הפרויקט ✅ הושלם

- [x] יצירת פרויקט Next.js + TypeScript (דרך create-next-app)
- [x] הרצה מקומית מוצלחת (localhost:3000)
- [x] הוספת חבילת הקבצים: מבנה תיקיות, ARCHITECTURE.md, .env.example
- [x] הפעלת strict mode ב-tsconfig (בדיקה שקיים)
- [x] ריפו ב-GitHub + push ראשון

### שלב 2 — דיפלוי ראשון ✅ הושלם

- [x] חיבור הריפו ל-Vercel
- [x] עמוד "Hello TripPlan" עולה לאוויר
- [x] וידוא: push ל-main ⟵ דיפלוי אוטומטי

### שלב 3 — Supabase ו-Auth ✅ הושלם

- [x] יצירת פרויקט Supabase (אזור: eu-central)
- [x] משתני סביבה (מקומית + ב-Vercel)
- [x] הרשמה / התחברות / התנתקות (אימייל+סיסמה + Google OAuth)
- [x] middleware להגנה על עמודים (`src/proxy.ts` — שם ה-middleware ב-Next.js 16)
- [x] טבלת profiles + יצירה אוטומטית בהרשמה (RLS + trigger, ב-`src/db/migrations/0001_profiles.sql`)

### שלב 4 — Database Design ✅ הושלם

- [x] טבלת trips עם שדה status
- [x] טבלאות suggested_destinations + itinerary_items
- [x] RLS policies — כל משתמש רואה רק את שלו
- [x] סכמות Zod תואמות לכל ישות

### שלב 5 — CI ✅ הושלם

- [x] GitHub Actions: type-check + lint על כל push
- [x] בדיקה שה-CI חוסם merge כשיש שגיאות (branch protection על main: PR + status check `Type-check & Lint`)

### שלב 6 — פרוסה דקה מקצה לקצה ✅ הושלם

- [x] יצירת טיול ריק מה-UI
- [x] שמירה בדאטהבייס
- [x] הצגת רשימת הטיולים בפרופיל

### שלב 7 — תשתית AI ✅ הושלם

- [x] שכבת הפשטה לספק AI
- [x] route בשם /api/ai/suggest
- [x] Structured Output + ולידציית Zod
- [x] Rate limiting בסיסי

### שלב 8 — מסך תכנון (הצעות יעדים) ✅ הושלם

- [x] עמוד טיול בודד `/trips/[id]`
- [x] הצעות ערים מ-AI (שכבה 1) + קליק לעמוד עיר
- [x] עמוד עיר `/trips/[id]/city/[city]` עם מדריך עיר עשיר (שכבה 2): רקע על העיר, "איך מגיעים", ואזורי לינה/מסעדות/אטרקציות/חוויות
- [x] בחירת פריטים ("הוסף לטיול") + הצגתם בעמוד הטיול
- [x] הוספת נקודת עניין ידנית (source=manual) — נדחה בשלב 8, **סופק ב-C2 (2026-08-13)**
- [ ] תמונות ליעדים/המלצות — נדחה (יחד עם Maps/Places בשלב 10)

### שלב 9 — מסך ביצוע (לו"ז) ✅ ליבה הושלמה

- [x] לו"ז יומי: ה-AI מסדר את הפריטים שנבחרו לימים ולפי שעות (מעבר ל-`executing`)
- [x] מחיקת פריט מהלו"ז + הסרה מ"מה שבחרתם" (ביטול בחירה)
- [x] המסלול המומלץ (סדר גאוגרפי/זמן הגיוני) — כפתור "פתח ב-Google Maps" בשלב 10
- [ ] הערות אישיות לעריכה (כרגע מוצגת הערת AI לכל פריט) — נדחה
- [ ] מילוי "חורים" בלו"ז — הצעות למשבצות ריקות (למשל ארוחת בוקר לפני אטרקציה שמתחילה ב-12:00) — רעיון להמשך

### שלב 10 — ניווט ומפה (חינמי) ✅ חלקי

> **כלל הפרויקט:** אין שירותים בתשלום. Google Maps Platform (דורש חיוב) הוחלף ב-OpenStreetMap + קישורי URL חינמיים ל-Google Maps.

- [x] קישורי "פתח ב-Google Maps" לכל פריט (לו"ז, נבחרים, מדריך עיר) — קישור URL, חינם
- [ ] מפה אינטראקטיבית עם סיכות (OpenStreetMap + Leaflet, חינם, בלי מפתח) — יבוצע בשלב 12 (טאב מפת מסלול)
- [x] דרכי הגעה/ניווט (קישורי Google Maps directions, או OSRM חינמי) — **סופק ב-C4 (2026-08-13)**: קישורי directions מהלינה לכל יעד, `travelmode=transit`

---

> **חזון חדש (2026-07-26):** המשתמש הוסיף רשימת פיצ'רים ל-UX ולפונקציונליות. קופלו כשלבים 11–17 למטה. **החלטות רוחביות שנסגרו:** (1) **אין שירותים בתשלום** — תמונות תמיד ממקור חינמי (Wikipedia REST API / Unsplash·Pexels free tier), חיפוש מקומות דרך OpenStreetMap Overpass, מזג אוויר דרך Open-Meteo. (2) **מפת מסלול** = תמונת "לנדינג" שעושה חשק (לא חייבת דיוק גאוגרפי), לצד מפה אינטראקטיבית אמיתית ללו"ז. (3) **סדר בנייה** — רוחב לפני עומק, פרוסה דקה בכל פעם; מתחילים משלב 11.
>
> **עיצוב (רוחבי, מתמשך):** ליטוש חוויית משתמש מודרנית וצעירה על בסיס פלטת Beige & Mocha הקיימת — צבעים חמים ורכים, פונט נעים, רספונסיבי, RTL. משתפרת עם כל שלב, לא כשלב נפרד.

### שלב 11 — תאריך יציאה וספירה לאחור ✅ הושלם (מוזג ב-PR #11, אומת ויזואלית)

> `trips.start_date` / `end_date` כבר קיימים ב-DB וב-`tripSchema` — אין צורך ב-migration.

- [x] בורר תאריך יציאה/חזרה בעמוד הטיול (בסוף התכנון) + Badge ספירה בכותרת
- [x] כרטיס "הטיול הקרוב" בפרופיל (מסך הבית): ספירת ימים עד היציאה
- [x] תמונת יעד חינמית בכרטיס (Wikipedia API, בלי מפתח; נפילה חיננית לגרדיאנט חם)

### שלב 12 — טאב מפת מסלול ✅ הושלם

**12א — מפה + geocoding** ✅ (ב-PR פתוח `feature/route-map`)

- [x] מפה אינטראקטיבית עם תחנות ממוספרות לפי סדר + קו מקווקו בין ערים — Leaflet + OSM
- [x] `src/lib/geocode.ts` — ויקיפדיה (he→en) ואז Nominatim כגיבוי, שניהם חינמיים ובלי מפתח
- [x] cache קואורדינטות בעמודות `latitude`/`longitude` הקיימות (שורת `overview` לכל עיר) — בלי migration חדש לסכמה
- [x] `Tabs` גנרי ב-`components/ui`; עמוד הטיול הורחב ל-`max-w-4xl`

> **סדר התחנות** נקבע לפי סדר ההוספה (`created_at`), לא לפי הלו"ז — כדי שהמפה תעבוד גם לפני שנבנה לו"ז. ישודרג ב-12ב.
>
> **דיוק:** לערים בעלות שם עברי הסיכה מגיעה מהערך המתאים בוויקיפדיה העברית ועשויה ליפול על אתר מוכר בעיר במקום על מרכזה (סטייה של קילומטרים בודדים). מספיק למפת מסלול ברמת ערים.

**12ב — לו"ז לצד המפה** ✅

- [x] migration 0006: עמודת `city` ל-`itinerary_items` + backfill ללו"ז קיים
- [x] `saveItinerary` מקבל את הפריטים שנבחרו וממפה `entry.name → city` (השוואה מנורמלת)
- [x] `itineraryItemSchema` יושרה למצב האמיתי של הטבלה (0002+0004+0006) — חוב שנסגר
- [x] לו"ז לצד המפה, ממוספר לפי מספרי התחנות + מספר לילות ליעד
- [x] סדר התחנות עבר מ-`created_at` לסדר הימים בלו"ז

> **ספירת לילות:** לילה = מעבר בין ימים, והיום האחרון של הטיול לא מוסיף לילה — ימים 1–3 ביעד = 3 ימים / 2 לילות.
>
> **יום שנוגע בכמה ערים** משויך לעיר של הפריט האחרון בו — שם ישנים.
>
> **סייג:** השיוך הוא לפי שם, כי ה-AI מתבקש להעתיק שמות מהרשימה. אם ינסח מחדש — הפריט יישאר בלי עיר ולא ייספר. הדרך ההרמטית היא להחזיר `city` מה-AI בסכמה.

**12ג — תמונת לנדינג** ✅

- [x] באנר בראש טאב המסלול: תמונת התחנה הראשונה + שם הטיול + "N תחנות · N ימים · N לילות"
- [x] אותה שפה ויזואלית כמו `UpcomingTripCard` במסך הבית (גרדיאנט כהה לקריאוּת, נפילה לגרדיאנט חם כשאין תמונה)
- [x] `routeSummary` בדומיין — ימים נספרים על פני כל המסלול, כך שיום מפוצל בין שתי ערים לא נספר פעמיים

### שלב 13 — חיפוש אטרקציות בזמן אמת ✅ הושלם

- [x] חיפוש לפי קטגוריות או חופשי — OSM Overpass (חינם, בלי מפתח); טאב שלישי "אטרקציות"
- [x] פתיחת תוצאה במודאל, סינון לפי יעד, "הוסף למסלול"
- [x] סימון על תוצאה שכבר במסלול — באיזה יום

> **שלוש עובדות על Overpass שנמדדו, לא נקראו בתיעוד** (ראו את ההערה בראש `src/lib/overpass.ts`):
> 1. **אין התאמת שם בשאילתה.** אין אינדקס על ערכי תגיות — `["name"~"x",i]` הוא סריקה: 57 שניות או 504. סינון לפי תגית מאונדקס (≈5ש'). השם מותאם ב-JS, וזה גם מחפש בכל וריאנטי השם.
> 2. **GET ולא POST.** Next לא שומר POST ב-cache, כך שה-`revalidate` היה חסר תוקף.
> 3. **504 הוא עומס רגעי, לא עלות.** אותה שאילתה נפלה ב-2 ק"מ והצליחה ב-5, ואותה שאילתת 5 ק"מ נפלה ופעלה בהפרש דקות. הרדיוס ומספר הקטגוריות לא מנבאים כלום → retry אחד.
>
> **שמות OSM הם בשפה המקומית** — הצגה: `name:he → name:en → name`; השם המקומי נשמר ומוצג במודאל.
>
> **סינון איכות:** 39% מהתוצאות הן שם ותו לא. מוצג רק מה שמוכר (ערך בוויקיפדיה) או בעל 2+ פרטים. דירוג: מוכר → עושר פרטים → מרחק.
>
> **⚠️ אין ביקורות ב-OSM.** מספרי ביקורות הם נכס מסחרי (Google/Foursquare). התג "מוכר" הוא התחליף הזמין. אם תרצו ביקורות אמיתיות — Foursquare free tier, ראו את פסקת ההחלטות.
>
> **`source='manual'`** — ערך ה-enum מ-0002 שלא היה בשימוש — הוא הבית של מקומות מהחיפוש. אלה השורות היחידות בטבלה שהקואורדינטות שלהן לא עברו גאוקודינג.

### שלב 14 — מסלול יומי גרפי ✅ הושלם

- [x] תצוגה גרפית של היום על ציר שעות — בלוקים לפי שעות, קווי שעה, מתג "ציר שעות/רשימה"
- [x] מעברים בין נקודות: פער הזמן + מרחק אווירי + זמן הליכה משוער איפה שיש קואורדינטות

> **הזמנים הם טקסט חופשי.** ה-prompt מבקש `"HH:MM"` ושום דבר לא אוכף. `parseTimeLabel` קולט `9:00`/`09.00`/`"בין 08:00 ל-09:00"` ודוחה `24:00`/`09:60`. פריט שזמנו לא נקרא **לא נזרק** — מופיע תחת "בלי שעה מוגדרת". סיום שקודם להתחלה מקבל בלוק ברירת מחדל.
>
> **⚠️ "תחבורה" סופקה חלקית, במכוון.** מרחק דורש קואורדינטות לשני הקצוות, ויש כאלה **רק** לפריטים שהגיעו מחיפוש האטרקציות (13ב) — פריטי מדריך AI הם שם בלבד. אז ברוב המעברים מוצג רק פער הזמן. **ניתוב תחבורה ציבורית לא קיים בחינם בכלל** — אין ספק. הליכה מוצגת רק עד 2.5 ק"מ (4.5 קמ"ש × 1.3 עיקוף רחובות); מעל זה לא נאמר כלום, כי מספר מומצא גרוע ממספר חסר.
>
> **הדרך לכיסוי מלא** אם ירצו: להחזיר קואורדינטות מה-AI בסכמת המדריך, או לגאוקד פריטי מדריך — שניהם יקרים ולא נבחרו.

### שלב 15 — פרטי טיול ולוגיסטיקה ✅ הושלם

- [x] ישויות חדשות: טיסות/רכבות/לינה — טבלה `trip_bookings` (migration 0008) + טאב "לוגיסטיקה"
- [x] הערות, מספרי אישור והתרעות על מה שמתקרב
- [x] תחזית מזג אוויר ליעדי הטיול (Open-Meteo, חינם, בלי מפתח)

> **טבלה אחת ולא שתיים.** טיסה, רכבת ומלון חולקים שלד — כותרת, טווח זמן, אישור, הערה — ונבדלים רק בכך שתחבורה צריכה מוצא+יעד ולינה כתובת אחת. זה עלה שתי עמודות nullable מול טבלה+שירות+RLS+UI כפולים.
>
> **התרעות נגזרות, לא נשמרות.** לשמור "בעוד 3 שעות" = להתחייב לסנכרן מול השעון.
>
> **⚠️ Open-Meteo — 16 ימים קדימה, קשיח.** מעבר לזה HTTP 400, לא תשובה ריקה. לכן `forecastWindow` מחליט לפני כל קריאת רשת, ולטיול רחוק מוצג "התחזית תהיה זמינה בעוד N ימים" — לא פאנל ריק שנראה שבור. טיול שחוצה את הקצה נחתך לחלק שניתן לחזות.
>
> **קואורדינטות למזג אוויר** מגיעות מה-cache של מפת המסלול — אפס גאוקודינג נוסף.

### שלב 16 — טאב מילים שימושיות ✅ הושלם

- [x] שיחון לפי נושאים: עברית / אנגלית / שפת היעד + **תעתיק בעברית** (דרך AI)
- [x] שפת היעד מוסקת מהערים בטיול — לא נשאלת מהמשתמש
- [x] נשמר ב-`trip_phrasebooks` (migration 0009) עם כפתור "בנה מחדש"

> **התעתיק הוא הפיצ'ר.** קורא עברית לא יכול לקרוא "ありがとう" — בלי שורת ההגייה, העמודה בשפת המקום היא קישוט. ה-prompt אומר את זה במפורש, וגם שפה באותיות לטיניות מקבלת תעתיק.
>
> **שפת היעד לא נשמרת על טיול** — האפליקציה מכירה ערים, לא מדינות. ה-AI מסיק אותה מהערים ומחזיר אותה, וזה נשמר, כך ששיחון תמיד יודע לומר באיזו שפה הוא.
>
> **`phrases` הוא jsonb ולא טבלה.** נקרא תמיד בשלמותו, אף פעם לא מסונן או מצורף — שורות היו קונות מבנה שאיש לא שואל עליו. הצורה נאכפת ב-Zod בכניסה **וביציאה**: מה שיש בעמודה הוא מה שנכתב אליה, ושינוי צורה צריך להתגלות כשיחון חסר ולא כקריסה באמצע רינדור.

### שלב 17 — שיחת AI לבניית מסלול ✅ הושלם

- [x] **17א** — צ'אט מתמשך שנשמר (`trip_chat_messages`, migration 0010) ומכיר את תוכן הטיול
- [x] **17ב** — "בנה מסלול מהשיחה": הפקה מובנית מהתמליל → תצוגה מקדימה → אישור → כתיבה

> **התוספת היחידה לתשתית ה-AI מאז שלב 7:** `generateText` (רב-תורי) לצד `generateStructured`. פונקציות נפרדות ולא דגל — אחת מחזירה טקסט לאדם, השנייה אובייקט מאומת ל-DB.
>
> **תורות = שורות** (בניגוד לשיחון ב-0009 שהוא jsonb): נוספות אחת-אחת ונקראות לפי סדר. שאלה ותשובה נכתבות יחד, כדי שלא תיווצר שיחה שבה המודל דיבר בלי שנשאל. ההיסטוריה חסומה ב-24 תורות — כל תור נשלח מחדש בכל בקשה.
>
> **⚠️ עיקרון: השיחה לא משנה את הטיול בעצמה.** הכפתור **מציע** בלבד; ה-route לא כותב כלום. תצוגה מקדימה עם summary למעלה חושפת קריאה שגויה לפני שנשמר משהו, ורק אישור שני כותב.
>
> **הכתיבה מוסיפה בלבד.** `saveCities` הקיים מוחק לפני שהוא כותב — נכון לרענון הצעות, שגוי כאן. `savePlanFromChat` נפרד ממנו: ערים חדשות בלבד, שורות overview ב-`ignoreDuplicates` כדי לא לדרוס intro קיים, ופריטים כן ב-upsert כדי לסמן `selected` גם על פריט שכבר היה.

---

## 📔 יומן החלטות

| תאריך      | החלטה / שינוי                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-14 | נסגר הסטאק: Next.js + Supabase + Vercel + Gemini. נוצר מסמך זה.                                                                    |
| 2026-07-14 | הושלם שלב 0 (הרשמות). הוחלט: ריפו יחיד (לא מונורפו), ארגון Feature-Based, קומפוננטות בשתי רמות עם shadcn/ui. נוסף ARCHITECTURE.md. |
| 2026-07-14 | הושלם שלב 1: נוצר פרויקט Next.js + TypeScript (strict), נוספה חבילת הארכיטקטורה, הריפו עלה ל-GitHub בחשבון האישי.                  |
| 2026-07-14 | הוגדרה הפרדת זהויות גיט במחשב העבודה: config מקומי לריפו (אימייל אישי) + credential.useHttpPath לניהול הרשאות לפי ריפו.            |
| 2026-07-14 | תוקן: תיקיית app הועברה לתוך src (git mv) + עדכון paths ב-tsconfig, בהתאם ל-ARCHITECTURE.md.                                       |
| 2026-07-14 | הושלם שלב 2: הריפו חובר ל-Vercel, האתר חי בכתובת tripplan-ten.vercel.app, אומת דיפלוי אוטומטי על push.                             |
| 2026-07-23 | נוצר CLAUDE.md (תמצות תוכנית + ארכיטקטורה + אופן עבודה). סומנו שני הסעיפים הראשונים של שלב 3 שבוצעו ידנית: פרויקט Supabase נוצר ומשתני הסביבה הוגדרו מקומית וב-Vercel. |
| 2026-07-23 | נבנה פיצ'ר auth (`src/features/auth`) לפי שכבות הארכיטקטורה: סכמת Zod, שכבת infrastructure מול Supabase (`@supabase/ssr`), Server Actions, טפסים, ועמודי login/signup. נוספה גם התחברות עם Google (OAuth/PKCE) עם Route Handler ב-`/auth/callback`. תוקן ערך שגוי ב-`.env.local` (הוסרה סיומת `/rest/v1/` מכתובת ה-Supabase). |
| 2026-07-23 | נוסף `src/proxy.ts` (ה-middleware בשמו החדש ב-Next.js 16) עם עוזר `src/lib/supabase/middleware.ts`: רענון session בכל בקשה + הפניית משתמש לא-מחובר ל-`/login`. עמודים ציבוריים: `/`, `/login`, `/signup`, `/auth/*`. |
| 2026-07-23 | הושלם שלב 3: נוצרה טבלת `public.profiles` עם RLS (כל משתמש רק את שלו) ו-trigger `on_auth_user_created` ליצירה אוטומטית בהרשמה (כולל שאיבת שם/תמונה מ-Google). ה-SQL תועד ב-`src/db/migrations/0001_profiles.sql` ונוצרה סכמת Zod ב-`src/features/profile`. |
| 2026-07-23 | האפליקציה הותאמה לעברית ו-RTL: `lang="he" dir="rtl"`, גופן Heebo (עברית+לטינית), תרגום כל הטקסטים, הודעות Zod ושגיאות Supabase בעברית. שדות אימייל/סיסמה נשארים LTR. |
| 2026-07-23 | הושלם שלב 4: נוצרו טבלאות `trips` (status כ-enum, תאריכים אופציונליים), `suggested_destinations`, `itinerary_items`, עם RLS (כל משתמש רק את שלו, תת-ישויות דרך הטיול). ה-SQL ב-`src/db/migrations/0002_trips.sql`, סכמות Zod ב-`src/features/trips` (שלוש הישויות כאגרגט טיול). |
| 2026-07-23 | נוסף CI ב-GitHub Actions (`.github/workflows/ci.yml`): type-check + lint על כל push/PR (Node 20). נוסף סקריפט `type-check`. אומת: ה-run הראשון על main הסתיים בהצלחה. |
| 2026-07-23 | הושלם שלב 5: הוגדר branch protection על `main` (ruleset "main-protection"): חובה PR + מעבר status check `Type-check & Lint` לפני merge. מעתה אין push ישיר ל-main — כל שינוי דרך branch + PR. |
| 2026-07-23 | הושלם שלב 6 (פרוסה דקה מקצה לקצה): פיצ'ר `trips` קיבל שכבות infrastructure/application/components — יצירת טיול (שם בלבד) ושמירה ב-Supabase, והצגת רשימת הטיולים בעמוד `/profile` המוגן. נבנה על branch `feature/trips-slice` (זרימת PR ראשונה). |
| 2026-07-23 | הושלם שלב 7 (תשתית AI): שכבת הפשטה `src/lib/ai` מעל `@google/genai` (מודל `gemini-flash-latest`), Structured Output דרך `z.toJSONSchema` + ולידציית Zod, route `POST /api/ai/suggest` עם אימות ו-rate limiting בסיסי (`src/lib/rate-limit.ts`). תוקן ה-proxy כך שנתיבי `/api` מחזירים 401 במקום הפניה ל-login. סכמות AI ב-`src/features/trips/domain`. |
| 2026-07-23 | שלב 8 (חלק): נבנתה חוויית גלישה דו-שכבתית — הצעות ערים (שכבה 1) ומדריך עיר מקוטלג (מלונות/מסעדות/אטרקציות/חוויות) עם "עוד תוצאות" לכל קטגוריה (שכבה 2). routes: `/api/ai/city-guide`, `/api/ai/recommendations`. מוזג ב-PR #3. |
| 2026-07-23 | פאס עיצוב: מערכת עיצוב עם פלטת Beige & Mocha (משתני CSS, תמה בהירה יחידה, צללים חמים), רכיבי UI גנריים ב-`src/components/ui` (Button/Card/Input/Textarea/Badge), והלבשה מחדש רספונסיבית של כל המסכים. ההעדפה נשמרה בזיכרון. |
| 2026-07-23 | שלב 8 (cache): תוצאות התכנון נשמרות ב-DB — הצעות ערים (שכבה 1, per-trip) ומדריך העיר (שכבה 2, per-city) נטענים מ-`suggested_destinations` בכניסה חוזרת במקום קריאה חוזרת ל-AI; כפתור "רענן". migration 0003 הרחיב את הטבלה (`city`, `category`, `description`, `tip`). infra: `guide-service.ts`. |
| 2026-07-23 | הושלם שלב 8: "הוסף לטיול" (סימון `selected` + הצגה בעמוד הטיול); מדריך עיר v2 — "אזורי לינה" לפי אווירה במקום מלונות, רקע על העיר ו"איך מגיעים" (שורת overview), וכרטיסיות ברוחב מלא. תמונות והוספת נקודה ידנית נדחו. |
| 2026-07-23 | שלב 9 (ליבה): route `POST /api/ai/itinerary` בונה לו"ז יומי מהפריטים שנבחרו (סדר לפי שעות + הגיון גאוגרפי), שומר ב-`itinerary_items` (migration 0004) ומעביר את הטיול ל-`executing`. נוספו מחיקת פריט מהלו"ז והסרה מ"מה שבחרתם". infra: `itinerary-service.ts`. עריכת הערות ו-"פתח ב-Maps" נדחו. |
| 2026-07-26 | נקלטה רשימת חזון חדשה מהמשתמש (7 פיצ'רים) וקופלה כשלבים 11–17. החלטות רוחביות: תמונות תמיד ממקור חינמי (Wikipedia REST / Unsplash·Pexels free), חיפוש מקומות דרך OSM Overpass, מזג אוויר דרך Open-Meteo; מפת מסלול = תמונת "לנדינג" שעושה חשק לצד מפה אינטראקטיבית אמיתית; ליטוש עיצוב מודרני על בסיס Beige & Mocha כשכבה רוחבית מתמשכת. סדר בנייה: מתחילים משלב 11 (תאריך יציאה + ספירה לאחור). |
| 2026-07-26 | שלב 11 נבנה: `start_date`/`end_date` כבר קיימים ב-DB (אין migration). Domain: `setTripDatesSchema`, `daysUntil`, `formatCountdown`, `pickUpcomingTrip`. infra: `updateTripDates`, `getPrimaryDestination`. ספק תמונות חינמי `src/lib/place-image.ts` (Wikipedia, בלי מפתח, he→en, cache יומי, נפילה לגרדיאנט). Server Action `setTripDates`. רכיבים: `TripDatesForm` (עמוד טיול), `UpcomingTripCard` (hero במסך הבית). `next.config` allow-list ל-`upload.wikimedia.org`. אומת: type-check + lint + `next build` עברו. אימות ויזואלי חסום ע"י auth (אין להזין סיסמה) — ממתין לבדיקת המשתמש. |
| 2026-08-11 | שלב 11 אומת ויזואלית ומוזג (PR #11). שלב 12א נבנה: טאב "מפת מסלול" עם Leaflet + אריחי OSM (חינם, בלי מפתח) — תחנות ממוספרות לפי סדר ההוספה וקו מקווקו ביניהן. `src/lib/geocode.ts` חדש; קואורדינטות נשמרות בעמודות `latitude`/`longitude` שהיו קיימות וריקות, על שורת ה-`overview` של כל עיר (בלי migration לסכמה). `Tabs` גנרי ב-`components/ui` (הטאב הלא-פעיל לא מרונדר — Leaflet מודד את המכולה ב-mount); עמוד הטיול הורחב ל-`max-w-4xl`. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 12ב נבנה: migration 0006 מוסיף `city` ל-`itinerary_items` ועושה backfill ללו"ז קיים בהצלבת `title` מול `suggested_destinations.name`. התברר שה-route של הלו"ז כבר מחזיק את הפריטים שנבחרו (כולל `city`) וזרק אותם — אז `saveItinerary` פשוט מקבל אותם וממפה שם→עיר, בלי שינוי לסכמת ה-AI ובלי שאילתה נוספת. `itineraryItemSchema` יושרה למצב האמיתי של הטבלה. domain: `cityByDay` + `itineraryStops` (טהורים). סדר התחנות עבר לסדר הימים בלו"ז, עם ערים שלא שובצו בסוף. UI: לו"ז לצד המפה ב-`lg:flex-row`, ממוספר לפי תחנות, עם ימים ולילות לכל יעד. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 12ג נבנה ושלב 12 נסגר: `RouteHero` — באנר בראש טאב המסלול עם תמונת התחנה הראשונה (`place-image.ts` שכבר קיים משלב 11), שם הטיול וסיכום "תחנות · ימים · לילות". `routeSummary` בדומיין סופר ימים על פני כל המסלול כדי שיום מפוצל בין שתי ערים לא ייספר פעמיים. אותה שפה ויזואלית כמו `UpcomingTripCard`. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 13 נבנה בשלוש פרוסות. **13א:** `lib/overpass.ts` + `POST /api/places/search` (rate-limit 10/דק') + טאב "אטרקציות" עם מודאל פרטים. קטגוריות כתצורה ב-`domain/place.ts`. הרשאה דרך RLS — קריאת הטיול היא בדיקת ההרשאה. **13ב:** מקומות נשמרים כ-`source='manual'` (ערך enum מ-0002 שלא היה בשימוש) עם `external_id` (migration 0007). שלוש שאילתות סיננו `source='ai'` והיו מפילות אותם בשקט: `getSelectedDestinations`, `getRouteCities`, `setDestinationSelected` — כולן תוקנו; `deleteCityGuide` שמר את הפילטר בכוונה. גם אימות הקטגוריה היה שובר הסרה (שתי אוצרות מילים, עמודה אחת) → `selectableCategorySchema`. **13ג:** `getAddedPlaces` מקשר תוצאה→שורה→יום בלו"ז; כלל נירמול השמות אוחד ל-`lib/text.ts` (היו שני עותקים שיכלו להיפרד). אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 14 נבנה. **14א:** `domain/timeline.ts` טהור — `parseTimeLabel` הגנתי (נבדק מול 13 מקרים), `buildDayTimeline`, `positionPercent`; `DayTimeline` מצייר בלוקים על ציר שעות (76px/שעה, קבוע — שיום ארוך ייראה גבוה ולא צפוף) עם מתג "ציר שעות/רשימה". נמצא ונסגר **עותק שלישי** של תוויות הקטגוריות ב-prompt של הלו"ז — הוא לא קיבל את שש הקטגוריות מ-13ב, כלומר ה-AI קיבל `cafes` גולמי. **14ב:** `getItinerary` מצליב קואורדינטות מ-`suggested_destinations` לפי עיר+שם מנורמל (מדלג על שורות `overview` כדי שפריט בשם העיר לא ימוקם במרכזה); מרחק אווירי + הליכה עד 2.5 ק"מ. אומת מול מרחקים אמיתיים בטוקיו. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 15 נבנה. **15א:** `trip_bookings` (migration 0008, טבלה אחת ל-flight/train/lodging + RLS בתבנית של 0002), domain/service/actions/UI, טאב "לוגיסטיקה". **שני באגים נתפסו בבדיקת הלוגיקה לפני חיבור ל-UI:** דליים לפי מילישניות קראו לטיסה בעוד 8 שעות "מחר" כשהיא באותו ערב (תוקן ל-יום קלנדרי), ו-30 דקות עוגלו ל"שעה" (תוקן לדקות מתחת לשעה). לילות נספרים גם הם קלנדרית, כך ששהות באותו תאריך = בלי לילות. **15ב לא נדרשה** — שתי השורות הראשונות של השלב סופקו כבר ב-15א. **15ג:** Open-Meteo (חינם, בלי מפתח); `forecastWindow` טהור מחליט לפני קריאת רשת אם תחזית בכלל קיימת — 16 ימים זו תקרה קשיחה שמחזירה HTTP 400. אומת מול 9 מקרי גבול. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 16 נבנה (פרוסה אחת): `trip_phrasebooks` (migration 0009) — שורה אחת לטיול, `phrases` כ-jsonb כי הוא נקרא תמיד בשלמותו ואף פעם לא מסונן; Zod מאמת גם בכתיבה וגם בקריאה, כך ששינוי צורה מתגלה כשיחון חסר ולא כקריסה. `POST /api/ai/phrasebook` מסיק את שפת היעד מהערים שנבחרו (ובנפילה — מהערים המוצעות ומשם הטיול), כי האפליקציה מכירה ערים ולא מדינות. הנושאים הם `PHRASE_TOPICS` בדומיין ולא מחרוזת בתוך ה-prompt. **שדה `pronunciation` (תעתיק עברי) הוא הליבה** — בלעדיו העמודה בכתב היעד היא קישוט לקורא עברית; ה-prompt מדגיש זאת ודורש תעתיק גם לשפות לטיניות. טאב חמישי. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 17 נבנה — **התוכנית המקורית (0–17) הושלמה.** **17א:** `generateText` רב-תורי נוסף ל-`src/lib/ai` כפונקציה נפרדת מ-`generateStructured`; `trip_chat_messages` (migration 0010) כשורות ולא jsonb — נוספות אחת-אחת ונקראות לפי סדר; שאלה+תשובה נכתבות יחד; היסטוריה חסומה ב-24 תורות (כל תור נשלח מחדש בכל בקשה). **17ב:** `POST /api/ai/plan-from-chat` **מציע בלבד ולא כותב** — תצוגה מקדימה עם summary, ואישור שני שומר. `savePlanFromChat` נפרד מ-`saveCities` כי האחרון מוחק לפני שהוא כותב: כאן ערים חדשות בלבד, overview ב-`ignoreDuplicates` (לא לדרוס intro קיים), פריטים ב-upsert (לסמן `selected` גם על פריט קיים). אומת: type-check + lint + `next build` עברו. |
| 2026-08-12 | **פאזה B הושלמה (B2–B8).** **B2:** עמוד הטיול פוצל ל-route segments אמיתיים (`(tabs)/today|days|explore|map|more`) במקום `useState` — query param היה מריץ מחדש את כל שבעת הטוענים בכל לחיצת טאב. `(tabs)` הכרחית כדי ש-`city/[city]` לא יירש את סרגל הטאבים. **שבע קריאות `revalidatePath` קיבלו `"layout"`** — בלי זה הן היו מתאימות רק לעמוד ה-redirect וכל הטאבים היו מפסיקים להתרענן בשקט. `components/layout` נבנה (AppShell + BottomNav), `viewportFit: "cover"` ו-`min-h-dvh` נוספו. **B3:** `domain/trip-days.ts` — תאריכי הלו״ז **נגזרים** מ-`start_date + (day_number − 1)`, בלי migration. כל הפונקציות מקבלות `today` כפרמטר ואף אחת לא קוראת שעון, כי `todayIso` קורא את שעון המכונה (UTC ב-Vercel) והשרת והדפדפן היו חולקים על היום בין חצות ל-03:00. **37 מקרי קצה נבדקו לפני חיבור ל-UI** (התקדים משלב 15). `trips.status` הפסיק להיכתב — כלל ברזל #6 ב-ARCHITECTURE.md עודכן קודם. **B4/B5:** `CountdownHero` ו-`domain/tone.ts`; שיוך צבע לפי מיקום ולא hash, כי hash יכול לתת לשתי תחנות סמוכות אותו גוון. **נמצאה נפילת ניגודיות:** rose ו-peach היו 4.35 ו-3.89 מול סף AA — הוכהו עד שעברו. **B6:** רשת אריחי קטגוריה; **בלי תמונות לתוצאות**, ראו את ההערה למטה. **B7:** "עוד" פוצל לתפריט + שלושה תת-מסכים, מה ששבר את הסימון הפעיל בניווט (השוואת שוויון) — עבר להתאמה על גבול סגמנט. הזמנות תחבורה מוצגות ככרטיס עלייה למטוס ב-LTR כפוי. **B8:** `AppShell` הוחל גם על `/profile` ועמוד העיר, `loading.tsx` לכל טאב, `/design-preview` נמחק, ו-`SegmentedControl` עבר מ-`role="tab"` (ששולח קורא מסך לחפש tabpanel שלא קיים) ל-`role="group"` עם `aria-pressed`. |
| 2026-08-12 | **B0 + B1 נבנו.** B0: עמוד `/design-preview` הציג 3 מסכים × 2 וריאנטים; נבחר **"שובב ומזמין"**. הווריאנטים חלקו JSX אחד ונבדלו רק ב-CSS variables, כך שהבחירה הומרה לטוקנים ישירות. **B1:** `globals.css` גדל מ-41 שורות למערכת מלאה — 6 גווני פסטל בשלשה `tint/ink/dot`, טוקני `danger/success/warning`, רדיוסים, motion, וסולם תצוגה. **שמות הטוקנים נבחרו כדי לא להתנגש** ב-namespaces של Tailwind v4 (`--radius-lg`, `--ease-out`): הגדרה מחדש שלהם הייתה משנה בשקט כל `rounded-lg` קיים, ולכן נוספו `rounded-card`/`rounded-control`/`ease-snap`. **הצבע לעיר הוא משתנה ולא class** — `.tone-*` מציבה שלשה ו-`bg-tone`/`text-tone-ink` קוראים אותה, כי ב-B5 השיוך נקבע בזמן ריצה. פונט תצוגה Secular One. **`cn()` שוכתב ל-`clsx` + `tailwind-merge`, והתגלה שזה לא מספיק:** tailwind-merge לא מזהה שמות טוקן משלנו, אז `rounded-control rounded-full` שרדו כשניהם — נדרש `extendTailwindMerge` עם קבוצות `rounded` ו-`font-size` (צבעים וצללים כן נפתרים לבד). 17 מופעי `text-red-600` הוחלפו בטוקן. **`drop-shadow-sm` בשני קבצים אינו חוב** — הוא צל טקסט על תמונה, והממצא המקורי ספר אותו בטעות. CLAUDE.md תוקן: TanStack Query/Zustand/shadcn מעולם לא הותקנו ולא יותקנו בלי צורך. |
| 2026-08-12 | **נפתחה פאזה B — סבב עיצוב ופונקציונליות.** נסקרו כל הקוד והמסמכים והוגדרה טבלת B0–B8 (בסוף המסמך). החלטות: 5 טאבים לפי מסע במקום 6 לפי פיצ'ר, `route segments` אמיתיים במקום `useState` (query param היה עולה 7 שאילתות בכל לחיצת טאב במקום אפס), תאריכי לו"ז **נגזרים** מ-`start_date + (day_number - 1)` בלי migration, ו-`trips.status` נגזר במקום להישמר — כלל ברזל #6 ב-ARCHITECTURE.md יעודכן לפני הקוד. mobile-first כעיקרון רוחבי על כל הפריטים. מצב כהה נדחה. **לא מותקנות** חבילות רק כדי ליישר קו עם CLAUDE.md (TanStack/Zustand/shadcn) — CLAUDE.md יתוקן לשקף את המציאות. |
| 2026-08-13 | **C7 נבנה — AI עמוס כמצב שלישי.** נוסף מתוך אימות C5 ולא מתוך תכנון: Gemini החזיר 503 `UNAVAILABLE` פעמיים ברצף, ו-C1 מיפה רק 429, כך שעומס רגעי הוצג כ"נכשל". שלושה מצבים נפרדים עכשיו, כי העצה הנכונה שונה בכל אחד: 429 שלנו (חכו רגע), 503 `ai_quota_exceeded` (חזרו מחר), 503 `ai_busy` (נסו בעוד רגע). שניהם 503 כי זה הסטטוס הנכון לשניהם, וקוד הגוף מבדיל. **הסיווג רוכז ב-`domain/ai-errors.ts`** — לכל אחד מחמשת המסכים היה עותק של אותם ענפים, **וזה בדיוק איך שה-503 לא טופל באף אחד מהם**; עכשיו כלל אחד, עם fallback משלו לכל מסך. 503 בלי קוד מוכר נקרא כמכסה: הסיבה השכיחה יותר וגם העצה השמרנית. **ויתור מכוון:** הודעת ה-429 בצ׳אט הייתה "יותר מדי הודעות" ועכשיו "יותר מדי בקשות" — ניסוח ייחודי תמורת ביטול חמישה עותקים. **אימות:** 22 מקרי קצה על `aiErrorMessage`/`aiErrorFromResponse` לפני חיבור למסך, כולל HTML במקום JSON (לא זורק), שדה `error` לא-מחרוזתי, וגוף `null`. בזמן ריצה דרך route זמני שנמחק, עם `ApiError` אמיתי שנזרק מתוך ה-provider: 429→503 `ai_quota_exceeded`, 503→503 `ai_busy`, ו-500/400→502 `ai_failed` — שלוש הודעות שונות, וכל אחת נרשמה בלוג עם ה-status. `place-search.tsx` נשאר עם ה-503 שלו — זה Overpass ולא AI, ויש לו הודעה משלו. |
| 2026-08-13 | **C5 נבנה — "הצג עוד יעדים".** `exclude` נוסף ל-`aiSuggestRequestSchema` ול-prompt של `/api/ai/suggest`, בדיוק כמו שרמה 2 כבר עשתה מאז שלב 8; כפתור "הצג עוד יעדים" מוסיף לרשימה במקום להחליף. **⚠️ המלכודת: לשורות רמה 1 אין הגנת ייחודיות בכלל** — האינדקס מ-0003 הוא על `(trip_id, city, category, name)` ולשורות האלה שני השדות הם `NULL`, ו-Postgres מתייחס ל-NULL כשונה מ-NULL, כך שהאינדקס לא נורה עליהן לעולם (ההערה במיגרציה אומרת זאת, בהקשר אחר). לכן `upsert` עם `onConflict` היה חסר תוקף, והניכוי חייב לקרות בקוד: `mergeCitySuggestions` מסלקת כפילויות מול הקיים וגם בתוך התשובה עצמה, כי תשובה אחת של AI יכולה לנקוב באותה עיר פעמיים. `appendCities` נפרד מ-`saveCities` שמוחק לפני שהוא כותב — אותה הבחנה בדיוק שקיימת בין `saveCityGuide` ל-`savePlanFromChat`; והניכוי נעשה מול הטבלה ולא מול המסך, כדי ששני טאבים פתוחים לא יתחרו לכפילות. **ה-prompt המקורי לא נשמר**, ולכן `moreCitiesPrompt` נופלת לתיאור שנגזר מהערים עצמן — **הצורך בזה נמצא באימות ויזואלי**, כי בגרסה הראשונה הכפתור לא הופיע כלל על רשימה שנטענה מה-DB. **אימות:** 29 מקרי קצה על ה-merge/dedupe + 13 על גזירת התיאור, לפני חיבור למסך. בזמן ריצה **מול Gemini האמיתי** דרך route זמני שנמחק: סבב 1 החזיר קיוטו/אוסקה/טאקאיאמה/קנאזאווה/טוקיו, וסבב 2 עם `exclude` החזיר נארה/פוקואוקה/קוראשיקי/מאטסואה/הירושימה — **חפיפה 0**, מיזוג ל-10 ערים ייחודיות. ויזואלי ב-375/768/1440: עמודה אחת ב-375 ושתיים מ-768, הכפתור מיושר לימין מתחת לרשת, אין overflow. **תקלה שנתפסה תוך כדי:** Gemini החזיר 503 UNAVAILABLE ("high demand") בשני נסיונות רצופים — ראו את הפריט הנפרד למטה; **הלוג של C1 הוא מה שאפשר לאבחן את זה בשניות במקום לנחש.** |
| 2026-08-13 | **C4 נבנה — "איך מגיעים".** קישור Google Maps directions מהלינה של אותו לילה לכל פריט בלו"ז, `travelmode=transit`. סכמת ה-URL של Maps חינמית ובלי מפתח, בשונה מ-Directions API. שתי פונקציות טהורות ב-`domain/directions.ts`, אחת לכל קצה של המסלול, כי שני הקצוות יודעים דברים שונים: `lodgingOrigin` (כתובת מנצחת, אחרת שם+עיר — ובלי לכפול את העיר כשהשם כבר מכיל אותה, "ריוקאן קיוטו" בקיוטו) ו-`entryDestination` (קואורדינטות מנצחות — חד-משמעיות, וזו הסיבה שפריט מחיפוש האטרקציות מנווט לדלת הנכונה ולא לסניף אחר של הרשת; אחרת שם+עיר). **`(0,0)` נדחה במפורש** — נקודה במפרץ גינאה שכנתון אמיתי היא `null` שנכתב כמספר, ולנווט לאוקיינוס גרוע מליפול חזרה לשם. גם lat בלי lng, טווחים לא חוקיים, `NaN` ו-`Infinity` — אבל `lat=0` עם lng אמיתי (קו המשווה) ו-`lng=0` עם lat אמיתי (גריניץ') לגיטימיים ונשמרים. הקישור לא מוצג כשאין לינה, ולכן היום האחרון (בלי לילה) מקבל רק "פתח במפה". **🐞 באג קיים שנתפס ותוקן:** בפריט של 30 דקות (כרטיס 38px, `overflow-hidden`) שורת הקישורים נחתכה ומ-16px נשארו כ-3 — **וזה היה כך גם לפני C4**, כלומר "🗺️ במפה" היה מרונדר ובלתי לחיץ. עכשיו שורת הפעולות לא מרונדרת מתחת ל-52px, ושום דבר לא אובד כי תצוגת "רשימה" מציגה את שני הקישורים לכל פריט. **`withHebrewPrefix` ב-`lib/text.ts`:** "מShinjuku" הדביק תחילית עברית לכתב לטיני; הכלל שהאפליקציה כבר מיישמת ב-"פתח ב-Google Maps" הוא מקף לפני מילה לא-עברית, ועכשיו הוא פונקציה. **לא נעשה עם `\b`** — הוא מוגדר על ASCII ולא היה נורה סביב עברית או יפנית, כלומר לא היה תופס אף פעם. **אימות:** 41 מקרי קצה על `lodgingOrigin`/`entryDestination`/`googleMapsDirectionsUrl` + 12 על כפילות העיר + 14 על התחילית, כולם לפני חיבור למסך — כולל קידוד `&`, `#`, `?` ועברית ב-URL, ואימות שיש בו בדיוק סימן שאלה אחד. בזמן ריצה נבדקו ה-URLs שנוצרו בפועל: יעד עם קואורדינטות `35.7148,139.7967`, יעד בלי קואורדינטות כשם+עיר, מוצא שנופל לשם+עיר כשאין כתובת, `travelmode=transit` ו-`rel="noopener noreferrer"` בכולם, ויום בלי לינה בלי קישור. ויזואלי ב-375/768/1440 בשתי התצוגות: **אפס קישורים חתוכים**, אין overflow, RTL נכון, ותוויות ארוכות נשברות לשורה שנייה במקום לגלוש. **לא נבנה במכוון:** ניווט מפריט לפריט — הבקשה הייתה מהלינה, ושורת המעברים כבר מציגה פער זמן ומרחק. |
| 2026-08-13 | **C3 נבנה — "איפה אני ישן" לכל יום.** הפער לא היה באג אלא שאלה שאף פונקציה לא שאלה: `bookingsByDay` מדללת הזמנות לפי `starts_at` בלבד, ולכן מלון לחמישה לילות הופיע ביום הצ׳ק-אין ובשום יום אחר. `lodgingByDay` חדשה **לצד** `bookingsByDay` ולא במקומה — "מה קורה היום" ו"איפה אני ישן הלילה" הן שתי שאלות. **הכלל:** צ׳ק-אין ב-X וצ׳ק-אאוט ב-Y מכסים את הלילות `X..Y-1`; ביום Y בבוקר עוזבים, אז הוא נשאר ריק. תאריכים כמחרוזות `YYYY-MM-DD` שנפתרות ב-`zone`, כמו ב-`bookingsByDay`. **שלוש החלטות:** (1) **בלי `ends_at` — רק לילת ההגעה**; להניח שהשהות נמשכת עד ההזמנה הבאה היה ממציא לילות שלא הוזנו, ושגוי ברגע שיש מלון שני. המחיר גלוי: בלי צ׳ק-אאוט תוצג לינה ביום אחד. (2) צ׳ק-אאוט שאינו אחרי הצ׳ק-אין נחשב "לא ידוע", לא טווח שלילי. (3) חפיפה → הצ׳ק-אין המאוחר מנצח, כי חפיפה היא סתירת נתונים ולא תרחיש למיצוע. **הצגה:** `NightStay` (server component) ב-`/today` דרך `DayPager` וב-`/days` דרך `Itinerary`; **ביום הצ׳ק-אין הרצועה מוסתרת** כי כרטיס ההזמנה מעליה כבר מציג את אותו מלון עם שעה — כפילות נראית כמו באג. `nightStayLabel` בדומיין ולא ברכיב, כמו `dayLabel`. **אימות:** 30 מקרי קצה לפני חיבור למסך — כולל הגעה בלילה שלפני תחילת הטיול (מכוסה, אבל לא מסומן צ׳ק-אין), שהות שכולה מחוץ לחלון הטיול, `starts_at` לא־תקין שמדולג, ושני אזורי זמן שקוראים את אותו רגע (22:30Z) כימים שונים. בזמן ריצה: מלון טוקיו לילות 1–2 והריוקאן 3–4, יום 5 (צ׳ק-אאוט אחרון) ריק, הטיסה מתעלמת, וה-de-dup אומת — ביום 1 שם המלון מופיע **בדיוק פעם אחת**. ויזואלי ב-375/768/1440 מול viewport אמיתי: אין overflow, גובה רצועה קבוע 77px, RTL נכון (🏨 מימין, 🗺️ משמאל), וכתובת יפנית ארוכה נקטעת ב-375 ונכנסת מ-768. **הערה על שיטת האימות:** מדידה ראשונה הראתה overflow ב-768/1440 — התברר שזה ארטיפקט שלי, כי כפיתי רוחב על `main` בתוך viewport של 375 במקום לשנות את ה-viewport עצמו. נמדד מחדש נכון. |
| 2026-08-13 | **C2 נבנה — הוספת מקום ידנית.** טופס ב־`/explore` מתחת לחיפוש (שם · עיר · קטגוריה · כתובת), **בלי migration**: `source='manual'` + `external_id = NULL` הוא הזוג שמבדיל מקום מוקלד מתוצאת OSM, ו־`getAddedPlaces` כבר מסנן `external_id` לא־null כך שסימוני "כבר בטיול" בחיפוש לא נפגעים. הקטגוריות הן שש של `PLACE_CATEGORIES` (label+emoji קיימים, ו־`savedCountsByCategory` סופר בדיוק אותן). הכתובת נשמרת ב־`description` כמו ש־`addPlaceToTrip` כבר עושה — **והתגלה שהיא לא נראית בשום מקום**, כי `SelectedList` הציג שם בלבד, אז נוספה שורה מקוצצת מתחת לשם (`truncate`, כך שתיאור AI ארוך לא הופך את הרשימה לפסקאות; גובה הכרטיס נמדד קבוע 65px בכל אורך תיאור). קואורדינטות נשארות NULL במכוון — גאוקודינג של שם עסק הוא הבאג שהעיף את "האקונה" ללוס אנג'לס. **המלכודת המרכזית: `addManualPlace` הוא בכוונה לא upsert.** המפתח הטבעי `(trip_id,city,category,name)` יכול להתנגש בשורה קיימת, ו־upsert עיוור היה דורס `source`, `external_id` וקואורדינטות בערכים ריקים — הפיכת שורת `ai` ל־`manual` מוציאה אותה ממדריך העיר ומטווח `deleteCityGuide`, ואיפוס `external_id` שובר את ה־badge בחיפוש; **שתי התקלות שקטות**, בדיוק כמו באגי פילטר ה־source משלב 13ב. לכן שורה קיימת רק מסומנת `selected` ומקבלת כתובת אם נמסרה, עם fallback ל־23505 לתחרות. `manualPlaceDescription` מחזיר `null` לכתובת ריקה והשירות משמיט אז את העמודה — כדי שהוספה חוזרת לא תמחק תיאור קיים. העיר היא שדה טקסט עם `datalist` ולא `select`, כי בטיול חדש הרשימה ריקה והטופס הזה הוא לרוב מה שיוצר את העיר הראשונה. **אימות:** 40 מקרי קצה על `manualPlaceSchema` + `manualPlaceDescription` לפני חיבור למסך (כולל ששם של רווחים נדחה, כלומר `.trim()` רץ לפני `.min(1)`, וגבולות 200/120/300 כולל־קצה). בזמן ריצה: מסלול הוולידציה דרך Server Action אמיתי (עיר ריקה → הודעה עברית, `aria-invalid` על השדה הנכון בלבד, `aria-describedby` תואם ל־id), ומסלול הכשל — RLS סירב לכתיבה לא־מאומתת והשדות נשארו מלאים. הסירוב היה RLS ולא שגיאת עמודה, כלומר צורת ה־payload תקפה מול הסכמה. ויזואלי ב־375/768/1440: אין overflow, RTL נכון (שם מימין לעיר), שש גלולות הקטגוריה נשברות ל־3 שורות ב־375 ולשורה אחת מ־768, וכל 6 קישורי label↔input נפתרים. **לא אומת:** כתיבה מוצלחת ל־DB — דורשת session. |
| 2026-08-13 | **C1 נבנה — שגיאות AI אמיתיות.** הטיפול רוכז ב-`src/lib/ai/provider.ts` (נקודת החנק של כל שבעת ה-routes) ולא שוכפל שבע פעמים. שלושה חלקים: (1) **`console.error` לפני כל סיווג** — זה עיקר הפריט; ההודעה של Gemini נושאת `code`/`status`/`reason` וכל זה נזרק לפח עד עכשיו. (2) **`AiQuotaExceededError`** על `ApiError` עם `status === 429`, שה-routes ממפים ל-**503 `ai_quota_exceeded`** במקום 502 `ai_failed`. (3) הודעה בקליינט שלא מזמינה ניסיון חוזר מיידי. **נבחר 503 ולא 429 בכוונה** — ה-rate limiter שלנו כבר תופס 429, ושני המצבים חייבים להיראות שונה. **הסיכון האמיתי לא הונח אלא נבדק:** אם `@google/genai` היה נפתר לשני עותקים, `instanceof ApiError` היה תמיד `false` וענף המכסה היה **קוד מת ששום דבר לא חושף**. אומת מול error שה-SDK זרק בפועל, וגם חוצה את גבול ה-barrel דרך route זמני שנמחק (error שנזרק בתוך `handleProviderError` הגיע ל-route כ-`AiQuotaExceededError`, `cause` שמר `status: 429`, התשובה 503). `isQuotaExceeded` נבדק מול **9 מקרי קצה** לפני חיבור למסך — כולל אובייקט מחופש עם `status: 429` שאינו `ApiError`, שמסווג `false` בכוונה. אומת בזמן ריצה (לא רק build): מפתח פסול מייצר בלוג `status: 400` + `API_KEY_INVALID`, ומסלול הצלחה מול Gemini האמיתי ממשיך לעבוד — כלומר ה-`try/catch` לא שבר אותו. ויזואלי ב-375/768/1440: אין overflow אופקי, RTL וטוקן `danger-ink` תקינים, ובמקום היחיד שההודעה חולקת שורת flex עם כפתור ("רענן הצעות" במדריך העיר) היא נשברת לשתי שורות ב-375px בלי חפיפה. **מה שלא אומת:** מסך השגיאה חסום מאחורי auth (אין להזין סיסמה), ולכן ה-503 אומת בשרת ולא כלחיצה בדפדפן. |
| 2026-08-11 | **באג geocoding שנתפס באימות ויזואלי:** "האקונה" (יפן) הופיע בארה"ב. אומת מול ה-API — Nominatim על שם עברי חשוף מחזיר `Coalition for Human Immigrant Rights, Los Angeles`; עם הקשר (`"האקונה, יפן"`) הוא מחזיר מערך ריק, כלומר אין לו שמות עבריים בכלל. תוקן: ויקיפדיה (he→en) קודמת, עם שם הטיול כהקשר; דרישה שלערך יהיו קואורדינטות מסננת תוצאות שאינן מקום (בלי הקשר, "האקונה" מחזיר "האקונה מטטה" ו"מלך האריות 3"); העדפת ערך ששמו מכיל את שם העיר; Nominatim נשאר גיבוי ורק כשיש הקשר. migration 0005 מאפס את ה-cache כדי שקואורדינטות שגויות יימשכו מחדש. |

---

## 📖 מקרא מונחים

- **Repo (Repository)** — מאגר הקוד של הפרויקט ב-Git/GitHub.
- **LTS (Long Term Support)** — גרסה יציבה עם תמיכה ארוכת טווח; תמיד מעדיפים אותה.
- **Environment Variables** — משתני סביבה; הדרך לשמור מפתחות סודיים מחוץ לקוד.
- **RLS (Row Level Security)** — הרשאות ברמת השורה בדאטהבייס; משתמש ניגש רק לנתונים שלו.
- **Middleware** — קוד שרץ לפני כל בקשה; משמש להגנה על עמודים ממשתמשים לא מחוברים.
- **Migration** — קובץ שמתעד שינוי בסכמת הדאטהבייס, כדי שהיא תהיה משוחזרת ומנוהלת בקוד.
- **CI/CD (Continuous Integration/Deployment)** — בדיקות אוטומטיות על כל שינוי + דיפלוי אוטומטי.
- **Structured Output** — הנחיית מודל AI להחזיר JSON במבנה קבוע במקום טקסט חופשי.
- **Rate Limiting** — הגבלת כמות בקשות למשתמש בפרק זמן, להגנה על המכסה החינמית.
- **State Machine** — מידול ישות כאוסף מצבים ומעברים מוגדרים (תכנון ⟵ ביצוע ⟵ הושלם).
- **Monorepo** — ריפו אחד עם כמה אפליקציות; לא רלוונטי לנו (אפליקציה אחת).
- **Feature-Based Structure** — ארגון קוד לפי תחומי פיצ'ר; ההחלטה שלנו.
- **shadcn/ui** — ספריית קומפוננטות שמעתיקה את הקוד לפרויקט; הבחירה שלנו לאטומים.
- **YAGNI (You Aren't Gonna Need It)** — לא בונים תשתית לצורך שלא קיים.

---

## 🎨 פאזה הבאה — סבב שינויים (פונקציונליות ועיצוב)

> נפתחה 2026-08-11. התוכנית המקורית (0–17) סגורה; מכאן העבודה מונעת-משוב ולא מונעת-שלבים.
>
> **הפאזה הוגדרה 2026-08-12.** המשתמש ביקש שלושה דברים: עיצוב מודרני ונעים לעין, מסך "היום"
> שנפתח על הלו"ז של התאריך הנוכחי בזמן הטיול, וספירה לאחור מוחשית שיוצרת ציפייה.
> בנוסף סיפק צילומי מסך מאפליקציית טיול ליפן כרפרנס לכיוון הוויזואלי.

### מה שנקבע בשיחה

| החלטה | הבחירה |
| --- | --- |
| ניווט | 5 טאבים לפי מסע (**היום · ימים · מה עושים? · מפה · עוד**) + סרגל תחתון במובייל |
| אופי ויזואלי | **לא הוכרע** — ייבחר מול מוקאפ (B0) |
| סדר עבודה | תשתית עיצוב קודם |
| מצב כהה | לא עכשיו; הטוקנים ייבנו מוכנים לזה |
| רספונסיביות | **mobile first**, עם ניצול אמיתי של מסכים גדולים |

### הטבלה

| # | מה | סוג | סטטוס |
| - | -- | ---- | ------ |
| B0 | מוקאפ עיצוב — עמוד תצוגה זמני, 3 מסכים × 2 וריאנטים, לבחירת האופי | עיצוב | ✅ הושלם — נבחר **"שובב ומזמין"** |
| B1 | תשתית עיצוב v2 — טוקנים, פונטים, אייקונים, שדרוג `components/ui` | עיצוב | ✅ הושלם |
| B2 | שלד אפליקציה — 5 טאבים ב-URL + סרגל ניווט תחתון | פונקציונליות + עיצוב | ✅ הושלם |
| B3 | תאריכים אמיתיים ללו"ז + מסך "היום" | פונקציונליות | ✅ הושלם |
| B4 | ספירה לאחור מוחשית (hero) | עיצוב | ✅ הושלם |
| B5 | צבע ייחודי לכל עיר בכל האפליקציה | עיצוב | ✅ הושלם |
| B6 | "מה עושים?" — רשת קטגוריות + כרטיסי מקום | עיצוב + פונקציונליות | ✅ הושלם — **בלי תמונות לתוצאות**, ראו את ההערה למטה |
| B7 | "עוד" — פרטי טיול, שיחון עם חיפוש, שיחה | עיצוב | ✅ הושלם |
| B8 | ליטוש רוחבי — מצבים ריקים, טעינה, נגישות, רספונסיביות | עיצוב | ✅ הושלם |

**סדר תלויות:** B0 → B1 (כל השאר תלוי בו) → B2 → B3 → B4. B5 עצמאי אחרי B1; B6 משתפר עם B5; B7 תלוי ב-B2; B8 אחרון.

### עיקרון רוחבי — Mobile first

לא פריט בטבלה אלא **כלל שחל על B1 עד B8**. היום כל עמוד הוא `mx-auto max-w-4xl` ממורכז — על 1920px זו עמודה צרה עם שוליים ריקים, ועל 375px חלק מהמסכים צפופים (שורת 6 גלולות הטאבים גולשת).

בונים ל-375px קודם בלי prefix; כל `sm:`/`md:`/`lg:`/`xl:` הוא **תוספת** על בסיס עובד. שטח פנוי במסך רחב הופך לעמודה שנייה — לא למתיחה של הטקסט:

| מסך | ב-`lg` ומעלה |
| --- | --- |
| היום | ציר היום + עמודת צד: מה קרוב, מזג אוויר, ההזמנה הבאה |
| ימים | רצועת ימים קבועה בצד + היום הנבחר |
| מפה | מפה ורשימת תחנות זו לצד זו |
| מה עושים? | 3–4 עמודות; פילטרים כעמודת צד במקום גלולות גולשות |
| עוד | רשימת כניסות בצד + התוכן לצדה |
| `/profile` | hero רחב + רשת טיולים 2–3 עמודות |

`AppShell` (B2) יחזיק את הרוחב והריווח במקום אחד, במקום 8 עמודים שכל אחד חוזר על `max-w-4xl px-4 py-10` לחוד.

### ממצאים שהכתיבו את התוכנית

אלה עובדות שאומתו בקוד לפני שנכתבה התוכנית:

- **ללו"ז אין תאריכים.** `itinerary_items` מחזיק `day_number` ותוויות זמן כטקסט. `start_time`/`end_time` קיימות מ-0002 ו**מעולם לא נכתבו**. `start_date` מגיע ללו"ז רק כטקסט בתוך ה-prompt של ה-AI.
- **`trips.status` משקר.** נכתב במקום אחד — `api/ai/itinerary/route.ts` מסמן `executing` ברגע שנשמר לו"ז, אולי חודשיים לפני היציאה. `completed` לא נכתב אף פעם. שני ה-Badges שקוראים אותו מציגים מידע שגוי.
- **`trip_bookings` הוא הדאטה היחיד עם timestamps אמיתיים** (`starts_at`), ומוצג היום מנותק מהלו"ז.
- **הטאבים לא ב-URL** — `useState(items[0]?.id)`, לא ניתנים ללינק והטאב הפותח קבוע.
- **`cn()` בלי `tailwind-merge`** — class מאוחר לא גובר על מוקדם. מתפוצץ ברגע שמעבירים `className` לרכיבים.
- **shadcn/ui, TanStack Query ו-Zustand לא מותקנים**, למרות ש-CLAUDE.md מציג אותם כהחלטות סגורות. ההמלצה: לתקן את CLAUDE.md כך שישקף את המציאות, לא להתקין חבילות שאין בהן צורך.
- `text-red-600` hardcoded ב-15 מקומות; אין טוקן error/success/warning. `text-sm` מופיע 90 פעם — הטיפוגרפיה שטוחה.
- `src/components/layout/` מכיל `.gitkeep` בלבד.

### השוואה לאפליקציות אחרות

| אפליקציה | מה כדאי לקחת |
| --- | --- |
| TripIt | מסך הבית בזמן הטיול הוא **היום**, לא רשימת הטיולים |
| Google Trips | טיסה ומלון מופיעים **בתוך ציר היום**, לא בטאב נפרד |
| Wanderlog | תמונה לכל מקום, ו"הוסף ליום" בלחיצה אחת |
| Polarsteps | הספירה לאחור היא הגיבור של המסך |
| הרפרנס של המשתמש | **צבע ייחודי לכל עיר** — זה מה שגורם לזה להיראות מעוצב |

המכנה המשותף: כולן מסדרות מידע לפי *מתי צריך אותו*, לא לפי *איזה פיצ'ר בנה אותו*. הטאבים שלנו הם רשימת פיצ'רים — וזה מה ש-B2 מתקן.

**האיורים ברפרנס** הם אמנות מותאמת אישית לטיול ההוא ואין דרך חינמית לייצר אותם. התחליף: תצלום מוויקיפדיה (`place-image.ts` קיים ומשמש היום בשני מקומות בלבד) + אימוג'י + צבע לכל עיר.

> **B6 — למה אין תמונות לתוצאות החיפוש.** התוכנית ביקשה תמונה לכל כרטיס מקום.
> התמונות מגיעות מוויקיפדיה (`place-image.ts`), ותוצאות Overpass הן ברובן עסקים
> מקומיים קטנים שאין להם ערך — הדגל `notable` שנבנה בשלב 13 קיים בדיוק כי רק
> מיעוט מהתוצאות מוכר. 20 קריאות רשת לכל חיפוש שיחזירו ברובן `null` היו מאיטות
> את החיפוש תמורת מעט מאוד. במקום זה הכרטיסים קיבלו פס צד בצבע העיר, badge
> סטטוס, ותג "מוכר". תמונות ייתכנו בעתיד לפריטים שנבחרו (שם הרשימה קצרה
> וידועה מראש בשרת).

**איך לעבוד בפאזה הזו**

- לא לפתוח קוד לפני שהפריט רשום בטבלה ומוסכם.
- שינוי עיצוב שנוגע ליותר ממסך אחד — לבדוק את כל הטאבים, לא רק את זה שהתלוננו עליו.
- **בדיקה חזותית ב-375px, 768px ו-1440px בכל PR.**
- אותן פרוסות דקות ואותו PR לפיצ'ר. אין שירותים בתשלום.

---

## 🧭 פאזה C — שימוש אמיתי

> נפתחה 2026-08-13, אחרי ערב שימוש באפליקציה. מונעת-משוב: כל פריט הוא דבר
> שנתקלת בו בפועל, לא רעיון מהתוכנית.

### הטבלה

| # | מה | סוג | סטטוס |
| - | -- | ---- | ------ |
| C1 | שגיאות AI אמיתיות: מכסה מול תקלה, + לוג בשרת | באג | ✅ הושלם |
| C2 | הוספת מקום ידנית (שם, קטגוריה, עיר, כתובת) | פונקציונליות | ✅ הושלם |
| C3 | קישור לינה שהוזמנה אל התכנון — "איפה אני ישן" לכל יום | פונקציונליות | ✅ הושלם |
| C4 | "איך מגיעים" — מהלינה לכל יעד, דרך קישורי Google Maps | פונקציונליות | ✅ הושלם |
| C5 | "הצג עוד יעדים" בתכנון | פונקציונליות | ✅ הושלם |
| C6 | סימון ברור יותר בסרגל הניווט | עיצוב | ממתין |
| C7 | AI עמוס (503) כמצב שלישי, נפרד ממכסה ומתקלה | באג | ✅ הושלם |
| C8 | ניהול הזמנות: דאבל-בוקינג, תאריך ביטול חינם, והתראות מראש | פונקציונליות | 🔵 בעבודה — דומיין הושלם, UI פתוח |
| C9 | התראות פוש אמיתיות (Web Push, חינמי) | פונקציונליות | ממתין — דורש הגדרה של המשתמש |

> **C8 נוסף 2026-08-13 לפי בקשת המשתמש.** שני צרכים שעלו מאותו מקום — "מה
> הזמנתי ומה אני צריך לעשות בקשר לזה לפני שיהיה מאוחר":
>
> **8א — לינה כפולה ותאריך ביטול חינם.** כשמזמינים שני מלונות לאותם לילות (כדי
> להחליט אחר כך), צריך (1) **תיעוד ברור של כל הלינות**, (2) **סימון דאבל-בוקינג**
> כשיש חפיפה, (3) שדה **"ביטול חינם עד"** להזמנה, ו-(4) **התראה כמה ימים לפני**
> המועד הזה, כדי לא לשכוח לבטל ולשלם על חדר שלא ישנו בו.
>
> > 💡 **הבסיס כבר קיים מ-C3.** `lodgingByDay` כבר מזהה חפיפה בין שהיות — היא
> > מכריעה אותה לטובת הצ׳ק-אין המאוחר ומתועדת שם כ"סתירת נתונים". C8 הופך את
> > הסתירה הזו ממשהו שנפתר בשקט למשהו שנאמר למשתמש. גם `bookingAlert` כבר קיים
> > ונגזר מהשעון בלי להישמר, כך שהתראת ביטול היא הרחבה של דפוס עובד.
> >
> > ⚠️ **דורש migration** — עמודה `free_cancellation_until` ל-`trip_bookings`.
> > המשתמש מריץ migrations ידנית ב-Supabase SQL Editor.
>
> **8ב — התראה להזמין מראש.** דוגמה שהמשתמש נתן: רכבת ביפן שצריך להזמין חודש
> מראש. הרצון: **ברגע שקטע כזה הוא חלק מהמסלול, שתקפוץ תזכורת לא לשכוח להזמין.**
>
> **הוכרע 2026-08-13: המשתמש מזין "להזמין עד".** האפליקציה לא מכירה זמני הזמנה
> של מפעילים, ולנחש אותם זה להמציא ידע — אותו כלל שקבע בשלב 14 ש"מספר מומצא גרוע
> ממספר חסר". היא זוכרת ומתריעה, לא מנחשת. אם בעתיד ירצו, ה-AI יכול להציע תאריך
> **כטיוטה מסומנת** שהמשתמש מאשר.
>
> **8ג — זמן ההתראה נבחר (בקשה שנוספה 2026-08-13):** יום לפני / יומיים / או מספר
> ימים ידני. נשמר כ-`reminder_days_before` על ההזמנה; `null` = ברירת המחדל של
> האפליקציה (7 ימים), כך שמי שלא בוחר לא צריך לבחור.

### מה שנבנה עד כה ב-C8 (הדומיין)

`migration 0011` נכתב אך **עוד לא הורץ** — ראו את ההנחיה בהמשך. שלוש פונקציות
טהורות ב-`domain/booking.ts`, כולן נבדקו לפני שנגעתי ב-UI:

| פונקציה | מה היא עושה |
| --- | --- |
| `cancellationAlert` | התראת "ביטול חינם עד…" בתוך חלון ההתראה |
| `bookingTodoAlert` | התראת "להזמין עד…" לפריט שעוד לא הוזמן |
| `doubleBookedLodgingIds` | אילו לינות חולקות לילה עם לינה אחרת |

**⚠️ שתי החלטות שהתגלו רק בזכות הבדיקות, ושתיהן שינו את הסכמה:**

**1. מועד יעד הוא `date` ולא `timestamptz`** — הפעם היחידה שהמיגרציה הזו חורגת
מהתקדים של 0008. כתבתי בהתחלה `timestamptz` והנחתי בהערה שצהריים "בטוחים בכל אזור
זמן בעולם". **בדיקה שכתבתי הפריכה את זה:** Pacific/Kiritimati הוא UTC+14, ולכן
12:00 שם הוא כבר המחרת. הטווח האמיתי הוא UTC−12 עד UTC+14 — 26 שעות — ולכן **אין
שעה ביום שבטוחה בכל האזורים**. מועד ביטול הוא *תאריך קלנדרי*, לא רגע, ול-`date`
אין אזור זמן להתבלבל בו.

**2. קריאת התאריך חייבת להיות מקומית.** `new Date("2026-09-10")` נפרס כחצות UTC,
ולכן ממערב לגריניץ' הוא ה-9 וכל ספירת ימים יוצאת קצרה ביום. `parseLocalDate` בונה
מרכיבים. **זה מודגם בבדיקה ולא מונח:** אותו `new Date("2026-09-10")` נקרא כ-09-09
בלוס אנג'לס ובמידוויי.

> **הערה על שיטת האימות שנתפסה בדרך:** ניסיתי לאמת אזורי זמן בהרצת הבדיקות עם
> `TZ=...`. **זה לא עבד ולא הוכיח כלום** — Node במכונה הזו מתעלם מ-`TZ`
> (`process.env.TZ` יצא `undefined` וההיסט נשאר −180). חמש ההרצות היו כולן
> בירושלים. הוחלף באימות שמדגים את הבאג דרך `Intl` עם `timeZone` מפורש, שלא צריך
> תמיכת `TZ`. **אם בעתיד תרצו לבדוק אזורי זמן — לא לסמוך על `TZ` כאן.**

**מה שנשאר ב-C8:** service/action, שדות בטופס (תאריך ביטול, "להזמין עד", בורר זמן
התראה, מתג "כבר הזמנתי"), סימון דאבל-בוקינג ב-`BookingList`, והתראות ב-`UpNext`.

### C9 — התראות פוש, ולמה הן בכל זאת חינמיות

> נוסף 2026-08-13 לפי בקשת המשתמש: "שתקפוץ לי התראה כמו נוטיפיקיישן של אפליקציה".
>
> **ההנחה שהצהרתי עליה קודם — שפוש דורש שירות בתשלום — שגויה.** Web Push הוא תקן
> דפדפן: מפתחות VAPID נוצרים מקומית בחינם, ושירות הדחיפה עצמו מופעל על ידי יצרן
> הדפדפן בלי עלות. מה שכן נדרש הוא **מתזמן בצד השרת**, ולזה יש שתי דרכים חינמיות:
> **Vercel Cron** (בתוכנית Hobby — פעם ביום; מספיק בהחלט ל"הזכר לי 3 ימים לפני")
> או **GitHub Actions cron** שקורא ל-route באפליקציה.
>
> **חלקי המימוש:** service worker, זוג מפתחות VAPID במשתני סביבה, טבלת
> subscriptions, route שנקרא על ידי ה-cron וסורק מועדי יעד, ומתג הרשמה בהגדרות.
>
> ⚠️ **דורש פעולה של המשתמש** (יצירת מפתחות, הוספת משתני סביבה, הפעלת cron).
>
> **⚠️⚠️ המגבלה שקובעת אם זה בכלל שווה: באייפון פוש עובד רק כשהאתר מותקן במסך
> הבית** (iOS 16.4+). בטאב רגיל של Safari — לא עובד כלל, וזו מגבלה של אפל שאי
> אפשר לעקוף. לכן נדרש `manifest.json`, והמשתמש חייב "הוסף למסך הבית" לפני
> שיוכל לאשר התראות. באנדרואיד ובדסקטופ זה עובד בטאב רגיל.
>
> **✅ נבנה כבר: `domain/reminders.ts` — `dueReminders`.** זו הלוגיקה שמחליטה מה
> נשלח, והיא **בלתי תלויה במתזמן שייבחר**, כך שהיא נכונה בין אם Vercel Cron ובין
> אם GitHub Actions.
>
> **הכלל שהמשתמש ביקש במפורש (2026-08-13): התראה אחת, בתזמון שהוגדר — לא כל יום.**
> "הזכר לי 3 ימים לפני" הוא נוטיפיקציה אחת, לא שלוש. זה נאכף על ידי כך
> **שהשורה זוכרת שנשלחה** (`cancel_notified_at` / `book_by_notified_at`) ולא על
> ידי התזמון — כך שגם job שרץ פעמיים, נכשל וניסה שוב, או חפף לעצמו, לא יכול
> לייצר נוטיפיקציה שנייה. השאלה היא "האם זה נשלח", לא "האם היום היום הנכון".
>
> **החלון הוא `days <= lead` ולא `days === lead`** — בכוונה. אם ה-cron החמיץ
> הרצה, התאמה מדויקת הייתה מדלגת על התזכורת **לתמיד ובשקט**. השלמה בהרצה הבאה
> נכשלת בבטחה: עדיין נוטיפיקציה אחת בדיוק, רק אולי באיחור יום במקום לעולם לא.
>
> **אסימטריה מכוונת:** מועד ביטול שעבר → שקט (הדלת נסגרה, אין מה לעשות). מועד
> הזמנה שעבר → **כן שולח** (הרכבת לא תזמין את עצמה).
>
> 43 מקרי קצה נבדקו לפני חיבור לכל תשתית.

> **C7 — מה נבנה.** `AiUnavailableError` על `ApiError` עם `status === 503`, ושבעת
> ה-routes מחזירים `ai_busy` (גם הוא 503 — זה הסטטוס הנכון לשניהם, וקוד הגוף הוא
> מה שמבדיל). **הסיווג רוכז ב-`domain/ai-errors.ts`:** לכל אחד מחמשת המסכים היה
> עותק משלו של אותם ענפים, **וזה בדיוק איך שמצב ה-503 לא טופל באף אחד מהם.**
> עכשיו כלל אחד במקום אחד, עם fallback משלו לכל מסך ("בניית הלו״ז נכשלה" מול
> "טעינת המדריך נכשלה"), כי משפט משותף אחד היה פחות שימושי.
>
> **503 בלי קוד מוכר נקרא כמכסה** — הסיבה השכיחה יותר, וגם העצה השמרנית: לשלוח
> מישהו לנסות שוב בקשה שלא תצליח עד מחר מבזבז את זמנו.
>
> **מה שאבד בכוונה:** בצ׳אט הודעת ה-429 הייתה "יותר מדי **הודעות**" ועכשיו היא
> "יותר מדי בקשות". ויתרתי על הניסוח הייחודי תמורת ביטול חמישה עותקים — זה בדיוק
> הכפילות שיצרה את הבאג.
>
> **C7 נוסף 2026-08-13, מתוך אימות C5 ולא מתוך תכנון.** בזמן בדיקת `exclude` מול
> Gemini האמיתי חזר **503 `UNAVAILABLE` — "This model is currently experiencing
> high demand"** בשני נסיונות רצופים, והשלישי הצליח. C1 מיפה רק 429 (מכסה), ולכן
> עומס רגעי נפל ל-`ai_failed` הגנרי והוצג כ"נכשל". זה **מצב שלישי**: לא מכסה
> שנגמרה (חזרו מחר) ולא תקלה (משהו שבור) אלא עומס (נסו בעוד רגע). לאפליקציה כבר
> יש בדיוק את ההבחנה הזו ל-Overpass ("שירות המפות עמוס כרגע"), אז זו יישור קו.
> **הלוג של C1 הוא מה שאפשר לראות את זה בכלל** — לפניו זה היה 502 אילם.

**סדר:** C1 ראשון — הוא חוסם אבחון של כל השאר. C3 לפני C4, כי "מאיפה מגיעים"
צריך קודם לדעת איפה ישנים. **הפריט הבא: C5.**

> **C4 — "איך מגיעים".** קישור Google Maps directions מהלינה של אותו לילה לכל
> פריט בלו"ז, עם `travelmode=transit`. סכמת ה-URL של Maps חינמית ובלי מפתח,
> בשונה מ-Directions API — זו הסיבה שהיא נבחרה (ראו את המגבלה בהמשך).
>
> **שתי פונקציות טהורות בדומיין (`domain/directions.ts`), אחת לכל קצה של המסלול,
> כי שני הקצוות יודעים דברים שונים:**
> - `lodgingOrigin` — **כתובת מנצחת** כשיש, אחרת שם + עיר. הזמנה בלי כתובת עדיין
>   מקבלת קישור עובד, כי Google פותר "Shinjuku Granbell Hotel Tokyo" מצוין.
>   ואם השם כבר מכיל את העיר ("ריוקאן קיוטו" בקיוטו) העיר לא נכפלת.
> - `entryDestination` — **קואורדינטות מנצחות** כשיש: הן חד-משמעיות, וזו הסיבה
>   שפריט שהגיע מחיפוש האטרקציות מנווט לדלת הנכונה ולא לסניף אחר של אותה רשת.
>   רוב הפריטים הם פריטי מדריך AI בלי קואורדינטות, ואז שם + עיר.
>
> **`(0,0)` נדחה במפורש** — זה נקודה במפרץ גינאה, וכנתון אמיתי זה `null` שנכתב
> כמספר. לנווט מישהו לאוקיינוס גרוע מליפול חזרה לשם המקום. גם lat בלי lng,
> טווחים לא חוקיים, `NaN` ו-`Infinity` נדחים — אבל `lat=0` עם lng אמיתי (קו
> המשווה) ו-`lng=0` עם lat אמיתי (גריניץ') הם לגיטימיים ונשמרים.
>
> **הקישור לא מוצג כשאין לינה לאותו לילה** — מסלול ממקום ששום דבר לא מגדיר אינו
> מסלול. לכן היום האחרון של הטיול, שאין בו לילה, מקבל רק "פתח במפה".
>
> **🐞 באג קיים שנתפס בדרך ותוקן.** בכרטיס של פריט קצר בציר השעות (30 דקות = 38
> פיקסלים) שורת הקישורים נחתכה: הכרטיס הוא `overflow-hidden`, ומ-16 פיקסלים של
> קישור נשארו כ-3. **זה היה כך גם לפני C4** — הקישור "🗺️ במפה" היה שם, מרונדר
> ובלתי לחיץ. עכשיו שורת הפעולות פשוט לא מרונדרת מתחת ל-52 פיקסלים (הגובה שהיא
> צריכה: `p-2` למעלה ולמטה + שורת כותרת + שורת קישורים). **שום דבר לא אובד** —
> תצוגת "רשימה" מציגה את שני הקישורים לכל פריט בלי תלות באורכו.
>
> **`withHebrewPrefix` ב-`lib/text.ts`** — התווית בתצוגת הרשימה היא "איך מגיעים
> מ<שם המלון>", ו-"מShinjuku" הדביק תחילית עברית לכתב לטיני. הכלל שהאפליקציה כבר
> מיישמת ב-"פתח ב-Google Maps" הוא מקף לפני מילה לא-עברית, ועכשיו הוא פונקציה:
> "מריוקאן קיוטו" מול "מ-Shinjuku Granbell Hotel". **לא נעשה עם `\b`** — הוא
> מוגדר על תווי ASCII ולא היה נורה סביב עברית או יפנית, כלומר לא היה תופס אף פעם.
>
> **מה שלא נבנה, במכוון:** ניווט מפריט לפריט (ולא מהמלון). הבקשה הייתה מהלינה,
> ושורת המעברים בציר השעות כבר מציגה פער זמן ומרחק בין נקודות. זו הרחבה אפשרית.

> **C3 — הפער שהיה, ואיך נסגר.** `bookingsByDay` (מ-B3) מדלל כל הזמנה לפי
> `starts_at` **בלבד**. זה עונה על "מה קורה היום", ולכן מלון שהוזמן לחמישה
> לילות הופיע ביום הצ׳ק-אין ובשום יום אחר — ול"איפה אני ישן ביום 4" לא הייתה
> תשובה בכלל. **זה לא היה באג בקוד אלא שאלה שאף פונקציה לא שאלה.**
>
> `lodgingByDay` חדשה בדומיין, לצד `bookingsByDay` ולא במקומה — שתי שאלות שונות:
> "מה קורה היום" מול "איפה אני ישן הלילה".
>
> **הכלל:** הזמנה עם צ׳ק-אין ב-X וצ׳ק-אאוט ב-Y מכסה את הלילות `X..Y-1`. **ביום
> ה-Y לא ישנים שם** — בבוקר עוזבים, ולכן היום האחרון של השהות נשאר ריק. תאריכים
> מושווים כמחרוזות `YYYY-MM-DD` (שמסתדרות לקסיקוגרפית) ונפתרים ב-`zone`, מאותה
> סיבה ש-`bookingsByDay` מקבלת אזור: צ׳ק-אין ב-23:40 שייך לתאריך אחר תלוי לוח
> השנה של מי שואלים.
>
> **שלוש החלטות שהיו יכולות ללכת אחרת:**
> 1. **בלי `ends_at` — רק לילת ההגעה.** מפתה להניח שהשהות נמשכת עד ההזמנה הבאה,
>    אבל זה היה **ממציא לילות שהמשתמש לא הזין** — ושגוי ברגע שהזמין מלון שני.
>    המחיר: מי שלא הזין צ׳ק-אאוט יראה לינה ביום אחד בלבד. זה מכוון וגלוי.
> 2. **צ׳ק-אאוט שאינו אחרי הצ׳ק-אין נחשב "לא ידוע"** ולא כטווח שלילי — אותו
>    תאריך, או תאריך מוקדם יותר, לא מלמדים כלום על לילות מאוחרים.
> 3. **חפיפה בין שתי שהיות → הצ׳ק-אין המאוחר מנצח.** חפיפה היא סתירת נתונים, לא
>    תרחיש למיצוע; מי שעבר מלון ישן במלון שנכנס אליו אחרון.
>
> **הצגה:** רכיב `NightStay` (server component, בלי state) בשני מסכים — `/today`
> דרך `DayPager`, ו-`/days` דרך `Itinerary`. **ביום הצ׳ק-אין הרצועה מוסתרת**, כי
> כרטיס ההזמנה שמעליה כבר מציג את אותו מלון עם שעת הצ׳ק-אין — אותו דבר פעמיים
> נראה כמו באג. הרצועה לא מוצגת כלל בלילה בלי לינה: יום שאף אחד לא הזמין הוא
> תשובה לגיטימית, ונדנוד בכל לילה לא־מכוסה היה הופך את הרצועה לרעש.
>
> **`nightStayLabel` יושבת בדומיין** ולא ברכיב, מאותה סיבה ש-`dayLabel` שם:
> הניסוח הוא תכונה של הנתון, לא של המסך. ("צ׳ק-אין" / "ישנים כאן" /
> "הלילה האחרון כאן" / "לילה אחד כאן".)

> **C2 — מה נבנה, ואיפה הייתה המלכודת.** טופס ב־`/explore` מתחת לחיפוש:
> שם · עיר · קטגוריה · כתובת (לא חובה). **בלי migration.**
>
> - **`source='manual'` + `external_id = NULL`** הוא מה שמבדיל מקום שהוקלד ביד
>   ממקום שהגיע מחיפוש OSM. שני הסוגים חולקים את ערך ה־enum, אבל לתוצאת חיפוש
>   *תמיד* יש `external_id` ולמוקלד *לעולם* לא. `getAddedPlaces` כבר מסנן
>   `.not("external_id","is",null)`, ולכן מקום מוקלד לא יכול להפריע לסימוני
>   "כבר בטיול" בחיפוש. זה מה שאיפשר להימנע מ־migration.
> - **הקטגוריות הן שש של `PLACE_CATEGORIES`** ולא ארבע של מדריך ה־AI: יש להן
>   כבר label+emoji, ו־`savedCountsByCategory` סופר בדיוק אותן, כך שהאריחים
>   נשארים נכונים.
> - **הכתובת נשמרת ב־`description`** — בדיוק מה ש־`addPlaceToTrip` כבר עושה עם
>   `address`. **וגם נחשפה:** `SelectedList` הציג שם בלבד, כלומר כתובת הייתה
>   נשמרת ולא נראית. נוספה שורה מקוצצת (`truncate`) מתחת לשם.
> - **קואורדינטות נשארות NULL** במכוון. גאוקודינג של שם עסק הוא בדיוק הבאג
>   שהעיף את "האקונה" ללוס אנג'לס (migration 0005). המפה וחישוב המרחקים מדלגים
>   עליו כמו שהם מדלגים על כל פריט מדריך AI.
>
> **⚠️ המלכודת: `addManualPlace` הוא בכוונה *לא* upsert**, בשונה מ־`addPlaceToTrip`
> שלידו. המפתח הטבעי הוא `(trip_id, city, category, name)`, ושם מוקלד יכול
> להתנגש בשורה קיימת — ו־upsert עיוור היה דורס לה את `source`, `external_id`
> והקואורדינטות בערכים הריקים של שורה מוקלדת. הפיכת שורת `ai` ל־`manual` הייתה
> מוציאה אותה ממדריך העיר (`getSavedCityGuide` מסנן לפי source) ומטווח ההשגה של
> `deleteCityGuide`; איפוס `external_id` היה שובר את ה־badge בחיפוש. **שתי
> התקלות היו שקטות** — בדיוק ההתנהגות של באגי פילטר ה־source משלב 13ב. לכן:
> שורה קיימת רק מסומנת `selected` ומקבלת כתובת אם נמסרה, ושום דבר שכבר ידוע
> עליה לא נכתב מחדש. הקליינט אומר "כבר היה בטיול — סומן כנבחר" במקום להיראות
> כאילו הלחיצה לא עשתה כלום. נוסף גם fallback ל־23505 (unique violation) למקרה
> של שתי הוספות במקביל.
>
> **העיר היא שדה טקסט עם `datalist`** ולא `select`: בטיול חדש הרשימה ריקה, ו־select
> בלי אפשרויות הוא מסלול מתים — הטופס הזה הוא לרוב מה שיוצר את העיר הראשונה של
> הטיול. ה־`datalist` מוצג רק כשיש ערים.
>
> **מה שלא אומת, ולמה:** כתיבה מוצלחת ל־DB דורשת session, ואין לי דרך להזין
> סיסמה. **מה שכן אומת בזמן ריצה:** מסלול הוולידציה מקצה לקצה דרך קריאת Server
> Action אמיתית (עיר של רווחים → "יש למלא עיר." עם `aria-invalid` על השדה הנכון
> בלבד ו־`aria-describedby` שמצביע ל־id של ההודעה), ומסלול הכשל — RLS סירב
> לכתיבה לא־מאומתת, ההודעה הגנרית הוצגה, **והשדות נשארו מלאים** כדי שלא לאבד
> הקלדה. הסירוב הגיע כ־RLS ולא כשגיאת עמודה, כלומר **צורת ה־payload ושמות
> העמודות תקפים מול הסכמה האמיתית.**

> **C1 — מה נבנה בפועל.** נקודת החנק היא `src/lib/ai/provider.ts`, שכל שבעת
> ה-routes עוברים דרכה, ולכן הטיפול נעשה שם פעם אחת ולא שבע:
>
> - **`console.error` לפני כל סיווג** — זה עיקר הפריט. הודעת השגיאה של Gemini
>   כוללת `code`, `status` ו-`reason` (למשל `API_KEY_INVALID`), וכל זה נזרק לפח
>   עד עכשיו. אומת בזמן ריצה: מפתח פסול מייצר בלוג `status: 400` + `API_KEY_INVALID`.
> - **`AiQuotaExceededError`** נזרק כשה-SDK מחזיר `ApiError` עם `status === 429`,
>   וה-routes מחזירים עליו **503 `ai_quota_exceeded`** במקום 502 `ai_failed`.
>   בקליינט: "מכסת ה-AI היומית נגמרה. נסו שוב מחר." — הודעה שלא מזמינה ניסיון חוזר
>   מיידי, בשונה מ-429 שכן.
> - **429 שלנו מול 429 שלהם** — ה-rate limiter שלנו מקדים את הקריאה ומחזיר 429,
>   ולכן 429 מהספק לא יכול להתבלבל איתו. הבחירה ב-503 ולא ב-429 היא בדיוק כדי
>   ששני המצבים ייראו שונה בקליינט.
>
> **שלוש מלכודות שנבדקו במפורש, לא הונחו:**
> 1. **`instanceof` חייב לתפוס.** `ApiError` מיובא מ-`@google/genai`, וה-error
>    נזרק מתוך ה-SDK. אם החבילה הייתה נפתרת לשני עותקים, `instanceof` היה תמיד
>    `false` וענף המכסה היה קוד מת ש**שום דבר לא היה חושף**. אומת מול error
>    אמיתי שה-SDK זרק: `instanceof` = true, `status` = number.
> 2. **אותה שאלה חוצה את גבול המודולים** — ה-route מייבא מ-`@/lib/ai` (barrel)
>    ומה-provider זורק. אומת דרך route זמני שנמחק: error שנזרק **בתוך**
>    `handleProviderError` הגיע ל-route כ-`AiQuotaExceededError`, ה-`cause` נשמר
>    עם `status: 429`, והתשובה הייתה 503.
> 3. **לא לסווג לפי `status` בלבד.** אובייקט עם `status: 429` שאינו `ApiError`
>    מסווג `false` בכוונה — אחרת כל דבר שנושא את השדה הזה היה נראה כמו מכסה.
>
> `isQuotaExceeded` נבדק מול **9 מקרי קצה** לפני חיבור למסך (429/500/400,
> `Error` רגיל, אובייקט מחופש, `null`, `undefined`, מחרוזת, ו-`status` כמחרוזת
> שלא עוברת coercion). כולם עברו.
>
> **מה שלא אומת, ולמה:** מסך השגיאה עצמו חסום מאחורי auth ואין לי דרך להזין
> סיסמה. לכן ה-503 אומת בשרת (route זמני) ולא כמסלול לחיצה בדפדפן. **המשתמש
> יראה את ההודעה בפועל רק כשהמכסה תיגמר** — או בהתחברות + חסימה מלאכותית.
> העיצוב כן אומת: אין רכיב חדש, ההודעה נכנסת לאותו
> `<p className="text-sm text-danger-ink">` שכבר החזיק את הודעת ה-429.

### מה שנקבע בשיחה

| החלטה | הבחירה |
| --- | --- |
| "איך מגיעים" | **קישורי Google Maps directions** — ניווט תחבורה ציבורית אמיתי, חינם, בלי מפתח. יוצא לאפליקציה חיצונית |
| `/design-preview` | נמחק לפני העלייה לאוויר (בוצע ב-`5723f85`) |

### שני ממצאים שקדמו לטבלה

**C1 — למה הבקשות הפסיקו לעבוד.** כל שבעת ה-routes של ה-AI נגמרים ב-
`catch { return 502 "ai_failed" }`. **השגיאה נבלעת ואף לא נרשמת ללוג**, ולכן אין
דרך לדעת מה קרה. מה שכן ידוע: זה לא ה-rate limiter שלנו — הוא מחזיר 429 עם
הודעה מפורשת, והתסמין היה "פשוט לא עובד". החשוד המרכזי הוא **המכסה היומית של
Gemini free tier**, שנשארת חסומה עד למחרת. זה ניחוש כל עוד השגיאה נזרקת לפח,
ולכן זה הפריט הראשון.

> **✅ נסגר ב-C1 (2026-08-13).** השגיאה נרשמת ללוג עם `code`/`status`/`reason`,
> ומכסה (429 מהספק) מקבלת 503 נפרד מ-502. **ההשערה עצמה עוד לא אוששה** — בזמן
> הבנייה המכסה לא הייתה חסומה וקריאה אמיתית ל-Gemini עברה. הלוג הוא הכלי
> שיכריע בפעם הבאה שזה קורה, ולכן אין יותר צורך לנחש.

**C5 — למה חזרו מעט ערים.** `aiCitySuggestionsSchema` **לא מגביל** את מספר
הערים, ו-`aiSuggestRequestSchema` כבר תומך ב-`count` עד 10. כלומר הקיצור מגיע
מניסוח ה-prompt, **ואין בכלל מנגנון "עוד ערים"** — בעוד שרמה 2 (מדריך העיר) כן
מקבלת `exclude` + `count` בדיוק לזה. אז זו הרחבה של דפוס קיים, לא פיצ'ר חדש.

> **✅ נבנה ב-C5 (2026-08-13).** `exclude` נוסף ל-`aiSuggestRequestSchema`
> ול-prompt, וכפתור "הצג עוד יעדים" מוסיף לרשימה במקום להחליף אותה.
>
> **⚠️ המלכודת: לשורות רמה 1 אין הגנת ייחודיות בכלל.** האינדקס מ-0003 הוא על
> `(trip_id, city, category, name)`, ולשורות האלה גם `city` וגם `category` הם
> `NULL` — ו-Postgres מתייחס ל-NULL כשונה מ-NULL, כך שהאינדקס **לא נורה עליהן
> לעולם**. ההערה במיגרציה עצמה אומרת זאת במפורש ("NULL city/category rows are
> unaffected"), אבל בהקשר אחר. לכן `upsert` עם `onConflict` היה חסר תוקף כאן,
> והניכוי חייב לקרות בקוד: `mergeCitySuggestions` מסלקת כפילויות מול הקיים
> **וגם בתוך התשובה עצמה**, כי תשובה אחת של AI יכולה לנקוב באותה עיר פעמיים.
>
> **`appendCities` נפרד מ-`saveCities`**, שמוחק לפני שהוא כותב — נכון לרענון
> מ-prompt חדש, שגוי כאן. אותה הבחנה בדיוק שכבר קיימת בין `saveCityGuide`
> ל-`savePlanFromChat` (שלב 17). הניכוי נעשה מול מה שבטבלה ולא מול מה שהיה על
> המסך, כדי ששני טאבים פתוחים על אותו טיול לא יתחרו לכפילות.
>
> **ה-prompt המקורי לא נשמר** — ההצעות נשמרות, הבקשה שיצרה אותן לא. לכן בביקור
> חוזר הרשימה קיימת והתיאור אבד, וכפתור שדורש prompt היה נעלם בדיוק כשהטיול הכי
> צריך אותו. `moreCitiesPrompt` נופלת לתיאור **שנגזר מהערים עצמן** ("טיול באותו
> אזור ובאותו סגנון כמו…"), כי ה-AI קורא אזור וסגנון מ"קיוטו, אוסקה, טאקאיאמה"
> בלי בעיה — ואותם שמות נשלחים כ-`exclude` בכל מקרה. **זה נמצא באימות ויזואלי:**
> בגרסה הראשונה הכפתור פשוט לא הופיע על רשימה שנטענה מה-DB.
>
> **אם ה-AI מחזיר בכל זאת את אותן ערים** (prompt הוא בקשה, לא הבטחה) מוצגת הודעה
> "לא נמצאו יעדים חדשים" ולא כפתור שנראה שבור.

### מגבלה שחלה על C4

ניתוב תחבורה ציבורית בחינם **לא קיים כ-API** — לא ב-OSM ולא אצל אף ספק. מה
שקיים חינם: קישורי Google Maps directions (תחבורה ציבורית אמיתית, יוצא לאפליקציה
חיצונית), OSRM demo (הליכה ורכיבה בלבד, שרת בלי התחייבות), ומרחק אווירי + הליכה
שכבר מחושבים אצלנו לפריטים עם קואורדינטות. **נבחרו הקישורים.**

> **✅ מיושם ב-C4 (2026-08-13).** המגבלה עומדת בעינה: האפליקציה **לא** מחשבת
> מסלול תחבורה ציבורית, היא מעבירה את השאלה ל-Google Maps. מה שנשאר בתוך
> האפליקציה הוא מרחק אווירי + הליכה בשורת המעברים, ורק לפריטים עם קואורדינטות.
