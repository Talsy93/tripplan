# MyTrip — מסמך תוכנית ומעקב פרויקט

> **איך משתמשים בקובץ הזה:** זהו מקור האמת של הפרויקט. בתחילת כל שיחה חדשה עם Claude — מעלים את הקובץ הזה, ו-Claude יודע בדיוק איפה עצרנו.
> מסמנים `[x]` על כל משימה שהושלמה. מעדכנים את "השלב הנוכחי" בכל פעם שמתקדמים.
> הקובץ יושב בשורש הריפו ומתעדכן יחד עם הקוד.

---

## 🎯 מהות הפרויקט

אפליקציית ווב לתכנון טיולים. המשתמש יוצר פרופיל, יוצר טיולים, ולכל טיול שני מצבים: **תכנון** (קבלת הצעות ליעדים לפי דרישות וזמן, ובחירה מתוכן — כולל הצעות מ-AI) ו**ביצוע** (לו"ז מסודר של היעדים שנבחרו לפי זמנים). בפרופיל רואים את כל הטיולים, כולל תצוגה עתידית בגוגל מפות.

**מטרה כפולה:** אפליקציה שימושית + פרויקט ברמה גבוהה לתיק עבודות (Clean Architecture, TypeScript מלא, CI/CD).

---

## 🏗️ החלטות ארכיטקטורה (סגורות)

| תחום            | החלטה                                   | נימוק                                              |
| --------------- | --------------------------------------- | -------------------------------------------------- |
| פריימוורק       | Next.js + TypeScript (strict)           | Frontend + Backend בפרויקט אחד, סטנדרט תעשייה      |
| Database + Auth | Supabase (PostgreSQL)                   | חינמי, Auth מובנה, RLS להרשאות, SQL אמיתי          |
| אחסון ודיפלוי   | Vercel                                  | חיבור ישיר ל-GitHub, דיפלוי אוטומטי, URL חינמי     |
| CI              | GitHub Actions                          | lint + type-check על כל push                       |
| State מהשרת     | TanStack Query                          | ניהול data fetching, cache                         |
| State מקומי     | Zustand                                 | state של UI בלבד                                   |
| ולידציה         | Zod                                     | סכמה אחת = מקור אמת לטיפוסים, טפסים ו-API          |
| AI              | Gemini (דרך שכבת הפשטה)                 | Free tier נדיב; ההפשטה מאפשרת החלפת ספק בקובץ אחד  |
| פורמט תשובות AI | Structured Output (JSON + ולידציית Zod) | לא טקסט חופשי — נכנס ישר לדאטהבייס                 |
| מבנה ריפו       | ריפו יחיד (לא Monorepo)                 | אפליקציה אחת; מונורפו = מורכבות מיותרת (YAGNI)     |
| ארגון קוד       | Feature-Based                           | קוד שמשתנה יחד גר יחד; הסטנדרט המקובל              |
| קומפוננטות UI   | שתי רמות: ui גנרי / פיצ'ר               | הרעיון מ-Atomic Design בלי הטקס; shadcn/ui לאטומים |

### מבנה תיקיות מחייב

```
src/
├── app/              # Routes ו-UI (כולל app/api — צד שרת)
├── components/
│   ├── ui/           # אטומים גנריים (לא מכירים את הדומיין)
│   └── layout/       # header, navigation
├── features/         # כל פיצ'ר = קופסה סגורה
│   └── <feature>/
│       ├── domain/         # טיפוסים, entities, סכמות Zod
│       ├── application/    # use-cases, hooks
│       ├── infrastructure/ # גישה ל-Supabase / ספקים חיצוניים
│       ├── components/     # קומפוננטות שמכירות את הדומיין
│       └── index.ts        # נקודת הכניסה היחידה של הפיצ'ר
├── lib/              # clients, utils משותפים
└── db/               # סכמת DB, migrations
```

### כללי ברזל (מונעי סלט)

הפירוט המלא בקובץ `ARCHITECTURE.md`. תמצית:

1. פיצ'ר לא מייבא מהקרביים של פיצ'ר אחר — רק מ-index.ts שלו.
2. אף קומפוננטה לא פונה ל-Supabase ישירות — הכול דרך infrastructure.
3. כל ישות = סכמת Zod אחת שממנה נגזר הכול.
4. מפתחות וסודות — רק במשתני סביבה בצד השרת. לעולם לא בקוד.
5. רוחב לפני עומק — פרוסה דקה מקצה לקצה קודם.
6. טיול הוא מכונת מצבים: `planning → executing → completed`.
7. components/ui נשאר "טיפש" — לא מכיר את הדומיין.

### מודל נתונים ראשוני (ישויות)

- **profiles** — נתוני משתמש (מקושר ל-auth של Supabase)
- **trips** — שייך למשתמש; שדות: שם, תאריכים, סטטוס
- **suggested_destinations** — יעד מוצע לטיול; שדות: שם, קואורדינטות, משך משוער, מקור (AI/ידני), נבחר/לא
- **itinerary_items** — יעד שנבחר בלו"ז; שדות: זמן התחלה, זמן סיום, סדר

---

## 📋 שלבי הפרויקט

**השלב הנוכחי: פאזה M — ״שש הפערים שנשארו אחרי T7״ — הושלמה.** פאזה L (T0–T7) סגרה את המסגרת ואת שמונה המסכים; T0 מוזגה ב-PR #63, T1 ב-PR #64, ו-T2 עד T7 נדחפו ישירות ל-`main`. פאזה M סגרה את שישה הפערים שנשארו מול המוקאפ — חלוניות ההקשר, מדריך העיר, ארכיון טיולים, חיפוש חופשי ב-Explore, מחיקה בהחלקה ולחיצה ארוכה, וסצנת הרמס לפרימיטיב.

> **⚠️ חסימה אחת שאינה בקוד: מיגרציה `0020_trip_archive.sql` לא הורצה.** מיגרציות מורצות ביד בעורך ה-SQL של Supabase (README), ועד שהיא תרוץ `trip_status` לא מכיר את הערך `archived` — כלומר כפתור הארכיון ייכשל בזמן ריצה ויציג הודעת שגיאה. הקוד עצמו מלא ונדחף.

**פאזה K — האור, מקצה לקצה. 17 קומיטים, 58 קבצים, +2,322/−685. `type-check` ו-`lint` נקיים; 49 מסלולי `/preview` ומסלולים ציבוריים עברו רינדור בשרת; אומת ויזואלית ב-375, 768 ו-1280:**

1. **האור באמת מאיר.** הבסיס עלה מ-`#0b1020` ל-`#141c3a` ושמונה הפלטות הובהרו — אבל מה שבאמת שינה היה הגרדיאנט של הכתם: הוא השאיר את הגוון ב-0% ונפל לשחור ב-70%, כך שפיקסל בהיר אחד נמרח על ידי טשטוש 42px. עכשיו הגוון מחזיק עד 32%. נוסף `--aura-veil` נפרד מ-`--aura-base`, כי כל עוד הרצפה הייתה עשויה מהבסיס אי אפשר היה להבהיר אחד בלי לאבד את הכיתוב על השני.
2. **אייקוני קו במקום אימוג'י**, בכל הדומיין ובכל 15 המצבים הריקים. הכלל ב-CLAUDE.md התהפך. מפתחות ב-`domain/icons.ts`, מיפוי ב-`components/domain-icon.tsx`.
3. **מסך היום הוא ערימת כרטיסים**, לא רשת של 24 שעות. `daySequence()` ב-`domain/timeline.ts`. `axisHours` ו-`positionPercent` נמחקו.
4. **פס התמונה נמחק** — האור הוא התשובה היחידה לשאלה של מי הטיול. איתו הלכו `lib/place-image.ts`, ה-`remotePatterns` ב-next.config, והווריאנט `wash`.
5. **פעולות הרסניות לא יושבות במנוחה.** מחיקת טיול עברה מכל כרטיס ברשימה לתחתית `/more/trip`; הסרת הזמנה קיבלה אישור — קודם היא מחקה בלחיצה אחת בלי לשאול.
6. **הרַיל בדסקטופ נושא את האור של הטיול** במקום `--brand`. וריאנט `rail` ב-AuraField, סצנת `/preview/side-nav` בודקת שלוש פלטות.
7. **כרטיס "עכשיו"** — `dayNow()` בדומיין, `NowCard` הוא המשטח הכהה היחיד על מסך בהיר ומחזיק את שני הכפתורים היחידים בקיפול. מחזיר `null` כשאין תשובה. שלוש סצנות: עכשיו / נגמר בקרוב / בין פריטים.
8. **פריסת שתי חלוניות בדסקטופ** — `TwoPane`, מ-1280 ומעלה. "מה קרוב" עובר לחלונית ליד הלו״ז במקום מתחתיו. **לא סלוט של AppShell** — בנוי כך קודם, וזה לא עבד: ב-App Router עמוד לא יכול להעביר props ל-layout שלו, כך שאף טאב לא היה יכול למלא אותו. AppShell קיבל סלוט `banner` כדי שפס האור יישאר ברוחב מלא מחוץ לגריד — בלעדיו השוליים השליליים שלו היו זולגים לתוך החלונית ולא אל מחוץ למסך.
9. **הגיבור הוא פס בדסקטופ** — 13rem במקום 22rem מ-lg, הספירה לצד השם ולא מעליו.

**אימות:** `type-check` ו-`lint` נקיים; 49 מסלולי `/preview` והמסלולים הציבוריים (`/`, `/login`, `/signup`, `/reset`, `/privacy`, share עם טוקן שגוי) עברו רינדור בשרת בלי שגיאה; **`npm run build` עובר** — 35 מסלולים, 19 עמודים סטטיים, אפס שגיאות (הורץ על `main` אחרי המיזוג).

**איך מריצים build כשיש דיב-סרבר חי בתיקייה.** המלכודת המתועדת למעלה — build ו-dev חולקים `.next` — נפתרת עם worktree נפרד:

```
git worktree add --detach <tmp> main
cd <tmp> && npm ci && npm run build
git worktree remove --force <tmp>
```

שתי מלכודות בתוך הפתרון עצמו:

- **`node_modules` חייב להיות אמיתי בתוך ה-worktree.** ג'אנקשן (`mklink /J`) ל-`node_modules` של הריפו נכשל, ו-Turbopack נופל עם `Symlink [project]/node_modules is invalid, it points out of the filesystem root`. `npm ci` לוקח כ-22 שניות וזה כל התיקון.
- **ניקוי התיקייה בסוף** דורש `Remove-Item` עם קידומת `\\?\`. גם `rm -rf` וגם `git worktree remove` נכשלים על "Filename too long" בתוך node_modules.

10. **שני קצות הרַיל** — מחליף טיולים למעלה (קישור ל-`/profile`, לא תפריט) וספירה עם פס התקדמות למטה. טיול שהסתיים לא מקבל פס: פס מלא נקרא כמשימה שהושלמה.
11. **הפאנל המואר** — אלמנט מואר אחד במסך הגילוי, הצעת ה-AI. הצבע מגיע ממשתני CSS שהלייאאוט מפרסם (`domain/aura-vars.ts`), כי רכיב עמוק בטאב לא יכול לקבל hues ב-props בלי לגרור אותם דרך חמש שכבות, ואסור לו לחשב אותם מחדש — הקצאה מחדש מטיול בודד נותנת צבע אחר ממה שהפס מציג. `Button` קיבל וריאנט `onLight`.
12. **רצועת שלושת הנתונים** במסך "היום" — מזג אוויר, הוצאות, לינה. מאחורי Suspense משלה, כי היא הדבר היחיד במסך שפונה לרשת.
13. **שורות "עוד" מדווחות מצב** — הזמנות שדורשות תשומת לב (בצבע התראה), התקדמות אריזה, ועם כמה אנשים הטיול משותף. שלוש שאילתות, רצות יחד.
14. **חלונית המפה ליד תוצאות "מה עושים?"** — מ-1280 ומעלה. לשונית "מפה" נשארת: מסך מלא זו שאלה אחרת.
15. **צ׳יפים צפים מעל המפה** — לחיצה עפה לתחנה. `MapFocus` הוא ילד של `MapContainer`, כי `center` נקרא פעם אחת ב-mount ומתעלם משינויים.

**שתי מלכודות שנתקלתי בהן וכדאי שיהיו כתובות:**
- `<ZoomControl position="bottomleft" />` של react-leaflet **זורק**: הוא נטען לפני ש-`_controlCorners` של המפה נבנה, ו-`Control.addTo` ניגש לאובייקט הזה ישירות. המפה נופלת ל-error boundary והצ׳יפים ממשיכים להידלק — כלומר זה נראה כמו "לא עובד" ולא כמו שגיאה. הזזה ב-CSS אין לה מחזור חיים להפסיד בו מרוץ.
- **ה-URL של צ׳אנק ה-CSS לא משתנה בין בילדים.** `curl` לאותו נתיב פעמיים נראה כמו הוכחה שכלל לא נוסף, ואינו. איבדתי על זה זמן.

**מה שנשאר מהעיצוב ולא נבנה:** גיליון תחתון נגרר מעל המפה בטלפון (הצ׳יפים כן נבנו; הגיליון הוא עבודה של ממש מעל Leaflet ולא החלפת מחלקות), ורצועת הימים כאות-יום+תאריך במקום "יום N" — הבדל קוסמטי.

**תיקון לממצא שגוי:** בסקירה נטען שפסקאות העברית במדריך ממורכזות. הן לא — `text-align: start` בכל מקום, נמדד בדפדפן. הקצה השמאלי המשונן של טקסט RTL נראה כמו מירכוז בצילום מסך, וזה מה שקרה.

**פאזות קודמות (היסטוריה):** E ו-F מוזגו ב-PR #41. G, H, I ו-J נבנו על `feature/phase-g-search-map-flights` — **הענף מוזג מאז ל-main, והשורה הזאת תיארה אותו כממתין ל-PR עוד אחרי שנכנס.** מיגרציות 0014, 0015 ו-0016 הורצו. פאזה D הושלמה (D1–D11); D7 — אימות ויזואלי של המסכים המוגנים — עדיין דורש התחברות של המשתמש, וזו הסיבה שהרמס `/preview` קיים.
**פאזה C הושלמה במלואה (C1–C10).**
**✅ C9 (פוש) — מאומת מלא. Vercel Cron רץ לבד, ודחיפה אמיתית הגיעה לאייפון של המשתמש (מסך נעול) ב-2026-08-18.**
**✅ migrations 0011 ו-0012 הורצו (אושר על ידי המשתמש 2026-08-16).**
**✅ C10 (2026-08-17): שורש "שירות ה-AI עמוס" תוקן — `MODEL` הוחלף מ-`gemini-flash-latest` ל-`gemini-3.6-flash` קבוע. פרטים למטה.**

> **↪️ המשך בשיחה חדשה:** קרא קובץ זה + `ARCHITECTURE.md`. אופן העבודה (עברית, הסבר לפני ביצוע, צעד-צעד באישור, פרוסות דקות, PR לכל פיצ'ר, **אין שירותים בתשלום**) מוגדר ב-`CLAUDE.md` ובזיכרון. אחרי כל צעד: לעדכן קובץ זה.
>
> **✅ מצב הענפים (2026-08-31): הכול ממוזג.** `origin/main` = PR #60. כל
> הענפים המקומיים ממוזגים ל-main ונמחקו, ו-`origin` מחזיק רק את `main`.
> אין ענפים פתוחים.
>
> אין `gh` CLI במכונה — פתיחת PR ומיזוג דרך ה-UI של GitHub. יש branch protection
> על `main`: חובה PR + status check `Type-check & Lint`.
>
> **✅ `0010_trip_chat.sql` הורץ** (אושר על ידי המשתמש 2026-08-13). migrations 0001–0010 כולם בתוקף.
>
> _תוקן 2026-08-12: שתי אזהרות שהיו כאן היו שגויות. **שלב 17 כן מוזג** (PRs #17 ו-#18, `git log main --grep=trip-chat`), וכל קבצי הצ'אט כולל ה-migration נמצאים ב-main. הענף `feature/maps-links` כבר לא קיים._
>
> **מה קיים היום (אחרי פאזה B).** עמוד הטיול הוא חמישה מסלולים: `/trips/<id>/today` (ספירה לאחור לפני הטיול, לו"ז היום הנוכחי בזמנו) · `/days` (הלו"ז המלא עם תאריכים) · `/explore` (רשת קטגוריות, חיפוש OSM, נבחרים, גילוי יעדים) · `/map` (Leaflet + תחנות) · `/more` (תפריט → `/more/trip`, `/more/phrases`, `/more/chat`). `/trips/<id>` הוא redirect. בנוסף: `/profile` (hero + רשימת טיולים), `/trips/<id>/city/<city>` (מדריך עיר).
>
> **ספקים חיצוניים — כולם חינמיים ובלי מפתח, למעט Gemini:** Gemini (AI), OpenStreetMap Overpass (מקומות), Nominatim + ויקיפדיה (גאוקודינג ותמונות), Open-Meteo (מזג אוויר), אריחי OSM (מפה).
>
> **חוב ופריטים פתוחים:**
> - ~~**פגיעויות npm**~~ — **נסגרו 2026-08-12** (ענף `chore/security-updates`). `npm audit fix` סגר את `brace-expansion`, `js-yaml` ו-`nanoid`; שדרוג Next ל-16.3.0 סגר את `postcss` ו-`sharp`. `npm audit` מחזיר **0**
>
> ⚠️ **מלכודת סביבה שעלתה תוך כדי, שווה לדעת:** `npm run build` ו-`next dev` חולקים את `.next`, ואחרי build תיקיית פרודקשן נשארת שם. הפעלת dev עליה מייצרת מפל של `Module not found` על הפנימיים של Next עצמו — שנראה בדיוק כמו התקנה שבורה. **אבחנתי את זה לא נכון בהתחלה** והאשמתי את שדרוג Next, ביטלתי אותו, ורק אז גיליתי שהאפליקציה נשברה גם בלעדיו. הכלל: אחרי `npm run build`, לעצור שרתים ולמחוק `.next` לפני `npm run dev`. וגם — הקונסול של הדפדפן בכלי הפיתוח צובר שגיאות מכל הסשן ולא מתנקה בין הפעלות שרת, אז לאימות עדיף `fetch` שבודק את התשובה עצמה
> - **ביקורות למקומות** — ל-OSM אין דירוגים. חלופה: Foursquare free tier (התוכנית מתירה free tier של שירותים מסחריים), דורש שהמשתמש ייצור API key. לא נבחר
> - **תמונות לכרטיסי מקום** — ראו את ההערה על B6 בסוף המסמך
> - פריטים שנדחו במפורש מסומנים `[ ]` בשלבים 8 ו-9
> - ~~**העיצוב מעולם לא עבר סבב ייעודי**~~ — נסגר בפאזה B
>
> **מגבלות מוצהרות שכדאי להכיר לפני שמתכננים מחדש:**
> - **תחבורה בין נקודות** — מרחק מחושב רק כששני הקצוות מגיעים מחיפוש OSM; פריטי מדריך AI הם שם בלבד. ניתוב תחבורה ציבורית לא קיים בחינם **כ-API**, ולכן C4 מעביר את השאלה ל-Google Maps בקישור directions מהלינה לכל יעד — האפליקציה עצמה עדיין לא מחשבת מסלול
> - **מזג אוויר** — 16 יום קדימה, תקרה קשיחה של Open-Meteo
> - **גאוקודינג ערים** — ויקיפדיה עברית; לעיר בלי ערך ייעודי הסיכה עלולה ליפול על אתר מוכר בעיר ולא על מרכזה
> - **שיוך פריט↔עיר בלו"ז** — לפי שם. אם ה-AI ינסח שם מחדש, הפריט יישאר בלי עיר

### שלב 0 — הרשמות והכנת סביבה ✅ הושלם

- [x] התקנת Node.js (גרסת LTS)
- [x] התקנת Git
- [x] חשבון GitHub
- [x] חשבון Vercel (הרשמה דרך GitHub)
- [x] חשבון Supabase (הרשמה דרך GitHub)
- [x] מפתח API של Gemini (דרך Google AI Studio)
- [x] שמירת כל המפתחות במקום בטוח (לא בקוד!)

### שלב 1 — שלד הפרויקט ✅ הושלם

- [x] יצירת פרויקט Next.js + TypeScript (דרך create-next-app)
- [x] הרצה מקומית מוצלחת (localhost:3000)
- [x] הוספת חבילת הקבצים: מבנה תיקיות, ARCHITECTURE.md, .env.example
- [x] הפעלת strict mode ב-tsconfig (בדיקה שקיים)
- [x] ריפו ב-GitHub + push ראשון

### שלב 2 — דיפלוי ראשון ✅ הושלם

- [x] חיבור הריפו ל-Vercel
- [x] עמוד "Hello MyTrip" עולה לאוויר
- [x] וידוא: push ל-main ⟵ דיפלוי אוטומטי

### שלב 3 — Supabase ו-Auth ✅ הושלם

- [x] יצירת פרויקט Supabase (אזור: eu-central)
- [x] משתני סביבה (מקומית + ב-Vercel)
- [x] הרשמה / התחברות / התנתקות (אימייל+סיסמה + Google OAuth)
- [x] middleware להגנה על עמודים (`src/proxy.ts` — שם ה-middleware ב-Next.js 16)
- [x] טבלת profiles + יצירה אוטומטית בהרשמה (RLS + trigger, ב-`src/db/migrations/0001_profiles.sql`)

### שלב 4 — Database Design ✅ הושלם

- [x] טבלת trips עם שדה status
- [x] טבלאות suggested_destinations + itinerary_items
- [x] RLS policies — כל משתמש רואה רק את שלו
- [x] סכמות Zod תואמות לכל ישות

### שלב 5 — CI ✅ הושלם

- [x] GitHub Actions: type-check + lint על כל push
- [x] בדיקה שה-CI חוסם merge כשיש שגיאות (branch protection על main: PR + status check `Type-check & Lint`)

### שלב 6 — פרוסה דקה מקצה לקצה ✅ הושלם

- [x] יצירת טיול ריק מה-UI
- [x] שמירה בדאטהבייס
- [x] הצגת רשימת הטיולים בפרופיל

### שלב 7 — תשתית AI ✅ הושלם

- [x] שכבת הפשטה לספק AI
- [x] route בשם /api/ai/suggest
- [x] Structured Output + ולידציית Zod
- [x] Rate limiting בסיסי

### שלב 8 — מסך תכנון (הצעות יעדים) ✅ הושלם

- [x] עמוד טיול בודד `/trips/[id]`
- [x] הצעות ערים מ-AI (שכבה 1) + קליק לעמוד עיר
- [x] עמוד עיר `/trips/[id]/city/[city]` עם מדריך עיר עשיר (שכבה 2): רקע על העיר, "איך מגיעים", ואזורי לינה/מסעדות/אטרקציות/חוויות
- [x] בחירת פריטים ("הוסף לטיול") + הצגתם בעמוד הטיול
- [x] הוספת נקודת עניין ידנית (source=manual) — נדחה בשלב 8, **סופק ב-C2 (2026-08-13)**
- [ ] תמונות ליעדים/המלצות — נדחה (יחד עם Maps/Places בשלב 10)

### שלב 9 — מסך ביצוע (לו"ז) ✅ ליבה הושלמה

- [x] לו"ז יומי: ה-AI מסדר את הפריטים שנבחרו לימים ולפי שעות (מעבר ל-`executing`)
- [x] מחיקת פריט מהלו"ז + הסרה מ"מה שבחרתם" (ביטול בחירה)
- [x] המסלול המומלץ (סדר גאוגרפי/זמן הגיוני) — כפתור "פתח ב-Google Maps" בשלב 10
- [ ] הערות אישיות לעריכה (כרגע מוצגת הערת AI לכל פריט) — נדחה
- [ ] מילוי "חורים" בלו"ז — הצעות למשבצות ריקות (למשל ארוחת בוקר לפני אטרקציה שמתחילה ב-12:00) — רעיון להמשך

### שלב 10 — ניווט ומפה (חינמי) ✅ חלקי

> **כלל הפרויקט:** אין שירותים בתשלום. Google Maps Platform (דורש חיוב) הוחלף ב-OpenStreetMap + קישורי URL חינמיים ל-Google Maps.

- [x] קישורי "פתח ב-Google Maps" לכל פריט (לו"ז, נבחרים, מדריך עיר) — קישור URL, חינם
- [x] מפה אינטראקטיבית עם סיכות (OpenStreetMap + Leaflet, חינם, בלי מפתח) — **סופק בשלב 12** (טאב מפת מסלול), והורחב ב-K9/F9 בתיקון ערים שנחתו במדינה הלא נכונה
- [x] דרכי הגעה/ניווט (קישורי Google Maps directions, או OSRM חינמי) — **סופק ב-C4 (2026-08-13)**: קישורי directions מהלינה לכל יעד, `travelmode=transit`

---

> **חזון חדש (2026-07-26):** המשתמש הוסיף רשימת פיצ'רים ל-UX ולפונקציונליות. קופלו כשלבים 11–17 למטה. **החלטות רוחביות שנסגרו:** (1) **אין שירותים בתשלום** — תמונות תמיד ממקור חינמי (Wikipedia REST API / Unsplash·Pexels free tier), חיפוש מקומות דרך OpenStreetMap Overpass, מזג אוויר דרך Open-Meteo. (2) **מפת מסלול** = תמונת "לנדינג" שעושה חשק (לא חייבת דיוק גאוגרפי), לצד מפה אינטראקטיבית אמיתית ללו"ז. (3) **סדר בנייה** — רוחב לפני עומק, פרוסה דקה בכל פעם; מתחילים משלב 11.
>
> **עיצוב (רוחבי, מתמשך):** ליטוש חוויית משתמש מודרנית וצעירה על בסיס פלטת Beige & Mocha הקיימת — צבעים חמים ורכים, פונט נעים, רספונסיבי, RTL. משתפרת עם כל שלב, לא כשלב נפרד.

### שלב 11 — תאריך יציאה וספירה לאחור ✅ הושלם (מוזג ב-PR #11, אומת ויזואלית)

> `trips.start_date` / `end_date` כבר קיימים ב-DB וב-`tripSchema` — אין צורך ב-migration.

- [x] בורר תאריך יציאה/חזרה בעמוד הטיול (בסוף התכנון) + Badge ספירה בכותרת
- [x] כרטיס "הטיול הקרוב" בפרופיל (מסך הבית): ספירת ימים עד היציאה
- [x] תמונת יעד חינמית בכרטיס (Wikipedia API, בלי מפתח; נפילה חיננית לגרדיאנט חם)

### שלב 12 — טאב מפת מסלול ✅ הושלם

**12א — מפה + geocoding** ✅ (ב-PR פתוח `feature/route-map`)

- [x] מפה אינטראקטיבית עם תחנות ממוספרות לפי סדר + קו מקווקו בין ערים — Leaflet + OSM
- [x] `src/lib/geocode.ts` — ויקיפדיה (he→en) ואז Nominatim כגיבוי, שניהם חינמיים ובלי מפתח
- [x] cache קואורדינטות בעמודות `latitude`/`longitude` הקיימות (שורת `overview` לכל עיר) — בלי migration חדש לסכמה
- [x] `Tabs` גנרי ב-`components/ui`; עמוד הטיול הורחב ל-`max-w-4xl`

> **סדר התחנות** נקבע לפי סדר ההוספה (`created_at`), לא לפי הלו"ז — כדי שהמפה תעבוד גם לפני שנבנה לו"ז. ישודרג ב-12ב.
>
> **דיוק:** לערים בעלות שם עברי הסיכה מגיעה מהערך המתאים בוויקיפדיה העברית ועשויה ליפול על אתר מוכר בעיר במקום על מרכזה (סטייה של קילומטרים בודדים). מספיק למפת מסלול ברמת ערים.

**12ב — לו"ז לצד המפה** ✅

- [x] migration 0006: עמודת `city` ל-`itinerary_items` + backfill ללו"ז קיים
- [x] `saveItinerary` מקבל את הפריטים שנבחרו וממפה `entry.name → city` (השוואה מנורמלת)
- [x] `itineraryItemSchema` יושרה למצב האמיתי של הטבלה (0002+0004+0006) — חוב שנסגר
- [x] לו"ז לצד המפה, ממוספר לפי מספרי התחנות + מספר לילות ליעד
- [x] סדר התחנות עבר מ-`created_at` לסדר הימים בלו"ז

> **ספירת לילות:** לילה = מעבר בין ימים, והיום האחרון של הטיול לא מוסיף לילה — ימים 1–3 ביעד = 3 ימים / 2 לילות.
>
> **יום שנוגע בכמה ערים** משויך לעיר של הפריט האחרון בו — שם ישנים.
>
> **סייג:** השיוך הוא לפי שם, כי ה-AI מתבקש להעתיק שמות מהרשימה. אם ינסח מחדש — הפריט יישאר בלי עיר ולא ייספר. הדרך ההרמטית היא להחזיר `city` מה-AI בסכמה.

**12ג — תמונת לנדינג** ✅

- [x] באנר בראש טאב המסלול: תמונת התחנה הראשונה + שם הטיול + "N תחנות · N ימים · N לילות"
- [x] אותה שפה ויזואלית כמו `UpcomingTripCard` במסך הבית (גרדיאנט כהה לקריאוּת, נפילה לגרדיאנט חם כשאין תמונה)
- [x] `routeSummary` בדומיין — ימים נספרים על פני כל המסלול, כך שיום מפוצל בין שתי ערים לא נספר פעמיים

### שלב 13 — חיפוש אטרקציות בזמן אמת ✅ הושלם

- [x] חיפוש לפי קטגוריות או חופשי — OSM Overpass (חינם, בלי מפתח); טאב שלישי "אטרקציות"
- [x] פתיחת תוצאה במודאל, סינון לפי יעד, "הוסף למסלול"
- [x] סימון על תוצאה שכבר במסלול — באיזה יום

> **שלוש עובדות על Overpass שנמדדו, לא נקראו בתיעוד** (ראו את ההערה בראש `src/lib/overpass.ts`):
> 1. **אין התאמת שם בשאילתה.** אין אינדקס על ערכי תגיות — `["name"~"x",i]` הוא סריקה: 57 שניות או 504. סינון לפי תגית מאונדקס (≈5ש'). השם מותאם ב-JS, וזה גם מחפש בכל וריאנטי השם.
> 2. **GET ולא POST.** Next לא שומר POST ב-cache, כך שה-`revalidate` היה חסר תוקף.
> 3. **504 הוא עומס רגעי, לא עלות.** אותה שאילתה נפלה ב-2 ק"מ והצליחה ב-5, ואותה שאילתת 5 ק"מ נפלה ופעלה בהפרש דקות. הרדיוס ומספר הקטגוריות לא מנבאים כלום → retry אחד.
>
> **שמות OSM הם בשפה המקומית** — הצגה: `name:he → name:en → name`; השם המקומי נשמר ומוצג במודאל.
>
> **סינון איכות:** 39% מהתוצאות הן שם ותו לא. מוצג רק מה שמוכר (ערך בוויקיפדיה) או בעל 2+ פרטים. דירוג: מוכר → עושר פרטים → מרחק.
>
> **⚠️ אין ביקורות ב-OSM.** מספרי ביקורות הם נכס מסחרי (Google/Foursquare). התג "מוכר" הוא התחליף הזמין. אם תרצו ביקורות אמיתיות — Foursquare free tier, ראו את פסקת ההחלטות.
>
> **`source='manual'`** — ערך ה-enum מ-0002 שלא היה בשימוש — הוא הבית של מקומות מהחיפוש. אלה השורות היחידות בטבלה שהקואורדינטות שלהן לא עברו גאוקודינג.

### שלב 14 — מסלול יומי גרפי ✅ הושלם

- [x] תצוגה גרפית של היום על ציר שעות — בלוקים לפי שעות, קווי שעה, מתג "ציר שעות/רשימה"
- [x] מעברים בין נקודות: פער הזמן + מרחק אווירי + זמן הליכה משוער איפה שיש קואורדינטות

> **הזמנים הם טקסט חופשי.** ה-prompt מבקש `"HH:MM"` ושום דבר לא אוכף. `parseTimeLabel` קולט `9:00`/`09.00`/`"בין 08:00 ל-09:00"` ודוחה `24:00`/`09:60`. פריט שזמנו לא נקרא **לא נזרק** — מופיע תחת "בלי שעה מוגדרת". סיום שקודם להתחלה מקבל בלוק ברירת מחדל.
>
> **⚠️ "תחבורה" סופקה חלקית, במכוון.** מרחק דורש קואורדינטות לשני הקצוות, ויש כאלה **רק** לפריטים שהגיעו מחיפוש האטרקציות (13ב) — פריטי מדריך AI הם שם בלבד. אז ברוב המעברים מוצג רק פער הזמן. **ניתוב תחבורה ציבורית לא קיים בחינם בכלל** — אין ספק. הליכה מוצגת רק עד 2.5 ק"מ (4.5 קמ"ש × 1.3 עיקוף רחובות); מעל זה לא נאמר כלום, כי מספר מומצא גרוע ממספר חסר.
>
> **הדרך לכיסוי מלא** אם ירצו: להחזיר קואורדינטות מה-AI בסכמת המדריך, או לגאוקד פריטי מדריך — שניהם יקרים ולא נבחרו.

### שלב 15 — פרטי טיול ולוגיסטיקה ✅ הושלם

- [x] ישויות חדשות: טיסות/רכבות/לינה — טבלה `trip_bookings` (migration 0008) + טאב "לוגיסטיקה"
- [x] הערות, מספרי אישור והתרעות על מה שמתקרב
- [x] תחזית מזג אוויר ליעדי הטיול (Open-Meteo, חינם, בלי מפתח)

> **טבלה אחת ולא שתיים.** טיסה, רכבת ומלון חולקים שלד — כותרת, טווח זמן, אישור, הערה — ונבדלים רק בכך שתחבורה צריכה מוצא+יעד ולינה כתובת אחת. זה עלה שתי עמודות nullable מול טבלה+שירות+RLS+UI כפולים.
>
> **התרעות נגזרות, לא נשמרות.** לשמור "בעוד 3 שעות" = להתחייב לסנכרן מול השעון.
>
> **⚠️ Open-Meteo — 16 ימים קדימה, קשיח.** מעבר לזה HTTP 400, לא תשובה ריקה. לכן `forecastWindow` מחליט לפני כל קריאת רשת, ולטיול רחוק מוצג "התחזית תהיה זמינה בעוד N ימים" — לא פאנל ריק שנראה שבור. טיול שחוצה את הקצה נחתך לחלק שניתן לחזות.
>
> **קואורדינטות למזג אוויר** מגיעות מה-cache של מפת המסלול — אפס גאוקודינג נוסף.

### שלב 16 — טאב מילים שימושיות ✅ הושלם

- [x] שיחון לפי נושאים: עברית / אנגלית / שפת היעד + **תעתיק בעברית** (דרך AI)
- [x] שפת היעד מוסקת מהערים בטיול — לא נשאלת מהמשתמש
- [x] נשמר ב-`trip_phrasebooks` (migration 0009) עם כפתור "בנה מחדש"

> **התעתיק הוא הפיצ'ר.** קורא עברית לא יכול לקרוא "ありがとう" — בלי שורת ההגייה, העמודה בשפת המקום היא קישוט. ה-prompt אומר את זה במפורש, וגם שפה באותיות לטיניות מקבלת תעתיק.
>
> **שפת היעד לא נשמרת על טיול** — האפליקציה מכירה ערים, לא מדינות. ה-AI מסיק אותה מהערים ומחזיר אותה, וזה נשמר, כך ששיחון תמיד יודע לומר באיזו שפה הוא.
>
> **`phrases` הוא jsonb ולא טבלה.** נקרא תמיד בשלמותו, אף פעם לא מסונן או מצורף — שורות היו קונות מבנה שאיש לא שואל עליו. הצורה נאכפת ב-Zod בכניסה **וביציאה**: מה שיש בעמודה הוא מה שנכתב אליה, ושינוי צורה צריך להתגלות כשיחון חסר ולא כקריסה באמצע רינדור.

### שלב 17 — שיחת AI לבניית מסלול ✅ הושלם

- [x] **17א** — צ'אט מתמשך שנשמר (`trip_chat_messages`, migration 0010) ומכיר את תוכן הטיול
- [x] **17ב** — "בנה מסלול מהשיחה": הפקה מובנית מהתמליל → תצוגה מקדימה → אישור → כתיבה

> **התוספת היחידה לתשתית ה-AI מאז שלב 7:** `generateText` (רב-תורי) לצד `generateStructured`. פונקציות נפרדות ולא דגל — אחת מחזירה טקסט לאדם, השנייה אובייקט מאומת ל-DB.
>
> **תורות = שורות** (בניגוד לשיחון ב-0009 שהוא jsonb): נוספות אחת-אחת ונקראות לפי סדר. שאלה ותשובה נכתבות יחד, כדי שלא תיווצר שיחה שבה המודל דיבר בלי שנשאל. ההיסטוריה חסומה ב-24 תורות — כל תור נשלח מחדש בכל בקשה.
>
> **⚠️ עיקרון: השיחה לא משנה את הטיול בעצמה.** הכפתור **מציע** בלבד; ה-route לא כותב כלום. תצוגה מקדימה עם summary למעלה חושפת קריאה שגויה לפני שנשמר משהו, ורק אישור שני כותב.
>
> **הכתיבה מוסיפה בלבד.** `saveCities` הקיים מוחק לפני שהוא כותב — נכון לרענון הצעות, שגוי כאן. `savePlanFromChat` נפרד ממנו: ערים חדשות בלבד, שורות overview ב-`ignoreDuplicates` כדי לא לדרוס intro קיים, ופריטים כן ב-upsert כדי לסמן `selected` גם על פריט שכבר היה.
>
> **⚠️ דיווח מהמשתמש (2026-08-16): "שירות ה-AI עמוס כרגע" באמצע שיחה.**
> **אובחן: ההודעה אמיתית ומדויקת, לא באג בסיווג.** זה בדיוק המצב שגילינו וטיפלנו
> בו ב-C7 — Gemini עצמו מחזיר 503 UNAVAILABLE, וזה נתפס פעמיים ברצף גם באימות
> C5. C7 עובד כמתוכנן: לפני התיקון זה היה מוצג כ"נכשל" גנרי בלי לרמוז שכדאי
> לנסות שוב; עכשיו ההודעה אומרת בדיוק את זה.
>
> **אבל הבדיקה חשפה באג אמיתי אחר, בהתאוששות מהכשל:** בכל כשל בצ'אט (עומס,
> מכסה, או רשת) — ההודעה שכבר הוצגה באופטימיות **נשארה בתצוגה לצמיתות** בלי
> שנשמרה אי פעם בשרת, ושדה ההקלדה כבר התרוקן. בפועל: אחרי שגיאה יש "הודעת רפאים"
> בצ'אט שנראית שנשלחה ולא נשלחה, וניסוח ההודעה המקורי אבד — צריך להקליד הכול
> מחדש. תוקן: בכשל, ההודעה חוזרת לשדה ההקלדה במדויק והבועה האופטימית מוסרת.
> **אומת בזמן ריצה** דרך `/api/ai/chat` אמיתי (401 לא-מאומת, אותו מסלול קוד כמו
> כל כשל אחר): הטקסט המקורי שוחזר תו-במילה, אפס בועות יתומות, וכפתור השליחה
> מוכן לניסיון חוזר מיידי.
>
> **⚠️ תיקון להערכה שלמעלה — האבחון "עובד כמתוכנן" נעצר מוקדם מדי.** כשההודעה
> חזרה על עצמה על פני כמה תכונות AI שונות, "עומס רגעי" הפסיק להתאים. שורש הבעיה
> נמצא ותוקן ב-**C10** למטה: לא Gemini שעמוס אלא ה-alias `gemini-flash-latest`
> עצמו, שנכשל ב-503 בכ-2 מכל 3 קריאות. C7 סיווג נכון מה שקרה; C10 תיקן שזה
> יקרה בכלל.

---

## 📔 יומן החלטות

| תאריך      | החלטה / שינוי                                                                                                                      |
| ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 2026-07-14 | נסגר הסטאק: Next.js + Supabase + Vercel + Gemini. נוצר מסמך זה.                                                                    |
| 2026-07-14 | הושלם שלב 0 (הרשמות). הוחלט: ריפו יחיד (לא מונורפו), ארגון Feature-Based, קומפוננטות בשתי רמות עם shadcn/ui. נוסף ARCHITECTURE.md. |
| 2026-07-14 | הושלם שלב 1: נוצר פרויקט Next.js + TypeScript (strict), נוספה חבילת הארכיטקטורה, הריפו עלה ל-GitHub בחשבון האישי.                  |
| 2026-07-14 | הוגדרה הפרדת זהויות גיט במחשב העבודה: config מקומי לריפו (אימייל אישי) + credential.useHttpPath לניהול הרשאות לפי ריפו.            |
| 2026-07-14 | תוקן: תיקיית app הועברה לתוך src (git mv) + עדכון paths ב-tsconfig, בהתאם ל-ARCHITECTURE.md.                                       |
| 2026-07-14 | הושלם שלב 2: הריפו חובר ל-Vercel, האתר חי בכתובת tripplan-ten.vercel.app, אומת דיפלוי אוטומטי על push.                             |
| 2026-07-23 | נוצר CLAUDE.md (תמצות תוכנית + ארכיטקטורה + אופן עבודה). סומנו שני הסעיפים הראשונים של שלב 3 שבוצעו ידנית: פרויקט Supabase נוצר ומשתני הסביבה הוגדרו מקומית וב-Vercel. |
| 2026-08-27 | **תוקן: `ARCHITECTURE.md` תיאר סטאק שלא קיים.** פרק ה-Stack שלו עוד הצהיר על TanStack Query, Zustand ו-shadcn/ui. `CLAUDE.md` תיקן את זה ב-2026-08-12 והקובץ המחייב לא עודכן — כלומר במשך שבועיים המסמך הסמכותי היה זה עם המידע השגוי. עודכן, ונוסף לו פרק "Access Model" ורשומות לכל הישויות החדשות. |
| 2026-08-27 | `README.md` הוחלף — הוא היה ה-boilerplate של create-next-app מ-14.7. בפרויקט תיק עבודות זה הקובץ הראשון שנפתח. |
| 2026-08-27 | **פאזה K:** מסך ציוד נכנס כערך ב״עוד״ ולא כטאב שישי — פאזה D צמצמה בכוונה מ-6 ל-5 טאבים, ורשימת אריזה בשימוש אינטנסיבי לכמה ימים ואז לא, אותו היגיון שמיקם שם את ״פרטי הטיול״. |
| 2026-08-27 | **הוכרע: זהות בשיתוף היא אימייל; מספר טלפון הוא ערוץ מסירה.** phone auth ב-Supabase דורש ספק SMS בתשלום, וגם ה-mailer המובנה מוגבל לכמה הודעות בשעה — לכן האפליקציה לא שולחת כלום ומייצרת קישור שהבעלים שולח מוואטסאפ/SMS/מייל של עצמו דרך `wa.me` / `sms:` / `mailto:`. חינמי, ונפתח יותר מהודעה טרנזקציונית מדומיין לא מזוהה. |
| 2026-08-27 | **הוכרע: חבר מזוהה רואה את הטיול לא מצונזר**, בשונה מהקישור הפומבי. הצנזור קיים כי קישור עובר לכל אחד; חבר הוא חשבון שהבעלים נקב בו בשמו, ומי שנוסע איתך צריך את כתובת המלון ומספר האישור. |
| 2026-08-27 | **נתפס חור אבטחה ב-migration 0018 בקריאה חוזרת:** `WITH CHECK` על `trips` נבדק מול השורה החדשה, אבל `can_edit_trip(id)` קורא מ-`trips` ורואה את השורה הישנה — כך ש-editor יכול היה לשלוח PATCH ל-REST API ולהפוך לבעלים. תוקן בטריגר `BEFORE UPDATE` (לא בהרשאת עמודה — Supabase מעניק UPDATE ברמת הטבלה, ושלילת עמודה בודדת ממנה משמיטה בשקט כל עמודה עתידית). |
| 2026-09-01 | **פאזה L, T0: הטור הראשי נשאר 660px והמידה עברה לטקסט.** בדיקת T0 דרשה שורה של ~70 תווים בטור הראשי, אבל 660px הוא הרוחב שהעיצוב מצייר בו כרטיסים, שורות יום וכרטיסי הזמנה — ובעברית שורה של 660px מגיעה ל-87 תווים. שני המספרים לא יכולים להתקיים יחד, וצמצום הטור היה מצמצם גם את כל מה שלידו. לכן נוסף `--container-measure: 58ch` שיושב על טקסט בלבד. 58 ולא ה-`prose` של Tailwind (65ch), כי `ch` הוא רוחב הספרה `0` והספרות של Heebo רחבות מהאותיות שלה: 65ch נותן 77–82 תווים עבריים, 58ch נותן 65–73 בכל גדלי הגופן שנבדקו. |
| 2026-08-27 | **תובנה שהוכללה לכלל:** `break-words` לא מקטין את min-content — רק `overflow-wrap: anywhere` (Tailwind `wrap-anywhere`). התיקון הראשון לחריגה האופקית נכשל בגלל זה, ורק מדידה בדפדפן גילתה שהעמודה נשארה 744px בתוך 320px. |
| 2026-08-28 | **באג פרודקשן שלי: שלילת EXECUTE מ-anon על פונקציות שמדיניות RLS קוראת להן הפילה את כל שמונה טבלאות הטיול** ב-42501 במקום להחזיר ״אין שורות״. מדיניות RLS מוערכת בתפקיד שמריץ את השאילתה. תוקן ב-0019, ו-0018 תוקן במקום. |
| 2026-08-28 | **באג פרודקשן שלי: `?next=` על כתובת ה-callback שבר התחברות Google.** Supabase מאמת את redirectTo מול רשימת Redirect URLs, וכתובת עם query לא מתאימה לרשומה בלי wildcard. היעד עבר לקוקי httpOnly קצר-חיים. |
| 2026-08-28 | **הוכרע: אין מסלול שחזור סיסמה = חשבון נעול לצמיתות.** נבנו `/reset` ו-`/reset/confirm` וכן ״קביעת/שינוי סיסמה״ בפרופיל. הבקשה מדווחת אותה תוצאה לכתובת רשומה ולא רשומה, כדי לא לאפשר בדיקה מי רשום כאן. **דורש הוספת `/reset/confirm` לרשימת ה-Redirect URLs ב-Supabase.** |
| 2026-08-28 | **הובהר: הרשמה דרך Google לא יוצרת סיסמה.** אין ״אותו אימות״ — צריך לקבוע סיסמה במפורש. `hasPasswordIdentity()` נגזר ממערך ה-identities, כי Supabase לא חושף ״יש סיסמה״ כשדה. |
| 2026-08-28 | **הוסרו קביעת סיסמה ושינוי סיסמה מתוך האפליקציה.** הסיסמה משתנה רק דרך הקישור במייל. זה משנה את מודל האיום לטובה: מי שהשתלט על סשן לא יכול לשנות סיסמה ולנעול את הבעלים בחוץ. |
| 2026-08-28 | **הוכרע: חסימת חשבון Google מקביעת סיסמה קורית ב-setNewPassword ולא בטופס הבקשה.** בטופס זו הייתה הופכת לאורקל — אפשר להקליד כל כתובת ולגלות אם לאדם יש חשבון ובאיזו שיטה. בנקודה שנבחרה הקורא כבר הוכיח שליטה בתיבה. |
| 2026-08-28 | **נוסף `/privacy` עם אישור חובה בהרשמה.** המדיניות נכתבה מהקוד — כל צד שלישי בה מופה מהמקור. `z.literal(true)` ולא `z.boolean()`, ואכיפה בצד השרת: `required` ב-HTML הוא נוחות ולא בקרה. |
| 2026-08-28 | **אישור מייל בהרשמה הוא הגדרה ב-Supabase ולא קוד.** הקוד מטפל בשני המצבים. ההפעלה גוררת שתי השלכות: מגבלת המיילים של ה-free tier חלה גם על הרשמות, וזרימת ההזמנה מתארכת בשלב אימות. |
| 2026-07-23 | נבנה פיצ'ר auth (`src/features/auth`) לפי שכבות הארכיטקטורה: סכמת Zod, שכבת infrastructure מול Supabase (`@supabase/ssr`), Server Actions, טפסים, ועמודי login/signup. נוספה גם התחברות עם Google (OAuth/PKCE) עם Route Handler ב-`/auth/callback`. תוקן ערך שגוי ב-`.env.local` (הוסרה סיומת `/rest/v1/` מכתובת ה-Supabase). |
| 2026-07-23 | נוסף `src/proxy.ts` (ה-middleware בשמו החדש ב-Next.js 16) עם עוזר `src/lib/supabase/middleware.ts`: רענון session בכל בקשה + הפניית משתמש לא-מחובר ל-`/login`. עמודים ציבוריים: `/`, `/login`, `/signup`, `/auth/*`. |
| 2026-07-23 | הושלם שלב 3: נוצרה טבלת `public.profiles` עם RLS (כל משתמש רק את שלו) ו-trigger `on_auth_user_created` ליצירה אוטומטית בהרשמה (כולל שאיבת שם/תמונה מ-Google). ה-SQL תועד ב-`src/db/migrations/0001_profiles.sql` ונוצרה סכמת Zod ב-`src/features/profile`. |
| 2026-07-23 | האפליקציה הותאמה לעברית ו-RTL: `lang="he" dir="rtl"`, גופן Heebo (עברית+לטינית), תרגום כל הטקסטים, הודעות Zod ושגיאות Supabase בעברית. שדות אימייל/סיסמה נשארים LTR. |
| 2026-07-23 | הושלם שלב 4: נוצרו טבלאות `trips` (status כ-enum, תאריכים אופציונליים), `suggested_destinations`, `itinerary_items`, עם RLS (כל משתמש רק את שלו, תת-ישויות דרך הטיול). ה-SQL ב-`src/db/migrations/0002_trips.sql`, סכמות Zod ב-`src/features/trips` (שלוש הישויות כאגרגט טיול). |
| 2026-07-23 | נוסף CI ב-GitHub Actions (`.github/workflows/ci.yml`): type-check + lint על כל push/PR (Node 20). נוסף סקריפט `type-check`. אומת: ה-run הראשון על main הסתיים בהצלחה. |
| 2026-07-23 | הושלם שלב 5: הוגדר branch protection על `main` (ruleset "main-protection"): חובה PR + מעבר status check `Type-check & Lint` לפני merge. מעתה אין push ישיר ל-main — כל שינוי דרך branch + PR. |
| 2026-07-23 | הושלם שלב 6 (פרוסה דקה מקצה לקצה): פיצ'ר `trips` קיבל שכבות infrastructure/application/components — יצירת טיול (שם בלבד) ושמירה ב-Supabase, והצגת רשימת הטיולים בעמוד `/profile` המוגן. נבנה על branch `feature/trips-slice` (זרימת PR ראשונה). |
| 2026-07-23 | הושלם שלב 7 (תשתית AI): שכבת הפשטה `src/lib/ai` מעל `@google/genai` (מודל `gemini-flash-latest`), Structured Output דרך `z.toJSONSchema` + ולידציית Zod, route `POST /api/ai/suggest` עם אימות ו-rate limiting בסיסי (`src/lib/rate-limit.ts`). תוקן ה-proxy כך שנתיבי `/api` מחזירים 401 במקום הפניה ל-login. סכמות AI ב-`src/features/trips/domain`. |
| 2026-07-23 | שלב 8 (חלק): נבנתה חוויית גלישה דו-שכבתית — הצעות ערים (שכבה 1) ומדריך עיר מקוטלג (מלונות/מסעדות/אטרקציות/חוויות) עם "עוד תוצאות" לכל קטגוריה (שכבה 2). routes: `/api/ai/city-guide`, `/api/ai/recommendations`. מוזג ב-PR #3. |
| 2026-07-23 | פאס עיצוב: מערכת עיצוב עם פלטת Beige & Mocha (משתני CSS, תמה בהירה יחידה, צללים חמים), רכיבי UI גנריים ב-`src/components/ui` (Button/Card/Input/Textarea/Badge), והלבשה מחדש רספונסיבית של כל המסכים. ההעדפה נשמרה בזיכרון. |
| 2026-07-23 | שלב 8 (cache): תוצאות התכנון נשמרות ב-DB — הצעות ערים (שכבה 1, per-trip) ומדריך העיר (שכבה 2, per-city) נטענים מ-`suggested_destinations` בכניסה חוזרת במקום קריאה חוזרת ל-AI; כפתור "רענן". migration 0003 הרחיב את הטבלה (`city`, `category`, `description`, `tip`). infra: `guide-service.ts`. |
| 2026-07-23 | הושלם שלב 8: "הוסף לטיול" (סימון `selected` + הצגה בעמוד הטיול); מדריך עיר v2 — "אזורי לינה" לפי אווירה במקום מלונות, רקע על העיר ו"איך מגיעים" (שורת overview), וכרטיסיות ברוחב מלא. תמונות והוספת נקודה ידנית נדחו. |
| 2026-07-23 | שלב 9 (ליבה): route `POST /api/ai/itinerary` בונה לו"ז יומי מהפריטים שנבחרו (סדר לפי שעות + הגיון גאוגרפי), שומר ב-`itinerary_items` (migration 0004) ומעביר את הטיול ל-`executing`. נוספו מחיקת פריט מהלו"ז והסרה מ"מה שבחרתם". infra: `itinerary-service.ts`. עריכת הערות ו-"פתח ב-Maps" נדחו. |
| 2026-07-26 | נקלטה רשימת חזון חדשה מהמשתמש (7 פיצ'רים) וקופלה כשלבים 11–17. החלטות רוחביות: תמונות תמיד ממקור חינמי (Wikipedia REST / Unsplash·Pexels free), חיפוש מקומות דרך OSM Overpass, מזג אוויר דרך Open-Meteo; מפת מסלול = תמונת "לנדינג" שעושה חשק לצד מפה אינטראקטיבית אמיתית; ליטוש עיצוב מודרני על בסיס Beige & Mocha כשכבה רוחבית מתמשכת. סדר בנייה: מתחילים משלב 11 (תאריך יציאה + ספירה לאחור). |
| 2026-07-26 | שלב 11 נבנה: `start_date`/`end_date` כבר קיימים ב-DB (אין migration). Domain: `setTripDatesSchema`, `daysUntil`, `formatCountdown`, `pickUpcomingTrip`. infra: `updateTripDates`, `getPrimaryDestination`. ספק תמונות חינמי `src/lib/place-image.ts` (Wikipedia, בלי מפתח, he→en, cache יומי, נפילה לגרדיאנט). Server Action `setTripDates`. רכיבים: `TripDatesForm` (עמוד טיול), `UpcomingTripCard` (hero במסך הבית). `next.config` allow-list ל-`upload.wikimedia.org`. אומת: type-check + lint + `next build` עברו. אימות ויזואלי חסום ע"י auth (אין להזין סיסמה) — ממתין לבדיקת המשתמש. |
| 2026-08-11 | שלב 11 אומת ויזואלית ומוזג (PR #11). שלב 12א נבנה: טאב "מפת מסלול" עם Leaflet + אריחי OSM (חינם, בלי מפתח) — תחנות ממוספרות לפי סדר ההוספה וקו מקווקו ביניהן. `src/lib/geocode.ts` חדש; קואורדינטות נשמרות בעמודות `latitude`/`longitude` שהיו קיימות וריקות, על שורת ה-`overview` של כל עיר (בלי migration לסכמה). `Tabs` גנרי ב-`components/ui` (הטאב הלא-פעיל לא מרונדר — Leaflet מודד את המכולה ב-mount); עמוד הטיול הורחב ל-`max-w-4xl`. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 12ב נבנה: migration 0006 מוסיף `city` ל-`itinerary_items` ועושה backfill ללו"ז קיים בהצלבת `title` מול `suggested_destinations.name`. התברר שה-route של הלו"ז כבר מחזיק את הפריטים שנבחרו (כולל `city`) וזרק אותם — אז `saveItinerary` פשוט מקבל אותם וממפה שם→עיר, בלי שינוי לסכמת ה-AI ובלי שאילתה נוספת. `itineraryItemSchema` יושרה למצב האמיתי של הטבלה. domain: `cityByDay` + `itineraryStops` (טהורים). סדר התחנות עבר לסדר הימים בלו"ז, עם ערים שלא שובצו בסוף. UI: לו"ז לצד המפה ב-`lg:flex-row`, ממוספר לפי תחנות, עם ימים ולילות לכל יעד. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 12ג נבנה ושלב 12 נסגר: `RouteHero` — באנר בראש טאב המסלול עם תמונת התחנה הראשונה (`place-image.ts` שכבר קיים משלב 11), שם הטיול וסיכום "תחנות · ימים · לילות". `routeSummary` בדומיין סופר ימים על פני כל המסלול כדי שיום מפוצל בין שתי ערים לא ייספר פעמיים. אותה שפה ויזואלית כמו `UpcomingTripCard`. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 13 נבנה בשלוש פרוסות. **13א:** `lib/overpass.ts` + `POST /api/places/search` (rate-limit 10/דק') + טאב "אטרקציות" עם מודאל פרטים. קטגוריות כתצורה ב-`domain/place.ts`. הרשאה דרך RLS — קריאת הטיול היא בדיקת ההרשאה. **13ב:** מקומות נשמרים כ-`source='manual'` (ערך enum מ-0002 שלא היה בשימוש) עם `external_id` (migration 0007). שלוש שאילתות סיננו `source='ai'` והיו מפילות אותם בשקט: `getSelectedDestinations`, `getRouteCities`, `setDestinationSelected` — כולן תוקנו; `deleteCityGuide` שמר את הפילטר בכוונה. גם אימות הקטגוריה היה שובר הסרה (שתי אוצרות מילים, עמודה אחת) → `selectableCategorySchema`. **13ג:** `getAddedPlaces` מקשר תוצאה→שורה→יום בלו"ז; כלל נירמול השמות אוחד ל-`lib/text.ts` (היו שני עותקים שיכלו להיפרד). אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 14 נבנה. **14א:** `domain/timeline.ts` טהור — `parseTimeLabel` הגנתי (נבדק מול 13 מקרים), `buildDayTimeline`, `positionPercent`; `DayTimeline` מצייר בלוקים על ציר שעות (76px/שעה, קבוע — שיום ארוך ייראה גבוה ולא צפוף) עם מתג "ציר שעות/רשימה". נמצא ונסגר **עותק שלישי** של תוויות הקטגוריות ב-prompt של הלו"ז — הוא לא קיבל את שש הקטגוריות מ-13ב, כלומר ה-AI קיבל `cafes` גולמי. **14ב:** `getItinerary` מצליב קואורדינטות מ-`suggested_destinations` לפי עיר+שם מנורמל (מדלג על שורות `overview` כדי שפריט בשם העיר לא ימוקם במרכזה); מרחק אווירי + הליכה עד 2.5 ק"מ. אומת מול מרחקים אמיתיים בטוקיו. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 15 נבנה. **15א:** `trip_bookings` (migration 0008, טבלה אחת ל-flight/train/lodging + RLS בתבנית של 0002), domain/service/actions/UI, טאב "לוגיסטיקה". **שני באגים נתפסו בבדיקת הלוגיקה לפני חיבור ל-UI:** דליים לפי מילישניות קראו לטיסה בעוד 8 שעות "מחר" כשהיא באותו ערב (תוקן ל-יום קלנדרי), ו-30 דקות עוגלו ל"שעה" (תוקן לדקות מתחת לשעה). לילות נספרים גם הם קלנדרית, כך ששהות באותו תאריך = בלי לילות. **15ב לא נדרשה** — שתי השורות הראשונות של השלב סופקו כבר ב-15א. **15ג:** Open-Meteo (חינם, בלי מפתח); `forecastWindow` טהור מחליט לפני קריאת רשת אם תחזית בכלל קיימת — 16 ימים זו תקרה קשיחה שמחזירה HTTP 400. אומת מול 9 מקרי גבול. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 16 נבנה (פרוסה אחת): `trip_phrasebooks` (migration 0009) — שורה אחת לטיול, `phrases` כ-jsonb כי הוא נקרא תמיד בשלמותו ואף פעם לא מסונן; Zod מאמת גם בכתיבה וגם בקריאה, כך ששינוי צורה מתגלה כשיחון חסר ולא כקריסה. `POST /api/ai/phrasebook` מסיק את שפת היעד מהערים שנבחרו (ובנפילה — מהערים המוצעות ומשם הטיול), כי האפליקציה מכירה ערים ולא מדינות. הנושאים הם `PHRASE_TOPICS` בדומיין ולא מחרוזת בתוך ה-prompt. **שדה `pronunciation` (תעתיק עברי) הוא הליבה** — בלעדיו העמודה בכתב היעד היא קישוט לקורא עברית; ה-prompt מדגיש זאת ודורש תעתיק גם לשפות לטיניות. טאב חמישי. אומת: type-check + lint + `next build` עברו. |
| 2026-08-11 | שלב 17 נבנה — **התוכנית המקורית (0–17) הושלמה.** **17א:** `generateText` רב-תורי נוסף ל-`src/lib/ai` כפונקציה נפרדת מ-`generateStructured`; `trip_chat_messages` (migration 0010) כשורות ולא jsonb — נוספות אחת-אחת ונקראות לפי סדר; שאלה+תשובה נכתבות יחד; היסטוריה חסומה ב-24 תורות (כל תור נשלח מחדש בכל בקשה). **17ב:** `POST /api/ai/plan-from-chat` **מציע בלבד ולא כותב** — תצוגה מקדימה עם summary, ואישור שני שומר. `savePlanFromChat` נפרד מ-`saveCities` כי האחרון מוחק לפני שהוא כותב: כאן ערים חדשות בלבד, overview ב-`ignoreDuplicates` (לא לדרוס intro קיים), פריטים ב-upsert (לסמן `selected` גם על פריט קיים). אומת: type-check + lint + `next build` עברו. |
| 2026-08-12 | **פאזה B הושלמה (B2–B8).** **B2:** עמוד הטיול פוצל ל-route segments אמיתיים (`(tabs)/today|days|explore|map|more`) במקום `useState` — query param היה מריץ מחדש את כל שבעת הטוענים בכל לחיצת טאב. `(tabs)` הכרחית כדי ש-`city/[city]` לא יירש את סרגל הטאבים. **שבע קריאות `revalidatePath` קיבלו `"layout"`** — בלי זה הן היו מתאימות רק לעמוד ה-redirect וכל הטאבים היו מפסיקים להתרענן בשקט. `components/layout` נבנה (AppShell + BottomNav), `viewportFit: "cover"` ו-`min-h-dvh` נוספו. **B3:** `domain/trip-days.ts` — תאריכי הלו״ז **נגזרים** מ-`start_date + (day_number − 1)`, בלי migration. כל הפונקציות מקבלות `today` כפרמטר ואף אחת לא קוראת שעון, כי `todayIso` קורא את שעון המכונה (UTC ב-Vercel) והשרת והדפדפן היו חולקים על היום בין חצות ל-03:00. **37 מקרי קצה נבדקו לפני חיבור ל-UI** (התקדים משלב 15). `trips.status` הפסיק להיכתב — כלל ברזל #6 ב-ARCHITECTURE.md עודכן קודם. **B4/B5:** `CountdownHero` ו-`domain/tone.ts`; שיוך צבע לפי מיקום ולא hash, כי hash יכול לתת לשתי תחנות סמוכות אותו גוון. **נמצאה נפילת ניגודיות:** rose ו-peach היו 4.35 ו-3.89 מול סף AA — הוכהו עד שעברו. **B6:** רשת אריחי קטגוריה; **בלי תמונות לתוצאות**, ראו את ההערה למטה. **B7:** "עוד" פוצל לתפריט + שלושה תת-מסכים, מה ששבר את הסימון הפעיל בניווט (השוואת שוויון) — עבר להתאמה על גבול סגמנט. הזמנות תחבורה מוצגות ככרטיס עלייה למטוס ב-LTR כפוי. **B8:** `AppShell` הוחל גם על `/profile` ועמוד העיר, `loading.tsx` לכל טאב, `/design-preview` נמחק, ו-`SegmentedControl` עבר מ-`role="tab"` (ששולח קורא מסך לחפש tabpanel שלא קיים) ל-`role="group"` עם `aria-pressed`. |
| 2026-08-12 | **B0 + B1 נבנו.** B0: עמוד `/design-preview` הציג 3 מסכים × 2 וריאנטים; נבחר **"שובב ומזמין"**. הווריאנטים חלקו JSX אחד ונבדלו רק ב-CSS variables, כך שהבחירה הומרה לטוקנים ישירות. **B1:** `globals.css` גדל מ-41 שורות למערכת מלאה — 6 גווני פסטל בשלשה `tint/ink/dot`, טוקני `danger/success/warning`, רדיוסים, motion, וסולם תצוגה. **שמות הטוקנים נבחרו כדי לא להתנגש** ב-namespaces של Tailwind v4 (`--radius-lg`, `--ease-out`): הגדרה מחדש שלהם הייתה משנה בשקט כל `rounded-lg` קיים, ולכן נוספו `rounded-card`/`rounded-control`/`ease-snap`. **הצבע לעיר הוא משתנה ולא class** — `.tone-*` מציבה שלשה ו-`bg-tone`/`text-tone-ink` קוראים אותה, כי ב-B5 השיוך נקבע בזמן ריצה. פונט תצוגה Secular One. **`cn()` שוכתב ל-`clsx` + `tailwind-merge`, והתגלה שזה לא מספיק:** tailwind-merge לא מזהה שמות טוקן משלנו, אז `rounded-control rounded-full` שרדו כשניהם — נדרש `extendTailwindMerge` עם קבוצות `rounded` ו-`font-size` (צבעים וצללים כן נפתרים לבד). 17 מופעי `text-red-600` הוחלפו בטוקן. **`drop-shadow-sm` בשני קבצים אינו חוב** — הוא צל טקסט על תמונה, והממצא המקורי ספר אותו בטעות. CLAUDE.md תוקן: TanStack Query/Zustand/shadcn מעולם לא הותקנו ולא יותקנו בלי צורך. |
| 2026-08-12 | **נפתחה פאזה B — סבב עיצוב ופונקציונליות.** נסקרו כל הקוד והמסמכים והוגדרה טבלת B0–B8 (בסוף המסמך). החלטות: 5 טאבים לפי מסע במקום 6 לפי פיצ'ר, `route segments` אמיתיים במקום `useState` (query param היה עולה 7 שאילתות בכל לחיצת טאב במקום אפס), תאריכי לו"ז **נגזרים** מ-`start_date + (day_number - 1)` בלי migration, ו-`trips.status` נגזר במקום להישמר — כלל ברזל #6 ב-ARCHITECTURE.md יעודכן לפני הקוד. mobile-first כעיקרון רוחבי על כל הפריטים. מצב כהה נדחה. **לא מותקנות** חבילות רק כדי ליישר קו עם CLAUDE.md (TanStack/Zustand/shadcn) — CLAUDE.md יתוקן לשקף את המציאות. |
| 2026-08-18 | **נפתחה ונבנתה פאזה D — סבב עיצוב רוחבי, בכיוון Booking.com.** המשתמש דיווח ש"אין לי סדר בעיניים" ושהאפליקציה נראית חובבנית, וביקש רספונסיביות אמיתית לטלפון ולמחשב. **האבחון הפריד בין שני דברים שנראו כמו אחד:** התשתית של פאזה B הייתה תקינה (טוקנים דו-שכבתיים, `extendTailwindMerge`, 36 תכונות RTL לוגיות מול 5 פיזיות מכוונות, 13 arbitrary values בכל הריפו) — מה שנשבר הוא שהיא לא נאכפה, ושלא הייתה בכלל שכבת פריסה למסך גדול. **חמש עדויות מדידות:** (1) `--text-display` ו-`--text-title` הוגדרו ב-B1 ובשימוש **אפס** פעמים, בזמן ש-124 מופעי `text-sm` נשאו את כל הטקסט; כותרת סקשן באותה רמה קיבלה **4 טיפולים שונים**, ושניים מהם באותו עמוד. (2) `--background` מול `--surface` היו ביחס **1.03:1** — כרטיס לא נקרא כמשטח. (3) 11 משפחות צבע יכלו לשבת על מסך אחד. (4) `sm:` 25 · `md:` 12 · `lg:` 9 · **`xl:` 0 · `2xl:` 0** — 27 קבצים שמרנדרים UI משמעותי בלי אף breakpoint. (5) segmented control ב-4 עותקים, filter pill ב-5, מצב ריק ב-4 טיפולים, `✕` ב-7 מקומות ואף אחד לא lucide. **בחירת הכיוון נעשתה מול העיניים ולא בטקסט:** הוצגה השוואה של מסך "היום" במצב הקיים מול שלושה כיוונים, והמשתמש בחר את הכיוון של Booking — קנבס ניטרלי, אקסנט אחד לפעולה, צהוב לתגים, צפיפות מידע גבוהה בדסקטופ. שלוש החלטות נוספות: **sidebar + שתי חלוניות מ-1024px**, **הפסטלים יורדים לנקודה ותג** ולא מילוי, ו**בלי מסך תצוגה זמני** — המשתמש ביקש לעבוד בחתיכות גדולות בלי אישור לכל פריט, ולכן האקסנט (כחול פעולה על אפור ניטרלי) נסגר בהחלטה ולא בהצבעה. |
| 2026-08-18 | **D1–D3 (תשתית).** `globals.css` נכתב מחדש: הקנבס `#eceef2` מול לבן הוא **1.16:1, נמדד** (במקום 1.03:1), נוסף `--border-strong`, ושלשות התפקיד עברו לקיבוץ של BUI (`action`/`callout`/`success`/`danger`) במקום שמות לפי גוון; `warning` נשמר כ-alias של `callout` כי שלושה משטחים כבר דיברו בשמו. סולם: 12/14/16/20/24/32/48, ו-`text-sm`/`text-base` קיבלו `line-height: 1.7` **גלובלית** — עברית צריכה יותר אוויר מלטינית, וזה השינוי הבודד עם ההשפעה הגדולה ביותר על הקריאוּת. **Secular One נמחק:** ההיררכיה באה מגודל ומשקל על משפחה אחת, ו-`--font-display` מצביע על Heebo כדי ש-33 אתרי הקריאה יישארו מתקמפלים. הרדיוסים הודקו (כרטיס 20px→12px), נוסף `--radius-modal`, והצללים קיבלו שכבת `modal` — קודם מודאל והירו סטטי חלקו צל אחד. **ממצא שדרש שינוי ארכיטקטוני קטן:** `@theme inline` **לא פולט** את המשתנה ל-`:root` (זו בדיוק המשמעות של `inline`), ולכן `route-map-canvas.tsx`, שבונה את הסיכה של Leaflet כמחרוזת CSS גולמית מחוץ לעץ של React, לא יכול לקרוא טוקן דרך `var()`. הצללים הועברו ל-`:root` בשם `--elevation-*` ומופו ל-`--shadow-*`; השם שונה בכוונה כי `--shadow-soft: var(--shadow-soft)` הוא הגדרה מעגלית. **פרימיטיבים חדשים:** `SectionHeading`, `ListRow`, `Banner`, `Dialog`, `Select`/`Field`, `Surface`, `ToneDot`. `Card` קיבל `padding` משלו, `SegmentedControl` קיבל `size` (מוריד 2 שיבוטים), `Chip` מייצא מחלקות ו-`ChipRadio` (מוריד 3, ומחזיר סמנטיקת radio נייטיבית שהעותקים זרקו). `EmptyState.action` — prop שהיה קיים ואף קורא לא העביר, כך שכל מצב ריק היה מבוי סתום — חובר. טבעת פוקוס אחת מיוצאת מ-`button.tsx`; היא נעדרה לגמרי משני סרגלי הניווט. **`AppShell` קיבל slot ל-`sidebar`**, והניווט הוא עכשיו `NavItem[]` אחד בשלוש מצגות (פס קבוע מתחת ל-md, שורת pills עד lg, rail בנייבי מעל) — הרחבה של הכלל ש-`trip-nav.tsx` כבר תיעד לשתיים. `AppHeader` חולץ; קודם הוא היה inline בתוך layout הטאבים עם `max-w-5xl` מקודד, ולכן ל-`/profile` ולעמוד העיר לא היה chrome בכלל. |
| 2026-08-18 | **D4–D6 (מסכים, שתי חלוניות, וחורים).** `/map` הפך לשתי חלוניות אמיתיות: המפה ממלאת את המסגרת (`h-[26rem]` הקבוע היה משוכפל ב-4 מקומות) והלוח נגלל לצידה עם `overflow-y` נפרד. `/days` קיבל אינדקס ימים sticky מ-lg. `day-timeline.tsx` **שומר פס-צד — היחיד באפליקציה**, ובמכוון: בלוק לוח-שנה הוא לא שורת רשימה, כי הקצה שם רץ לאורך הפריט ולכן מקודד את משכו; גובה השעה הפך ל-CSS variable כדי שהציר יתרחב ב-lg בזמן שהפריטים ממשיכים למקם את עצמם באחוזים. `/profile`: טופס "טיול חדש" הפתוח-תמיד באמצע מסך הבית עבר ל-`Dialog` — **זיהוי ההצלחה דרש תשומת לב**, כי `createTrip` מחזיר `undefined` בהצלחה וגם ה-state ההתחלתי הוא `undefined`, אז ref מתעד שהיה submit בפועל. `more` הפך לגריד, `city-guide`/`phrasebook`/`booking-list`/`selected-list` קיבלו גרידים ב-sm/xl, וה-composer של הצ'אט הפך sticky (קודם היה בזרימה רגילה בתחתית רשימה שגדלה). **החורים נסגרו:** `not-found.tsx`, `error.tsx` ו-`global-error.tsx` נבנו — קודם כל 7 קריאות `notFound()` נפלו ל-404 של Next, שחור-על-לבן, LTR ובאנגלית; נוספו כותרות דפדפן לכל route דרך `generateMetadata` ב-layout הטאבים עם template (קודם כל מסך הציג את אותה כותרת); ורוחבי ה-skeletons יושרו ל-`AppShell` (`profile` אמר `max-w-2xl` מול עמוד `max-w-6xl`, ולכן כל טעינה נגמרה בקפיצה של הטור). **באג שנתפס תוך כדי עבודה:** `import { Map } from "lucide-react"` מסתיר את `Map` הגלובלי, ו-`new Map<string, T>()` באותו קובץ הפסיק להתקמפל — כל המופעים עברו ל-`Map as MapIcon`. **אימות:** type-check, lint ו-`next build` עוברים (16 עמודים, `_not-found` נוצר); נמדד בדפדפן שהקנבס `rgb(236,238,242)`, כרטיס לבן ברדיוס 12px, גובה הכותרת 56px (=ה-offset של ה-sidebar), `letter-spacing: normal`, ו-`line-height` של טקסט גוף **1.70 בדיוק**. **מה שלא אומת: המסכים המוגנים** — ה-proxy מפנה כל route ל-`/login`, ואין ל-Claude סיסמה. |
| 2026-08-18 | **C9 אומת מקצה לקצה — פאזה C סגורה במלואה (C1–C10).** Vercel Cron רץ באופן עצמאי בזמנו המתוזמן, מצא הזמנה עם תזכורת שהגיע זמנה, ושלח דחיפה אמיתית שהופיעה על מסך נעול באייפון של המשתמש דרך ה-PWA המותקן. השרשרת המלאה עובדת: cron ← query חוצה-משתמשים עם מפתח השירות ← VAPID ← שירות הדחיפה של אפל ← Service Worker ← notification. המשתמש שאל אם צריך גם cron ב-GitHub Actions — **לא נחוץ**: Vercel Cron כבר ממלא את התפקיד, ומניעת הכפילות ממילא ברמת הנתונים (`cancel_notified_at`/`book_by_notified_at`) ולא ברמת התזמון, כך שאין תועלת אמיתית במתזמן שני. |
| 2026-08-17 | **C10 נבנה — תוקן שורש הבעיה מאחורי "שירות ה-AI עמוס".** האבחון הקודם (2026-08-16) עצר מוקדם מדי: קבע ש-Gemini מחזיר 503 אמיתי ושה-UI עובד כמתוכנן, אבל לא בדק **למה** זה קורה שוב ושוב. כשהדיווח חזר על כמה תכונות AI שונות, זה כבר לא תואם עומס רגעי — ונבדק ישירות מול ה-API של Google בעקיפת הקוד לגמרי (`curl` עם אותו מפתח). **הממצא: `gemini-flash-latest` נכשל ב-503 בכ-2 מתוך כל 3 קריאות**, כי ה-alias מנתב חלק מהתנועה לרמת preview עמוסה (`gemini-3.7-flash`, שגם היא נכשלה בבדיקה ישירה). הודעת השגיאה של Google על `gemini-2.5-flash` המיושן הצביעה בעצמה על התחליף: `gemini-3.6-flash`. `MODEL` ב-`provider.ts` שונה משם alias לשם קבוע — **היפוך מפורש של החלטה קודמת**: ה-`-latest` נבחר כדי להימנע מעדכון ידני כשגרסה מתיישנת, אבל התברר שהוא גורם לתקלה אמיתית ולא פתרון לה. המחיר גלוי: השם הזה ייפרש מתישהו וידרוש עדכון ידני — עדיף על כשל שקט ב-2 מכל 3 קריאות. **אימות:** נמשכה רשימת 50 המודלים הזמינים למפתח; שלושה שמות נבדקו ישירות מול Google לפני שינוי קוד (`gemini-3.5-flash` ו-`gemini-3.6-flash` הצליחו בעקביות, `gemini-3.7-flash` נכשל כמו ה-alias); אחרי השינוי, 6 קריאות אמיתיות דרך `generateStructured`/`generateText` **שלנו עצמנו** — 6 מתוך 6 הצליחו. |
| 2026-08-16 | **C6 נבנה — סימון ברור בסרגל הניווט.** נוסף pill מלא (`bg-primary-tint`) מאחורי האייקון הפעיל ב-`BottomNav` (נייד) — לפני כן ההבדל היה רק צבע וטקסט מודגש, שנשטף באור שמש. אותה שפה "מלא=נבחר" שכבר קיימת בפס העליון של דסקטופ וב-`SegmentedControl`. אומת: רק הטאב הפעיל מקבל רקע, אין גלישה ב-375px. |
| 2026-08-16 | **דווח ותוקן: הודעת "שירות ה-AI עמוס" באמצע שיחה.** אובחן כאמיתי ולא כבאג — Gemini עצמו מחזיר 503, בדיוק המצב שטופל ב-C7; ההודעה עובדת כמתוכנן. אבל נמצא בדרך באג אמיתי בהתאוששות: `TripChat` הציג את ההודעה באופטימיות, ובכל כשל היא נשארה בתצוגה לצמיתות בלי שנשמרה בשרת, ושדה ההקלדה כבר התרוקן — "הודעת רפאים" שנראית שנשלחה וניסוח שאבד. תוקן: בכשל, הטקסט חוזר לשדה ההקלדה והבועה מוסרת. אומת בזמן ריצה מול `/api/ai/chat` אמיתי: שחזור מדויק של הטקסט, אפס בועות יתומות. |
| 2026-08-13 | **C7 נבנה — AI עמוס כמצב שלישי.** נוסף מתוך אימות C5 ולא מתוך תכנון: Gemini החזיר 503 `UNAVAILABLE` פעמיים ברצף, ו-C1 מיפה רק 429, כך שעומס רגעי הוצג כ"נכשל". שלושה מצבים נפרדים עכשיו, כי העצה הנכונה שונה בכל אחד: 429 שלנו (חכו רגע), 503 `ai_quota_exceeded` (חזרו מחר), 503 `ai_busy` (נסו בעוד רגע). שניהם 503 כי זה הסטטוס הנכון לשניהם, וקוד הגוף מבדיל. **הסיווג רוכז ב-`domain/ai-errors.ts`** — לכל אחד מחמשת המסכים היה עותק של אותם ענפים, **וזה בדיוק איך שה-503 לא טופל באף אחד מהם**; עכשיו כלל אחד, עם fallback משלו לכל מסך. 503 בלי קוד מוכר נקרא כמכסה: הסיבה השכיחה יותר וגם העצה השמרנית. **ויתור מכוון:** הודעת ה-429 בצ׳אט הייתה "יותר מדי הודעות" ועכשיו "יותר מדי בקשות" — ניסוח ייחודי תמורת ביטול חמישה עותקים. **אימות:** 22 מקרי קצה על `aiErrorMessage`/`aiErrorFromResponse` לפני חיבור למסך, כולל HTML במקום JSON (לא זורק), שדה `error` לא-מחרוזתי, וגוף `null`. בזמן ריצה דרך route זמני שנמחק, עם `ApiError` אמיתי שנזרק מתוך ה-provider: 429→503 `ai_quota_exceeded`, 503→503 `ai_busy`, ו-500/400→502 `ai_failed` — שלוש הודעות שונות, וכל אחת נרשמה בלוג עם ה-status. `place-search.tsx` נשאר עם ה-503 שלו — זה Overpass ולא AI, ויש לו הודעה משלו. |
| 2026-08-13 | **C5 נבנה — "הצג עוד יעדים".** `exclude` נוסף ל-`aiSuggestRequestSchema` ול-prompt של `/api/ai/suggest`, בדיוק כמו שרמה 2 כבר עשתה מאז שלב 8; כפתור "הצג עוד יעדים" מוסיף לרשימה במקום להחליף. **⚠️ המלכודת: לשורות רמה 1 אין הגנת ייחודיות בכלל** — האינדקס מ-0003 הוא על `(trip_id, city, category, name)` ולשורות האלה שני השדות הם `NULL`, ו-Postgres מתייחס ל-NULL כשונה מ-NULL, כך שהאינדקס לא נורה עליהן לעולם (ההערה במיגרציה אומרת זאת, בהקשר אחר). לכן `upsert` עם `onConflict` היה חסר תוקף, והניכוי חייב לקרות בקוד: `mergeCitySuggestions` מסלקת כפילויות מול הקיים וגם בתוך התשובה עצמה, כי תשובה אחת של AI יכולה לנקוב באותה עיר פעמיים. `appendCities` נפרד מ-`saveCities` שמוחק לפני שהוא כותב — אותה הבחנה בדיוק שקיימת בין `saveCityGuide` ל-`savePlanFromChat`; והניכוי נעשה מול הטבלה ולא מול המסך, כדי ששני טאבים פתוחים לא יתחרו לכפילות. **ה-prompt המקורי לא נשמר**, ולכן `moreCitiesPrompt` נופלת לתיאור שנגזר מהערים עצמן — **הצורך בזה נמצא באימות ויזואלי**, כי בגרסה הראשונה הכפתור לא הופיע כלל על רשימה שנטענה מה-DB. **אימות:** 29 מקרי קצה על ה-merge/dedupe + 13 על גזירת התיאור, לפני חיבור למסך. בזמן ריצה **מול Gemini האמיתי** דרך route זמני שנמחק: סבב 1 החזיר קיוטו/אוסקה/טאקאיאמה/קנאזאווה/טוקיו, וסבב 2 עם `exclude` החזיר נארה/פוקואוקה/קוראשיקי/מאטסואה/הירושימה — **חפיפה 0**, מיזוג ל-10 ערים ייחודיות. ויזואלי ב-375/768/1440: עמודה אחת ב-375 ושתיים מ-768, הכפתור מיושר לימין מתחת לרשת, אין overflow. **תקלה שנתפסה תוך כדי:** Gemini החזיר 503 UNAVAILABLE ("high demand") בשני נסיונות רצופים — ראו את הפריט הנפרד למטה; **הלוג של C1 הוא מה שאפשר לאבחן את זה בשניות במקום לנחש.** |
| 2026-08-13 | **C4 נבנה — "איך מגיעים".** קישור Google Maps directions מהלינה של אותו לילה לכל פריט בלו"ז, `travelmode=transit`. סכמת ה-URL של Maps חינמית ובלי מפתח, בשונה מ-Directions API. שתי פונקציות טהורות ב-`domain/directions.ts`, אחת לכל קצה של המסלול, כי שני הקצוות יודעים דברים שונים: `lodgingOrigin` (כתובת מנצחת, אחרת שם+עיר — ובלי לכפול את העיר כשהשם כבר מכיל אותה, "ריוקאן קיוטו" בקיוטו) ו-`entryDestination` (קואורדינטות מנצחות — חד-משמעיות, וזו הסיבה שפריט מחיפוש האטרקציות מנווט לדלת הנכונה ולא לסניף אחר של הרשת; אחרת שם+עיר). **`(0,0)` נדחה במפורש** — נקודה במפרץ גינאה שכנתון אמיתי היא `null` שנכתב כמספר, ולנווט לאוקיינוס גרוע מליפול חזרה לשם. גם lat בלי lng, טווחים לא חוקיים, `NaN` ו-`Infinity` — אבל `lat=0` עם lng אמיתי (קו המשווה) ו-`lng=0` עם lat אמיתי (גריניץ') לגיטימיים ונשמרים. הקישור לא מוצג כשאין לינה, ולכן היום האחרון (בלי לילה) מקבל רק "פתח במפה". **🐞 באג קיים שנתפס ותוקן:** בפריט של 30 דקות (כרטיס 38px, `overflow-hidden`) שורת הקישורים נחתכה ומ-16px נשארו כ-3 — **וזה היה כך גם לפני C4**, כלומר "🗺️ במפה" היה מרונדר ובלתי לחיץ. עכשיו שורת הפעולות לא מרונדרת מתחת ל-52px, ושום דבר לא אובד כי תצוגת "רשימה" מציגה את שני הקישורים לכל פריט. **`withHebrewPrefix` ב-`lib/text.ts`:** "מShinjuku" הדביק תחילית עברית לכתב לטיני; הכלל שהאפליקציה כבר מיישמת ב-"פתח ב-Google Maps" הוא מקף לפני מילה לא-עברית, ועכשיו הוא פונקציה. **לא נעשה עם `\b`** — הוא מוגדר על ASCII ולא היה נורה סביב עברית או יפנית, כלומר לא היה תופס אף פעם. **אימות:** 41 מקרי קצה על `lodgingOrigin`/`entryDestination`/`googleMapsDirectionsUrl` + 12 על כפילות העיר + 14 על התחילית, כולם לפני חיבור למסך — כולל קידוד `&`, `#`, `?` ועברית ב-URL, ואימות שיש בו בדיוק סימן שאלה אחד. בזמן ריצה נבדקו ה-URLs שנוצרו בפועל: יעד עם קואורדינטות `35.7148,139.7967`, יעד בלי קואורדינטות כשם+עיר, מוצא שנופל לשם+עיר כשאין כתובת, `travelmode=transit` ו-`rel="noopener noreferrer"` בכולם, ויום בלי לינה בלי קישור. ויזואלי ב-375/768/1440 בשתי התצוגות: **אפס קישורים חתוכים**, אין overflow, RTL נכון, ותוויות ארוכות נשברות לשורה שנייה במקום לגלוש. **לא נבנה במכוון:** ניווט מפריט לפריט — הבקשה הייתה מהלינה, ושורת המעברים כבר מציגה פער זמן ומרחק. |
| 2026-08-13 | **C3 נבנה — "איפה אני ישן" לכל יום.** הפער לא היה באג אלא שאלה שאף פונקציה לא שאלה: `bookingsByDay` מדללת הזמנות לפי `starts_at` בלבד, ולכן מלון לחמישה לילות הופיע ביום הצ׳ק-אין ובשום יום אחר. `lodgingByDay` חדשה **לצד** `bookingsByDay` ולא במקומה — "מה קורה היום" ו"איפה אני ישן הלילה" הן שתי שאלות. **הכלל:** צ׳ק-אין ב-X וצ׳ק-אאוט ב-Y מכסים את הלילות `X..Y-1`; ביום Y בבוקר עוזבים, אז הוא נשאר ריק. תאריכים כמחרוזות `YYYY-MM-DD` שנפתרות ב-`zone`, כמו ב-`bookingsByDay`. **שלוש החלטות:** (1) **בלי `ends_at` — רק לילת ההגעה**; להניח שהשהות נמשכת עד ההזמנה הבאה היה ממציא לילות שלא הוזנו, ושגוי ברגע שיש מלון שני. המחיר גלוי: בלי צ׳ק-אאוט תוצג לינה ביום אחד. (2) צ׳ק-אאוט שאינו אחרי הצ׳ק-אין נחשב "לא ידוע", לא טווח שלילי. (3) חפיפה → הצ׳ק-אין המאוחר מנצח, כי חפיפה היא סתירת נתונים ולא תרחיש למיצוע. **הצגה:** `NightStay` (server component) ב-`/today` דרך `DayPager` וב-`/days` דרך `Itinerary`; **ביום הצ׳ק-אין הרצועה מוסתרת** כי כרטיס ההזמנה מעליה כבר מציג את אותו מלון עם שעה — כפילות נראית כמו באג. `nightStayLabel` בדומיין ולא ברכיב, כמו `dayLabel`. **אימות:** 30 מקרי קצה לפני חיבור למסך — כולל הגעה בלילה שלפני תחילת הטיול (מכוסה, אבל לא מסומן צ׳ק-אין), שהות שכולה מחוץ לחלון הטיול, `starts_at` לא־תקין שמדולג, ושני אזורי זמן שקוראים את אותו רגע (22:30Z) כימים שונים. בזמן ריצה: מלון טוקיו לילות 1–2 והריוקאן 3–4, יום 5 (צ׳ק-אאוט אחרון) ריק, הטיסה מתעלמת, וה-de-dup אומת — ביום 1 שם המלון מופיע **בדיוק פעם אחת**. ויזואלי ב-375/768/1440 מול viewport אמיתי: אין overflow, גובה רצועה קבוע 77px, RTL נכון (🏨 מימין, 🗺️ משמאל), וכתובת יפנית ארוכה נקטעת ב-375 ונכנסת מ-768. **הערה על שיטת האימות:** מדידה ראשונה הראתה overflow ב-768/1440 — התברר שזה ארטיפקט שלי, כי כפיתי רוחב על `main` בתוך viewport של 375 במקום לשנות את ה-viewport עצמו. נמדד מחדש נכון. |
| 2026-08-13 | **C2 נבנה — הוספת מקום ידנית.** טופס ב־`/explore` מתחת לחיפוש (שם · עיר · קטגוריה · כתובת), **בלי migration**: `source='manual'` + `external_id = NULL` הוא הזוג שמבדיל מקום מוקלד מתוצאת OSM, ו־`getAddedPlaces` כבר מסנן `external_id` לא־null כך שסימוני "כבר בטיול" בחיפוש לא נפגעים. הקטגוריות הן שש של `PLACE_CATEGORIES` (label+emoji קיימים, ו־`savedCountsByCategory` סופר בדיוק אותן). הכתובת נשמרת ב־`description` כמו ש־`addPlaceToTrip` כבר עושה — **והתגלה שהיא לא נראית בשום מקום**, כי `SelectedList` הציג שם בלבד, אז נוספה שורה מקוצצת מתחת לשם (`truncate`, כך שתיאור AI ארוך לא הופך את הרשימה לפסקאות; גובה הכרטיס נמדד קבוע 65px בכל אורך תיאור). קואורדינטות נשארות NULL במכוון — גאוקודינג של שם עסק הוא הבאג שהעיף את "האקונה" ללוס אנג'לס. **המלכודת המרכזית: `addManualPlace` הוא בכוונה לא upsert.** המפתח הטבעי `(trip_id,city,category,name)` יכול להתנגש בשורה קיימת, ו־upsert עיוור היה דורס `source`, `external_id` וקואורדינטות בערכים ריקים — הפיכת שורת `ai` ל־`manual` מוציאה אותה ממדריך העיר ומטווח `deleteCityGuide`, ואיפוס `external_id` שובר את ה־badge בחיפוש; **שתי התקלות שקטות**, בדיוק כמו באגי פילטר ה־source משלב 13ב. לכן שורה קיימת רק מסומנת `selected` ומקבלת כתובת אם נמסרה, עם fallback ל־23505 לתחרות. `manualPlaceDescription` מחזיר `null` לכתובת ריקה והשירות משמיט אז את העמודה — כדי שהוספה חוזרת לא תמחק תיאור קיים. העיר היא שדה טקסט עם `datalist` ולא `select`, כי בטיול חדש הרשימה ריקה והטופס הזה הוא לרוב מה שיוצר את העיר הראשונה. **אימות:** 40 מקרי קצה על `manualPlaceSchema` + `manualPlaceDescription` לפני חיבור למסך (כולל ששם של רווחים נדחה, כלומר `.trim()` רץ לפני `.min(1)`, וגבולות 200/120/300 כולל־קצה). בזמן ריצה: מסלול הוולידציה דרך Server Action אמיתי (עיר ריקה → הודעה עברית, `aria-invalid` על השדה הנכון בלבד, `aria-describedby` תואם ל־id), ומסלול הכשל — RLS סירב לכתיבה לא־מאומתת והשדות נשארו מלאים. הסירוב היה RLS ולא שגיאת עמודה, כלומר צורת ה־payload תקפה מול הסכמה. ויזואלי ב־375/768/1440: אין overflow, RTL נכון (שם מימין לעיר), שש גלולות הקטגוריה נשברות ל־3 שורות ב־375 ולשורה אחת מ־768, וכל 6 קישורי label↔input נפתרים. **לא אומת:** כתיבה מוצלחת ל־DB — דורשת session. |
| 2026-08-13 | **C1 נבנה — שגיאות AI אמיתיות.** הטיפול רוכז ב-`src/lib/ai/provider.ts` (נקודת החנק של כל שבעת ה-routes) ולא שוכפל שבע פעמים. שלושה חלקים: (1) **`console.error` לפני כל סיווג** — זה עיקר הפריט; ההודעה של Gemini נושאת `code`/`status`/`reason` וכל זה נזרק לפח עד עכשיו. (2) **`AiQuotaExceededError`** על `ApiError` עם `status === 429`, שה-routes ממפים ל-**503 `ai_quota_exceeded`** במקום 502 `ai_failed`. (3) הודעה בקליינט שלא מזמינה ניסיון חוזר מיידי. **נבחר 503 ולא 429 בכוונה** — ה-rate limiter שלנו כבר תופס 429, ושני המצבים חייבים להיראות שונה. **הסיכון האמיתי לא הונח אלא נבדק:** אם `@google/genai` היה נפתר לשני עותקים, `instanceof ApiError` היה תמיד `false` וענף המכסה היה **קוד מת ששום דבר לא חושף**. אומת מול error שה-SDK זרק בפועל, וגם חוצה את גבול ה-barrel דרך route זמני שנמחק (error שנזרק בתוך `handleProviderError` הגיע ל-route כ-`AiQuotaExceededError`, `cause` שמר `status: 429`, התשובה 503). `isQuotaExceeded` נבדק מול **9 מקרי קצה** לפני חיבור למסך — כולל אובייקט מחופש עם `status: 429` שאינו `ApiError`, שמסווג `false` בכוונה. אומת בזמן ריצה (לא רק build): מפתח פסול מייצר בלוג `status: 400` + `API_KEY_INVALID`, ומסלול הצלחה מול Gemini האמיתי ממשיך לעבוד — כלומר ה-`try/catch` לא שבר אותו. ויזואלי ב-375/768/1440: אין overflow אופקי, RTL וטוקן `danger-ink` תקינים, ובמקום היחיד שההודעה חולקת שורת flex עם כפתור ("רענן הצעות" במדריך העיר) היא נשברת לשתי שורות ב-375px בלי חפיפה. **מה שלא אומת:** מסך השגיאה חסום מאחורי auth (אין להזין סיסמה), ולכן ה-503 אומת בשרת ולא כלחיצה בדפדפן. |
| 2026-08-11 | **באג geocoding שנתפס באימות ויזואלי:** "האקונה" (יפן) הופיע בארה"ב. אומת מול ה-API — Nominatim על שם עברי חשוף מחזיר `Coalition for Human Immigrant Rights, Los Angeles`; עם הקשר (`"האקונה, יפן"`) הוא מחזיר מערך ריק, כלומר אין לו שמות עבריים בכלל. תוקן: ויקיפדיה (he→en) קודמת, עם שם הטיול כהקשר; דרישה שלערך יהיו קואורדינטות מסננת תוצאות שאינן מקום (בלי הקשר, "האקונה" מחזיר "האקונה מטטה" ו"מלך האריות 3"); העדפת ערך ששמו מכיל את שם העיר; Nominatim נשאר גיבוי ורק כשיש הקשר. migration 0005 מאפס את ה-cache כדי שקואורדינטות שגויות יימשכו מחדש. |
| 2026-08-24 | **שם האפליקציה שונה ל-MyTrip.** הוחלף כל מה שמשתמש רואה: כותרות הדפדפן בכל route, המיתוג ב-`AppHeader` ובעמוד הנחיתה, `manifest.json` (`name` + `short_name`), כותרת ההתראה ב-`sw.js`, ה-`aria-label` בשני קבצי האייקון, ועמוד השיתוף הציבורי. **האייקון עצמו לא נגע** — הוא סיכה בלי אותיות, כך שאין בו מה לעצב מחדש. הוחלף גם שם ה-npm (`package.json` + `package-lock.json`) ומחרוזות ה-User-Agent ש-Nominatim/Overpass/Wikimedia מקבלות, ובהן **שם האפליקציה התעדכן וכתובת הריפו נשארה** — הריפו באמת יושב ב-`github.com/Talsy93/tripplan`, ומחרוזת זיהוי שמצביעה לכתובת שלא קיימת מפרה את מדיניות השימוש שלהן. `place-image.ts` אמר `"TripPlan/1.0 (portfolio project)"`, שלא זיהה אף אחד, ויושר לשתי האחרות. **מה שנשאר בכוונה:** `aiTripPlanSchema`/`AiTripPlan`/`trip-plan.ts` — אלה מתארים "תוכנית טיול" כמושג בדומיין ולא את שם המוצר; ה-tag `tripplan-reminder` ב-`sw.js` ו-`mailto:noreply@tripplan.app` כברירת מחדל ל-VAPID — מזהים פנימיים בלתי נראים, ולהחליף דומיין שאף אחד לא מחזיק בו בדומיין אחר שאף אחד לא מחזיק בו אינו שיפור; `tripplan-dev` ב-`.claude/launch.json` (תווית של שרת פיתוח מקומי); ורשומת היומן מ-2026-07-14 שמזכירה את `tripplan-ten.vercel.app` — זה תיעוד של מה שקרה, ולשנות אותו פירושו שהיומן ישקר. **⚠️ שם הריפו, שם פרויקט ה-Vercel והכתובת עצמה לא שונו — הם דורשים פעולה של המשתמש.** אומת: type-check, lint ו-`next build` עוברים (הסקריפטים מדווחים עכשיו `mytrip@0.1.0`); בדפדפן — `document.title`, המיתוג בכותרת, שורת התיאור, `manifest.name`/`short_name` ו-`apple-mobile-web-app-title` כולם "MyTrip", ואפס מופעים של השם הישן בעמוד; כותרות `/login`, `/signup` ו-404 נבדקו ב-curl. |
| 2026-08-23 | **נפתחה ונבנתה פאזה F — תשעה דיווחים נוספים.** הפירוט המלא בפרק "פאזה F" בסוף המסמך. **התובנה המרכזית: שלושה מהפריטים היו אותה בעיה.** קיבוץ לפי מדינות (F4), המפה השבורה (F9) וסידור לפי קרבה גאוגרפית (F7) נראו כשלושה, ולשלושתם חסר בדיוק אותו נתון — איפה כל עיר נמצאת באמת. **הפתרון הוא הכיוון ההפוך:** גאוקודינג של שם עיר בעברית הוא ניחוש (זה מה ששלח את "האקונה" ללוס אנג'לס בשלב 12), אבל זוג קואורדינטות יושב במדינה אחת בדיוק ו-Nominatim עונה על זה בוודאות. `reverseCountry` נקרא פעם אחת לעיר ונשמר ב-`suggested_destinations.country` (migration 0015); מרגע שיש מדינה לכל עיר, סיכה שנחתה מחוץ למדינת הרוב של הטיול הופכת מובנת מאליה ו-`repairMisplacedCities` מריץ אותה מחדש עם שם המדינה כהקשר — הקשר חזק בהרבה משם הטיול שהניסיון הראשון השתמש בו. **שלושה סייגים מכוונים:** לא רץ מתחת ל-3 ערים, לא רץ כשאין מדינה דומיננטית (`dominantCountry` מחזיר null על תיקו, כך שטיול אמיתי בשתי מדינות לא "מתוקן" לתוך חצי מעצמו), וניסיון חוזר שנוחת שוב מחוץ למדינה נזרק — הזזה בלתי מוסברת גרועה מטעות שרואים. תיקון שכן קרה מוכרז בבאנר במקום להיות מוחל בשקט, ונוסף כפתור "רענון המיקומים" לסיכה שגויה **בתוך** המדינה הנכונה, שלה אין תשובה אוטומטית. **F7 מגיע חינם מאותו נתון:** `orderByProximity` הוא nearest-neighbour, **לא אופטימלי במכוון** — המסלול האופטימלי הוא בעיית הסוכן הנוסע וההפרש בלתי נראה בחמש תחנות, בעוד ש"אוסקה בין שני ימי טוקיו" מאוד נראה; ערים שכבר בלו״ז לא זזות, רק הזנב הלא-משובץ. **F6 (שיתוף פומבי): מה שמשותף הוא רדקציה של הטיול, לא הטיול** — העמוד מרונדר מטיפוס `SharedTrip` ולא מהאגרגט, כך שעמודה עתידית בלתי נראית לזרים עד שכותבים אותה לשם במפורש, וזה ההפך מכשל ברירת המחדל שבו נתונים חדשים דולפים מאליהם. לפי בחירת המשתמש הוחרגו `confirmation` (קוד הזמנה מספיק כדי לבטל אצל רוב הספקים), `address` ומחירים. **⚠️ נשקלה ונדחתה policy של RLS:** `using (share_token is not null)` נראה נכון עד שמבחינים שהוא גם מתיר `select * from trips where share_token is not null`, כלומר מנייה של כל הטיולים המשותפים — הטוקן היה מאבד משמעות; במקום זה קריאה דרך service-role בהתאמה מדויקת לטוקן, הקורא השני והאחרון של `createAdminClient`. **באג שנתפס באימות:** `/share/<טוקן תקין שלא קיים>` החזיר 500 ולא 404, כי `createAdminClient` זורק כשהמפתח חסר ולעמוד לא היה guard — נמצא רק כי המפתח לא קיים ב-`.env.local` המקומי, כלומר סביבת הפיתוח החסרה היא שחשפה אותו. **אימות:** 30+ מקרי קצה על כל הפונקציות הטהורות דרך route זמני שנמחק — כולל שהרדקציה באמת משמיטה confirmation/address/מחירים, ש-5 צורות טוקן פסולות נדחות, שקונקשן מזוהה חוצה הבדל רישיות (`IST`/`ist`) ונדחה מעל 12 שעות, ושתיקו בין שתי מדינות מחזיר null. בזמן ריצה: `/share/<לא קיים>`→404, `/share/<מעוות>`→404 (זהים במכוון — הבחנה ביניהם הייתה מאשרת שטוקן היה תקף פעם), `/trips/x`→307 ל-login (האימות לא נפגע). ויזואלית ב-375: אפס border/רקע/צל על נקודות המכירה בעמוד הנחיתה ואפס אלמנט לחיץ בתוכן, אין גלישה אופקית, אפס שגיאות קונסול. **מה שלא אומת:** כל מה שמאחורי התחברות, ועמוד השיתוף עצמו (דורש service-role key שקיים רק ב-Vercel). |
| 2026-08-20 | **נפתחה ונבנתה פאזה E — תשעה דיווחים מהמשתמש, בריצה אחת.** פורטו לטבלה E1–E7 (בסוף המסמך). **שני התיקונים המהותיים ביותר קשורים זה בזה:** ה-prompt של הלו"ז ביקש מהמודל "לכבד" חלוקת ימים בטקסט חופשי (D9) — וזו בקשה, לא הבטחה, בדיוק כמו שדווח ("מלון עם כמות ימים ברצף מופיע בלו"ז של יעד אחר"). **E7** הופך את זה לעובדה שהקוד עצמו אוכף: `buildDayCityPlan` (trip-days.ts) בונה מפה יום→עיר מהתאריכים האמיתיים של הזמנות הלינה (אותו מקור אמת ש-`lodgingByDay` כבר סומך עליו מ-C3) + `travelDayCount` שסופר ימי נסיעה מזמן הנחיתה של הזמנת התחבורה המוקדמת ביותר — טיסה שיוצאת ב-1.11 ונוחתת ב-2.11 מייצרת יום 1 מסוג "נסיעה" ולא מתוזמנות בו פעילויות, מה שסוגר גם את E8 (זמן נחיתה) באותה מנגנון. **ה-prompt מקבל את זה כעובדה קבועה ולא כבקשה**, אבל ההבטחה האמיתית היא **`reconcileItineraryWithDayPlan`** (`domain/itinerary-plan.ts`): מתקן כל פריט שחזר מהמודל ביום שסותר את התוכנית — ללא תלות בציות המודל. פועל רק כשיש לטיול תאריך התחלה+סיום קבועים (דורש dayCount ידוע); בלעדיהם חוזר בדיוק להתנהגות הקודמת. **E1 (שיבויה/טוקיו):** שכונה שנוצרת כ"עיר" נפרדת מטופלת בשתי שכבות — הנחיה מפורשת ב-prompt של `/api/ai/suggest` לא להציע שכונה בתוך עיר שכבר ברשימה, וגיבוי דטרמיניסטי: `withoutNearbyDuplicates` (guide-service.ts) מגאוקד מועמדים חדשים מול ערים קיימות ומול עצמם באותו סבב, ומשמיט כל מועמד שנופל בטווח 30 ק"מ (`DISTRICT_MERGE_RADIUS_KM`, ai-suggestion.ts) — גם פתרון חלקי ל-E5 (מיקומים שגויים בגלל שכונה שנשקלת כעיר נפרדת). **תקלה שנתפסה בבנייה: circular import.** `ai-suggestion.ts` ייבא `distanceKm` מ-`place.ts`, ש-**כבר** מייבא מ-`ai-suggestion.ts` (בשביל `aiCategoryKeySchema`) — מעגל שעבר type-check ולינט בשקט אבל הפיל את ה-build עם `ReferenceError: Cannot access 'k' before initialization` בקובץ לא קשור (`push-actions.ts`), כי Turbopack פירק אותו לצ'אנק לפי סדר הערכה. `distanceKm` עבר ל-`lib/geo.ts` ניטרלי; שני הקבצים מייבאים משם. **E4/E3:** תג "יעד" נדבק (`sticky`) בטאב האטרקציות עם רקע מלא ברוחב `<main>` (מרג'ין שלילי תואם ל-`px-4 md:px-6 lg:px-8`); "עוד תוצאות" הוא פילוח client-side של רשימה שהשרת כבר מחזיר גדולה יותר (`MAX_RESULTS` הועלה 60→150 ב-`overpass.ts`) — אין קריאת Overpass נוספת; ו"חיפוש ליד כאן" ממרכז שאילתה חדשה סביב הקואורדינטות של תוצאה ספציפית (`near` אופציונלי ב-`placeSearchRequestSchema`) במקום מרכז העיר כולה. **E6/E2:** `trip_bookings` קיבל `cost_amount`/`cost_currency` (migration 0014) — **בלי המרת מטבעות**: אין שירות FX חינמי אמין, אז `costTotalsByCurrency` מסכם בתוך כל מטבע בנפרד ומציג טוטלים זה לצד זה, לא מספר אחד מומצא. `BookingForm` הורחב לתמוך גם בעריכה (`booking?` prop קיים → `editBooking` action במקום `addBooking`) כדי לספק גם את E2. `ToastProvider` חדש ב-`components/ui` (כרטיסון פינתי, נעלם לבד אחרי 3.5 שניות) עטוף סביב כל האפליקציה ב-`layout.tsx`, מופעל אחרי הוספת/עדכון הזמנה. **אימות:** `buildDayCityPlan`/`travelDayCount`/`reconcileItineraryWithDayPlan` נבדקו מול טיול מלא (טיסה עם נחיתה למחרת, מלון טוקיו 6 לילות, מלון אוסקה 2 לילות, יום פתוח, ופריט קיוטו בלי לינה) דרך route זמני שנמחק — כולל פריטים שהמודל (המדומה) שיבץ ביום נסיעה וביום של עיר אחרת, ואימות ששניהם זזים למקום הנכון בעוד פריט קיוטו (בלי לינה) נשאר על מקומו ביום הפתוח. `type-check`/`lint`/`next build` עוברים (16 עמודים). **מה שלא אומת בריצה אמיתית:** כל תשעת הפריטים דורשים session — הטוסט, ה-sticky, "עוד תוצאות", "חיפוש ליד כאן", עריכת הזמנה, מחשבון ההוצאות, ומיזוג היעדים (זה האחרון גם דורש קריאת Gemini אמיתית, שלא הורצה כדי לא לצרוך מ-20 הבקשות לדקה בלי לתאם עם המשתמש קודם). |

---

## 📖 מקרא מונחים

- **Repo (Repository)** — מאגר הקוד של הפרויקט ב-Git/GitHub.
- **LTS (Long Term Support)** — גרסה יציבה עם תמיכה ארוכת טווח; תמיד מעדיפים אותה.
- **Environment Variables** — משתני סביבה; הדרך לשמור מפתחות סודיים מחוץ לקוד.
- **RLS (Row Level Security)** — הרשאות ברמת השורה בדאטהבייס; משתמש ניגש רק לנתונים שלו.
- **Middleware** — קוד שרץ לפני כל בקשה; משמש להגנה על עמודים ממשתמשים לא מחוברים.
- **Migration** — קובץ שמתעד שינוי בסכמת הדאטהבייס, כדי שהיא תהיה משוחזרת ומנוהלת בקוד.
- **CI/CD (Continuous Integration/Deployment)** — בדיקות אוטומטיות על כל שינוי + דיפלוי אוטומטי.
- **Structured Output** — הנחיית מודל AI להחזיר JSON במבנה קבוע במקום טקסט חופשי.
- **Rate Limiting** — הגבלת כמות בקשות למשתמש בפרק זמן, להגנה על המכסה החינמית.
- **State Machine** — מידול ישות כאוסף מצבים ומעברים מוגדרים (תכנון ⟵ ביצוע ⟵ הושלם).
- **Monorepo** — ריפו אחד עם כמה אפליקציות; לא רלוונטי לנו (אפליקציה אחת).
- **Feature-Based Structure** — ארגון קוד לפי תחומי פיצ'ר; ההחלטה שלנו.
- **shadcn/ui** — ספריית קומפוננטות שמעתיקה את הקוד לפרויקט; הבחירה שלנו לאטומים.
- **YAGNI (You Aren't Gonna Need It)** — לא בונים תשתית לצורך שלא קיים.

---

## 🎨 פאזה הבאה — סבב שינויים (פונקציונליות ועיצוב)

> נפתחה 2026-08-11. התוכנית המקורית (0–17) סגורה; מכאן העבודה מונעת-משוב ולא מונעת-שלבים.
>
> **הפאזה הוגדרה 2026-08-12.** המשתמש ביקש שלושה דברים: עיצוב מודרני ונעים לעין, מסך "היום"
> שנפתח על הלו"ז של התאריך הנוכחי בזמן הטיול, וספירה לאחור מוחשית שיוצרת ציפייה.
> בנוסף סיפק צילומי מסך מאפליקציית טיול ליפן כרפרנס לכיוון הוויזואלי.

### מה שנקבע בשיחה

| החלטה | הבחירה |
| --- | --- |
| ניווט | 5 טאבים לפי מסע (**היום · ימים · מה עושים? · מפה · עוד**) + סרגל תחתון במובייל |
| אופי ויזואלי | **לא הוכרע** — ייבחר מול מוקאפ (B0) |
| סדר עבודה | תשתית עיצוב קודם |
| מצב כהה | לא עכשיו; הטוקנים ייבנו מוכנים לזה |
| רספונסיביות | **mobile first**, עם ניצול אמיתי של מסכים גדולים |

### הטבלה

| # | מה | סוג | סטטוס |
| - | -- | ---- | ------ |
| B0 | מוקאפ עיצוב — עמוד תצוגה זמני, 3 מסכים × 2 וריאנטים, לבחירת האופי | עיצוב | ✅ הושלם — נבחר **"שובב ומזמין"** |
| B1 | תשתית עיצוב v2 — טוקנים, פונטים, אייקונים, שדרוג `components/ui` | עיצוב | ✅ הושלם |
| B2 | שלד אפליקציה — 5 טאבים ב-URL + סרגל ניווט תחתון | פונקציונליות + עיצוב | ✅ הושלם |
| B3 | תאריכים אמיתיים ללו"ז + מסך "היום" | פונקציונליות | ✅ הושלם |
| B4 | ספירה לאחור מוחשית (hero) | עיצוב | ✅ הושלם |
| B5 | צבע ייחודי לכל עיר בכל האפליקציה | עיצוב | ✅ הושלם |
| B6 | "מה עושים?" — רשת קטגוריות + כרטיסי מקום | עיצוב + פונקציונליות | ✅ הושלם — **בלי תמונות לתוצאות**, ראו את ההערה למטה |
| B7 | "עוד" — פרטי טיול, שיחון עם חיפוש, שיחה | עיצוב | ✅ הושלם |
| B8 | ליטוש רוחבי — מצבים ריקים, טעינה, נגישות, רספונסיביות | עיצוב | ✅ הושלם |

**סדר תלויות:** B0 → B1 (כל השאר תלוי בו) → B2 → B3 → B4. B5 עצמאי אחרי B1; B6 משתפר עם B5; B7 תלוי ב-B2; B8 אחרון.

### עיקרון רוחבי — Mobile first

לא פריט בטבלה אלא **כלל שחל על B1 עד B8**. היום כל עמוד הוא `mx-auto max-w-4xl` ממורכז — על 1920px זו עמודה צרה עם שוליים ריקים, ועל 375px חלק מהמסכים צפופים (שורת 6 גלולות הטאבים גולשת).

בונים ל-375px קודם בלי prefix; כל `sm:`/`md:`/`lg:`/`xl:` הוא **תוספת** על בסיס עובד. שטח פנוי במסך רחב הופך לעמודה שנייה — לא למתיחה של הטקסט:

| מסך | ב-`lg` ומעלה |
| --- | --- |
| היום | ציר היום + עמודת צד: מה קרוב, מזג אוויר, ההזמנה הבאה |
| ימים | רצועת ימים קבועה בצד + היום הנבחר |
| מפה | מפה ורשימת תחנות זו לצד זו |
| מה עושים? | 3–4 עמודות; פילטרים כעמודת צד במקום גלולות גולשות |
| עוד | רשימת כניסות בצד + התוכן לצדה |
| `/profile` | hero רחב + רשת טיולים 2–3 עמודות |

`AppShell` (B2) יחזיק את הרוחב והריווח במקום אחד, במקום 8 עמודים שכל אחד חוזר על `max-w-4xl px-4 py-10` לחוד.

### ממצאים שהכתיבו את התוכנית

אלה עובדות שאומתו בקוד לפני שנכתבה התוכנית:

- **ללו"ז אין תאריכים.** `itinerary_items` מחזיק `day_number` ותוויות זמן כטקסט. `start_time`/`end_time` קיימות מ-0002 ו**מעולם לא נכתבו**. `start_date` מגיע ללו"ז רק כטקסט בתוך ה-prompt של ה-AI.
- **`trips.status` משקר.** נכתב במקום אחד — `api/ai/itinerary/route.ts` מסמן `executing` ברגע שנשמר לו"ז, אולי חודשיים לפני היציאה. `completed` לא נכתב אף פעם. שני ה-Badges שקוראים אותו מציגים מידע שגוי.
- **`trip_bookings` הוא הדאטה היחיד עם timestamps אמיתיים** (`starts_at`), ומוצג היום מנותק מהלו"ז.
- **הטאבים לא ב-URL** — `useState(items[0]?.id)`, לא ניתנים ללינק והטאב הפותח קבוע.
- **`cn()` בלי `tailwind-merge`** — class מאוחר לא גובר על מוקדם. מתפוצץ ברגע שמעבירים `className` לרכיבים.
- **shadcn/ui, TanStack Query ו-Zustand לא מותקנים**, למרות ש-CLAUDE.md מציג אותם כהחלטות סגורות. ההמלצה: לתקן את CLAUDE.md כך שישקף את המציאות, לא להתקין חבילות שאין בהן צורך.
- `text-red-600` hardcoded ב-15 מקומות; אין טוקן error/success/warning. `text-sm` מופיע 90 פעם — הטיפוגרפיה שטוחה.
- `src/components/layout/` מכיל `.gitkeep` בלבד.

### השוואה לאפליקציות אחרות

| אפליקציה | מה כדאי לקחת |
| --- | --- |
| TripIt | מסך הבית בזמן הטיול הוא **היום**, לא רשימת הטיולים |
| Google Trips | טיסה ומלון מופיעים **בתוך ציר היום**, לא בטאב נפרד |
| Wanderlog | תמונה לכל מקום, ו"הוסף ליום" בלחיצה אחת |
| Polarsteps | הספירה לאחור היא הגיבור של המסך |
| הרפרנס של המשתמש | **צבע ייחודי לכל עיר** — זה מה שגורם לזה להיראות מעוצב |

המכנה המשותף: כולן מסדרות מידע לפי *מתי צריך אותו*, לא לפי *איזה פיצ'ר בנה אותו*. הטאבים שלנו הם רשימת פיצ'רים — וזה מה ש-B2 מתקן.

**האיורים ברפרנס** הם אמנות מותאמת אישית לטיול ההוא ואין דרך חינמית לייצר אותם. התחליף: תצלום מוויקיפדיה (`place-image.ts` קיים ומשמש היום בשני מקומות בלבד) + אימוג'י + צבע לכל עיר.

> **B6 — למה אין תמונות לתוצאות החיפוש.** התוכנית ביקשה תמונה לכל כרטיס מקום.
> התמונות מגיעות מוויקיפדיה (`place-image.ts`), ותוצאות Overpass הן ברובן עסקים
> מקומיים קטנים שאין להם ערך — הדגל `notable` שנבנה בשלב 13 קיים בדיוק כי רק
> מיעוט מהתוצאות מוכר. 20 קריאות רשת לכל חיפוש שיחזירו ברובן `null` היו מאיטות
> את החיפוש תמורת מעט מאוד. במקום זה הכרטיסים קיבלו פס צד בצבע העיר, badge
> סטטוס, ותג "מוכר". תמונות ייתכנו בעתיד לפריטים שנבחרו (שם הרשימה קצרה
> וידועה מראש בשרת).

**איך לעבוד בפאזה הזו**

- לא לפתוח קוד לפני שהפריט רשום בטבלה ומוסכם.
- שינוי עיצוב שנוגע ליותר ממסך אחד — לבדוק את כל הטאבים, לא רק את זה שהתלוננו עליו.
- **בדיקה חזותית ב-375px, 768px ו-1440px בכל PR.**
- אותן פרוסות דקות ואותו PR לפיצ'ר. אין שירותים בתשלום.

---

## 🧭 פאזה C — שימוש אמיתי

> נפתחה 2026-08-13, אחרי ערב שימוש באפליקציה. מונעת-משוב: כל פריט הוא דבר
> שנתקלת בו בפועל, לא רעיון מהתוכנית.

### הטבלה

| # | מה | סוג | סטטוס |
| - | -- | ---- | ------ |
| C1 | שגיאות AI אמיתיות: מכסה מול תקלה, + לוג בשרת | באג | ✅ הושלם |
| C2 | הוספת מקום ידנית (שם, קטגוריה, עיר, כתובת) | פונקציונליות | ✅ הושלם |
| C3 | קישור לינה שהוזמנה אל התכנון — "איפה אני ישן" לכל יום | פונקציונליות | ✅ הושלם |
| C4 | "איך מגיעים" — מהלינה לכל יעד, דרך קישורי Google Maps | פונקציונליות | ✅ הושלם |
| C5 | "הצג עוד יעדים" בתכנון | פונקציונליות | ✅ הושלם |
| C6 | סימון ברור יותר בסרגל הניווט | עיצוב | ✅ הושלם |
| C7 | AI עמוס (503) כמצב שלישי, נפרד ממכסה ומתקלה | באג | ✅ הושלם |
| C8 | ניהול הזמנות: דאבל-בוקינג, תאריך ביטול חינם, והתראות מראש | פונקציונליות | ✅ הושלם |
| C9 | התראות פוש אמיתיות (Web Push, חינמי) | פונקציונליות | ✅ הושלם ואומת — דחיפה אמיתית התקבלה |
| C10 | "שירות ה-AI עמוס" חוזר על עצמו בכל תכונת AI | באג | ✅ הושלם |

> **C10 — האבחון הראשון היה חלקי, ותוקן.** דיווח ראשון על "שירות ה-AI עמוס"
> בצ'אט אובחן כ-503 אמיתי מ-Gemini — כלומר C7 עבד כמתוכנן — ותוקן באג אמיתי
> שנמצא תוך כדי: הבועה האופטימית של ההודעה נשארה בשיחה לצמיתות בלי שנשמרה
> בפועל, והטיוטה כבר נמחקה משדה הקלט. עכשיו כישלון משיב את הטקסט המדויק לשדה
> ומסיר את הבועה שלא נשלחה — כישלון הופך לניסיון חוזר ולא להקלדה מחדש.
>
> **אבל "עבד כמתוכנן" לא היה כל הסיפור.** כשהתלונה חזרה על עצמה על פני כמה
> תכונות AI שונות, זה כבר לא תואם "עומס רגעי" — ונבדק ישירות מול Google, בעקיפת
> הקוד שלנו לגמרי (`curl` ישיר ל-API עם אותו מפתח). **הממצא: `gemini-flash-latest`
> נכשל ב-503 בכ-2 מתוך כל 3 קריאות**, לא לסירוגין נדיר. ה-alias של "-latest"
> מנתב חלק מהתנועה לרמת preview עמוסה (`gemini-3.7-flash`, שגם היא נכשלה ב-503
> בבדיקה ישירה), בעוד ששם קבוע מצליח תמיד. **ההודעה של Google עצמה על
> `gemini-2.5-flash` המיושן הצביעה על התחליף:** `models/gemini-3.6-flash`.
>
> `MODEL` ב-`src/lib/ai/provider.ts` שונה מ-`gemini-flash-latest` הקבוע ל-
> **`gemini-3.6-flash` (שם קבוע, לא alias).** זה מהפך במפורש החלטה קודמת —
> ה-`-latest` נבחר מלכתחילה כדי *להימנע* מהצורך לעדכן שם ידנית כשגרסה מתיישנת.
> **ה-alias התברר כגורם לתקלה אמיתית, לא כפתרון לה.** המחיר גלוי: Google יפרוש
> גם את השם הזה מתישהו, ואז יהיה צריך לעדכן כאן ידנית — עלות ידועה, עדיפה על
> כשל שקט שמגיע ב-2 מכל 3 קריאות.
>
> **אימות:** רשימת המודלים הזמינים למפתח נמשכה במלואה (50 מודלים). שלושה שמות
> נבדקו ישירות מול ה-API האמיתי לפני שינוי הקוד: `gemini-3.5-flash` ו-
> `gemini-3.6-flash` הצליחו בעקביות (200), `gemini-3.7-flash` (preview חדש)
> נכשל כמו ה-alias. אחרי השינוי — 6 קריאות אמיתיות דרך `generateStructured`
> ו-`generateText` **שלנו עצמנו** (לא curl גולמי), **6 מתוך 6 הצליחו**.

> **C9 — נבנה 2026-08-13. נבחר Vercel Cron; היעד הוא אייפון וגם אנדרואיד.**
> אותו קוד מכסה את שניהם — באייפון נדרש שלב התקנה נוסף, באנדרואיד לא.
>
> | קובץ | תפקיד |
> | --- | --- |
> | `public/sw.js` | Service worker — הקוד היחיד שרץ כשהאפליקציה סגורה |
> | `public/manifest.json` + `icon*.svg` | הופך את האפליקציה להתקנה — תנאי לפוש ב-iOS |
> | `src/lib/push.ts` | שליחה מול VAPID, בהפשטה כמו `src/lib/ai` |
> | `src/lib/supabase/admin.ts` | הלקוח היחיד שעוקף RLS, לג'וב הלילי בלבד |
> | `0012_push_subscriptions.sql` | מכשיר לכל שורה, עם RLS |
> | `PushToggle` | מתג הרשמה, כולל הנחיית ההתקנה ל-iOS |
> | `/api/notifications/run` + `vercel.json` | הג'וב, מוגן ב-`CRON_SECRET`, רץ 06:00 |
>
> **🐞 באג קריטי שנתפס באימות ריצה, ושהיה שובר את הפיצ'ר כולו בשקט.**
> `proxy.ts` החזיר **307 ל-`/manifest.json` ול-`/sw.js`** — ה-matcher החריג
> תמונות אבל לא `.json`/`.js`, אז שני הקבצים הופנו ל-login. הדפדפן מבקש אותם
> **בלי session**, ולכן ה-service worker לא היה נרשם לעולם והאפליקציה לא הייתה
> ניתנת להתקנה — כלומר **באייפון פוש לא היה עובד בכלל**. זה נכשל בשקט: הפניה
> נראית כמו תשובה תקינה עד שבודקים את הסטטוס. תוקן ואומת: שניהם 200.
>
> **שלוש הגנות על מפתח השירות** (`admin.ts`): נקרא ממשתנה בלי `NEXT_PUBLIC_`,
> **זורק במקום ליפול חזרה ל-anon** (נפילה שקטה הייתה מייצרת ג'וב ש"עובד" ולא
> מזכיר לאיש), ובלי session מתמיד. אומת בריצה: בלי המפתח הראוט נופל בקול.
>
> **הראוט מסמן "נשלח" רק אחרי שליחה מוצלחת.** אם אין מכשיר רשום, או אם הדחיפה
> נכשלה — השורה **לא** מסומנת, כך שהתזכורת תישלח בהרצה הבאה במקום ללכת לאיבוד.
> מנוי מת (404/410) נמחק במקום להיות מנוסה כל לילה לנצח.
>
> **אומת בריצה:** 401 בלי header, 401 עם סוד שגוי, והראוט נכנס לפעולה עם הסוד
> הנכון; `/sw.js` ו-`/manifest.json` מחזירים 200; ה-service worker נרשם בפועל
> (`swScriptUrl` פעיל); המתג מציג נכון מצב "חסום" בלי כפתור, כי הדפדפן לא ישאל
> שוב. 375/768/1440 בלי גלישה.
>
> **✅ אומת מקצה לקצה ב-2026-08-18.** Vercel Cron רץ באופן עצמאי בזמנו המתוזמן,
> מצא הזמנה עם תזכורת שהגיע זמנה, שלח דחיפה, וזו הופיעה על מסך נעול באייפון של
> המשתמש דרך ה-PWA המותקן. השרשרת המלאה — cron ← query חוצה-משתמשים עם המפתח
> המנוהל ← VAPID ← שירות הדחיפה של אפל ← Service Worker ← notification — עובדת.
>
> **אין צורך ב-cron נוסף ב-GitHub Actions.** Vercel Cron כבר ממלא את התפקיד;
> שני מתזמנים מקבילים היו רק מייצרים ניסיון כפול לאותה בדיקה (והכפילות
> נמנעת ברמת הנתונים על ידי `cancel_notified_at`/`book_by_notified_at`,
> לא ברמת התזמון — ראו C9 למעלה — כך שגם אם ירוץ cron שני בטעות, לא תישלח
> התראה כפולה בפועל, אבל אין סיבה טובה להוסיף אותו).

> **C8 נוסף 2026-08-13 לפי בקשת המשתמש.** שני צרכים שעלו מאותו מקום — "מה
> הזמנתי ומה אני צריך לעשות בקשר לזה לפני שיהיה מאוחר":
>
> **8א — לינה כפולה ותאריך ביטול חינם.** כשמזמינים שני מלונות לאותם לילות (כדי
> להחליט אחר כך), צריך (1) **תיעוד ברור של כל הלינות**, (2) **סימון דאבל-בוקינג**
> כשיש חפיפה, (3) שדה **"ביטול חינם עד"** להזמנה, ו-(4) **התראה כמה ימים לפני**
> המועד הזה, כדי לא לשכוח לבטל ולשלם על חדר שלא ישנו בו.
>
> > 💡 **הבסיס כבר קיים מ-C3.** `lodgingByDay` כבר מזהה חפיפה בין שהיות — היא
> > מכריעה אותה לטובת הצ׳ק-אין המאוחר ומתועדת שם כ"סתירת נתונים". C8 הופך את
> > הסתירה הזו ממשהו שנפתר בשקט למשהו שנאמר למשתמש. גם `bookingAlert` כבר קיים
> > ונגזר מהשעון בלי להישמר, כך שהתראת ביטול היא הרחבה של דפוס עובד.
> >
> > ⚠️ **דורש migration** — עמודה `free_cancellation_until` ל-`trip_bookings`.
> > המשתמש מריץ migrations ידנית ב-Supabase SQL Editor.
>
> **8ב — התראה להזמין מראש.** דוגמה שהמשתמש נתן: רכבת ביפן שצריך להזמין חודש
> מראש. הרצון: **ברגע שקטע כזה הוא חלק מהמסלול, שתקפוץ תזכורת לא לשכוח להזמין.**
>
> **הוכרע 2026-08-13: המשתמש מזין "להזמין עד".** האפליקציה לא מכירה זמני הזמנה
> של מפעילים, ולנחש אותם זה להמציא ידע — אותו כלל שקבע בשלב 14 ש"מספר מומצא גרוע
> ממספר חסר". היא זוכרת ומתריעה, לא מנחשת. אם בעתיד ירצו, ה-AI יכול להציע תאריך
> **כטיוטה מסומנת** שהמשתמש מאשר.
>
> **8ג — זמן ההתראה נבחר (בקשה שנוספה 2026-08-13):** יום לפני / יומיים / או מספר
> ימים ידני. נשמר כ-`reminder_days_before` על ההזמנה; `null` = ברירת המחדל של
> האפליקציה (7 ימים), כך שמי שלא בוחר לא צריך לבחור.

### מה שנבנה עד כה ב-C8 (הדומיין)

`migration 0011` נכתב אך **עוד לא הורץ** — ראו את ההנחיה בהמשך. שלוש פונקציות
טהורות ב-`domain/booking.ts`, כולן נבדקו לפני שנגעתי ב-UI:

| פונקציה | מה היא עושה |
| --- | --- |
| `cancellationAlert` | התראת "ביטול חינם עד…" בתוך חלון ההתראה |
| `bookingTodoAlert` | התראת "להזמין עד…" לפריט שעוד לא הוזמן |
| `doubleBookedLodgingIds` | אילו לינות חולקות לילה עם לינה אחרת |

**⚠️ שתי החלטות שהתגלו רק בזכות הבדיקות, ושתיהן שינו את הסכמה:**

**1. מועד יעד הוא `date` ולא `timestamptz`** — הפעם היחידה שהמיגרציה הזו חורגת
מהתקדים של 0008. כתבתי בהתחלה `timestamptz` והנחתי בהערה שצהריים "בטוחים בכל אזור
זמן בעולם". **בדיקה שכתבתי הפריכה את זה:** Pacific/Kiritimati הוא UTC+14, ולכן
12:00 שם הוא כבר המחרת. הטווח האמיתי הוא UTC−12 עד UTC+14 — 26 שעות — ולכן **אין
שעה ביום שבטוחה בכל האזורים**. מועד ביטול הוא *תאריך קלנדרי*, לא רגע, ול-`date`
אין אזור זמן להתבלבל בו.

**2. קריאת התאריך חייבת להיות מקומית.** `new Date("2026-09-10")` נפרס כחצות UTC,
ולכן ממערב לגריניץ' הוא ה-9 וכל ספירת ימים יוצאת קצרה ביום. `parseLocalDate` בונה
מרכיבים. **זה מודגם בבדיקה ולא מונח:** אותו `new Date("2026-09-10")` נקרא כ-09-09
בלוס אנג'לס ובמידוויי.

> **הערה על שיטת האימות שנתפסה בדרך:** ניסיתי לאמת אזורי זמן בהרצת הבדיקות עם
> `TZ=...`. **זה לא עבד ולא הוכיח כלום** — Node במכונה הזו מתעלם מ-`TZ`
> (`process.env.TZ` יצא `undefined` וההיסט נשאר −180). חמש ההרצות היו כולן
> בירושלים. הוחלף באימות שמדגים את הבאג דרך `Intl` עם `timeZone` מפורש, שלא צריך
> תמיכת `TZ`. **אם בעתיד תרצו לבדוק אזורי זמן — לא לסמוך על `TZ` כאן.**

### C8 — ה-UI (הושלם 2026-08-16)

`BookingForm` קיבל מתג "כבר הזמנתי", שדות `date` ל"ביטול חינם עד" ול"להזמין עד"
(השני מוצג רק כשלא הוזמן), ובורר זמן התראה — 1/2/3/7/14/30 או "אחר" עם שדה מספר.
`BookingList` מציג באנר דאבל-בוקינג, badge "לינה כפולה" על כל לינה חופפת,
ואת המועדים שהוזנו גם כשאין עדיין התראה. `UpNext` מציג את מה שצריך **לעשות**
מעל מה שעומד **לקרות**, ובלי תקרה — מועד ביטול שמפספסים עולה כסף.

> **🐞 באג שנתפס רק באימות ריצה, ולא היה נראה בקוד: checkbox מבוקר מול reset של
> React.** אחרי שה-Server Action מסתיים React מאפס את ה-DOM של הטופס, ו-reset
> משחזר כל input מתוך התכונה `defaultChecked`/`defaultValue` — **שאותה React לא
> מתחזקת עבור input שמונע דרך `checked`**. התוצאה: ה-checkbox חזר ל"מסומן" בזמן
> שה-state מאחוריו נשאר "לא הוזמן". **התסמין שקט ומגעיל:** על המסך כתוב "כבר
> הזמנתי" והטופס מתנהג הפוך, והשליחה הבאה שולחת את ההיפך ממה שרואים.
>
> **תיקון ראשון לא עבד** — סנכרון ב-render השאיר את הפער, כי ה-reset קורה *אחרי*
> הרינדור. הפתרון: לתת ל-DOM לנהל את ה-checkbox (`defaultChecked`, ש-reset כן
> מכבד) ולהרכיב אותו מחדש (`key`) בכל תוצאת action, כך שהברירת מחדל נטענת ממה
> שנשלח בפועל. אומת בארבעה מצבים רצופים כולל שליחה שנדחתה — `inSync` בכולם.

**אימות C8 (UI):** בזמן ריצה — באנר "2 לינות שחופפות", badge על שתי החופפות
ו**לא** על השלישית שאינה חופפת, "עבר מועד ההזמנה!" על רכבת שלא הוזמנה,
`aria-invalid` על השדה הנכון בלבד עם ההודעה העברית, וכל מה שהוקלד שרד דחייה.
שדה אחד בדיוק בשם `reminderDaysBefore` בשני המצבים (בלי שליחה כפולה).
375/768/1440 בלי גלישה, גלולות הזמן נשברות ל-2 שורות ב-375.

### C9 — התראות פוש, ולמה הן בכל זאת חינמיות

> נוסף 2026-08-13 לפי בקשת המשתמש: "שתקפוץ לי התראה כמו נוטיפיקיישן של אפליקציה".
>
> **ההנחה שהצהרתי עליה קודם — שפוש דורש שירות בתשלום — שגויה.** Web Push הוא תקן
> דפדפן: מפתחות VAPID נוצרים מקומית בחינם, ושירות הדחיפה עצמו מופעל על ידי יצרן
> הדפדפן בלי עלות. מה שכן נדרש הוא **מתזמן בצד השרת**, ולזה יש שתי דרכים חינמיות:
> **Vercel Cron** (בתוכנית Hobby — פעם ביום; מספיק בהחלט ל"הזכר לי 3 ימים לפני")
> או **GitHub Actions cron** שקורא ל-route באפליקציה.
>
> **חלקי המימוש:** service worker, זוג מפתחות VAPID במשתני סביבה, טבלת
> subscriptions, route שנקרא על ידי ה-cron וסורק מועדי יעד, ומתג הרשמה בהגדרות.
>
> ⚠️ **דורש פעולה של המשתמש** (יצירת מפתחות, הוספת משתני סביבה, הפעלת cron).
>
> **⚠️⚠️ המגבלה שקובעת אם זה בכלל שווה: באייפון פוש עובד רק כשהאתר מותקן במסך
> הבית** (iOS 16.4+). בטאב רגיל של Safari — לא עובד כלל, וזו מגבלה של אפל שאי
> אפשר לעקוף. לכן נדרש `manifest.json`, והמשתמש חייב "הוסף למסך הבית" לפני
> שיוכל לאשר התראות. באנדרואיד ובדסקטופ זה עובד בטאב רגיל.
>
> **✅ נבנה כבר: `domain/reminders.ts` — `dueReminders`.** זו הלוגיקה שמחליטה מה
> נשלח, והיא **בלתי תלויה במתזמן שייבחר**, כך שהיא נכונה בין אם Vercel Cron ובין
> אם GitHub Actions.
>
> **הכלל שהמשתמש ביקש במפורש (2026-08-13): התראה אחת, בתזמון שהוגדר — לא כל יום.**
> "הזכר לי 3 ימים לפני" הוא נוטיפיקציה אחת, לא שלוש. זה נאכף על ידי כך
> **שהשורה זוכרת שנשלחה** (`cancel_notified_at` / `book_by_notified_at`) ולא על
> ידי התזמון — כך שגם job שרץ פעמיים, נכשל וניסה שוב, או חפף לעצמו, לא יכול
> לייצר נוטיפיקציה שנייה. השאלה היא "האם זה נשלח", לא "האם היום היום הנכון".
>
> **החלון הוא `days <= lead` ולא `days === lead`** — בכוונה. אם ה-cron החמיץ
> הרצה, התאמה מדויקת הייתה מדלגת על התזכורת **לתמיד ובשקט**. השלמה בהרצה הבאה
> נכשלת בבטחה: עדיין נוטיפיקציה אחת בדיוק, רק אולי באיחור יום במקום לעולם לא.
>
> **אסימטריה מכוונת:** מועד ביטול שעבר → שקט (הדלת נסגרה, אין מה לעשות). מועד
> הזמנה שעבר → **כן שולח** (הרכבת לא תזמין את עצמה).
>
> 43 מקרי קצה נבדקו לפני חיבור לכל תשתית.

> **C7 — מה נבנה.** `AiUnavailableError` על `ApiError` עם `status === 503`, ושבעת
> ה-routes מחזירים `ai_busy` (גם הוא 503 — זה הסטטוס הנכון לשניהם, וקוד הגוף הוא
> מה שמבדיל). **הסיווג רוכז ב-`domain/ai-errors.ts`:** לכל אחד מחמשת המסכים היה
> עותק משלו של אותם ענפים, **וזה בדיוק איך שמצב ה-503 לא טופל באף אחד מהם.**
> עכשיו כלל אחד במקום אחד, עם fallback משלו לכל מסך ("בניית הלו״ז נכשלה" מול
> "טעינת המדריך נכשלה"), כי משפט משותף אחד היה פחות שימושי.
>
> **503 בלי קוד מוכר נקרא כמכסה** — הסיבה השכיחה יותר, וגם העצה השמרנית: לשלוח
> מישהו לנסות שוב בקשה שלא תצליח עד מחר מבזבז את זמנו.
>
> **מה שאבד בכוונה:** בצ׳אט הודעת ה-429 הייתה "יותר מדי **הודעות**" ועכשיו היא
> "יותר מדי בקשות". ויתרתי על הניסוח הייחודי תמורת ביטול חמישה עותקים — זה בדיוק
> הכפילות שיצרה את הבאג.
>
> **C7 נוסף 2026-08-13, מתוך אימות C5 ולא מתוך תכנון.** בזמן בדיקת `exclude` מול
> Gemini האמיתי חזר **503 `UNAVAILABLE` — "This model is currently experiencing
> high demand"** בשני נסיונות רצופים, והשלישי הצליח. C1 מיפה רק 429 (מכסה), ולכן
> עומס רגעי נפל ל-`ai_failed` הגנרי והוצג כ"נכשל". זה **מצב שלישי**: לא מכסה
> שנגמרה (חזרו מחר) ולא תקלה (משהו שבור) אלא עומס (נסו בעוד רגע). לאפליקציה כבר
> יש בדיוק את ההבחנה הזו ל-Overpass ("שירות המפות עמוס כרגע"), אז זו יישור קו.
> **הלוג של C1 הוא מה שאפשר לראות את זה בכלל** — לפניו זה היה 502 אילם.

**סדר:** C1 ראשון — הוא חוסם אבחון של כל השאר. C3 לפני C4, כי "מאיפה מגיעים"
צריך קודם לדעת איפה ישנים.

> **C6 — מה נבנה.** ב-`BottomNav` (הסרגל התחתון בנייד) לטאב הפעיל היה רק שינוי
> צבע וטקסט מודגש — בלי רקע. חמישה אייקונים כמעט שווי-משקל זה לצד זה, וצבע לבדו
> הוא בדיוק סוג האות שנשטף באור שמש ישיר — התאורה שאפליקציית טיול נמצאת בה
> הכי הרבה. נוסף **pill מלא** (`bg-primary-tint`) מאחורי האייקון הפעיל, אותה
> שפה של "מלא = נבחר" שכבר קיימת בפס העליון של דסקטופ וב-`SegmentedControl`.
> הגרסה לדסקטופ (`md:` ומעלה) לא נגעה — היא כבר השתמשה ב-pill מלא לפני C6.
> אומת: רק הטאב הפעיל מקבל את הרקע (`rgb(239, 229, 218)` = `--primary-tint`),
> אין גלישה ב-375px, והתווית הארוכה ביותר ("מה עושים?") עדיין נכנסת. **הפריט הבא: C5.**

> **C4 — "איך מגיעים".** קישור Google Maps directions מהלינה של אותו לילה לכל
> פריט בלו"ז, עם `travelmode=transit`. סכמת ה-URL של Maps חינמית ובלי מפתח,
> בשונה מ-Directions API — זו הסיבה שהיא נבחרה (ראו את המגבלה בהמשך).
>
> **שתי פונקציות טהורות בדומיין (`domain/directions.ts`), אחת לכל קצה של המסלול,
> כי שני הקצוות יודעים דברים שונים:**
> - `lodgingOrigin` — **כתובת מנצחת** כשיש, אחרת שם + עיר. הזמנה בלי כתובת עדיין
>   מקבלת קישור עובד, כי Google פותר "Shinjuku Granbell Hotel Tokyo" מצוין.
>   ואם השם כבר מכיל את העיר ("ריוקאן קיוטו" בקיוטו) העיר לא נכפלת.
> - `entryDestination` — **קואורדינטות מנצחות** כשיש: הן חד-משמעיות, וזו הסיבה
>   שפריט שהגיע מחיפוש האטרקציות מנווט לדלת הנכונה ולא לסניף אחר של אותה רשת.
>   רוב הפריטים הם פריטי מדריך AI בלי קואורדינטות, ואז שם + עיר.
>
> **`(0,0)` נדחה במפורש** — זה נקודה במפרץ גינאה, וכנתון אמיתי זה `null` שנכתב
> כמספר. לנווט מישהו לאוקיינוס גרוע מליפול חזרה לשם המקום. גם lat בלי lng,
> טווחים לא חוקיים, `NaN` ו-`Infinity` נדחים — אבל `lat=0` עם lng אמיתי (קו
> המשווה) ו-`lng=0` עם lat אמיתי (גריניץ') הם לגיטימיים ונשמרים.
>
> **הקישור לא מוצג כשאין לינה לאותו לילה** — מסלול ממקום ששום דבר לא מגדיר אינו
> מסלול. לכן היום האחרון של הטיול, שאין בו לילה, מקבל רק "פתח במפה".
>
> **🐞 באג קיים שנתפס בדרך ותוקן.** בכרטיס של פריט קצר בציר השעות (30 דקות = 38
> פיקסלים) שורת הקישורים נחתכה: הכרטיס הוא `overflow-hidden`, ומ-16 פיקסלים של
> קישור נשארו כ-3. **זה היה כך גם לפני C4** — הקישור "🗺️ במפה" היה שם, מרונדר
> ובלתי לחיץ. עכשיו שורת הפעולות פשוט לא מרונדרת מתחת ל-52 פיקסלים (הגובה שהיא
> צריכה: `p-2` למעלה ולמטה + שורת כותרת + שורת קישורים). **שום דבר לא אובד** —
> תצוגת "רשימה" מציגה את שני הקישורים לכל פריט בלי תלות באורכו.
>
> **`withHebrewPrefix` ב-`lib/text.ts`** — התווית בתצוגת הרשימה היא "איך מגיעים
> מ<שם המלון>", ו-"מShinjuku" הדביק תחילית עברית לכתב לטיני. הכלל שהאפליקציה כבר
> מיישמת ב-"פתח ב-Google Maps" הוא מקף לפני מילה לא-עברית, ועכשיו הוא פונקציה:
> "מריוקאן קיוטו" מול "מ-Shinjuku Granbell Hotel". **לא נעשה עם `\b`** — הוא
> מוגדר על תווי ASCII ולא היה נורה סביב עברית או יפנית, כלומר לא היה תופס אף פעם.
>
> **מה שלא נבנה, במכוון:** ניווט מפריט לפריט (ולא מהמלון). הבקשה הייתה מהלינה,
> ושורת המעברים בציר השעות כבר מציגה פער זמן ומרחק בין נקודות. זו הרחבה אפשרית.

> **C3 — הפער שהיה, ואיך נסגר.** `bookingsByDay` (מ-B3) מדלל כל הזמנה לפי
> `starts_at` **בלבד**. זה עונה על "מה קורה היום", ולכן מלון שהוזמן לחמישה
> לילות הופיע ביום הצ׳ק-אין ובשום יום אחר — ול"איפה אני ישן ביום 4" לא הייתה
> תשובה בכלל. **זה לא היה באג בקוד אלא שאלה שאף פונקציה לא שאלה.**
>
> `lodgingByDay` חדשה בדומיין, לצד `bookingsByDay` ולא במקומה — שתי שאלות שונות:
> "מה קורה היום" מול "איפה אני ישן הלילה".
>
> **הכלל:** הזמנה עם צ׳ק-אין ב-X וצ׳ק-אאוט ב-Y מכסה את הלילות `X..Y-1`. **ביום
> ה-Y לא ישנים שם** — בבוקר עוזבים, ולכן היום האחרון של השהות נשאר ריק. תאריכים
> מושווים כמחרוזות `YYYY-MM-DD` (שמסתדרות לקסיקוגרפית) ונפתרים ב-`zone`, מאותה
> סיבה ש-`bookingsByDay` מקבלת אזור: צ׳ק-אין ב-23:40 שייך לתאריך אחר תלוי לוח
> השנה של מי שואלים.
>
> **שלוש החלטות שהיו יכולות ללכת אחרת:**
> 1. **בלי `ends_at` — רק לילת ההגעה.** מפתה להניח שהשהות נמשכת עד ההזמנה הבאה,
>    אבל זה היה **ממציא לילות שהמשתמש לא הזין** — ושגוי ברגע שהזמין מלון שני.
>    המחיר: מי שלא הזין צ׳ק-אאוט יראה לינה ביום אחד בלבד. זה מכוון וגלוי.
> 2. **צ׳ק-אאוט שאינו אחרי הצ׳ק-אין נחשב "לא ידוע"** ולא כטווח שלילי — אותו
>    תאריך, או תאריך מוקדם יותר, לא מלמדים כלום על לילות מאוחרים.
> 3. **חפיפה בין שתי שהיות → הצ׳ק-אין המאוחר מנצח.** חפיפה היא סתירת נתונים, לא
>    תרחיש למיצוע; מי שעבר מלון ישן במלון שנכנס אליו אחרון.
>
> **הצגה:** רכיב `NightStay` (server component, בלי state) בשני מסכים — `/today`
> דרך `DayPager`, ו-`/days` דרך `Itinerary`. **ביום הצ׳ק-אין הרצועה מוסתרת**, כי
> כרטיס ההזמנה שמעליה כבר מציג את אותו מלון עם שעת הצ׳ק-אין — אותו דבר פעמיים
> נראה כמו באג. הרצועה לא מוצגת כלל בלילה בלי לינה: יום שאף אחד לא הזמין הוא
> תשובה לגיטימית, ונדנוד בכל לילה לא־מכוסה היה הופך את הרצועה לרעש.
>
> **`nightStayLabel` יושבת בדומיין** ולא ברכיב, מאותה סיבה ש-`dayLabel` שם:
> הניסוח הוא תכונה של הנתון, לא של המסך. ("צ׳ק-אין" / "ישנים כאן" /
> "הלילה האחרון כאן" / "לילה אחד כאן".)

> **C2 — מה נבנה, ואיפה הייתה המלכודת.** טופס ב־`/explore` מתחת לחיפוש:
> שם · עיר · קטגוריה · כתובת (לא חובה). **בלי migration.**
>
> - **`source='manual'` + `external_id = NULL`** הוא מה שמבדיל מקום שהוקלד ביד
>   ממקום שהגיע מחיפוש OSM. שני הסוגים חולקים את ערך ה־enum, אבל לתוצאת חיפוש
>   *תמיד* יש `external_id` ולמוקלד *לעולם* לא. `getAddedPlaces` כבר מסנן
>   `.not("external_id","is",null)`, ולכן מקום מוקלד לא יכול להפריע לסימוני
>   "כבר בטיול" בחיפוש. זה מה שאיפשר להימנע מ־migration.
> - **הקטגוריות הן שש של `PLACE_CATEGORIES`** ולא ארבע של מדריך ה־AI: יש להן
>   כבר label+emoji, ו־`savedCountsByCategory` סופר בדיוק אותן, כך שהאריחים
>   נשארים נכונים.
> - **הכתובת נשמרת ב־`description`** — בדיוק מה ש־`addPlaceToTrip` כבר עושה עם
>   `address`. **וגם נחשפה:** `SelectedList` הציג שם בלבד, כלומר כתובת הייתה
>   נשמרת ולא נראית. נוספה שורה מקוצצת (`truncate`) מתחת לשם.
> - **קואורדינטות נשארות NULL** במכוון. גאוקודינג של שם עסק הוא בדיוק הבאג
>   שהעיף את "האקונה" ללוס אנג'לס (migration 0005). המפה וחישוב המרחקים מדלגים
>   עליו כמו שהם מדלגים על כל פריט מדריך AI.
>
> **⚠️ המלכודת: `addManualPlace` הוא בכוונה *לא* upsert**, בשונה מ־`addPlaceToTrip`
> שלידו. המפתח הטבעי הוא `(trip_id, city, category, name)`, ושם מוקלד יכול
> להתנגש בשורה קיימת — ו־upsert עיוור היה דורס לה את `source`, `external_id`
> והקואורדינטות בערכים הריקים של שורה מוקלדת. הפיכת שורת `ai` ל־`manual` הייתה
> מוציאה אותה ממדריך העיר (`getSavedCityGuide` מסנן לפי source) ומטווח ההשגה של
> `deleteCityGuide`; איפוס `external_id` היה שובר את ה־badge בחיפוש. **שתי
> התקלות היו שקטות** — בדיוק ההתנהגות של באגי פילטר ה־source משלב 13ב. לכן:
> שורה קיימת רק מסומנת `selected` ומקבלת כתובת אם נמסרה, ושום דבר שכבר ידוע
> עליה לא נכתב מחדש. הקליינט אומר "כבר היה בטיול — סומן כנבחר" במקום להיראות
> כאילו הלחיצה לא עשתה כלום. נוסף גם fallback ל־23505 (unique violation) למקרה
> של שתי הוספות במקביל.
>
> **העיר היא שדה טקסט עם `datalist`** ולא `select`: בטיול חדש הרשימה ריקה, ו־select
> בלי אפשרויות הוא מסלול מתים — הטופס הזה הוא לרוב מה שיוצר את העיר הראשונה של
> הטיול. ה־`datalist` מוצג רק כשיש ערים.
>
> **מה שלא אומת, ולמה:** כתיבה מוצלחת ל־DB דורשת session, ואין לי דרך להזין
> סיסמה. **מה שכן אומת בזמן ריצה:** מסלול הוולידציה מקצה לקצה דרך קריאת Server
> Action אמיתית (עיר של רווחים → "יש למלא עיר." עם `aria-invalid` על השדה הנכון
> בלבד ו־`aria-describedby` שמצביע ל־id של ההודעה), ומסלול הכשל — RLS סירב
> לכתיבה לא־מאומתת, ההודעה הגנרית הוצגה, **והשדות נשארו מלאים** כדי שלא לאבד
> הקלדה. הסירוב הגיע כ־RLS ולא כשגיאת עמודה, כלומר **צורת ה־payload ושמות
> העמודות תקפים מול הסכמה האמיתית.**

> **C1 — מה נבנה בפועל.** נקודת החנק היא `src/lib/ai/provider.ts`, שכל שבעת
> ה-routes עוברים דרכה, ולכן הטיפול נעשה שם פעם אחת ולא שבע:
>
> - **`console.error` לפני כל סיווג** — זה עיקר הפריט. הודעת השגיאה של Gemini
>   כוללת `code`, `status` ו-`reason` (למשל `API_KEY_INVALID`), וכל זה נזרק לפח
>   עד עכשיו. אומת בזמן ריצה: מפתח פסול מייצר בלוג `status: 400` + `API_KEY_INVALID`.
> - **`AiQuotaExceededError`** נזרק כשה-SDK מחזיר `ApiError` עם `status === 429`,
>   וה-routes מחזירים עליו **503 `ai_quota_exceeded`** במקום 502 `ai_failed`.
>   בקליינט: "מכסת ה-AI היומית נגמרה. נסו שוב מחר." — הודעה שלא מזמינה ניסיון חוזר
>   מיידי, בשונה מ-429 שכן.
> - **429 שלנו מול 429 שלהם** — ה-rate limiter שלנו מקדים את הקריאה ומחזיר 429,
>   ולכן 429 מהספק לא יכול להתבלבל איתו. הבחירה ב-503 ולא ב-429 היא בדיוק כדי
>   ששני המצבים ייראו שונה בקליינט.
>
> **שלוש מלכודות שנבדקו במפורש, לא הונחו:**
> 1. **`instanceof` חייב לתפוס.** `ApiError` מיובא מ-`@google/genai`, וה-error
>    נזרק מתוך ה-SDK. אם החבילה הייתה נפתרת לשני עותקים, `instanceof` היה תמיד
>    `false` וענף המכסה היה קוד מת ש**שום דבר לא היה חושף**. אומת מול error
>    אמיתי שה-SDK זרק: `instanceof` = true, `status` = number.
> 2. **אותה שאלה חוצה את גבול המודולים** — ה-route מייבא מ-`@/lib/ai` (barrel)
>    ומה-provider זורק. אומת דרך route זמני שנמחק: error שנזרק **בתוך**
>    `handleProviderError` הגיע ל-route כ-`AiQuotaExceededError`, ה-`cause` נשמר
>    עם `status: 429`, והתשובה הייתה 503.
> 3. **לא לסווג לפי `status` בלבד.** אובייקט עם `status: 429` שאינו `ApiError`
>    מסווג `false` בכוונה — אחרת כל דבר שנושא את השדה הזה היה נראה כמו מכסה.
>
> `isQuotaExceeded` נבדק מול **9 מקרי קצה** לפני חיבור למסך (429/500/400,
> `Error` רגיל, אובייקט מחופש, `null`, `undefined`, מחרוזת, ו-`status` כמחרוזת
> שלא עוברת coercion). כולם עברו.
>
> **מה שלא אומת, ולמה:** מסך השגיאה עצמו חסום מאחורי auth ואין לי דרך להזין
> סיסמה. לכן ה-503 אומת בשרת (route זמני) ולא כמסלול לחיצה בדפדפן. **המשתמש
> יראה את ההודעה בפועל רק כשהמכסה תיגמר** — או בהתחברות + חסימה מלאכותית.
> העיצוב כן אומת: אין רכיב חדש, ההודעה נכנסת לאותו
> `<p className="text-sm text-danger-ink">` שכבר החזיק את הודעת ה-429.

### מה שנקבע בשיחה

| החלטה | הבחירה |
| --- | --- |
| "איך מגיעים" | **קישורי Google Maps directions** — ניווט תחבורה ציבורית אמיתי, חינם, בלי מפתח. יוצא לאפליקציה חיצונית |
| `/design-preview` | נמחק לפני העלייה לאוויר (בוצע ב-`5723f85`) |

### שני ממצאים שקדמו לטבלה

**C1 — למה הבקשות הפסיקו לעבוד.** כל שבעת ה-routes של ה-AI נגמרים ב-
`catch { return 502 "ai_failed" }`. **השגיאה נבלעת ואף לא נרשמת ללוג**, ולכן אין
דרך לדעת מה קרה. מה שכן ידוע: זה לא ה-rate limiter שלנו — הוא מחזיר 429 עם
הודעה מפורשת, והתסמין היה "פשוט לא עובד". החשוד המרכזי הוא **המכסה היומית של
Gemini free tier**, שנשארת חסומה עד למחרת. זה ניחוש כל עוד השגיאה נזרקת לפח,
ולכן זה הפריט הראשון.

> **✅ נסגר ב-C1 (2026-08-13).** השגיאה נרשמת ללוג עם `code`/`status`/`reason`,
> ומכסה (429 מהספק) מקבלת 503 נפרד מ-502. **ההשערה עצמה עוד לא אוששה** — בזמן
> הבנייה המכסה לא הייתה חסומה וקריאה אמיתית ל-Gemini עברה. הלוג הוא הכלי
> שיכריע בפעם הבאה שזה קורה, ולכן אין יותר צורך לנחש.

**C5 — למה חזרו מעט ערים.** `aiCitySuggestionsSchema` **לא מגביל** את מספר
הערים, ו-`aiSuggestRequestSchema` כבר תומך ב-`count` עד 10. כלומר הקיצור מגיע
מניסוח ה-prompt, **ואין בכלל מנגנון "עוד ערים"** — בעוד שרמה 2 (מדריך העיר) כן
מקבלת `exclude` + `count` בדיוק לזה. אז זו הרחבה של דפוס קיים, לא פיצ'ר חדש.

> **✅ נבנה ב-C5 (2026-08-13).** `exclude` נוסף ל-`aiSuggestRequestSchema`
> ול-prompt, וכפתור "הצג עוד יעדים" מוסיף לרשימה במקום להחליף אותה.
>
> **⚠️ המלכודת: לשורות רמה 1 אין הגנת ייחודיות בכלל.** האינדקס מ-0003 הוא על
> `(trip_id, city, category, name)`, ולשורות האלה גם `city` וגם `category` הם
> `NULL` — ו-Postgres מתייחס ל-NULL כשונה מ-NULL, כך שהאינדקס **לא נורה עליהן
> לעולם**. ההערה במיגרציה עצמה אומרת זאת במפורש ("NULL city/category rows are
> unaffected"), אבל בהקשר אחר. לכן `upsert` עם `onConflict` היה חסר תוקף כאן,
> והניכוי חייב לקרות בקוד: `mergeCitySuggestions` מסלקת כפילויות מול הקיים
> **וגם בתוך התשובה עצמה**, כי תשובה אחת של AI יכולה לנקוב באותה עיר פעמיים.
>
> **`appendCities` נפרד מ-`saveCities`**, שמוחק לפני שהוא כותב — נכון לרענון
> מ-prompt חדש, שגוי כאן. אותה הבחנה בדיוק שכבר קיימת בין `saveCityGuide`
> ל-`savePlanFromChat` (שלב 17). הניכוי נעשה מול מה שבטבלה ולא מול מה שהיה על
> המסך, כדי ששני טאבים פתוחים על אותו טיול לא יתחרו לכפילות.
>
> **ה-prompt המקורי לא נשמר** — ההצעות נשמרות, הבקשה שיצרה אותן לא. לכן בביקור
> חוזר הרשימה קיימת והתיאור אבד, וכפתור שדורש prompt היה נעלם בדיוק כשהטיול הכי
> צריך אותו. `moreCitiesPrompt` נופלת לתיאור **שנגזר מהערים עצמן** ("טיול באותו
> אזור ובאותו סגנון כמו…"), כי ה-AI קורא אזור וסגנון מ"קיוטו, אוסקה, טאקאיאמה"
> בלי בעיה — ואותם שמות נשלחים כ-`exclude` בכל מקרה. **זה נמצא באימות ויזואלי:**
> בגרסה הראשונה הכפתור פשוט לא הופיע על רשימה שנטענה מה-DB.
>
> **אם ה-AI מחזיר בכל זאת את אותן ערים** (prompt הוא בקשה, לא הבטחה) מוצגת הודעה
> "לא נמצאו יעדים חדשים" ולא כפתור שנראה שבור.

### מגבלה שחלה על C4

ניתוב תחבורה ציבורית בחינם **לא קיים כ-API** — לא ב-OSM ולא אצל אף ספק. מה
שקיים חינם: קישורי Google Maps directions (תחבורה ציבורית אמיתית, יוצא לאפליקציה
חיצונית), OSRM demo (הליכה ורכיבה בלבד, שרת בלי התחייבות), ומרחק אווירי + הליכה
שכבר מחושבים אצלנו לפריטים עם קואורדינטות. **נבחרו הקישורים.**

> **✅ מיושם ב-C4 (2026-08-13).** המגבלה עומדת בעינה: האפליקציה **לא** מחשבת
> מסלול תחבורה ציבורית, היא מעבירה את השאלה ל-Google Maps. מה שנשאר בתוך
> האפליקציה הוא מרחק אווירי + הליכה בשורת המעברים, ורק לפריטים עם קואורדינטות.

---

## 🎨 פאזה D — סבב עיצוב רוחבי (Booking.com)

> **נפתחה 2026-08-18.** הבקשה: "אני רוצה עיצוב נקי ומסודר שיתאים לפלואו של
> האפליקציה", רספונסיבי למחשב ולטלפון לפי הסטנדרט של החברות המובילות.
> ההחלטות שנסגרו עם המשתמש: **כיוון Booking.com** · **sidebar + שתי חלוניות
> מ-1024px** · **הפסטלים לעיר יורדים לנקודה ותג** · העבודה בחתיכות גדולות בלי
> אישור לכל פריט.

| # | מה | סטטוס |
| - | -- | ---- |
| D1 | תשתית טוקנים v3 — משטחים נמדדים, שלשות תפקיד בסגנון BUI, סולם טיפוגרפי אכוף, `line-height: 1.7` לעברית, סקרימים כטוקנים | ✅ |
| D2 | פרימיטיבים — `SectionHeading`/`ListRow`/`Banner`/`Dialog`/`Select`/`Field`/`Surface`; `Card` עם padding; מחיקת 5 שיבוטים | ✅ |
| D3 | App shell — slot ל-sidebar, `AppHeader` מחולץ, ניווט בשלוש מצגות | ✅ |
| D4 | שתי חלוניות — `/map` (מפה + לוח), `/days` (אינדקס ימים sticky) | ✅ |
| D5 | כל המסכים לפי הדקדוק החדש, כולל תיקון ה-IA במסך הבית | ✅ |
| D6 | חורים וליטוש — `not-found`/`error`/`global-error`, כותרות דפדפן, יישור skeletons | ✅ |
| D8 | מחיקת טיול מהרשימה, עם אישור שמפרט מה נמחק | ✅ נבנה, לא אומת בריצה |
| D9 | ימים בכל עיר: נגזר מהלינה, נדרס ידנית, ונכנס ל-prompt כאילוץ | ✅ נבנה · **דורש migration 0013** |
| D10 | עריכת פריט בלוח: שעות, יום, הערה, ופירוט הגעה + זמן הגעה | ✅ נבנה · **דורש migration 0013** |
| D11 | סימון יום דל והפניה להוספת פעילויות (בלי קריאת AI) | ✅ נבנה |
| D7 | אימות ויזואלי של המסכים המוגנים ב-375/768/1024/1440/1920 | ⏳ דורש התחברות של המשתמש |

### שלוש הכרעות שכדאי לזכור

**הפס בעובי 4px הוא מה שהפך פסטל לרקע.** עשרה מקומות ציירו
`border-s-4` בארבעה צבעים ובשני עוביים. בגובה מלא ובעובי 4px פסטל מפסיק להיות
אקסנט והופך למילוי — וזה, יותר מכל דבר אחר, מה שייצר את "11 משפחות צבע על מסך".
`ListRow` החליף אותו בנקודה. **החריג היחיד הוא `day-timeline`**, כי שם הקצה רץ
לאורך הבלוק ולכן מקודד את משך הפריט; זה מידע, לא קישוט.

**`@theme inline` לא פולט את המשתנה.** זו המשמעות של `inline`, וזה אומר
שכל קוד שקורא טוקן דרך `var()` מחוץ ל-utilities של Tailwind — כמו מחרוזת ה-CSS
שבונה את הסיכה של Leaflet — לא יראה אותו. הצללים חיים ב-`:root` בשם
`--elevation-*` ומופים ל-`--shadow-*`. **השם חייב להיות שונה:**
`--shadow-soft: var(--shadow-soft)` הוא הגדרה מעגלית.

**ה-prompt של הלו"ז לא ראה את ההזמנות — וזה היה באג, לא פיצ'ר חסר.** עד D9,
`buildPrompt` קיבל שם טיול, תאריכים, ורשימה שטוחה של פריטים, ו-`listBookings` לא
נקרא באף route של AI. כלומר מלון ל-3 לילות ברומא לא השפיע בכלום, והמודל יכול היה
להחזיר 5 ימים ברומא ואפס בפירנצה — בזמן שהאפליקציה ממשיכה להציג את המלון ברומא
ליד יום בפירנצה. ההזמנות הן ההצהרה האמינה ביותר שכבר קיימת (שילמו עליהן), והן
נכנסות עכשיו ל-prompt גם כרשימת תאריכי לינה וגם כאילוץ מפורש על חלוקת הימים.

**סדר העדיפות לימים בעיר הוא דריסה ← לינה ← כלום**, ובמכוון: מה שהמשתמש הקליד
מנצח, אחריו מה שהזמין, ורק אם אין אף אחד מהם המודל מחליט. **הנגזר לא נשמר** —
הטבלה `trip_city_days` מחזיקה רק דריסות, כך ששינוי הזמנה ממשיך להיות מקור האמת
בלי backfill. `days = nights + 1`, כי לילה הוא המרווח בין שני ימים.

**בדיקת מקרי הקצה תפסה טעות — בבדיקה, לא בקוד.** 22 מקרים נבדקו לפני חיבור למסך,
ואחד נכשל: צ'ק-אין 15:00Z וצ'ק-אאוט 22:00Z באותו יום UTC. בדיקה שלו הראתה
ש-`bookingNights` צודק — ב-Asia/Jerusalem זה 18:00 עד 01:00 למחרת, כלומר לילה
אחד. **הציפייה שלי הייתה שגויה, לא המימוש.** מה שכן נלמד מזה: הספירה תלויה באזור
הזמן של הריצה, ולכן התוכנית מחושבת פעם אחת בשרת ומועברת כערך — אותה דיסציפלינה
ש-`trip-days.ts` מפעיל על `today`.

**"בנייה מחדש" הייתה מוחקת את פירוט ההגעה.** `saveItinerary` מוחק את כל השורות
ומכניס מחדש, כך שהוספת `travel_note`/`travel_minutes` בלי טיפול הייתה יוצרת מלכודת
איבוד נתונים: המשתמש מקליד איך מגיעים, לוחץ "בנייה מחדש", והכול נעלם בשקט. הערכים
עוברים עכשיו לפי שם מנורמל — אותה התאמה שהפונקציה כבר עושה כדי לשחזר עיר.

**שעות נבדקות עם `parseTimeLabel` ולא עם regex חדש.** זו הפונקציה שהציר משתמש בה
כדי למקם בלוק, ולכן ההבטחה אמיתית: כל מה שנשמר ניתן לציור. השעה גם מנורמלת
ל-HH:MM, אז יום לא מקבל "9:00" ליד "09:00" ליד "בין 8 ל-9".

**מחיקת טיול לא צריכה migration.** כל חמש טבלאות הבנות כבר מוגדרות
`trip_id ... references public.trips (id) on delete cascade` —
`suggested_destinations` ו-`itinerary_items` (0002), `trip_bookings` (0008),
`trip_phrasebooks` (0009) ו-`trip_chat_messages` (0010) — וה-policy
"Users manage own trips" הוא `for all`, כך שהוא כולל delete. לכן `delete from
trips` בודד הוא מחיקה שלמה, אטומית, ובלי שורות יתומות. **מה שכן נדרש הוא בדיקת
`count`:** Postgres מדווח על delete שלא התאים לשום שורה כהצלחה, ובלי הבדיקה
משתמש שמנסה למחוק טיול שאינו שלו היה מקבל "נמחק" על שורה שקיימת.

**הכרטיס בטיולים כבר לא עטוף ב-`<Link>`.** יש בו גם כפתור מחיקה, ו-`<button>`
בתוך `<a>` הוא HTML לא חוקי שגם נותן לקורא מסך שני פקדים במקום קישור אחד. במקומו
הקישור נושא `after:absolute after:inset-0` שמכסה את הכרטיס — כל המשטח נשאר לחיץ,
וכפתור המחיקה יושב מעליו ב-`z-10`.

**`import { Map } from "lucide-react"` שובר `new Map()`.** האייקון מסתיר את
הבנאי הגלובלי, ו-TypeScript מדווח על זה כ-"'new' expression whose target lacks a
construct signature" — הודעה שלא מרמזת על השורש. הכלל בפרויקט: אייקון ששמו מתנגש
עם גלובל מיובא עם alias (`Map as MapIcon`).

---

## 🧳 פאזה E — תיקוני שימוש אמיתי

> נפתחה מרשימת תשעה דיווחים שהמשתמש כתב אחרי שימוש בטיול אמיתי. שתיים מהן
> (E7, E8) הן אותה תקלה שורשית: הלו"ז נבנה על ידי בקשה מהמודל לכבד חלוקת ימים
> בטקסט חופשי, במקום עובדה שהקוד אוכף. השאר תיקוני UX/דיוק נקודתיים. ענף:
> `feature/phase-e-trip-fixes`.

### הטבלה

| # | מה | סטטוס |
| - | -- | ---- |
| E1 | שכונה (שיבויה) לא יוצרת עיר כפולה — הנחיית AI + מיזוג לפי מרחק גיאוקודינג | ✅ נבנה |
| E2 | טוסט פינתי אחרי הוספת/עדכון הזמנה (מלון/טיסה/רכבת) | ✅ נבנה |
| E3 | שם היעד דביק (sticky) בגלילת תוצאות בטאב האטרקציות | ✅ נבנה |
| E4 | "עוד תוצאות" + "חיפוש ליד כאן" בטאב האטרקציות | ✅ נבנה |
| E5 | דיוק מיקומים — "חיפוש ליד כאן" ממורכז על תוצאה ספציפית, לא מרכז העיר | ✅ נבנה חלקית (ראו E1) |
| E6 | עלות לכל הזמנה (migration 0014) + מחשבון הוצאות בטאב הלוגיסטיקה | ✅ נבנה |
| E7 | עריכת הזמנה קיימת (לא רק הוספה/מחיקה) | ✅ נבנה |
| — | תכנון הלו"ז מתחשב בזמן נחיתה של טיסה, ובונה בלוק ימים שלם לפי עיר במקום לפזר לפי סדר | ✅ נבנה — ראו E7/E8 בטבלה למעלה, אותו מנגנון |
| — | אימות ויזואלי מלא (תשעת הפריטים דורשים session) | ⏳ דורש התחברות של המשתמש |

### ההכרעה המרכזית: מהבקשה למודל לעובדה שהקוד אוכף

`cityDayPlan` (city-days.ts, D9) כבר חישב **כמה** ימים לכל עיר מהזמנות לינה, אבל
לפי **סדר הוספת הערים** ולא לפי התאריכים האמיתיים — וה-prompt ביקש מהמודל "לא
לשנות" את החלוקה, בקשה שאין לה אכיפה. זה בדיוק מה שדווח: מלון לשבוע בטוקיו יכול
היה להופיע בלו"ז לצד יום של יעד אחר, כי המודל בונה מסלול לפי כל הפריטים לפי הסדר
ומכניס את הלינה לפי תאריכים בנפרד מהחלטת התזמון עצמה.

**`buildDayCityPlan` (trip-days.ts) בונה מפה יום→עיר מהעובדות בפועל:**
`lodgingByDay` (C3) כבר יודע איזו הזמנת לינה מכסה כל לילה מהתאריך האמיתי שלה —
המפה הזו רק שואלת את אותה שאלה לכל יום 1..N של הטיול. יום עם לינה מקבל את העיר
של ההזמנה (`kind: "lodging"`); יום לפני ההגעה בפועל (מ-`travelDayCount`, שסופר
מזמן הנחיתה של הזמנת התחבורה המוקדמת ביותר) מקבל `kind: "travel"` ואסור לתזמן
בו כלום; כל יתר הימים הם `kind: "open"` — כאן, ורק כאן, למודל יש חופש, בדיוק
כמו קודם.

**זה עובד רק כשיש לטיול תאריך התחלה וסיום קבועים** (כדי לדעת `dayCount` מראש).
בלעדיהם — טיול פתוח — הכול חוזר בדיוק להתנהגות הקודמת (`cityDaysLine` הישן),
כי אין דרך לבנות מפה יום-ביום בלי לדעת כמה ימים יש.

**ההבטחה האמיתית היא לא ה-prompt, אלא `reconcileItineraryWithDayPlan`**
(`domain/itinerary-plan.ts`), שרץ **אחרי** תשובת המודל: לכל פריט, אם היום שהמודל
בחר סותר את התוכנית (עיר לא נכונה, או יום נסיעה) — הפריט זז ליום הנכון ליעד שלו
(היום הראשון עם לינה לאותה עיר, או ליום פתוח אם אין לה לינה בכלל). פריט שכבר
נכון נשאר במקומו. כך התוצאה נכונה **גם אם המודל מתעלם מההנחיה לגמרי** — בדיוק
הפער שהדיווח תיאר.

### תקלה שנתפסה בבנייה: circular import שהפיל את ה-build בלי אזהרה

`domain/ai-suggestion.ts` קיבל צורך חדש (E1) בפונקציית מרחק, ויובא אותה מ-
`domain/place.ts` — ש**כבר** מייבא מ-`ai-suggestion.ts` (בשביל `aiCategoryKeySchema`).
מעגל ייבוא בין שני קבצים. `type-check` ו-`lint` עברו נקי — אף אחד מהם לא בודק
סדר הערכה של מודולים — אבל `next build` נכשל עם `ReferenceError: Cannot access
'k' before initialization` בתוך `push-actions.ts`, קובץ שלישי שלא נגעו בו כלל,
כי Turbopack פירק את שני הקבצים לאותו chunk וסדר ההערכה שלו השאיר symbol אחר
ב-temporal dead zone. **הלקח: מעגל ייבוא לא בהכרח נכשל שקט — הוא יכול לשבור קובץ
שנראה לא קשור, וה-build הוא הבודק היחיד שתופס את זה.** הפתרון: `distanceKm` עבר
ל-`lib/geo.ts` ניטרלי, ושני הקבצים מייבאים משם — במקום ייבוא זה מזה.

### שאר הפריטים, בקצרה

**E1 — מיזוג שיבויה/טוקיו, בשתי שכבות.** הנחיה מפורשת ב-prompt של
`/api/ai/suggest`: אזור/שכונה בתוך עיר גדולה יותר מוצע בשם העיר, לא בשם השכונה.
גיבוי דטרמיניסטי ל-`saveCities`/`appendCities` (guide-service.ts):
`withoutNearbyDuplicates` מגאוקד מועמדים חדשים מול הערים הקיימות בטיול **וגם
מול עצמם** באותו סבב (כי AI יכול להציע שתי שכונות של אותה עיר לא-מוכרת בתשובה
אחת), ומשמיט כל מועמד בטווח 30 ק"מ (`DISTRICT_MERGE_RADIUS_KM`). Best-effort:
שם שלא מצליח לעבור גאוקודינג נשאר כפי שהוא, במקום לחסום את כל הסבב על שגיאת
רשת אחת. ריצה ראשונה (בלי ערים קיימות) מדלגת על הגאוקודינג לגמרי.

**E5 — דיוק מיקומים, חלקית.** "חיפוש ליד כאן" (E4) פותר את המקרה שבו מרכז
החיפוש הוא מרכז העיר כולה במקום נקודה ספציפית — ה-API מקבל `near` אופציונלי
שדורס את הגאוקודינג של שם העיר. לא נבדק/נפתר: דיוק הגאוקודינג של שם עיר עצמו
(מגבלה מוכרת מאז שלב 12), ותוצאות Overpass שגויות מסיבות שאינן קשורות למרכז
החיפוש.

**E4 — "עוד תוצאות" בלי קריאת Overpass נוספת.** `MAX_RESULTS` ב-`overpass.ts`
עלה מ-60 ל-150 — השרת כבר מדרג ומחזיר יותר ממה שמוצג; "עוד תוצאות" רק חושף עוד
מהרשימה שכבר התקבלה, `PAGE_SIZE=20` בכל פעם. אותה שאילתה בין כה נשמרת ב-cache
של Next ליום שלם, כך שהעלאת המספר לא מוסיפה עומס אמיתי על Overpass.

**E3 — שם היעד דביק.** פס עליון sticky מעל תוצאות החיפוש בטאב האטרקציות,
full-bleed (מרג'ין שלילי תואם בדיוק לריפוד של `<main>` ב-`AppShell`:
`-mx-4 md:-mx-6 lg:-mx-8`), כולל שורת הצ'יפים למעבר בין ערים.

**E6 — עלות ומחשבון הוצאות, בלי המרת מטבעות.** `trip_bookings.cost_amount` +
`cost_currency` (migration 0014, שני עמודות nullable). **בכוונה בלי FX:** שער
חליפין דורש שירות חיצוני (ולוקח בחשבון שהוא צריך להתעדכן כדי להישאר אמין) —
נגד כלל "אין שירותים בתשלום", וגם נגד "לא לנחש נתון". `costTotalsByCurrency`
(domain/expenses.ts) מסכם בתוך כל מטבע בנפרד ומציג טוטלים זה לצד זה — טיול עם
מלון בשקלים וטיסה בדולרים מציג שני מספרים, לא אחד מומצא.

**E2 — עריכת הזמנה.** `BookingForm` כבר היה טופס יחיד לשלושת סוגי ההזמנה;
קיבל `booking?` אופציונלי שמחליף את הפעולה ל-`editBooking` וממלא את כל השדות
מברירת המחדל שלו (`bookingDefaults`), כולל המרה הפוכה של `datetime-local`
(`toDateTimeLocal` — מסתמכת על אותה הנחה שכתיבת הטופס כבר עושה: הערך נכתב בלי
אזור זמן ו-Postgres שומר אותו כפי שהוא). `BookingList` פותח אותו בתוך `Dialog`
קיים, עם `Fragment` במקום `Card` כעטיפה כשעורכים (כדי לא לקבל כרטיס בתוך כרטיס).

**טוסט (חדש, לא ברשימה הממוספרת אך משרת את E2/E6).** `ToastProvider`/`useToast`
ב-`components/ui`, עטוף סביב כל האפליקציה ב-`layout.tsx`. כרטיסון פינתי
(`top-4 start-4`, `max-w-72`), נעלם לבד אחרי 3.5 שניות. מופעל מ-effect ולא
מרינדור — קריאה ל-`showToast` במהלך רינדור של קומפוננטה אחרת (`BookingForm`)
לעדכון state של קומפוננטת אב (`ToastProvider`) אינה חוקית ב-React.


---

## 🌍 פאזה F — שיתוף, מטבעות, ותיקון המפה

> תשעה דיווחים נוספים מהמשתמש אחרי שראה את פאזה E. נבנו באותו ענף
> (`feature/phase-e-trip-fixes`), כי ה-PR של E עוד לא נפתח ואין טעם בשני PRs
> שאחד מהם חוסם את השני.

### הטבלה

| # | מה | סטטוס |
| - | -- | ---- |
| F1 | עמוד הנחיתה: שלוש נקודות המכירה כבר לא כרטיסים — נראו כמו כפתורים לחיצים | ✅ נבנה ואומת |
| F2 | מטבע: ברירת מחדל שקל, בחירה בין ₪/$/€, וסימני מטבע במקום קודי אותיות | ✅ נבנה |
| F3 | סינון סיכום ההוצאות לפי יעד בטיול | ✅ נבנה |
| F4 | חלוקת תצוגת התחנות לפי מדינות | ✅ נבנה · **דורש migration 0015** |
| F5 | קישור בין טיסות שהן קונקשן, כולל זמן המתנה | ✅ נבנה |
| F6 | שיתוף פומבי של הטיול, לצפייה בלבד | ✅ נבנה · **דורש migration 0015** |
| F7 | תכנון מסלול לפי קרבה גאוגרפית — יעדים סמוכים ברצף | ✅ נבנה |
| F8 | כל הימים בלו״ז, כולל ריקים, עם הצעות AI ליום פנוי | ✅ נבנה |
| F9 | תיקון המפה — סיכות שנחתו במקום הלא נכון | ✅ נבנה · **דורש migration 0015** |

### ההכרעה המרכזית: גאוקודינג הפוך פותר שלושה פריטים בבת אחת

F4 (מדינות), F9 (מפה שבורה) ו-F7 (קרבה גאוגרפית) נראו כשלוש בעיות ונפתרו במנגנון
אחד, כי לשלושתן חסר בדיוק אותו נתון: **איפה כל עיר נמצאת באמת.**

**הכיוון ההפוך הוא האמין.** גאוקודינג של שם עיר בעברית הוא ניחוש — זה מה ששלח
את "האקונה" ללוס אנג'לס בשלב 12, וזה שורש הדיווח "חלק מהיעדים לא נכונים". אבל
**זוג קואורדינטות יושב במדינה אחת בדיוק**, ו-Nominatim עונה על זה בלי ספק.
`reverseCountry` ב-`lib/geocode.ts` עושה בדיוק את זה, פעם אחת לעיר, ונשמר
בעמודה `country` (migration 0015).

מרגע שלכל עיר יש מדינה, טעות שהייתה בלתי נראית הופכת מובנת מאליה:
`repairMisplacedCities` מוצא את מדינת הרוב של הטיול, ולכל עיר שנחתה מחוץ לה
מריץ גאוקודינג מחדש — **הפעם עם שם המדינה כהקשר**, שהוא הרבה יותר חזק משם
הטיול שהניסיון הראשון השתמש בו (`"יפן 2026"` מאבד את הספרות ועשוי לא לנקוב
במדינה בכלל). ניסיון שנוחת שוב מחוץ למדינה **נזרק והסיכה המקורית נשארת** —
הזזה בלתי מוסברת גרועה מטעות שהמשתמש רואה ויכול לדווח עליה. תיקון שכן קרה
**מוכרז בבאנר** ולא מוחל בשקט.

**שלושה סייגים מכוונים:** המנגנון לא רץ מתחת ל-3 ערים (אין רוב לערער עליו),
ולא רץ כשאין מדינה דומיננטית — `dominantCountry` מחזיר `null` על תיקו, כך
שטיול אמיתי בשתי מדינות לא "מתוקן" לתוך חצי מעצמו. ולסיכה שגויה ב-20 ק"מ בתוך
המדינה הנכונה אין תשובה אוטומטית, ולכן נוסף כפתור **"רענון המיקומים"** שמנקה
את ה-cache ומחשב הכול מחדש — אותה תרופה ש-migration 0005 יישם פעם אחת, גלובלית.

**F7 מגיע חינם מאותו נתון.** `orderByProximity` ב-`lib/geo.ts` הוא
nearest-neighbour מהעיר הראשונה. **לא אלגוריתם אופטימלי במכוון:** המסלול
האופטימלי הוא בעיית הסוכר הנוסע, וההפרש בלתי נראה בחמש תחנות — בעוד ש"אוסקה
בין שני ימי טוקיו", מה שהסדר לפי מועד ההוספה ייצר, מאוד נראה. ערים שכבר
משובצות בלו״ז **לא זזות** (הימים שלהן הוכרעו); רק הזנב הלא-משובץ ממוין, ומתחיל
מהתחנה המשובצת האחרונה.

### F6 — שיתוף פומבי, ולמה הוא לא משתמש ב-RLS

**מה שמשותף הוא רדקציה של הטיול, לא הטיול.** עמוד השיתוף מרונדר מטיפוס
`SharedTrip` ולא מהאגרגט — כך ששדה יכול להגיע לזר רק אם מישהו הוסיף אותו
במפורש ל-`redactBooking`. עמודה עתידית ב-`trip_bookings` בלתי נראית לעמוד
הציבורי עד שכותבים אותה לשם, וזה **ההפך** מכשל ברירת המחדל הרגיל שבו נתונים
חדשים דולפים מאליהם. שלושה שדות הוחרגו לפי בחירת המשתמש: `confirmation` (קוד
הזמנה מספיק כדי לשנות או לבטל אצל רוב הספקים — השדה המסוכן ביותר כאן),
`address` (הבניין המדויק שישנים בו, על קישור בלי סיסמה שאפשר להעביר לכל מקום)
ו-`cost_amount`/`cost_currency`.

**⚠️ נשקלה ונדחתה policy של RLS.** `create policy ... using (share_token is not
null)` נראה נכון — עד שמבחינים שהוא גם מתיר
`select * from trips where share_token is not null`, כלומר **מנייה של כל
הטיולים המשותפים בדאטהבייס**, מה שהופך את הטוקן לחסר משמעות. במקום זה העמוד
קורא דרך ה-service-role client בהתאמה מדויקת לטוקן — אין משטח שאפשר לתשאל בלי
לדעת מראש את התשובה. זה הקורא **השני והאחרון** של `createAdminClient`, וההערה
בקובץ עודכנה בהתאם. הטוקן: 128 ביט מ-`crypto.getRandomValues`, מאומת כ-32 תווי
hex **לפני** שהוא מגיע ל-DB.

**באג שנתפס באימות:** `/share/<טוקן תקין שלא קיים>` החזיר **500** ולא 404, כי
`createAdminClient` **זורק** כשהמפתח חסר ולעמוד לא היה guard. זה נמצא רק כי
המפתח לא קיים ב-`.env.local` המקומי (הוא רק ב-Vercel) — כלומר סביבת הפיתוח
החסרה היא שחשפה את זה. תוקן: הזריקה נתפסת, נרשמת בלוג בקול, והמבקר מקבל 404.

### שאר הפריטים

**F2 — סימנים, לא קודים.** `formatMoney` מציב את הסימן ידנית ולא דרך
`currencyDisplay: "narrowSymbol"`, כי he-IL מרנדר חלק מהמטבעות עם הקוד בכל
זאת — וכל הנקודה היא שזה לעולם לא יקרה. Intl עדיין מפרמט את **המספר**, שזה מה
שנותן מפרידי אלפים ושתי ספרות עשרוניות. המטבע הוא `Select` משלוש אפשרויות ולא
שדה טקסט: קוד מוקלד הוא טעות הקלדה שמפצלת מטבע אחד לשני סכומים שלא מתחברים.
**סייג שנתפס:** ברגע שיש ברירת מחדל, כל שליחה נושאת קוד מטבע — ולכן הכלל
"מטבע בלי סכום = שגיאה" בוטל, ובמקומו השירות שומר `null` בשניהם כשאין סכום.

**F5 — קונקשן נגזר, לא נשמר.** שתי הזמנות תחבורה הן מסע אחד כשהשנייה יוצאת
מהמקום שהראשונה נחתה בו, זמן קצר אחריה. **שום דבר חדש לא נשמר** — זה יחס שהשורות
כבר מתארות, ולשמור קישור פירושו לסנכרן אותו מול שני חותמי זמן שהמשתמש יכול
לערוך בכל רגע. תקרה של 12 שעות: מעבר לזה זו לינה, כלומר יעד ולא המתנה. נדרשת
שעת נחיתה לרגל הראשונה — בלי `ends_at` אין ממה למדוד, ולהניח היה להמציא המתנה.

**F8 — יום ריק הוא מידע.** הלו״ז נבנה מ-`itinerary_items`, וליום ריק אין שורות,
ולכן הוא פשוט לא הופיע — שבוע בטוקיו עם שלושה ימים מתוכננים הוצג כשלושה ימים,
מה שנקרא כטיול קצר יותר ולא כארבעה ימים פנויים. `withEmptyDays` ממלא את הפערים
עד `tripDayCount`. העיר של יום ריק מגיעה מהלינה (אין פריט לקרוא ממנו), ובלחיצה
נפתח `DaySuggestionsDialog` שמושך הצעות מ-`/api/ai/recommendations` — ה-route
שקיים מאז שלב 8; השאלה זהה, רק מקום התצוגה שונה. שתי הקטגוריות נמשכות במקביל
(`Promise.all`) כי זה מודאל שמסתכלים עליו.

### תקלה שחזרה על עצמה: circular import, שוב

`domain/place.ts` ייבא `distanceKm` מ-`ai-suggestion.ts` — אותו מעגל שהפיל את
ה-build בפאזה E, בכיוון ההפוך. הפעם הוא נמנע מראש: `distanceKm` כבר ישב
ב-`lib/geo.ts` הניטרלי, ו-`CURRENCY_CODES` הוגדר ב-`booking.ts` במקום להיות
מיובא מ-`expenses.ts` (שמייבא את `booking.ts` בשביל `BOOKING_KINDS`), עם הערה
שמסבירה למה. **הכלל שנלמד:** שני קבצי דומיין שצריכים אותו ערך — הערך עובר
ל-`lib/`, לא מיובא מאחד לשני.

---

## 🔎 פאזה G — חיפוש אזורים, דיוק המפה, וטיסות בלו״ז

> ארבעה דיווחים אחרי שפאזות E ו-F עלו לפרודקשן (PR #41). ענף:
> `feature/phase-g-search-map-flights`.

### הטבלה

| # | מה | סטטוס |
| - | -- | ---- |
| G1 | חיפוש אזור בתוך יעד (Omotesando בטוקיו) — OSM + AI | ✅ נבנה |
| G2 | דיוק המפה: מיקום עיר נגזר מהמקומות בפועל, ותיקון באג cache שקט | ✅ נבנה ואומת |
| G3 | סיכות לכל מקום על המפה, לא רק לערים | ✅ נבנה |
| G4 | טיסות ורכבות על ציר הלו״ז, לפי השעה האמיתית | ✅ נבנה ואומת |

### G1 — למה חיפוש "Omotesando" החזיר אפס

זה לא היה באג אלא **הנחה שגויה בארכיטקטורת החיפוש**. `searchPlaces` שולח
ל-Overpass שאילתה לפי **תגית קטגוריה** ברדיוס 5 ק"מ ממרכז העיר, ואז מתאים את
מחרוזת החיפוש מול **שם המקום** ב-JS (ראו את ההערה בראש `lib/overpass.ts` — אין
אינדקס על ערכי תגיות, ולכן התאמת שם בשאילתה היא סריקה של 57 שניות). כלומר
`query` יכול למצוא רק מקום ש**נקרא** Omotesando — ואין בית קפה בשם הזה.
Omotesando הוא שכונה, ואין קטגוריה שמכילה שכונות. אפס תוצאות לאחד האזורים
העמוסים בטוקיו, מה שנקרא כ"אין שם כלום".

**הפתרון: שדה `area` נפרד שהופך לנקודת המרכז של החיפוש** במקום להיות מחרוזת
להתאמה. `resolveAreaInCity` מגאוקד את שם האזור **עם שם העיר כהקשר** — בדיוק
המקרה שבשבילו `lib/geocode.ts` מגדיר הקשר — ואז **מוודא שהתוצאה באמת בתוך
העיר** (`isSameDestination`, 30 ק"מ). הבדיקה הזו הכרחית: בלעדיה הפיצ'ר היה דרך
חדשה לייצר את באג הסיכה השגויה, כי שכונה עם שגיאת כתיב הייתה ממרכזת את החיפוש
ביבשת אחרת ומחזירה רשימה משכנעת של בתי קפה במדינה הלא נכונה. הרדיוס יורד
ל-1.5 ק"מ (`AREA_RADIUS_M`) כי שכונה היא מרחק הליכה, ורדיוס עירוני היה מחזיר
את כל העיר וקובר את מה שבאמת באזור. **`area_not_located` הוא קוד שגיאה נפרד
מ-`city_not_located`** — "לא מצאנו את טוקיו" ו"לא מצאנו את השכונה הזו בטוקיו"
דורשים תגובה שונה, והשנייה היא בדרך כלל שגיאת כתיב שאפשר לתקן.

**והחצי שה-OSM לא יכול לעשות:** Overpass מוצא מה ש**מתויג** ברדיוס. אין לו מושג
ש-Omotesando זה בוטיקים וסוג מסוים של בתי קפה, ואיזה מהמקומות שם שווים את
הביקור. לכן נוסף כפתור "מה ה-AI ממליץ ב<אזור>" שמעביר `area` ל-
`/api/ai/recommendations`. **ה-prompt אומר במפורש שאסור להרחיב לאזורים אחרים** —
זה כשל שקט וסביר: מודל שנשאל על Omotesando ישמח לענות במקומות המפורסמים של
העיר, והרשימה נראית נכונה עד שמבחינים שאף פריט בה לא באזור שנשאל עליו. שני
המקורות מוצגים זה לצד זה ולא במקום זה, כי הם יודעים דברים שונים.

### G2 — שני באגים אמיתיים במפה, שנמצאו בקריאת הקוד

**🐞 באג 1: ה-cache של הקואורדינטות לא נכתב בכלל לחלק מהערים.** שורת ה-`overview`
היא המקום שבו נשמרות קואורדינטות של עיר — והיא **נוצרת רק על ידי
`saveCityGuide`**, כלומר כשהמשתמש פותח את עמוד המדריך של אותה עיר. לעיר שנכנסה
לטיול דרך חיפוש האטרקציות או הטופס הידני **אין שורת overview בכלל**.
`cacheCoordinates` היה `update` חשוף, ו-Postgres מדווח על update שלא התאים לשום
שורה כהצלחה — כך שלערים האלה ה-cache **מעולם לא התמלא**: כל רינדור גאוקד אותן
מחדש (בקצב בקשה לשנייה, על השירות המשותף של Nominatim), **וגם חיפוש המדינה
ותיקון הסיכה של פאזה F כתבו את התוצאות שלהם לאוויר** — וזו הסיבה שסיכה שתוקנה
חזרה להיות שגויה. תוקן ל-`upsert` עם `ignoreDuplicates: false`.

**🐞 באג 2 (הגדול): המפה זרקה את הנתון המדויק ביותר שיש לאפליקציה.** כל מקום
שנוסף מחיפוש האטרקציות נושא את הקואורדינטות של OpenStreetMap עצמו — **השורות
היחידות בטבלה שלא עברו גאוקודינג, ולכן היחידות שלא יכולות להיות שגויות.** ובכל
זאת הסיכה של העיר הייתה תמיד ניחוש מגאוקודינג של שם עברי.
`deriveCityCentresFromPlaces` גוזר את מיקום העיר מהמקומות שבתוכה, ורק אם אין
אף אחד נופל לגאוקודינג. **חציון ולא ממוצע** — מקום אחד שמתויג לא נכון ב-OSM היה
מגרר ממוצע על פני המפה, ועיוות בודד הוא בדיוק מה שזה אמור לעמוד בפניו. אומת:
ארבע נקודות בטוקיו + אחת בלוס אנג'לס → `35.67/139.73`, כלומר בטוקיו.
**הניסוח שמסביר את ההיגיון:** חיפוש שם יכול לשים את "האקונה" בלוס אנג'לס, אבל
חמש מסעדות אמיתיות בטוקיו לא יכולות להתמצע לשום מקום אחר מלבד טוקיו.

**G3:** ומאותו נתון — המפה מציירת עכשיו **סיכה קטנה לכל מקום**, לא רק דיסקית
ממוספרת לכל עיר. גבולות המפה מחושבים מהערים בלבד במכוון, כדי שנקודה אחת
מתויגת-לא-נכון לא תרחיק את הזום של כל המפה כדי להכיל אותה.

### G4 — טיסות על ציר הלו״ז

הטיסות היו קיימות ב-`bookingsByDay` ומוצגות בטאב "היום", אבל **הציר הגרפי
ב-`/days` לא ידע עליהן** — לו״ז עם טיסה ב-06:40 הציג יום שמתחיל בפעילות
הראשונה ב-10:00, בלי שום סימן שיש טיסה.

`buildDayTimeline` מקבל עכשיו את הזמנות היום ואת התאריך. `TimelineBooking`
נשמר **נפרד** מ-`TimelineEntry` ולא מומר אליו, כי אלה שני סוגי עובדה: פריט
לו״ז הוא הצעה עם שעה בטקסט חופשי שהמודל כתב ואפשר לערוך, והזמנה היא חותם זמן
אמיתי ששולם עליו — לא נערך כאן, לא נמחק כאן, **והדבר היחיד ביום שבאמת לא יכול
לזוז.** ההזמנות ממוקמות **לפני** שהציר נמדד, כי טיסה ב-06:40 חייבת להרחיב את
הציר; אחרת היא הייתה נצמדת לקצה העליון ונקראת כאילו יצאה יחד עם ארוחת הבוקר.
הן מצוירות **בנתיב צר משלהן** ולא בעמודה הראשית: טיסה ופעילות אינן חלופות
שמתחרות על אותה משבצת, וטיסה ארוכה בעמודה אחת הייתה מכסה כל פעילות מתחתיה.

**באג שנתפס בבדיקת מקרי הקצה, לא בקוד שנכתב בעיוורון:** הזמנה **בלי שעת הגעה**
צוירה כבלוק של תשע שעות עד חצות, כי ענף ה-"אין סוף" והענף של "הסוף ביום אחר"
היו מאוחדים. שלושה מצבים שונים עכשיו: אין סוף → בלוק מינימלי קריא (ההצהרה
הכנה היא "יוצא ב-20:00", לא "תופס את שאר היום"); הסוף ביום מאוחר יותר → עד
חצות ומסומן כממשיך; הסוף היום → המשך האמיתי.

**כפילות שהוסרה:** `DayPager` הציג את ההזמנות כרשימה **וגם** העביר אותן לציר
שמתחתיו, כלומר אותן שלוש עובדות פעמיים ברצף. גרוע מזה — הוא קרא את השעה עם
`getHours()`, כלומר **שעון הצופה**, בזמן שהציר פותר אותה באזור הזמן של הטיול.
שתי תשובות שונות לאותה יציאה הן הסיבה האמיתית לבחור אחת.

**אימות:** 12 מקרי קצה על `buildDayTimeline` דרך route זמני שנמחק — טיסת לילה
שיוצאת 1.11 ונוחתת 2.11 (יום 1: 20:00 עד חצות ומסומן ממשיך · יום 2: מ-00:00
עד 14:30 · יום 3: לא מופיעה בכלל), מסע רכבת של שלושה ימים שהיום האמצעי שלו
מכוסה למרות שאין בו אף קצה, `22:30Z` שנופל על יום אחר ב-UTC ובטוקיו (הסיבה
ש-`zone` הוא פרמטר), וחותם זמן פסול שמדולג ולא זורק.

---

## 🛟 פאזה H — באג אובדן נתונים ומדריך שלבי עבודה

> שני פריטים, והראשון חמור. אותו ענף כמו פאזה G
> (`feature/phase-g-search-map-flights`), כי ה-PR שלה עוד לא נפתח.

| # | מה | סטטוס |
| - | -- | ---- |
| H1 | 🐞 "רענון הצעות" מחק את כל מה שהמשתמש הוסיף לטיול | ✅ תוקן |
| H2 | מדריך "איך זה עובד" — 7 שלבים עם קישור לכל מסך | ✅ נבנה ואומת |

### H1 — הבאג, והשורש שלו

המשתמש דיווח: כניסה ליעד, לחיצה על "רענון הצעות", **וכל מה שהוסיף ללו״ז נעלם.**
הדיווח מדויק, וזה היה באג אמיתי של אובדן נתונים.

`deleteCityGuide` היה:

```
.delete().eq("trip_id", …).eq("city", …).eq("source", "ai")
```

כלומר **כל** שורות ה-AI של אותה עיר. שתי קטגוריות של נתונים נמחקו שלא היה לו
שום עסק איתן:

1. **כל מה שהמשתמש הוסיף לטיול.** שורה עם `selected = true` היא החלטה שהמשתמש
   קיבל, ו"הראה לי הצעות אחרות" היא לא בקשה לבטל אותה. הלחיצה רוקנה בשקט את
   הטיול מכל פריט שנבחר באותה עיר — הוא נעלם מ"מה שבחרתם", ממפת המסלול,
   ומהבנייה הבאה של הלו״ז. שום דבר ב-UI לא רמז שזה יקרה, ושום דבר לא יכול היה
   להחזיר את זה.
2. **שורת ה-`overview`** — שם נשמרות הקואורדינטות והמדינה של העיר
   (route-service.ts). כלומר רענון מדריך זרק גם את ה-cache של המפה לאותה עיר.
   ואחרי פאזה G, שגוזרת את מיקום העיר מהמקומות שבתוכה, מחיקת המקומות **הזיזה
   את הסיכה** — כלומר הבאג הזה היה גם אחד הגורמים לדיווח על המפה.

**התיקון חוסם משני הצדדים:** `.eq("selected", false)` ו-
`.neq("category", "overview")`. פריטים שנבחרו נשארים, ו-`saveCityGuide` כותב
אותם ב-`ignoreDuplicates` כך שהדגל שלהם לא מתאפס.

**שני תיקונים נגררים שנדרשו כדי שהתיקון יהיה שלם:**

- **שורת ה-overview שורדת עכשיו, ולכן ה-intro לא היה מתרענן** (היא נכתבה
  ב-`ignoreDuplicates: true`). היא הופרדה לכתיבה משלה עם `ignoreDuplicates:
  false` — **ובמכוון בלי `latitude`/`longitude`/`country` ב-payload**, כי
  PostgREST כותב רק את העמודות שהוא מקבל, וכך ה-cache נשמר. לכלול אותן כ-
  `undefined` היה מאפס אותו בכל רענון — בדיוק הבאג שהמחיקה הפסיקה לעשות בדרך
  אחרת.
- **התצוגה הייתה משקרת.** `generate()` בנה את המצב מתשובת ה-AI, שבה כל
  `selected` הוא `false` — כך שפריט ששרד היה מוצג כ"הוספה לטיול" למרות שהוא
  בטיול. `saveGuide` מחזיר עכשיו את המדריך **כפי שהוא בדאטהבייס** אחרי הכתיבה,
  ומשם נקרא המצב. תשובת ה-AI לא יכולה לדעת מה בטיול, ולכן היא לא המקור לכך.

נוסף גם באנר שאומר כמה פריטים נשמרו, ומשפט הסבר ליד הכפתור — הרשימה לא תיראה
חדשה לגמרי, וזו הכוונה.

**נבדקו גם שאר מסלולי המחיקה:** `saveItinerary` מוחק את כל הלו״ז, אבל זו
פעולת "בנייה מחדש" מפורשת ופירוט ההגעה מועבר לפי שם (D10). `saveCities` מוחק
רק כרטיסי ערים ברמה 1 (`category is null`), שאינם ניתנים לבחירה כלל — נוספה
הערה שמסבירה למה זה בטוח, כדי שההבחנה תהיה מפורשת ולא מקרית.

### H2 — מדריך שלבי עבודה

`domain/workflow.ts` מחזיק את שבעת השלבים **כנתון ולא כ-JSX**: לשלבים יש סדר
אמיתי שמסכים אחרים ירצו להסיק ממנו, והנוסח של "בשביל מה השלב הזה" הוא קופי
מוצר שצריך לגור במקום אחד ולא להשתכפל למצב ריק ולעמוד עזרה שיתפצלו עם הזמן.
כל שלב נושא את הטאב שבו הוא קורה, כך שהמדריך **מקשר למסך האמיתי של הטיול הזה**
במקום לתאר איפה למצוא אותו. `/trips/<id>/more/guide`, עם כרטיס ב-״עוד״.

הכרטיס האחרון בעמוד הוא **הכלל שנלמד מ-H1**: שום דבר שהוספתם לא נמחק מעצמו,
והדרך היחידה להוציא פריט היא להסיר אותו במפורש.

**אימות:** route זמני שנמחק בדק ש-7 השלבים מצביעים ל-routes שקיימים באמת (אפס
קישורים מתים), שכל חמשת הטאבים מוזכרים, ושלכל שלב יש טיפים; `/more/guide`
מחזיר 307 ל-login כמו כל מסך מוגן, ו-`next build` רושם אותו.

---

## 🕐 פאזה I — שעות הטיסה זזו בשמירה

> דיווח: "אני רוצה 22:20 וברגע שאני שומר זה הופך ל-00:20." אותו ענף כמו G ו-H.
> **דורש migration 0016.**

### השורש: הכתיבה והקריאה לא הסכימו

שדה `datetime-local` מייצר `"2026-11-01T22:20"` — **קריאת שעון בלי אזור זמן**.
העמודה `trip_bookings.starts_at` היא `timestamptz` — **רגע בזמן**. אלה שני
דברים שונים, והאפליקציה כתבה את הראשון לתוך השני בלי להמיר: Postgres פותר
מחרוזת בלי אזור לפי אזור ה-session, שהוא **UTC** ב-Supabase. אז 22:20 נשמר
כ-`22:20+00`.

כל צד הקריאה פותר ב-`Asia/Jerusalem` (`APP_TIME_ZONE`), ולכן `22:20+00` הוצג
כ-**00:20 למחרת**. הפרש של בדיוק היסט אזור הזמן.

**וזה לא היה רק תצוגה.** אותה הזזה העבירה הזמנות ל**יום הלא נכון** בכל מקום
שמדלה לפי תאריך קלנדרי — `bookingsByDay`, `lodgingByDay`, `travelDayCount`
ו-`toTimelineBooking` שנוסף בפאזה G. טיסה ב-22:20 שויכה ליום הבא בלו״ז, וכל
המנגנון הדטרמיניסטי שנבנה בפאזה E הסתמך על נתון מוזז.

### התיקון

`src/lib/datetime.ts` — קובץ ניטרלי ב-`lib/` (לא בדומיין, כדי לא לחזור על
מלכודת ה-circular import מפאזה E). שתי פונקציות שהן **הגבול היחיד** שבו שעה
מוקלדת הופכת לרגע בזמן ובחזרה:

- `wallClockToInstant(wall, zone)` — ההיסט נבדק ברגע **התשובה** ולא בשאלה,
  והתשובה היא מה שמחפשים; לכן הוא מוחל פעם אחת ואז מאומת שוב. המעבר השני
  משנה משהו רק במעבר שעון קיץ, ובשאר השנה הוא no-op.
- `instantToWallClock(iso, zone)` — הכיוון ההפוך, למילוי טופס העריכה.

**בלי ספרייה:** Intl כבר יודע את ההיסט של כל אזור כולל היסטוריית שעון הקיץ, וזה
החלק הקשה היחיד; `date-fns-tz` הייתה תלות שרק חושפת אותו מחדש.

**תיקונים נגררים שבלעדיהם התיקון לא שלם:**

- **`toDateTimeLocal` היה `iso.slice(0, 16)`** — נכון רק כל עוד הבאג היה קיים,
  כלומר כל עוד הספרות נשמרו כמו שהוקלדו. עכשיו הוא ממיר בחזרה, אחרת פתיחת
  הזמנה לעריכה הייתה מציגה שעה מוזזת ושמירה חוזרת הייתה מזיזה אותה שוב.
- **`formatWhen` ברשימה היה `toLocaleString` חשוף**, כלומר אזור הזמן של הדפדפן
  — אותה הזמנה נקראה אחרת ממחשב בחו״ל. עכשיו `APP_TIME_ZONE` מפורש: טיסה יוצאת
  בשעה שהיא יוצאת **במקום שממנו היא יוצאת**.
- **עמוד השיתוף כפה UTC**, שהיה נכון רק כשהעמודה החזיקה ספרות מוקלדות. עכשיו
  אותו אזור כמו הבעלים — קודם הם לא הסכימו.

### migration 0016 — היחיד בפרויקט שאינו אידמפוטנטי, ולכן היחיד ששומר על עצמו

התיקון לשורות הקיימות:
`(starts_at at time zone 'UTC') at time zone 'Asia/Jerusalem'` — הספרות
כפי שנשמרו, נקראות מחדש כשעון ירושלים. **Postgres מחיל את כללי שעון הקיץ של כל
שורה בנפרד**, כך שהזמנת קיץ זזה 3 שעות וחורף 2; interval קבוע היה משחית את חצי.

זו **הזזה אריתמטית**, לא `add column if not exists` — הרצה כפולה הייתה מזיזה
הכול פעם שנייה בלי דרך להבחין. לכן נוצרת טבלת `applied_migrations` וה-migration
בודק את עצמו בתוכה. הטבלה שווה את עצמה ממילא: 16 migrations בפנים ולא הייתה שום
דרך לשאול את הדאטהבייס מה כבר רץ.

**⚠️ חלון סיכון בסדר הפעולות:** ה-migration מזיז את **כל** השורות, כולל כאלה
שייכתבו נכון על ידי הקוד החדש. אם הקוד יעלה לפרודקשן ותיווצר הזמנה **לפני**
שה-migration ירוץ, היא תוזז בטעות. ההנחיה: להריץ את ה-migration ולמזג בסמיכות,
ולא להוסיף או לערוך הזמנה בין לבין.

**אימות:** route זמני שנמחק בדק את ההמרה מול המקרה שדווח (22:20 → נשמר
`20:20Z` → חוזר לטופס כ-22:20 → מוצג כ-22:20), מול חורף מול קיץ (היסטים שונים,
שניהם חוזרים נכון), מול מעבר שעון קיץ באביב ובסתיו (כולל השעה הכפולה, שלא
זורקת), מול חצות ו-23:59 שלא גולשים ליום סמוך, מול ארבע מחרוזות פסולות שמוחזרות
כ-`null` במקום להישמר כ-Invalid Date, ומול אזור זמן אחר (טוקיו) כדי לוודא ששום
דבר לא מקודד קשיח לישראל. **ובעיקר: 365 ימים רצופים ב-22:20, אפס כשלים** — כדי
שכלל שעון קיץ שלא חשבתי עליו לא יוכל להסתתר. אומת גם שה-SQL מייצר בדיוק את אותו
רגע שהקוד החדש כותב (`22:20+00` → `20:20+00`), כך שהשורות הישנות והחדשות
יסכימו.

---

## 🎨 פאזה J — אייקון אפליקציה חדש

> המשתמש ביקש לוגו חדש למסך הבית באייפון, "כמו הלוגו של האתר". נבחר מול
> העיניים: ארבע אפשרויות הוצגו, כל אחת גם **בגודל האמיתי של מסך הבית**, ואז
> שלוש גרסאות עם כיתוב. הסופי: **תכלת `#0071c2` + מטוס לבן + קו מסלול מקווקו,
> בלי כיתוב.**
>
> **התהליך שווה תיעוד יותר מהתוצאה.** המשתמש בחר תחילה גרסה עם הכיתוב MyTrip.
> היא נבנתה במלואה, ואז — אחרי שראה אותה מרונדרת בגודל האמיתי לצד סימולציה של
> מסך הבית עם התווית ש-iOS כותב בעצמו — הוא הפך את ההחלטה. זה בדיוק מה שהצגת
> אפשרויות **בגודל שבו הן ייראו** אמורה לחשוף, וזו הסיבה שההסתייגות הוצגה
> ויזואלית ולא רק נכתבה.

**האייקון הקודם לא תאם כלום.** הוא היה סיכת מפה בפלטה הישנה
(`#faf7f2` / `#9a7658` — חום מוקה), כלומר לא הסמל שבכותרת (מטוס) ולא הצבעים
שהאפליקציה משתמשת בהם מאז פאזה D. הסמל עכשיו הוא **בדיוק** ה-path של
`plane` מ-lucide, אותו אחד שמרונדר ב-`AppHeader`, על `--primary` (`#0071c2`).

### `scripts/generate-icons.mjs` — הגדרה אחת, שלוש צורות

הפלטפורמות רוצות דברים שונים, וזו הסיבה שיש סקריפט ולא קובץ:

| צורה | לאן | למה כך |
| ---- | --- | ------ |
| מעוגלת | `icon.svg`, `icon-192`, `icon-512` | דפדפנים מציגים as-is, ולכן היא נושאת רדיוס משלה |
| **ריבוע מלא** | `apple-touch-icon.png` | **iOS מחיל מסכה משלו** — מקור מעוגל מקבל עיגול כפול ופינות כהות |
| maskable | `icon-maskable-512` | אנדרואיד חותך לצורת המשגר, ולכן האיור יושב ב-80% המרכזיים והרקע יוצא לקצה |

**⚠️ ה-SVG-ים ב-`public/` הם פלט של הסקריפט ולא מקור.** עריכה ידנית שלהם מוציאה
אותם מסינכרון עם ה-PNG-ים, וה-PNG-ים הם מה ש-iOS ואנדרואיד באמת קוראים. נוסף
`npm run icons`.

**למה אין כיתוב:** בגודל שבו האייקון נצפה באמת — בערך 60px במסך הבית — האותיות
נמרחות, ו-**iOS ממילא מדפיס את שם האפליקציה מתחת לאייקון**. השם הופיע פעמיים
ואחת מהשתיים לא הייתה קריאה. הסמל לבדו הוא מה ששורד בגודל הזה, ולכן הוא כל
האייקון.

**ממצא ששווה לזכור, גם אחרי שהכיתוב ירד:** בגרסה עם הטקסט הוא נחרט כ-`<path>`
ולא כ-`<text>` — sharp מרסטר דרך librsvg, שפותר פונטים דרך fontconfig,
ובמכונה שבה המשפחה חסרה הוא **מחליף בשקט או משמיט את הטקסט לגמרי**. כל טקסט
שייכנס לאייקון או לתמונה מיוצרת בעתיד חייב להיות outlines, אחרת הוא נראה אחרת
ב-CI מאשר מקומית.

**כשהכיתוב ירד נדרש איזון מחדש, לא רק מחיקה.** המטוס וקו המסלול היו ממוקמים
כדי לפנות מקום לטקסט מלמטה; בלעדיו נקודות הקו חתכו את הזנב של המטוס. הקו הורד
והמטוס הוקטן קלות כך שהזנב מנקה את קודקוד הקו — נקודות שחוצות מכחול לבן הן רעש
דווקא בגודל שבו האייקון צריך לעבוד הכי חזק.

**אימות:** כל שלושת הפלטים **נבדקו כתמונות** ולא רק בקוד היציאה של הסקריפט:
ה-`apple-touch-icon` הוא ריבוע מלא בלי עיגול עצמי, ה-maskable שומר על אזור
הבטיחות, והקומפוזיציה מאוזנת. נבדק גם ב-60px ו-120px על רקע כהה — הגודל האמיתי
במסך הבית — שם המטוס חד לגמרי והמקווקו נשאר פרט משני שקט.
כל ההפניות ב-`manifest.json` וב-`layout.tsx` ממשיכות להצביע לאותם שמות קבצים,
ולכן לא נדרש שינוי בהן. `type-check`, `lint` ו-`build` עוברים.

---

## 🎒 פאזה K — ציוד, מדריך במסך הבית, רספונסיביות, ושיתוף אמיתי

**ענף:** `feature/phase-k-gear-sharing-responsive` (מעל `main`, אחרי מיזוג פאזה G ב-PR #46).

חמש בקשות משתמש, בריצה אחת. שתי הכרעות נלקחו מראש: מסך הציוד נכנס כערך בתפריט
״עוד״ ולא כטאב שישי, ו-migrations 0015+0016 אושרו כמורצים.

### הטבלה

| # | מה | קובץ מרכזי |
| - | -- | ---------- |
| K1 | רשימת ציוד ידנית, עם קטגוריות, מדד התקדמות והצעות התחלה | `domain/gear.ts`, `components/gear-list.tsx`, migration 0017 |
| K2 | ״איך זה עובד״ במסך הבית ובדיאלוג טיול חדש | `components/workflow-summary.tsx` |
| K3 | תיקון החריגה האופקית + סריקה רוחבית | `components/booking-list.tsx`, `globals.css` |
| K4 | שיתוף עבר לכותרת של כל מסך טיול ולמסך משלו | `components/share-button.tsx`, `more/share/page.tsx` |
| K5 | גישה אמיתית ממכשיר אחר, עם צפייה או עריכה | `domain/membership.ts`, migration 0018, `app/invite/[token]` |

### K3 — הבאג לא היה מה שחשבתי, והמדידה היא זו שגילתה את זה

הדיווח היה ״כרטיס טיסה ארוך מדי מגדיל לי את כל העמוד במובייל״. התיקון הראשון
שכתבתי — `min-w-0` על הכותרת ו-`basis-0` על שני קצות הטיסה — **לא פתר את זה**,
ורק מדידה בדפדפן הראתה למה: `gridTemplateColumns` נשאר `744px` בתוך מסגרת של
320px. שני שורשים, ושניהם נחוצים:

1. **ל-`<li>` בגריד יש `min-width: auto`.** מסלול `1fr` הוא בפועל
   `minmax(auto, 1fr)`, וה-`auto` הזה רוצף את העמודה על min-content של הפריט. מחרוזת
   אחת בלי נקודת שבירה בכרטיס אחד קבעה את רוחב כל הטבלה.
2. **`break-words` לא מקטין min-content.** `overflow-wrap: break-word` מרשה
   שבירה בתוך מילה כדי למנוע חריגה, אבל התרומה למינימום נשארת רוחב המילה הארוכה
   ביותר. רק `overflow-wrap: anywhere` (ב-Tailwind: `wrap-anywhere`, קיים מ-4.1)
   מקטין אותה בפועל.

**אחרי:** העמודה 316px במסגרת 320px, אפס אלמנטים חורגים ב-320/375/414/768.

**הסריקה הרוחבית נעשתה כאיתור שיטתי ולא כניחוש:** נמצאו 18 שימושי `truncate`
בקוד; חמישה מהם היו על פריט flex בשורה בלי `min-w-0` (`day-pager`,
`itinerary`, `route-map`, `place-search` ×2) ועוד שלושה ב-`day-timeline`. חמישה
נוספים נבדקו ונמצאו **תקינים** — `ListRow`, `trip-list`, `bottom-nav`,
`more/page` ו-`day-timeline:240` הם כולם flex-col, שבו הרוחב הוא ציר הצלב ולכן
אין מינימום אוטומטי. `ListRow` הוא הפרימיטיב הנפוץ בקוד וטוב שלא נגעתי בו לחינם.

נוסף גם `html { overflow-x: clip }` כרשת ביטחון. **`clip` ולא `hidden`:**
`overflow-x: hidden` הופך את האלמנט ל-scroll container, וכזה בין `position: sticky`
לבין ה-scrollport שלו הורג את ה-sticky בשקט — והאפליקציה מסתמכת עליו בשלושה
מקומות לפחות.

### K5 — ההכרעה: מה זה ״לפי מייל או מספר טלפון״ כשאין תקציב

**זהות = אימייל. מספר טלפון = ערוץ מסירה.**

Supabase phone auth דורש ספק SMS בתשלום (Twilio / MessageBird / Vonage), וזה
מחוץ לכלל של הפרויקט. אבל גם *שליחת* המייל לא באמת חינמית: ה-mailer המובנה של
Supabase מוגבל לכמה הודעות בשעה ב-free tier. לכן האפליקציה **לא שולחת כלום** —
היא מייצרת קישור, והבעלים שולח אותו מוואטסאפ, מ-SMS או מהמייל של עצמו דרך
`wa.me` / `sms:` / `mailto:`. זה חינמי, וגם עדיף: הודעה ממספר שהנמען מכיר נפתחת,
מה שלא תמיד נכון על מייל טרנזקציוני מדומיין לא מזוהה.

`normalizePhone` הוא מה שהופך את זה לעובד — 050-1234567 ‏→ 972501234567, כולל
`+`, `00`, סוגריים ומקפים, ועם `null` על כל מה שלא יכול להיות מספר אמיתי.

**שלוש רמות גישה, ושלושתן שונות בכוונה** (הפירוט ב-`ARCHITECTURE.md`):
קישור אנונימי מצונזר (0015) · `viewer` מזוהה שרואה הכול · `editor` שגם כותב.
**חבר מזוהה רואה את הטיול לא מצונזר** — הצנזור קיים כי קישור אפשר להעביר לכל
אחד, וכאן הבעלים נקב בחשבון מסוים; מי שנוסע איתך צריך את כתובת המלון ואת מספר
האישור.

**למה SECURITY DEFINER ולא EXISTS בתוך המדיניות:** מדיניות על `trips` צריכה
להתייעץ עם `trip_members`, ומדיניות על `trip_members` צריכה להתייעץ עם `trips`
כדי לתת לבעלים לנהל את הרשימה — וזו רקורסיה אינסופית. היא מתפוצצת בזמן שאילתה
ולא בזמן ה-migration, כלומר **הייתה עולה לפרודקשן**. שלוש פונקציות
`SECURITY DEFINER` חותכות את המעגל, וכל שמונה טבלאות הטיול עוברות דרכן — כך שכלל
הגישה כתוב במקום אחד.

### חור אבטחה שנתפס בקריאה חוזרת של ה-migration שכתבתי

מדיניות ה-UPDATE על `trips` היא `using (can_edit_trip(id))` עם `WITH CHECK` תואם.
`WITH CHECK` נבדק מול השורה **החדשה**, אבל `can_edit_trip(id)` עונה דרך `select`
מ-`trips` — שבתוך אותה פקודה עדיין רואה את השורה הישנה. כלומר `editor` יכול היה
לשלוח `PATCH /trips?id=eq.<id> {"user_id": "<שלו>"}` ישר ל-REST API של Supabase,
הבדיקה תעבור על השורה שלפני העדכון, והוא יהפוך לבעלים של טיול של מישהו אחר.
שום דבר באפליקציה לא עושה את זה — **ובדיוק לכן זה לא היה נתפס**.

התיקון הוא טריגר `BEFORE UPDATE` ולא הרשאת עמודה: Supabase מעניק UPDATE ברמת
הטבלה, ו-PostgreSQL לא יכול לשלול עמודה בודדת מהענקה כזו — זה היה מחייב שלילת
UPDATE כולו והענקה מחדש עמודה-עמודה, שאז משמיטה בשקט כל עמודה שתתווסף בעתיד.

### מה נבדק, ואיך

Claude לא יכול להתחבר לאפליקציה, ולכן נבנה probe זמני (הדפוס מ-HANDOFF, נמחק
לפני הקומיט) שרינדר את הרכיבים במסגרות ברוחב קבוע. **מדידה ולא צילום מסך:**
חיפוש אלמנטים שה-`getBoundingClientRect` שלהם חורג מהמסגרת הוא תשובה מדויקת,
וגם עובד כשה-`overflow-x: clip` מסתיר את הבעיה מ-`scrollWidth`.

- **43 בדיקות של פונקציות טהורות, כולן עוברות:** `groupGear`, `gearProgress`
  (כולל שהרשימה הריקה **לא** נחשבת ״הכול ארוז״), `normalizeCategory`,
  `starterSuggestions`, `normalizePhone` (13 מקרים) ו-`safeNext` (13 מקרים).
- **אפס אלמנטים חורגים** ב-320/375/414/768 עבור `BookingList`, `GearList`,
  `MemberList` ו-`InviteForm`.
- **הגנת ה-open redirect אומתה בדף האמיתי:** ארבעה ערכים עוינים
  (`https://evil.example/login`, `//evil.example`, `/\evil.example`,
  `javascript:alert(1)`) — אף אחד לא הגיע ל-HTML.
- `/invite/<טוקן מעוות>` ו-`/invite/<טוקן תקין שלא קיים>` מחזירים **404 זהה**.
- `type-check`, `lint` ו-`build` עוברים; 29 routes.

**⚠️ מה שלא אומת בריצה אמיתית:** כל מסך שדורש התחברות, וכל זרימת ההזמנה
מקצה לקצה. ראו HANDOFF.


---

## 🔑 פאזה K2 — תיקוני פרודקשן ושחזור סיסמה

**ענף:** `fix/rls-grants-and-oauth-callback` (מעל `main` אחרי מיזוג פאזה K ב-PR #47).

### שני באגי פרודקשן שפאזה K הכניסה

**1. כל טבלת טיול החזירה שגיאה במקום ״אין שורות״.** ב-0018 סיימתי כל פונקציית
עזר ב-`revoke all ... from public, anon`, בנימוק שהן עונות על שאלה לגבי
`auth.uid()` ש-`anon` אין לו. **הנימוק שגוי, וזה נראה כמו הקשחה.**

מדיניות RLS מוערכת **בתפקיד שמריץ את השאילתה**. כלומר `anon` שבוחר מ-`trips`
מריץ את גוף המדיניות, שקורא ל-`can_view_trip()`. בלי EXECUTE, PostgreSQL לא
מחזיר `false` — הוא זורק `42501: permission denied for function` ומפיל את כל
השאילתה. **נמדד מול הפרודקשן:** שמונה טבלאות נכשלו (`trips`,
`suggested_destinations`, `itinerary_items`, `trip_bookings`, `trip_city_days`,
`trip_gear`, `trip_phrasebooks`, `trip_chat_messages`); `profiles`, שב-0018 לא
נגעתי, עבד. אחרי migration 0019 — שמונה מתוך שמונה חזרו ל-`[]`.

**הכלל:** כל פונקציה שמדיניות RLS מפנה אליה חייבת להיות ניתנת להרצה על ידי כל
תפקיד שמגיע לטבלה — כולל `anon` ו-`service_role`. הגבלת EXECUTE על פונקציה כזו
לא מצמצמת גישה; היא מהפכת ״אין שורות״ ל״השאילתה נכשלה״.

**2. התחברות עם Google החזירה את המשתמש לדף ההתחברות.** הוספתי `?next=` לכתובת
ה-`redirectTo` שנשלחת ל-Supabase. Supabase מאמת אותה מול רשימת ה-Redirect URLs
של הפרויקט, ושם רשומה הכתובת החשופה `/auth/callback` בלי wildcard — כתובת עם
query string לא מתאימה לה, ולכן Supabase נפל חזרה ל-Site URL. המסלול הזה לא
מחליף את הקוד לסשן, אז המשתמש חזר מנותק וכל מסך מוגן זרק אותו ל-`/login`.
**היעד עבר לקוקי httpOnly קצר-חיים**, שעוקף את הרשימה לגמרי וגם מוציא את יעד
ההפניה מכתובת ששני צדדים אחרים מתעדים בדרך.

### תקלה באבחון ששווה לזכור

הצהרתי בביטחון שהפרודקשן מריץ בילד ישן, על בסיס `grep 'overflow-x:clip'` בחבילת
ה-CSS שחזר ריק. **הבדיקה עצמה הייתה שבורה** — שרשרת `&&` שבה `echo` הצליח
והטעה, ומחרוזת שלא שרדה את המינימייזר. בדיקה חוזרת עם ארבעה סמנים
(`clip`, `overflow-wrap`, `anywhere`, `min-width:0`) הראתה שכל ארבעתם קיימים,
כלומר פאזה K כן הייתה בפרודקשן. **הלקח:** בדיקה שמחזירה ״לא נמצא״ צריכה לאמת
שהיא מסוגלת למצוא משהו לפני שמסיקים ממנה.

### שחזור וקביעת סיסמה — פער שהיה שם לפני פאזה K

לאפליקציה **לא היה שום מסלול איפוס סיסמה**. שתי השלכות: חשבון שנרשם דרך Google
אין לו סיסמה בכלל (Supabase לא יוצר סיסמה בהתחברות דרך ספק) ולכן לא יוכל אף פעם
להיכנס באימייל+סיסמה; ומי שנרשם בסיסמה ושכח אותה — **נעול לצמיתות**.

| מה | קובץ |
| -- | ---- |
| `/reset` — בקשת קישור | `components/reset-request-form.tsx` |
| `/reset/confirm` — קביעת הסיסמה, מחליף את הקוד לסשן בעצמו | `app/reset/confirm/page.tsx` |
| ״קביעת/שינוי סיסמה״ בפרופיל | `components/password-settings.tsx` |
| קישור ״שכחתם סיסמה״ בדף ההתחברות | `app/login/page.tsx` |

**⚠️ דורש הגדרה ב-Supabase:** `/reset/confirm` הוא ה-`redirectTo` של מייל
השחזור, ולכן חייב להיות ברשימת ה-Redirect URLs. בלעדיו Supabase מוריד את המשתמש
ב-Site URL בלי סשן.

**שתי הכרעות:**

- **הבקשה מדווחת אותה תוצאה בין אם הכתובת רשומה ובין אם לא.** ״אין משתמש כזה״
  היה הופך את הטופס לדרך לבדוק אם לאדם מסוים יש כאן חשבון. החריג היחיד הוא
  מגבלת הקצב, שהיא עובדה על הבקשה של הקורא עצמו ולא על חשבון של מישהו אחר.
- **`/reset/confirm` מחליף את הקוד בעצמו ולא עובר דרך `/auth/callback`.** אחרת
  היה צריך להגיד לקולבק ״זה שחזור״, והמקום היחיד לכך הוא query param — בדיוק מה
  ששבר את Google. קוקי לא יכול לשמש כאן: מייל נפתח לרוב במכשיר אחר מזה שביקש.

`hasPasswordIdentity()` נגזר ממערך ה-identities ולא מעמודה — Supabase לא חושף
״יש סיסמה״, אבל חשבון אימייל+סיסמה נושא תמיד identity עם provider `email`.


---

## 🔒 פאזה K3 — פרטיות, אישור מייל, וצמצום מסלולי הסיסמה

**ענף:** `feature/privacy-and-email-confirmation`.

### הוסר: קביעת סיסמה, ושינוי סיסמה מתוך האפליקציה

מסך ״קביעת/שינוי סיסמה״ שנבנה ב-K2 הוסר כולו, לבקשת המשתמש. **הסיסמה משתנה
עכשיו רק דרך הקישור שנשלח למייל.** זו לא רק הסרה של פיצ׳ר — היא משנה את מודל
האיום לטובה: כל שינוי סיסמה מאושר בהוכחת שליטה בתיבת המייל, ולכן **מי שהשתלט
על סשן פעיל לא יכול לשנות את הסיסמה ולנעול את הבעלים בחוץ.**

הוסרה גם האפשרות להוסיף סיסמה לחשבון Google. חשבון כזה נכנס דרך Google, ואישור
דרך מייל הוא ממילא מה שהמשתמש ביקש — הוספת אישור שני רק מרחיבה את מה שאפשר
לגנוב.

**איפה נחסמת בקשה של חשבון Google, ולמה שם:** ב-`setNewPassword`, כלומר **אחרי**
שהמשתמש הוכיח שליטה בתיבה. לא בטופס הבקשה — שם זו הייתה הופכת לאורקל: אפשר
להקליד כל כתובת ולגלות אם לאדם יש כאן חשבון ובאיזו שיטה הוא נכנס. בנקודה
שנבחרה, הקורא כבר יכול לראות הכול על החשבון שלו, ולכן ההודעה ״החשבון הזה נכנס
דרך Google״ לא חושפת דבר — והיא הדבר היחיד המועיל שאפשר לומר למי ששכח שנרשם כך.

### נוסף: דף פרטיות ואישור לפני הרשמה

| מה | קובץ |
| -- | ---- |
| `/privacy` — המדיניות עצמה | `app/privacy/page.tsx` |
| צ׳קבוקס חובה בהרשמה, עם ולידציה בצד השרת | `domain/schemas.ts`, `components/credentials-form.tsx` |
| גילוי מפורש מעל כפתור Google בהרשמה | `app/signup/page.tsx` |
| קישור בתחתית עמוד הנחיתה | `app/page.tsx` |

**המדיניות נכתבה מהקוד ולא מתבנית.** כל צד שלישי שמופיע בה הוא כזה שהאפליקציה
באמת קוראת אליו, אחרי מיפוי של כל ה-hosts בקוד: Supabase, Vercel, Gemini,
Nominatim/Overpass/אריחי OSM, ו-Open-Meteo. **מדיניות שמונה שירותים שלא בשימוש,
או משמיטה כאלה שכן, גרועה מכלום** — כי היא טענה ספציפית שקורה שהיא לא נכונה.
לכן גם נכתב בה במפורש מה **לא** נשמר (אמצעי תשלום, מיקום GPS, כלי אנליטיקה,
ומספרי הטלפון שמוקלדים בשיתוף).

**⚠️ כל שינוי בצדדים השלישיים ב-`src/lib/ai`, `src/app/api` או
`src/features/*/infrastructure` מחייב עדכון של הדף באותו commit.**

**הצ׳קבוקס נאכף בצד השרת.** `required` ב-HTML הוא נוחות למי שממלא, לא בקרה —
Server Action הוא endpoint של HTTP. `z.literal(true)` ולא `z.boolean()`, כדי
ש-`false` ייכשל ולא יעבור. **נבדק:** 12 בדיקות, כולל דחייה של `"on"` ושל `1`,
שהם truthy ועלולים לחמוק דרך בדיקה רשלנית, ווידוא שהשגיאה נתלית בשדה
`acceptedPrivacy` כך שהצ׳קבוקס יכול להציג אותה.

**מסלול Google לא רואה את הצ׳קבוקס** — הוא יוצא מהאתר לפני ששום טופס נשלח, ולכן
אין מה לאמת. שם האישור מוצג כתוצאה של הפעולה, הדפוס המקובל בהתחברות דרך ספק.
לחסום את הכפתור עצמו היה מחייב להוציא את הצ׳קבוקס מהטופס ולשתף את מצבו, וגם היה
חוסם משתמשים חוזרים שנכנסים ולא נרשמים.

### אישור מייל בהרשמה — הגדרה, לא קוד

**זו הגדרה בפרויקט Supabase** (Authentication → Providers → Email → Confirm
email), ולא משהו שהקוד קובע. הקוד מטפל בשני המצבים, ולכן הפעלה או כיבוי לא
דורשים דיפלוי. מה שנוסף: כשההרשמה מחזירה ״ממתין לאישור״, הטופס **מתחלף** במסך
״אמתו את כתובת המייל״ עם הכתובת שהוזנה, במקום להשאיר טופס שנראה כאילו לא עשה
כלום ולהזמין שליחה חוזרת שתיכשל ב״כבר רשום״.

**שני דברים שההפעלה גוררת, וכדאי לדעת עליהם מראש:**

- **מגבלת המיילים של ה-free tier** (כמה הודעות בשעה, לכל הפרויקט) חלה עכשיו גם
  על הרשמות וגם על איפוסי סיסמה. שני אנשים שנרשמים באותה דקה — השני עלול לא
  לקבל מייל. שתי ההודעות בממשק אומרות את זה במפורש.
- **זרימת ההזמנה נעשית ארוכה יותר:** מי שמוזמן צריך להירשם, לאמת מייל, ורק אז
  לפתוח את קישור ההזמנה מחדש.


---

## 📱 פאזה K4 — השדות שנשברו במובייל

**ענף:** `fix/mobile-inputs`.

הדיווח: ״יש עדיין INPUT שנשברים במובייל ולא רספונסיביים כמו שצריך״.

**המדידה בדפדפן דסקטופ החזירה אפס חריגות** — כלומר הבעיה מובייל-ספציפית, ולא
overflow רגיל. שני שורשים, שניהם נמדדו:

**1. iOS Safari מזום את כל העמוד כשנוגעים בשדה עם `font-size` מתחת ל-16px, ולא
חוזר לבד.** מאותו רגע העמוד באמת רחב מדי, סרגל התחתית מתנתק מהקצה, וכל הקשה
הבאה נופלת במקום הלא נכון. זה נראה בדיוק כמו ״השדות נשברים״.

לשדות לא היה `font-size` משל עצמם והם ירשו אותו, ולכן אלה שיושבים בטופס צפוף
(`text-sm` על ה-label העוטף) היו **14px**: `text`, `datetime-local`, `number`,
`select` ו-`textarea`. `email`, `password` ו-`date` יצא שיושבים במכלים שהשאירו
אותם ב-16px — **וזו הסיבה שהבאג נראה שרירותי.**

התיקון ב-`globals.css` תחת `@media (pointer: coarse)` ולא תחת breakpoint של
רוחב: **הטריגר הוא מגע ולא גודל מסך**, כך שאייפד ב-768px צריך את זה וחלון
דסקטופ ב-375px לא. הדסקטופ נשאר בדיוק על 14px כמו שעוצב.

**2. לכל השדות היה `min-width: auto`.** `w-full` הוא `width: 100%` ולא אומר כלום
על כמה קטן הקופסה יכולה להיות — פריט flex/grid נשאר רצוף על min-content שלו.
ל-`<input>` הרצפה היא הרוחב שנגזר מ-`size` (בערך 20 תווים), ול-`date` /
`datetime-local` היא רחבה יותר כי הקופסה צריכה להכיל ״dd/mm/yyyy --:--״ ועוד
אייקון בורר. שדה תאריך אחד יכול היה להחזיק מסלול גריד פתוח רחב מהטלפון.

`min-w-0` נוסף ל-`control` בפרימיטיב, ל-wrapper של `Select`, ול-`Field` עצמו —
כמה קריאות כבר העבירו `min-w-0` בעצמן, וזה סימן שהמקום שלו בפרימיטיב.

**המדידה:**

| | לפני | אחרי |
| - | ---- | ---- |
| `min-width` על שדות | `auto` | `0px` |
| שדות מתחת ל-16px במגע | 5 סוגים | **0** |
| font-size בדסקטופ | 14px | 14px (לא שונה) |
| חריגות ב-320/360/375/414 | 0 | 0 |
| חריגות ב-375 עם מגע, אחרי הגדלת הפונט | — | **0** |

נבדק על שישה טפסים אמיתיים (`TripDatesForm`, `BookingForm`, `InviteForm`,
`ManualPlaceForm`, והרשמה/התחברות) ועל חמשת הדפים הנגישים בלי התחברות.

**כל השדות הגולמיים בקוד הם checkbox/radio/hidden** ולכן מוחרגים נכון מהכלל —
נבדק, ולא הונח.


---

## 🗺️ פאזה K5 — הסיכות במקום הלא נכון: השורש, לא עוד תיקון במורד הזרם

**ענף:** `fix/mobile-inputs` (יחד עם תיקון השדות).

הדיווח: ״טאקיאמה, שנמצאת ביפן, מסומנת מחוץ ליפן. אי אפשר לאשר דבר כזה.״

### השורש היה שורה אחת, והוא הסביר את כל ההיסטוריה

```ts
const best = places.find((page) => page.title.includes(name)) ?? places[0];
```

ה-`?? places[0]`. הקוד **העדיף** כתבה שהכותרת שלה מזכירה את השם, אבל **נפל לתוצאה
המדורגת ראשונה בכל מקרה** — וה-API הוא `generator=search`, כלומר **חיפוש טקסט
חופשי ולא חיפוש מקומות**.

נמדד מול ה-API האמיתי. חיפוש ״טאקיאמה״ בוויקיפדיה העברית מחזיר:

1. ״פרס גראמי לאלבום השנה״ — בלי קואורדינטות
2. ״**איסהאיה**״ → 32.86, 130.08 — **עיר יפנית אחרת, 700 ק״מ משם** ← נבחרה
3. ״קמקורה״ → 35.32, 139.55 — עוד עיר אחרת

**והמקרה שממש מפחיד:** חיפוש ״טוקיו״ מדרג במקום שני את ״פאלה דה טוקיו״, שהוא
בניין **בפריז**. אם הכתבה של טוקיו הייתה חסרה, טוקיו הייתה ננעצת בצרפת.

זה מסביר למה פאזות F ו-G חזרו לנושא הזה שוב ושוב. גאוקודינג הפוך למדינה, ותיקון
אוטומטי של סיכות מחוץ למדינת הרוב — שניהם **תיקונים במורד הזרם** לגאוקודר שמחזיר
בביטחון את העיר הלא נכונה. הם יכולים לתפוס רק את המקרים שבמקרה נחתו בחו״ל;
איסהאיה היא ביפן, ולכן **שום מנגנון קיים לא היה תופס אותה**.

### התיקון: לאמת את התשובה, בשני המקורות

**1. ויקיפדיה — חובה שכותרת הכתבה תנקוב במקום שביקשנו.** אין fallback. אם אף
מועמד לא מתאים, זו החטאה. **החטאה היא התשובה הנכונה כאן** — אין סיכה זה כנה,
סיכה שגויה בביטחון גרועה יותר. ההשוואה מנרמלת ניקוד ופיסוק, ומקבלת גם התאמת
תחילית, כי ויקיפדיה מצרפת מפרק לכותרת: ״שירקאווה-גו וגוקאיאמה״ היא הכתבה של
״שירקאווה-גו״.

**2. Nominatim מותר עכשיו גם בלי הקשר, כי התשובה מסוננת.** האיסור הקודם נשען על
תצפית נכונה — ״האקונה״ לבד מחזיר משרד בלוס אנג׳לס — אבל `addresstype` מפריד את
זה בדיוק:

| שאילתה | addresstype | מדינה | |
| ------ | ----------- | ----- | - |
| טאקיאמה | `city` | jp | ✅ העיר |
| האקונה | `office` | us | ❌ נדחה |
| ניקו | `highway` | — | ❌ נדחה |

`province` ו-`state` נכללים ברשימה כי מחוזות-עיר ביפן ממופים כך — ״טוקיו״ היא
`addresstype=province` ב-OSM, והוצאתה הייתה דוחה את היעד הנפוץ ביותר בנתונים.

**וזה מה שפותר את טאקיאמה:** הכתבה ״טאקיאמה״ בוויקיפדיה העברית **קיימת אבל בלי
קואורדינטות**, כלומר מסלול ויקיפדיה יכול רק להחטיא אותה — ו-Nominatim עונה
36.1396, 137.2510, העיר הנכונה, מהשם העברי לבד.

### האימות

נבדק דרך `geocodePlace` עצמו מול ה-APIs האמיתיים, ולא דרך חיקוי ב-curl. הערכי
הייחוס נבדקו ביד:

| יעד | תוצאה | מרחק מהייחוס |
| --- | ----- | ------------ |
| טוקיו | 35.683, 139.767 | 0 ק״מ |
| אוסקה | 34.683, 135.500 | 1 ק״מ |
| קיוטו | 35.017, 135.767 | 1 ק״מ |
| **טאקיאמה** | **36.140, 137.251** | **0 ק״מ** |
| הירושימה | 34.385, 132.455 | 1 ק״מ |
| נארה | 34.683, 135.800 | 1 ק״מ |
| ניקו | 36.750, 139.617 | 8 ק״מ |
| שירקאווה-גו | 36.257, 136.906 | 0 ק״מ |
| האקונה | לא נפתר | ✅ כמצופה |

**9/9.** ״קנזאווה״ באיות הזה לא נפתרת באף אחד מהמקורות — וזו התוצאה הנכונה: היא
תישאר בלי סיכה במקום לקבל סיכה שגויה.

**טעות שלי בבדיקה, ששווה לתעד:** סימנתי את ״ניקו״ כ״אמורה לא להיפתר״ על בסיס
בדיקת curl שהראתה `addresstype=highway`. היא כן נפתרת — דרך ויקיפדיה עם התאמת
הכותרת — והתוצאה נכונה. הציפייה שלי הייתה שגויה, לא הקוד.

### ⚠️ קואורדינטות שגויות שכבר נשמרו לא מתקנות את עצמן

הן ב-cache על שורת ה-`overview` של כל עיר. **אחרי הדיפלוי צריך ללחוץ ״רענון
המיקומים״** במסך המפה (`route-map.tsx`, הכפתור ״סיכה במקום הלא נכון?״), שמנקה
`latitude`/`longitude`/`country` וגורם לגאוקודינג מחדש — עכשיו דרך הקוד המתוקן.


---

## 🧪 פאזה K6 — הכלל שעבר בבילד המקומי ולא הגיע לפרודקשן

**ענף:** `fix/ios-zoom-utility`.

תיקון הזום של iOS (K4) נכתב כ-`@media (pointer: coarse)` ידני ב-`globals.css`.
הוא **עבר בבילד מקומי ולא הגיע לחבילת ה-CSS שוורסל מגישה.**

### איך זה נתפס, ולמה כמעט לא נתפס

הבדיקה הראשונה שלי בפרודקשן חיפשה `pointer: *coarse` — ורגקס כזה **דורש רווח
אחד לפחות**, בעוד המינימייזר כותב `pointer:coarse` בלי רווח. הבדיקה חזרה ריקה
מסיבה שאין לה שום קשר לשאלה. זו **הפעם השנייה** בפרויקט הזה שבדיקה שלי החזירה
״לא נמצא״ בגלל שהיא לא הייתה מסוגלת למצוא, ואחרי הפעם הראשונה נכתב הכלל הבא
ב-HANDOFF — ולא יישמתי אותו:

> **בדיקה שמחזירה ״לא נמצא״ חייבת להוכיח שהיא מסוגלת למצוא משהו לפני שמסיקים
> ממנה.**

עם בקרה (`overflow-x`, שכן נמצא באותו קובץ) התמונה התבררה, ו-`diff` בין ה-CSS
המקומי לזה שבפרודקשן הראה בדיוק שורה אחת חסרה:

```
> @media (pointer:coarse){input:not([type=checkbox]):not([type=radio]),select,textarea{font-size:1rem}
```

### מה שנשלל, ולמה

לא ״הדיפלוי לא רץ״ ולא ״הקומיט לא מוזג״: **`min-w-0` מאותו קומיט בדיוק כן
נמצא** ב-HTML של הפרודקשן, כלומר הקומיט מדופלוי וה-HTML עדכני. גם `/privacy`
ו-הצ׳קבוקס חיים. רק שורת ה-CSS הזו חסרה.

**ההבדל שנמצא:** הבילד המקומי מייצר `static/chunks/*.css`, והפרודקשן מגיש
`static/immutable/chunks/*.css` — **שני pipelines שונים.** ה-at-rule הידני לא שרד
את זה של וורסל.

### התיקון: להשתמש במנגנון שהוכח שמגיע

`min-w-0`, שיושב באותה שורה בדיוק ב-`field.tsx`, **כן** הגיע לפרודקשן. כלומר
utility של Tailwind הוא המנגנון שעובד. הכלל הועבר ל-`pointer-coarse:text-base`
על ה-`control` בפרימיטיב.

**אומת בבילד production אמיתי** ולא בדב-סרבר:

```
@media (pointer:coarse){.pointer-coarse\:text-base{font-size:var(--text-base);...}}
```

ובריצה עם אמולציית מגע: `coarse: true`, שני השדות ב-`16px`, הקלאס נוכח על
שניהם, ו-`scrollWidth` שווה לרוחב הויופורט.

הכלל הידני ב-`globals.css` **נשאר** בכוונה, כרשת שנייה: הוא חל גם על `<input>`
שלא עובר דרך הפרימיטיב — למשל שדה שנוצר על ידי Leaflet. הוא לא מזיק בפרודקשן,
הוא פשוט לא לבד שם.


---

## 📐 פאזה K7 — סריקת רספונסיביות על כל האתר, במדידה

**ענף:** `fix/responsive-sweep`.

הדיווח: ״יש מלא מקומות או שה-INPUT שבור ויוצא מהמסגרת של המובייל או שיש
כרטיסיות עם טקסט ארוך שמאריך את כל הקומפוננטה.״

### איך זה נבדק

נבנה probe שמרנדר **31 רכיבים** עם נתונים עוינים בכוונה — משפט עברי ארוך,
טוקן אחד בלי נקודת שבירה, וכתובת URL מלאה — בתוך מסגרות ברוחב קבוע
(320/360/375/414), ובנוסף ברוחב מלא ב-430/768/1024/1440.

**שתי מדידות שונות, כי הן תופסות דברים שונים:**

1. **קופסה שבורחת מהמסגרת** — `getBoundingClientRect` מחוץ לגבול. זה ״מותח את
   הקומפוננטה״.
2. **תוכן רחב מהקופסה שלו** (`scrollWidth > clientWidth`) עם `overflow: visible`
   — זה ״נצבע בחוץ״. המדידה הראשונה לבדה החזירה **אפס** ופספסה את כל הבאגים.

**סוננו כלא-באגים:** `truncate` (שמשמעותו בדיוק ״התוכן רחב ונחתך״) ו-`<input>`
(שגולל בתוך עצמו). בלי הסינון הזה היו 48 ״ממצאים״ שרובם התנהגות תקינה.

**ולכל מדידה שלילית הורצה קנרית** — אלמנט ברוחב 900px שנשתל ונמדד — כדי להוכיח
שהגלאי מסוגל למצוא. זה הכלל שנכתב ב-K6 אחרי שהופר פעמיים; **הפעם הוא גם תפס
משהו:** הקנרית הראשונה שלי בדקה כיוון אחד בלבד, ובעמוד RTL בלוק רחב מדי בורח
שמאלה — כלומר הבדיקה עצמה הייתה עיוורת לחצי מהמקרים.

### הטעות בביקורת הסטטית שקדמה למדידה

grep על `truncate` בלי `min-w-0` החזיר שישה מקרים, ופסלתי חמישה מהם בנימוק
״הם בתוך `flex-col`, שם הרוחב הוא ציר הצלב ולכן אין מינימום אוטומטי״.

**הנימוק שגוי.** ילד של flex-col אכן נמתח לרוחב ההורה, אבל הוא **עדיין תורם את
ה-min-content שלו לרוחב המינימלי של ההורה** — ו-`truncate` הוא
`white-space: nowrap`, שה-min-content שלו הוא המחרוזת כולה. `TripList` הוא בדיוק
המקרה הזה: מסלול הגריד נמדד **978px בתוך מסגרת של 320px**. המדידה תפסה את מה
שהניתוח שלי פסל.

### מה תוקן

| רכיב | מה |
| ---- | -- |
| `ListRow` | `min-w-0` על שתי השורות המקוצרות. משמש בכעשרה מקומות — לכן התיקון בפרימיטיב |
| `SectionHeading` | `min-w-0` + `wrap-anywhere` על הכותרת ועל התיאור |
| `EmptyState` | `wrap-anywhere` על הכותרת והתיאור |
| `TripList` | `min-w-0` על ה-`li`, על ה-`Card` ועל הקישור המקוצר |
| `RouteHero`, `CountdownHero` | `min-w-0` + `wrap-anywhere` על שם הטיול; צ׳יפ עיר מוגבל ומקוצר |
| `SelectedList` | `min-w-0` על הגריד, על ילדיו ועל פריטי הרשימה |
| `Phrasebook` | `min-w-0` על פריטי הגריד, `wrap-anywhere` על ארבע שורות הביטוי |
| `day-timeline`, `more`, `bottom-nav` | `min-w-0` על שורות מקוצרות |

**צ׳יפ העיר קוצר ולא נשבר, בכוונה:** `flex-wrap` מעביר צ׳יפ שלם לשורה הבאה אבל
לא יכול לשבור אחד שרחב מהשורה. צ׳יפ שגדל לשלוש שורות שובר את הקצב שקידוד הצבע
מסתמך עליו, והשם המלא ממילא מופיע במפה ובלו״ז.

### באג פרודקשן שה-probe מצא, ולא חיפשתי אותו

```
ReferenceError: window is not defined
  at ShareTrip (share-trip.tsx:24)
```

`share-trip.tsx` הוא `"use client"`, אבל רכיב לקוח **עדיין מרונדר בשרת** ל-HTML
הראשוני — והביטוי `window.location.origin` יושב בגוף הרכיב ולא ב-event handler.

**זה לא נתפס בגלל ה-`token ?` שלפניו:** בלי קישור שיתוף הענף לא מוערך בכלל.
כלומר זה נשבר **בדיוק אצל מי שכבר יצר קישור** — האנשים שהמסך הזה נועד להם —
ונשבר בטעינה, לא בלחיצה.

התיקון: `src/lib/origin.ts` גוזר את ה-origin מכותרות הבקשה בשרת. זה שומר על
התכונה שההערה המקורית הגנה עליה — הקישור נכון ב-localhost, ב-preview ובפרודקשן
בלי להגדיר כלום — בלי להסתמך על כך שהרכיב לעולם לא ירונדר בשרת.
**אותו דפוס היה גם ב-`InviteForm`**, שם הוא במקרה בטוח כי הרכיב מרונדר רק אחרי
פעולת לקוח — אבל זו תכונה של הקריאה ולא של הרכיב, ולכן תוקן גם הוא.

### התוצאה

| מדידה | לפני | אחרי |
| ----- | ---- | ---- |
| קופסאות שבורחות (320–414) | 104 | **0** |
| תוכן רחב מהקופסה, `overflow: visible` | 11 שורשים | **0** |
| אלמנטים שקרסו לרוחב 0 (430/768/1024/1440) | — | **0** |
| רכיבים מכוסים | — | 31 |

**ארטיפקט של ה-probe ששווה לדעת עליו:** מסגרת של 320px בתוך ויופורט של 1440
מפעילה את `xl:grid-cols-3`, כלומר שלוש עמודות ברוחב טלפון, והתוכן קורס ל-0. זה
נראה כמו 38 באגים והוא לא — לכן נוספה מסגרת ברוחב מלא, שבה אפס.


---

## 🎨 פאזה K8 — הרקע השחור באייקון

**ענף:** `fix/responsive-sweep`.

הדיווח: ״הלוגו של האפליקציה — כשאני מוריד למסך הבית יש רקע שחור.״

### המדידה

`public/apple-touch-icon.png`: 1024×1024, 525KB, **65.4% מהפיקסלים לא אטומים**,
ארבע הפינות ב-`alpha=0`. **iOS מרכיב אייקון שקוף על שחור** — ולכן שני שליש
מהאריח היו שחורים.

`git log` על הקובץ הראה שהקומיט האחרון שנגע בו הוא `e635b12` בשם ״logo״, של
המשתמש. כלומר הוא החליף ביד קובץ שהסקריפט מייצר, וזה בדיוק מה שההערה בראש
`generate-icons.mjs` מזהירה מפניו. **לא דרסתי את הלוגו שלו** — הוא נשמר כמקור.

### שלושה לוגואים שונים בו-זמנית

כי רק קובץ אחד הוחלף: הלוגו החדש ב-iOS, המטוס מפאזה J ב-Android וב-PWA, וברירת
המחדל של `create-next-app` בלשונית הדפדפן — `src/app/favicon.ico` מ-14 ביולי,
מעולם לא הוחלפה. אימות ויזואלי של שלושת הקבצים הוא מה שחשף את זה; המדידה לבדה
הייתה מראה רק את השקיפות.

### מה נעשה

הסקריפט נכתב מחדש לגזור **את כל הסט מקובץ מקור אחד**, `assets/logo-source.png`:

- **הצבע נדגם ולא נקבע קשיח** (`#2292d1`, 50% מהפיקסלים האטומים) — כדי שמקור חדש
  לא ישאיר בשקט את הצבע הישן.
- **`apple-touch-icon`**: `trim()` מסיר את השוליים השקופים, `flatten()` ממלא את
  השקיפות בצבע הרקע — כך הפינות המעוגלות נעלמות לתוך הכחול ומתקבל ריבוע מלא
  ואטום, 180×180 כמו שמוצהר ב-`layout.tsx`.
- **`icon-192/512`**: נשארים מעוגלים עם שקיפות מחוץ לפינה — זה תקין לדפדפן.
- **`icon-maskable-512`**: רקע עד הקצה, איור ב-80% המרכזיים.
- **`favicon-32`**: חדש, אטום.
- **ה-SVG-ים נמחקו.** `icon.svg` היה **ראשון** ברשימה ב-`layout.tsx` ולכן הוא זה
  שהדפדפן בחר, והוא צייר את המטוס. המקור ראסטר עכשיו, ואין SVG שיהיה נכון.

### האימות, ומקום אחד שבו העין שיקרה

| קובץ | גודל | לא-אטום | דרישה |
| ---- | ---- | ------- | ----- |
| `apple-touch-icon.png` | 180×180, 15.5KB | **0.0%** | אטום ✅ |
| `favicon-32.png` | 32×32, 0.8KB | 0.0% | אטום ✅ |
| `icon-maskable-512.png` | 512×512, 79KB | 0.0% | אטום ✅ |
| `icon-192.png` | 192×192, 18KB | 5.1% | מעוגל ✅ |
| `icon-512.png` | 512×512, 117KB | 4.6% | מעוגל ✅ |

**נדמה היה לי שנשארו פינות כהות** אחרי התיקון, כשהסתכלתי על התמונה. דגימת RGB
הראתה `#2292d1` מדויק בארבע הפינות, והפיקסל הכהה ביותר בתמונה כולה הוא `#02619c`
— כחול מתוך האיור. **העין שגתה, המדידה לא.**

`apple-touch-icon` ירד מ-525KB ל-15.5KB. הוא היה 1024×1024 בעוד `layout.tsx`
מצהיר 180×180, כלומר כל אייפון הוריד חצי מגהבייט וכיווץ אותו בעצמו.


---

## 📍 פאזה K9 — יעדים שנעלמו מהמפה

**ענף:** `fix/unresolved-destinations`.

תיקון הגאוקודר (K5) גרם לעיר שלא נפתרת בביטחון לקבל `null` במקום סיכה שגויה.
נכון — אבל מהמסך זה נראה כמו היעלמות, וזה מה שהמשתמש דיווח.

המפה כבר הציגה ״לא הצלחנו למקם על המפה: קנזאווה״ — **משפט אפור בלי שום דרך
לתקן.** זה היה נסבל כשהגאוקודר ניחש, כי הוא כמעט אף פעם לא ויתר. מרגע שהוא מאמת
את התשובה, הוויתור נעשה נפוץ והמשפט הפך למבוי סתום.

**השאלה שנשאלת עכשיו היא ״איך קוראים לזה באנגלית״ ולא ״מה הקואורדינטות״.**
״קנזאווה״ לא מאונדקסת לא בוויקיפדיה ולא ב-OSM; ״Kanazawa״ נפתרת מיד. הסיבה היא
האיות, ולכן האיות הוא מה שמבקשים — שאלה שמטייל יכול לענות עליה. שם העיר בטיול
לא משתנה; נכתב רק המיקום, והמדינה מנוקה כדי שתיגזר מחדש מהקואורדינטות החדשות.

**נוסף גם למצב שבו אף עיר לא נפתרה** — שם המסך הציג ״הוסיפו יעדים״ למי שכבר יש
לו כמה, בלי מוצא.

**נבדק מול ה-APIs האמיתיים:** קנזאווה → ״Kanazawa״ 36.561,136.656 (0 ק״מ),
האקונה → ״Hakone״ 35.189,139.025 (9 ק״מ). שניהם לא נפתרים בעברית. ״Shirakawa-go״
עם המקף לא נפתר — אבל שירקאווה-גו **כן** נפתרת בעברית ולכן לא תגיע לרשימה הזו
מלכתחילה; מקרה בדיקה שבחרתי לא נכון.


---

## 🔬 פאזה K10 — רמוני תצוגה למסכים שמאחורי התחברות

**ענף:** `feature/preview-harness` (מעל `fix/unresolved-destinations`).

לאורך כל הפרויקט חזרה אותה שורה: ״נבנה, עבר type-check/lint/build, ולא הופעל אף
פעם״ — כי כל מסך משמעותי נמצא מאחורי התחברות, ואסור לי להזין סיסמה.

**`/preview`** פותר את זה בלי סשן ובלי סיסמה: 29 סצנות, כל אחת רכיב אמיתי במצב
אחד, עם נתונים קבועים. פיתוח בלבד — `notFound()` מחוץ ל-development, בבדיקה
שיושבת בכל entry point ולא רק ב-middleware.

**חלוקה לפי מצב ולא לפי רכיב.** זה לא סידור שרירותי: המצבים ששוברים פריסה הם
בדיוק אלה שקשה להגיע אליהם בחשבון אמיתי. טיול ריק, יום בלי פעילויות, עיר בלי
מיקום, רשימת חברים כפי שהיא נראית ל-`viewer` ולא לבעלים.

### הרמוני עדיף על התחברות אמיתית — לצורך הזה

בחשבון אמיתי צריך ליצור את הנתונים ביד בכל פעם, והם מה שיש ולא מה שצריך. כאן כל
מצב הוא URL אחד ודטרמיניסטי.

**מה שהוא לא מכסה, וזה מהותי:** מסלול הנתונים. RLS, Server Actions שכותבות, וכל
מה שתלוי בהרשאות — בלתי נראים. **סצנה מרונדרת; זה לא מוכיח שהכפתור עובד.**

### שני באגים שנמצאו בהרצה הראשונה

**`Banner` דחף את ״היום״ ל-510px בויופורט 375.** ה-children יושבים ב-`min-w-0`
אבל בלי יכולת לשבור מילה, וכמעט כל מה שבאנר אומר מצטט טקסט של משתמש או של ספק —
כותרת הזמנה, שם עיר, שגיאה מ-API. תוקן ב-`wrap-anywhere`; הוא בשימוש ב-15 מקומות.

**`SegmentedControl` חרג ב-18px ב-320px.** מספר הלשוניות הוא נתון: מסנן ההזמנות
מגדל לשונית לכל סוג שקיים בטיול, אז טיסה+רכבת+לינה = ארבע. עכשיו הוא גולל
אופקית עם `shrink-0` על הלשוניות, כי פקד מקוטע שנשבר לשתי שורות מפסיק להיקרא
כמסלול אחד.

**הסריקה הקודמת (K7) פספסה את שניהם** — הפיקסצ׳רים שלה הכילו שני סוגי הזמנה
בלבד, והפקד מרונדר רק מעל שני מקטעים. זו בדיוק הסיבה שרמוני קבוע עדיף על probe
חד-פעמי.

### המלכודת בבדיקה עצמה

הפילטר שמתעלם מאלמנטים חתוכים (אריחי Leaflet, כל ילד של `truncate`) טיפס עד
`<html>` — ושם יושב ה-`overflow-x: clip` שהוספנו ב-K3 כרשת ביטחון. **כל אלמנט
נחשב חתוך, והבדיקה השביתה את עצמה ודיווחה ״29/29 נקי״.**

הקנרית תפסה. הטיפוס נעצר עכשיו בשורש הסצנה, **ובאנר ההצלחה מרונדר רק כאשר
`detectorWorks`** — אי אפשר לקבל ״נקי״ בלי הוכחה שהגלאי חי. אחרי התיקון:
29 סצנות × 3 רוחבים, אפס חריגות, אפס שדות מתחת ל-16px.


---

## ✨ פאזה K11 — ״אור״: שכבת עיצוב חדשה, פרוסה אחת

**ענף:** `feature/aura-design` (מעל `main`, אחרי מיזוג #52 ו-#54).

הבקשה הייתה ״האתר מרגיש מיושן״. הוצגו חמישה כיוונים בקנבס עיצוב
([Artifact](https://claude.ai/code/artifact/8a11131a-118d-4a16-b0ac-92b4e58dc20f));
נבחר הכיוון החמישי, **״אור״**, ומיושם כפרוסה דקה — שכבת טוקנים מלאה, שני רכיבי
UI, ומסך אמיתי אחד — לפי כלל הברזל 5 (רוחב לפני עומק).

### מה החליט את הכיוון, ולא התחושה

שפות העיצוב החיות של 2026 — Liquid Glass של אפל ו-Material 3 Expressive של גוגל —
מסכימות על שלושה דברים שבהם MyTrip הייתה בצד ההפוך:

1. **הקְרוֹם מרחף מעל התוכן ולא ממסגר אותו.** התוכן מגיע מקצה לקצה ונוסע מתחת
   לשורת הניווט.
2. **תנועה היא פיזיקה, לא משך זמן.** גוגל החליף את מנוע האנימציה לקפיצים; צורות
   מתמרפות בין מצבים במקום שאייקון יתחלף.
3. **היררכיה מניגוד גדלים** — 72px ליד 12px — ולא מתיבות.

ושתי אזהרות שחוזרות בשני המקורות: **קריאוּת מעל טשטוש, ועומס GPU.** גרדיאנט זוהר
״מת מאחורי טבלאות, טפסים וקריאה ארוכה״, והכלל הוא **רגע זוהר אחד לכל מסך**.
MyTrip היא בעיקר רשימות וטפסים, ולכן הכיוון בנוי נגד הפיתוי: האור מופיע פעם אחת
מאחורי הגיבור ונגמר שם, וכל טקסט שצריך לקרוא יושב על לבן אטום.

### הרעיון: לכל טיול יש אור משלו

כל גיבור באפליקציה היה קודם תמונה מעל scrim אפור נייטרלי — כלומר **לכל טיול היה
בדיוק אותו צבע**. עכשיו יש שדה של אור צבוע, שנגזר מהיעדים.

`--*-aura` ב-`globals.css` הם המקבילות העמוקות של שש ה-`.tone-*` הקיימות, כך
שהאפליקציה ממשיכה לדבר בסט צבעים אחד.

**מה שהאור לא חולק עם `tone.ts` הוא כלל ההקצאה, וזה מכוון.** `cityToneMap` מקצה
לפי מקום בטיול — ולכן העיר הראשונה של **כל** טיול היא rose. זה נכון לתפקיד שלו
(להבדיל בין שתי תחנות במסלול) וחסר תועלת לתפקיד הזה. **נמדד ברשימת הטיולים: שלושה
טיולים, שלושה אריחים, כולם מובלים באותו rose.** חתימה שכל טיול חולק היא לא חתימה.

לכן ההיסט מגיע מ-hash של שמות היעדים. `tone.ts` נמנע מ-hash כדי ששתי תחנות סמוכות
לא יקבלו אותו צבע; **הנימוק לא חל כאן, כי שני טיולים ברשימה הם לא מסלול.** ה-hash
ממוין לפני החישוב, ולכן הגיבור (שמקבל ערים בסדר המסלול) והאריח (בסדר ההוספה)
מייצרים אור זהה לאותו טיול.

**טיול בלי יעדים מקבל בסיס עמוק בלי אור בכלל** — לא כתקלה: לטיול אין אור עד שיש
לו לאן ללכת.

### ההנחה שהקוד הפריך

הנחתי שאין תמונות ולכן האור מחליף אותן. **`src/lib/place-image.ts` קיים מזמן**
ומביא תמונות מוויקיפדיה בחינם. זה שיפר את הרעיון: אין תמונה → שדה האור לבד
(`solid`); יש תמונה → אותו שדה כשכבה צובעת מעליה (`wash`). מערכת אחת, שני מצבים.

### ארבעה באגים שנמצאו במדידה, לא בקריאה

**1. `transparent` הוא שקוף־שחור.** `radial-gradient(hue, transparent)` מתאדה
לכיוון שחור, ולכן שלושה גוונים יחד נהפכו לחום בוץ — בדיוק כשל ה-aurora המתועד.
הכרטיס נראה כתיבה כהה ולא כאור. התיקון: הפריחות מתאדות ל-`#000` עם
`mix-blend-mode: screen`. שחור הוא ה-identity של screen, ולכן אין פס כהה בכלל,
**וחפיפה בין שני גוונים מבהירה במקום לכסות** — זה מה שמאפשר לשלושה גוונים רוויים
לחלוק תיבה אחת.

**2. שכבת הקריאוּת מלמעלה ביטלה את הצביעה מעל תמונה.** screen מבהיר כהים ולא נוגע
בבהירים, ולכן מעל שמיים בהירים הוא לא עשה כלום. **התיקון נובע מאותה פיזיקה במקום
להילחם בה:** ב-`wash` השכבה יורדת **מתחת** לפריחות. היא מכהה את הפס התחתון,
הפריחות עושות screen מעליו — ושם הצבע מופיע — והתמונה שמעל נשארת כמעט נגועה.
כלל אחד, שתי תוצאות.

**3. `blur(42px)` על תיבה של 44px** פיזר את הפריחה עד שאריחי החתימה ברשימה יצאו
שחורים לגמרי. הטשטוש הוא prop עכשיו, כי ל-CSS אין blur באחוזים ורק הקורא יודע
מה גודלו. ובעקבות זה נוסף וריאנט שלישי, `chip`: **על אריח דקורטיבי אין טקסט,
ולכן אין לו צורך בשכבת קריאוּת בכלל** — היא רק עלתה באור.

**4. המספר ב-72px הצריך גובה.** נמדד: הכרטיס דרש 288px וקיבל 256, ולכן ״עד
ההמראה״ נחתך ב-12px. תוקן ב-`h-72` + `line-clamp-2` על השם. **הרוחב לא היה
המכשול** — שלוש ספרות ויחידה הן 165px בתוך 303px הפנויים.

### כלל שקיים ונשמר

`globals.css` אומר **״No letter-spacing anywhere — it damages Hebrew״**, והמוקאפים
שלי הפרו אותו: ריווח־אותיות גם על תוויות בעברית. בקוד `--tracking-latin` קיים
לסמל ולקודי שדה תעופה בלבד, וההיררכיה בעברית באה ממשקל, גודל וצבע.

### הבודק שצעק זאב

בדיקת הרספונסיביות דיווחה על ״שדה ב-14px״ ב-`booking-form` בשלושה רוחבים.
**נמדד:** עם אמולציית מגע כל שדה שאפשר להקליד בו חוזר ב-16px ומעלה — התיקון
מ-K4 עובד. בלעדיה כולם 14px, כי הכלל יושב מאחורי `@media (pointer: coarse)`
שלא יכול להתאים ב-iframe בדפדפן שולחני.

**זו הייתה false positive, וזה מסוכן יותר מבאג:** בדיקה שצועקת זאב מאבדת אמון,
והכשל האמיתי הבא ייקרא כאותו רעש. הבדיקה מודדת עכשיו את `pointer: coarse`
בעצמה, ואם היא לא רואה את הכלל היא **אומרת שלא בדקה אותו** במקום להיכשל.
באנר ההצלחה מדווח בדיוק על מה שנבדק.

### מה נמדד ומה לא

- 31 סצנות × 320/375/414: **אפס חריגות.** באמולציית מגע: גם אפס שדות מתחת ל-16px.
- הגיבור, האריחים ושלושת המצבים (עם תמונה / בלי תמונה / בלי יעדים) — נראו בעין
  בטלפון ובדסקטופ.
- **מה שלא נמדד:** יחס ניגודיות מספרי של טקסט לבן מעל **הרכב** של תמונה + שכבה +
  blend. זה תלוי בתמונה, ואין לי כלי לקרוא פיקסלים מהרכב הזה מכאן. מה שכן — השכבה
  ב-`wash` לא יורדת מ-20% אף פעם, וזה תוחם את המקרה הגרוע; ונבדק בעין על תצלום
  עיר בשמש, שהוא בקירוב התמונה הבהירה ביותר שהאפליקציה תקבל.
- **`getSelectedCitiesByTrip` לא הורץ מול דאטהבייס אמיתי** — הוא מאחורי התחברות.
  ההרכבה נבדקה בהרמוני עם פיקסצ׳רים.

### המחיר, בכנות

`backdrop-filter` הוא הדבר היחיד כאן שיכול להוריד פריימים במובייל חלש, וצריך למדוד
אותו על מכשיר אמיתי. **כרגע הוא עדיין לא בשימוש בפרוסה הזאת** — `Glass` נכתב
ומיוצא, והמסך הראשון שישתמש בו הוא שורת הניווט. כל השאר CSS טהור, אפס JavaScript.
הנייבי `#003b95` לא נזרק — הוא הבסיס העמוק של שדה האור.

### הצעד הבא, אם הכיוון נשמר

1. שורת הניווט כזכוכית מרחפת (`BottomNav`) — שם `Glass` מתחיל לעבוד, ושם צריך
   למדוד ביצועים.
2. להעביר היסט per-trip גם ל-`cityToneMap` עצמו, כדי שהאור והצ׳יפים יסכימו איזה
   גוון שייך לאיזו עיר. כרגע הם מסכימים על הטיול ולא על העיר.
3. שאר המסכים: תכנון, ביצוע, הזמנות, ציוד, מפה, פרופיל.

---

## 🎨 פאזה K12 — להצמיד את הקוד לעיצוב שאושר

**ענף:** `feature/aura-design` (המשך ישיר של K11).

K11 יישם את **רעיון** האור על המסך הקיים. המשתמש הצביע על הפער: הקנבס והקוד נראו
שונה, ובצדק. הפער נמדד בקבצים ולא הוערך מהזיכרון:

| | המוקאפ שאושר | K11 בקוד |
|---|---|---|
| צבעים | נבחרו ביד: `#e2622f`, `#7b3f7a`, `#1b2a6b`, `#1f8fa8` | נגזרו משש ה-tones: `#b5325f`, `#d9861f`, `#6f45c4`… |
| גוונים | 4 | 3 |
| סוג האור | אלפא, בלי blend | `mix-blend-mode: screen` |
| שורת טאבים | זכוכית מרחפת | `Glass` נכתב, לא בשימוש |
| הגיבור | מסך מעוצב מחדש, מקצה לקצה | ה-`CountdownHero` הקיים עם שכבת אור |

**הכשל בדיווח, לא רק בקוד:** ״פרוסה דקה״ הובנה כ*שכבת טוקנים + מסך אחד*, והבחירה
לגזור את הגוונים מה-tones הכפיפה את הפלטה שאושרה לפלטה שהייתה — בלי להציג את זה
כהחלטה. **את הפער הזה היה צריך לומר בדיווח.**

### התיקון: פלטות, לא טבעת של גוונים

`cityToneMap` מקצה **לפי מקום בטיול**, ולכן העיר הראשונה של כל טיול היא rose.
נמדד: שלושה טיולים, שלושה אריחים, כולם מובלים באותו rose. וגרוע מזה — השילושים
היו שרירותיים: ״יפן בסתיו״ קיבל **כחול + כתום + ורוד**. טבעת שנבנתה כדי להפריד שתי
תחנות במסלול לא אומרת דבר על אילו שלושה צבעים מסתדרים יחד.

המוקאפ מעולם לא עבד כך: לכל טיול בו היה **שילוש מתוכנן**. עכשיו יש שמונה שילושים
כאלה. `ember`, `grove` ו-`dusk` הם בדיוק הכרטיסים של יפן, איטליה ופראג מהמוקאפ;
`coral`, `lagoon`, `moss`, `azure` ו-`crimson` נוספו באותה רוח.

### ההתנגשות שנמדדה, ומה שסגר אותה

חמש פלטות ובחירה לפי hash נתנו **52% סיכוי** שמשתמש עם שלושה טיולים יראה שניים
באותו צבע — וזה קרה מיד בפיקסצ׳רים, ששני הטיולים שלהם קיבלו `ember`. גם הפיזור
היה עקום: 7 מ-24 על `lagoon`, 2 על `dusk` (הביטים התחתונים של djb2 חלשים).

**שני תיקונים:**

1. **מיקס avalanche** אחרי ה-hash. הפיזור על 200 טיולים: 16–33 לכל פלטה, מול 25
   אידיאלי.
2. **הקצאה ברמת הרשימה** — `assignTripAuras`. כל טיול מקבל את הפלטה שהוא מעדיף אם
   היא פנויה, ואם לא — מדלג סביב הגלגל.

**הסדר הוא לפי תאריך יצירה, וזה מה שמחזיק את היציבות.** טיול ותיק מוקצה תמיד לפני
חדש, ולכן יצירת טיול לא יכולה לשנות את הצבע של טיול שכבר קיים. שינוי שם, תאריך או
מיון — גם לא. רק שינוי היעדים של הטיול עצמו משנה את האור שלו, וזה נכון: האור **הוא**
היעדים.

**נמדד:** התנגשות ברשימה של 3 טיולים — 33.7% ל-hash לבד, **0% אחרי ההקצאה**.
יציבות: **0 מתוך 2000** טיולים קיימים שינו צבע כשנוסף טיול חדש.

מעל שמונה טיולים הגלגל מתאפס וחזרות מתחילות. מחזוריות עדיפה על להיגמר.

### `AuraHero` — המסך, לא רק הצבע

רכיב חדש, לפי המוקאפ ולא כשכבה על הקיים:

- **מקצה לקצה** — שוליים שליליים שמבטלים את ה-padding של `AppShell`, וקצה תחתון
  מעוגל בלבד.
- **72px מול 12px** — ניגוד גדלים, בלי תיבה.
- **צ׳יפים בזכוכית** במקום כפתורי לבן. **זה המקום הראשון ש-`Glass` באמת עובד בו.**
- **בלי תמונה, בכוונה.** המוקאפ הוא שדה אור. `getPlaceImage` ממשיך לשרת את מסך
  ״היום״ של הטיול, שם תצלום המקום הוא הנושא.
- ה-`className` להיסט העליון יושב **באתר הקריאה**, כי הרכיב לא צריך להכיר את
  ה-padding של מי שהציב אותו.

**המסך:** הגיבור ראשון, ואין לו יותר כותרת עמוד. קודם היו ״הטיולים שלי״ ואז ״הטיול
הקרוב״ ואז כרטיס — שלוש שכבות קְרוֹם כדי להציג טיול אחד.

### שורות הרשימה

`[אריח אור 44px] [שם / ״בטיול · 4 יעדים״] [צ׳יפ תאריך] [מחיקה]`. ה-`Badge`
וה-chevron יצאו: הפאזה קראה במילים ובפִּיל צבעוני על אותו תפקיד, וספירת היעדים
עונה על ״האם הטיול הזה בכלל מתוכנן״ — שאף דבר בשורה לא אמר.

**תאריך הסיום ירד מהרשימה בכוונה,** ובזה יש אובדן מידע: טווח + פאזה + ספירה הם
יותר ממה ששורה מחזיקה בלי לחתוך משהו. הטווח המלא בעמוד הטיול.

### חמישה באגים שנמצאו במדידה

1. **הגיבור יצא 520px** במקום 356px של המוקאפ — ערימת paddings בלי גובה מוגדר.
   `min-h-[22rem]` + `mt-auto`. אחרי: **383px**.
2. **״+1״ התהפך ל-״1+״** — בפסקה RTL הדפדפן שם את הסימן אחרי הספרה. עכשיו ״ועוד 1״.
3. **התאריך היה יתום** מתחת למספר. נקפל לתוך התווית: ״יוצאים בעוד · 24.9״.
4. **שתי כותרות שוות** במצב בלי תאריך: ״עוד לא נקבע תאריך״ ושם הטיול באותו גודל
   בדיוק, ואי אפשר היה לדעת מה השם. עכשיו **השם** הוא שגדל כשאין ספירה.
5. **ember ו-coral היו דומות מדי** בגודל אריח. הגוון המוביל נושא את הזהות, ולכן
   coral הועבר לוורוד־מגנטה.

### מלכודת בכלים, לא בקוד

שלוש הפלטות החדשות יצאו שחורות בדפדפן. הטוקנים היו בקובץ; `getPropertyValue` החזיר
`MISSING`. **Turbopack לא קלט עריכת CSS שנעשתה מחוץ לעורך** (כתיבה דרך node).
ריסטארט לשרת פתר. בדיקה במחשב ולא בעין היא מה שהבדיל בין ״הצבע לא טוב״ ל״הצבע לא
הגיע״.

### מה עוד לא נעשה

- **שורת ניווט זכוכית מרחפת.** `Glass` עובד עכשיו בצ׳יפים ובכפתור המפה, אבל הסרגל
  התחתון הוא עדיין מסגרת. זה גם המקום שבו צריך למדוד `backdrop-filter` על מכשיר
  אמיתי.
- **רדיוס הכרטיסים.** המוקאפ עובד ב-20px; `--radius-card` הוא 0.75rem, ו-v3 הקטין
  אותו מ-20px **בכוונה מתועדת** (״רדיוס 20px על רשימה צפופה קרא כרך ולא כמסודר״).
  לא שיניתי טוקן גלובלי נגד החלטה מתועדת בלי אישור. **זו שאלה למשתמש.**
- שאר המסכים.

---

## 🫧 פאזה K13 — ״חלק ונעים לעין״: הכיוון יוצא מהמסך הבודד

**ענף:** `feature/aura-design` (המשך של K11–K12).

הבקשה: ״מודרני, חלק, נעים לעין וקל לשימוש וברור״. שלושת השינויים למטה הם התשובה
לשתי השאלות שהושארו פתוחות ב-K12, כי ההנחיה הזאת עונה עליהן.

### 1. רדיוסים רכים — היפוך של החלטה מתועדת, לפי הוראה

| טוקן | היה | עכשיו |
|---|---|---|
| `--radius-control` | 0.5rem | 0.75rem |
| `--radius-card` | 0.75rem | 1.125rem |
| `--radius-tile` | 1rem | 1.25rem |
| `--radius-modal` | 1rem | 1.5rem |

**v3 הקטין את `card` מ-20px ל-12px בכוונה מתועדת:** ״רדיוס 20px על רשימה צפופה של
כרטיסים הוא מה שגרם למסכים הישנים להיקרא כרכים ולא כמסודרים״. **ההערה ההיא לא
הייתה שגויה, ולכן היא לא נמחקה — נכתב מתחתיה מה השתנה:**

- הרשימה הצפופה שעליה היא נכתבה לא קיימת יותר. שורת טיול מתחילה עכשיו באריח אור
  של 44px, שמעגן את השורה חזק בהרבה ממה שפינה חדה עשתה.
- המסך שקובע את הטון הוא הגיבור, שהוא פאנל של 28px מקצה לקצה. כרטיסים של 12px
  מתחתיו נקראים כמערכת אחרת וישנה, לא כאיפוק.

`control` נשאר יוצא הדופן ב-12px ולא 18: אלה כפתורים ושדות בגובה 32–44px, ורדיוס
שהוא כמחצית הגובה הופך אותם לגלולות — החלטה אחרת ורועשת יותר מזו.

### 2. ״חלק״ הוא לא רק פינות

`transition-colors` היה כל המשוב באפליקציה, וזו הסיבה שלחיצה נקראה שטוחה: המילוי
דהה ושום דבר אחר לא הודה שנגעו בו. חצי מהפקדים גם לא עשו transition על מה שהם
בכלל שינו — כרטיס התרומם מיד וכפתור דהה.

**שני טוקנים חדשים**, כדי שכל משטח באפליקציה יסכים על מהירות התגובה:

- `--duration-press: 150ms` — מהיר מספיק להיקרא כתשובה לאצבע ולא כאנימציה.
- `--duration-settle: 250ms` — לריחוף ולגובה, שהם שינוי מצב ולא תשובה.

`Button`, `IconButton` ו-`Chip` נלחצים פנימה (`active:scale-[0.97]`) על
`--ease-snap`. `active:` ולא `:hover:` — לטלפון אין ריחוף, וזה המשוב שחשוב שם.
`Card` בגרסה האינטראקטיבית מתרומם ומתיישב במקום להחליף צל בלי תנועה.

ה-guard הגלובלי של `prefers-reduced-motion` מכווץ כל משך באפליקציה ל-0.01ms, ולכן
אין כאן opt-out נפרד לתחזק.

### 3. הסרגל התחתון — זכוכית מרחפת

היה: רצועה מקצה לקצה עם גבול עליון ומילוי אטום. קְרוֹם שממסגר את התוכן וחותך את
המסך בקו קשה.

עכשיו: פאן זכוכית מעוגל, מוסט משלושת הקצוות, **והתוכן נוסע מתחתיו.** זה הרעיון
המבני היחיד ששתי שפות העיצוב של 2026 באמת מסכימות עליו, וזה המקום הראשון ש-`Glass`
עובד בו בסרגל.

**הלשונית הפעילה היא דיו ולא תכלת.** על זכוכית לגוון חיוור אין כמעט מה להישען
מולו, והלשונית הנבחרת הפסיקה להיות מובנת מאליה. דיו הוא הניגוד הגבוה ביותר שיש,
והוא גם אומר ״מצב״ ולא ״לחץ עליי״ — מה שהתכלת, צבע הפעולה בכל שאר האפליקציה, לא
אמר.

**שני דברים שצריך לדעת לפני שנוגעים בו:**

- `pointer-events-none` על ה-`<nav>` ו-`auto` רק על הפאן עצמו, כדי ש-12px הדף
  שמשני צדי סרגל מרחף לא יהיו רצועה מתה שבולעת נגיעות שיועדו לתוכן שמאחוריו.
- ה-padding התחתון של `AppShell` עלה מ-5rem ל-6rem. **נמדד ב-375px:** הסרגל בגובה
  80px והחלק העליון שלו יושב 92px מעל תחתית הויופורט, כלומר הוא צריך 5.75rem.
  ב-5rem השורה האחרונה של רשימה נשארה 12px מאחורי הזכוכית.

### סצנה חדשה: `bottom-nav`

לסרגל לא הייתה סצנה בכלל — וכך הוא עבר מ״רצועה עם גבול״ ל״פאן זכוכית מרחף״ בלי
שום דבר לבדוק אותו מולו. הסצנה מרנדרת אותו עם שמונה שורות מלל, כדי להראות תוכן
עובר מתחת לזכוכית ולחשוף מיד אם ה-padding מפסיק לפנות לו מקום.

### מה נמדד

**36 סצנות × 320/375/414 — אפס חריגות; באמולציית מגע גם אפס שדות מתחת ל-16px.**
זו הסריקה שחייבת לרוץ אחרי שינוי רדיוס גלובלי, והיא רצה.

הסרגל: גובה 80px, רדיוס 20px, `backdrop-filter: blur(24px) saturate(1.8)`,
`pointer-events` נכונים בשתי השכבות. טפסים והזמנות נבדקו בעין אחרי שינוי הרדיוס.

### מה לא נמדד

- **ביצועי `backdrop-filter` על מכשיר אמיתי.** עכשיו זה כבר לא תיאורטי: הסרגל
  קבוע במסך בכל עשרת מסכי הטיול, וזה בדיוק המקרה שיכול להוריד פריימים במובייל
  חלש. **צריך למדוד על הטלפון, לא להניח.**
- כל מה שמאחורי התחברות.

### מה עוד לא בכיוון

מסכי הטיול עצמם — ״היום״, ההזמנות, הציוד, המפה — עדיין בשפה הקודמת: תכלת ראשי,
אימוג׳י לזהות, מפרידים מקווקווים. הם עקביים בתוך עצמם, פשוט לא ״אור״. זה החלק הבא.

---

## 🧩 פאזה K14 — הכיוון על כל האפליקציה, דרך הפרימיטיבים

**ענף:** `feature/aura-design` (המשך של K11–K13).

״תמשיך תעשה את זה על הכל.״ העבודה נעשתה **דרך הפרימיטיבים המשותפים ולא מסך-מסך**,
כי פרימיטיב אחד מכפיל את עצמו על כל מסך בבת אחת, ומסך-מסך היה מייצר שוב את הבעיה
שפאזה D תיקנה: אותה כותרת בארבעה כתיבים שונים.

### השפה, כחוקים ולא כטעם

1. **מצב הוא דיו, פעולה היא תכלת.**
2. **היררכיה מניגוד גדלים** — תווית שקטה מעל תוכן, לא כותרת שמתחרה בו.
3. **תווית זהות יושבת באריח**, לא כגליף בודד.
4. **קְרוֹם שקט** — מקווקו קורא כלא־גמור.

### מה השתנה

**`SegmentedControl`** — המקטע הנבחר הוא דיו ולא פיל לבן על מסלול אפור. הזוג
לבן/`surface-2` הוא 1.16:1 — מספיק לראות קצה של כרטיס, לא מספיק כדי שמסנן נבחר
יהיה מובן מאליו, וזה התפקיד היחיד של הפקד הזה. עכשיו זה גם נראה כמו הסרגל התחתון,
כך ש״נבחר״ נראה אותו דבר בכל האפליקציה.

**`SectionHeading` — `sub` הפסיק להיות כותרת קטנה והפך לתווית.** ב-`text-base
font-semibold` הוא היה צעד אחד מגוף הטקסט שמתחתיו, ובמסך עם שלוש כאלה התוויות
התחרו בתוכן שהן מתייגות. כ-caption במיוט הוא יוצא מהדרך, וההיררכיה באה מפער הגדלים
— אותו מהלך שהגיבור עושה עם 72px מול 12px, בקצה השני של הסקאלה.

**`EmptyState`** — המסגרת המקווקוות הוסרה. מקווקו הוא הקונבנציה ל״משהו אמור להיות
כאן וחסר״ (drop target, placeholder), ומצב ריק הוא לא תקלה אלא **המצב הראשון
הנורמלי של מסך אמיתי.** מצויר כתקלה, הוא גרם לחשבון חדש להיראות כתקלה.

**`Glyph` — פרימיטיב חדש.** `CLAUDE.md` מחייב אימוג'י לזהות דומיין וזו החלטה טובה
שלא שונתה; מה שחסר היה **מכל**. כתוב חשוף, כל אימוג'י רונדר לפי המטריקות של עצמו:
אותה שורה נשאה ✈️ ב-20px ו-🏨 ב-17px, הם ישבו על בסיסים שונים, ואף שתי שורות ברשימה
לא התחילו באותו x. **הקופסה היא מה שהופך רשימה לרשימה.**

מיושם ב-`up-next`, `night-stay`, `booking-list` ובכותרות הקטגוריות של הציוד.
ב-`up-next` האריח נושא את צבע העיר, כך שבשש שורות העיר קריאה לפני כל הטקסט.

**`Banner`** — פינה רכה יותר ונשימה.

### שני תיקוני בהירות אמיתיים, לא קוסמטיים

**שני סימנים לאותו מידע ב-`up-next`.** האריח הצבוע והנקודה הצבועה אמרו את אותו
דבר. הנקודה הוסרה.

**הבאדג׳ דחס את הכותרת.** כ-`trailing` הוא היה `shrink-0` והכותרת לא, אז ״צ׳ק-אין
היום, בעוד 6 שעות״ — 26 תווים — לקח את רוחבו המלא ב-375px וקיצר את שם המלון לתשעה
תווים. **השם הוא הדבר שמזוהה; הזמן הוא מה שנאמר עליו.** הבאדג׳ עבר מתחת לכותרת.

**התכלת חזרה להיות צבע פעולה.** `day-timeline` צייר שורות הזמנה ואת סמני הציר
ב-`bg-action-tint` — צבע ה״לחץ עליי״ של האפליקציה — על מידע שאי אפשר ללחוץ עליו.
נסרקו כל 11 הקבצים שמשתמשים בגוון הזה; תשעה הם פעולות או סמלי זהות ונשארו, ושני
המקומות האלה תוקנו לנייטרלי.

### מה נמדד

**36 סצנות × 320/375/414 — אפס חריגות; באמולציית מגע גם אפס שדות מתחת ל-16px.**
נבדקו בעין: ההזמנות, ״מה מתקרב״, המצב הריק, הטפסים, הסרגל.

### מה עוד לא נגעתי בו, ביודעין

`place-search`, `city-guide`, `explore` ופאנלי המפה — רכיבים גדולים (300–450 שורות)
שהשינוי בהם הוא ריסטרוקטורה ולא החלפת מחרוזות. הם מקבלים את הרדיוסים, המשוב, הפקדים
והפרימיטיבים החדשים אוטומטית, אז הם **לא יוצאי דופן** — הם פשוט לא עוצבו מחדש.

---

## 🌅 פאזה K15 — הפס: הכיוון נכנס לתוך הטיול

**ענף:** `feature/aura-design` (המשך של K11–K14).

**המשתמש אמר: ״אני באמת לא רואה שום שינוי מהותי מבחינה עיצובית, חוץ מהעמוד הראשי.״
והוא צדק.** K11–K14 שינו את **השלדה** — טוקנים, רדיוסים, משוב מגע, פרימיטיבים — ולא
את המסכים. האור, שהוא כל הזהות של הכיוון, היה קיים בדיוק במסך אחד. עשרת המסכים
שבתוך הטיול נראו זהים לגרסה הקודמת.

**הכשל הוא בשיפוט שלי, לא בביצוע:** בחרתי לעבוד דרך הפרימיטיבים כי זה נכון
הנדסית — פרימיטיב אחד מכפיל את עצמו על כל מסך — אבל שינוי בפרימיטיב הוא **בלתי
נראה כמעט לגמרי** למי שמסתכל על האפליקציה. עבודה נכונה שלא נראית היא לא עבודה
שהסתיימה.

### נקודת המנוף האמיתית

`src/app/trips/[id]/(tabs)/layout.tsx` הוא המקום היחיד באפליקציה שבו שינוי אחד מגיע
לעשרה מסכים: היום, ימים, מה עושים, מפה, ושישה מסכים תחת ״עוד״. שם נכנס **`TripAuraBand`**.

**כל מסך בתוך טיול נפתח עכשיו בתמונה של היעד, צבועה באור של הטיול,** עם הספירה
לאחור או מספר היום ב-32px מול תווית של 12px.

### זה קְרוֹם, לא גיבור — וההבדל מכוון

- 11rem ולא 22rem של הגיבור הביתי, כי הוא יושב **מעל** התוכן של המסך ולא **הוא**
  התוכן. נמדד: 180px, 22% מהמסך ב-375px.
- הכותרת היא `text-display` ולא `text-mega`, מאותה סיבה. ניגוד הגדלים מול ה-12px
  נשאר המנגנון, צעד אחד למטה.
- **בלי כפתורים.** הפעולות של מסך שייכות למסך, והסרגל הדביק שמעל כבר מחזיק חזרה,
  שיתוף ופאזה.

**כותרת אחת לפי מקום הטיול בחיים של עצמו:** לפני היציאה — כמה ימים נשארו, שזה הדבר
היחיד שבשבילו פותחים את האפליקציה; בזמן הטיול — איזה יום; אחרי — השם, כי זה כל מה
שנשאר לומר.

### הכפילות שהפס יצר, ושתי מחיקות בעקבותיה

הפס יוצר בדיוק את הבעיה שהוא בא לפתור אם משאירים את מה שהיה:

**`CountdownHero` במסך ״היום״** — שני פאנלים כהים זה על זה, שניהם עם שם הטיול ואותה
תמונה. הוסר מהמסך.

**`RouteHero` במסך המפה** — אותו דבר בדיוק. הוסר מ-`route-map-panel`, וזה גם חוסך
קריאת `getPlaceImage` מהטאב, כי הפס כבר עשה אותה.

**ואז שני הרכיבים נשארו בשימוש רק בהרמוני** — כלומר קוד מת בפרודקשן. שניהם נמחקו,
עם שלוש הסצנות שלהם. **סצנה שמציגה מסך שלא קיים גרועה מחוסר כיסוי:** היא מדווחת
״נקי״ על משהו שאף אחד לא רואה.

ה-skeleton של הטעינה ב״היום״ שיקף את `CountdownHero` — כלומר גובה של גיבור שלא
מרונדר יותר. הוא מודל עכשיו את הפס: 11rem ומקצה לקצה, כי בלוק מרופד של 11rem היה
מזנק גם הצידה כשהאמיתי מגיע.

### שלוש סצנות חדשות לפס

הפס יושב בלייאאוט, ולכן **טעות בו היא טעות בעשרה מסכים בבת אחת** — זו הסיבה שיש לו
כיסוי. שלוש הסצנות הן שלוש הכותרות שהוא יכול לשאת: לפני, בזמן, ובלי תמונה/תאריך/יעדים.

### למה הלייאאוט מריץ `listTrips` ו-`getSelectedCitiesByTrip`

זה נראה יותר ממה שהפס צריך, וזה מכוון. **אור של טיול מוקצה על פני כל הרשימה**
(`domain/aura.ts`) כדי ששני טיולים לא יחלקו פלטה, ולכן הדרך היחידה שהפס יראה את אותו
צבע כמו האריח של אותו טיול במסך הבית היא **להריץ את אותה הקצאה.** גזירה מהערים של
הטיול לבד הייתה נותנת פלטה אחרת בכל פעם שהדה-קונפליקט הזיז אותו — וטיול אחד בשני
צבעים הוא הבאג שמיון ה-hash נולד כדי להרוג.

הקריאות רוכבות על לייאאוט שכבר ממתין לשאילתות, ו-App Router **לא** מרנדר לייאאוט
מחדש במעבר בין הילדים שלו — אז זו קריאה אחת לטיול ולא אחת למעבר טאב.

### מה נמדד

**35 סצנות × 320/375/414 — אפס חריגות; באמולציית מגע גם אפס שדות מתחת ל-16px.**
הפס: 180px, מקצה לקצה (375 מתוך 375), כותרת 32px, בלי גלישה אופקית. `build` עובר.

### מה לא נמדד

- **`backdrop-filter` על מכשיר אמיתי** — הסרגל התחתון קבוע בכל עשרת המסכים.
- הכול מאחורי התחברות: הפס עצמו על נתונים אמיתיים, וההקצאה מול דאטהבייס.

---

## 🖼️ פאזה L — אחד לאחד עם העיצוב

שלושה מסמכי עיצוב (מסמך ההעברה, המסכים בטלפון, הדסקטופ) הגדירו שמונה משימות,
T0–T7. **T0 חוסמת את כולן:** כל דף יושב בתוך המסגרת, וכל עוד היא לא נכונה אין
טעם לתקן דף.

### T0 — המסגרת בדסקטופ ✅

**ענף:** `fix/desktop-frame`. שני קומיטים.

**מה היה שבור.** `AppShell` עטף גם את הרַיל וגם את התוכן ב-`mx-auto max-w-shell`,
כך שברוחב 1911px נוצרו ~230px לבנים משני צדי האפליקציה והרַיל צף באמצע שלהם.

**מה נעשה.**

- [x] **הרַיל נעוץ בקצה החלון.** הוא אח של הקופסה הממורכזת ולא ילד שלה: 248px,
      גובה מלא, צמוד לקצה, ורק טור התוכן מוגבל וממורכז במה שנשאר.
- [x] **הכותרת נכנסה לתוך טור התוכן.** בעיצוב הרַיל הוא הגבוה מהשניים ונושא את
      `MYTRIP` מעל מחליף הטיולים, והכותרת שייכת לטיול — שם, שלב, שיתוף. הלוגו
      יצא מ-`AppHeader`, ותג השלב עבר מקצה הסרגל לצמוד לשם שהוא מתאר. `brand`
      משאיר את הלוגו בשני המסכים שאין להם רַיל (`/profile`, עמוד העיר); הוא
      יורד מ-`/profile` ב-T6.
- [x] **הרוחבים הם של המוקאפ, מדודים:** רַיל 248, טור ראשי 660, חלונית 372,
      אזור תוכן 1180 של תוכן ממש. `--container-shell` נמחק עם המסגרת שהוא
      שייך לה.
- [x] **פס האור מגיע לקצה אזור התוכן.**

**שתי מלכודות שנמצאו במדידה ולא בקריאה:**

1. `var(--container-pane)` בתוך `grid-template` **לא מתפרש**. טוקני ה-container
   יושבים ב-`@theme inline`, כלומר Tailwind מזריק את הערך לתוך ה-utility ולא
   מפיץ אותו כמשתנה CSS. תבנית הגריד כתובה במספר מפורש.
2. תקרת 660px חייבת להיות מגודרת ל-`xl` — הרוחב שבו החלונית מופיעה. בלי הגידור,
   ב-768 היא השאירה 45px מתים בקצה כל שורה, מה שנקרא כהזחה מקרית.

**המידה עברה לטקסט** — ההחלטה וההנמקה ביומן ההחלטות, 2026-09-01. `--container-measure`
הוחל על תיאור `SectionHeading` (שם יושב רוב הטקסט המסביר באפליקציה, במקום אחד),
על ה-intro ועל ״איך מגיעים״ במדריך העיר, על גוף שלב ב-`WorkflowGuide` ועל הערה
בפריט לו״ז. `EmptyState` כבר חסם את שלו ב-`max-w-sm`. מחרוזות סטטוס קצרות נשארו
כמו שהן, ושאר הסבב שייך ל-T7.

### סצנת ההרמס הראשונה שמרנדרת את המסגרת

כל סצנה אחרת בהרמס היא רכיב שנמדד לבד, ולכן **אף אחת מהן לא הייתה יכולה לתפוס
את זה** — 230px הלבנים היו במסגרת, ואף סצנה לא רנדרה רַיל וטור תוכן יחד. נוסף
`bleed: true` לסצנה, שמוציא אותה מהטור המרופד של ההרמס ומעמיד אותה מול החלון:
רַיל שצריך לגעת בקצה החלון אי אפשר לשפוט בתוך קופסה עם 16px ריפוד.

### מה נמדד

| רוחב | רַיל | אזור התוכן | טור ראשי | חלונית | גלישה אופקית |
| --- | --- | --- | --- | --- | --- |
| 1920 | 1672→1920 | 214→1458, ממורכז | 660 | 372 | אין |
| 1440 | 1192→1440 | 0→1192 | 660 | 372 | אין |
| 1280 | 1032→1280 | 0→1032 | 572 | 372 | אין |
| 768 | מוסתר, שורת גלולות | 0→753 | 705 (ממלא) | בזרימה | אין |
| 375 | מוסתר, סרגל צף | 0→375 | 343 | בזרימה | אין |

אורך שורה בפסקה בטור הראשי ב-1920: 65–73 תווים. `type-check`, `lint` ו-`build`
עוברים; 48 מסלולי `/preview` מחזירים 200 ברינדור שרת.

### מה לא נמדד

- **המסכים האמיתיים מאחורי התחברות.** המסגרת נבדקה דרך סצנת ההרמס, שהיא
  `AppShell` אמיתי עם רַיל, כותרת, פס ושתי חלוניות — אבל לא דרך
  `/trips/[id]/today` עם נתוני אמת.
- **האווטאר בכותרת.** במוקאפ יש אווטאר גם בכותרת וגם בַרַיל. נוסף רק לרַיל: בכותרת
  אין מאחוריו תפריט, ועיגול לא־פעיל הוא קישוט.
- **גובה הכותרת 60px.** במסמך הדסקטופ כתוב 60, בקוד `h-14` (56). נשאר 56 — הוא
  לא ברשימת השינויים של T0, ושמונה מקומות בקוד מקזזים sticky בדיוק מולו.

### T1 — היום ✅

**ענף:** `feature/today-before-departure`. קומיט אחד.

**מה היה שבור.** ברוחב 1911px הטור הראשי היה **817px של כלום.** `today/page.tsx`
מרנדר את `NowCard` רק כשיש יום נוכחי ואת `DayPager` רק בשלב `during`/`after` —
ובטיול שיוצא בעוד 62 יום שניהם שקריים, אז הטור רינדר `<h1 class="sr-only">` ותו
לא, וכל מה שהמסך כן החזיק ישב בחלונית של 372px לצידו.

**שני המצבים הם לא מסך אחד עם חלקים חסרים,** ולכן העמוד מתפצל.

- [x] **״לפני היציאה״: הטור נושא מה שכן קיים** — מה קרוב, ומה עוד פתוח.
      ״מה קרוב״ עבר מהחלונית לטור.
- [x] **החלונית נושאת תחזית והוצאות, או נעלמת.** התחזית מוכרעת בעמוד ולא ברכיב:
      ״האם קיימת תחזית לתאריכים האלה״ עונה בלי קריאת רשת, ורינדור הפאנל תמיד היה
      שם ״התחזית עוד לא קיימת״ בחלונית של כל טיול שרחוק יותר מ-16 יום — כרטיס
      שכל תוכנו הוא שאין לו תוכן.
- [x] **״בטיול״ לא נגעתי.** כרטיס ״עכשיו״ → שלושת הנתונים → רצועת הימים →
      הרשימה. היה בנוי, ונשאר.

### ״עדיין פתוח״ — פונקציית דומיין, לא JSX

הסדר **הוא** הטיעון, ולכן הוא בדומיין: תאריכים קודם (בלעדיהם אין מה לחשב ואין על
מה לפעול), אז יעדים, אז הלו״ז, אז לינה והגעה. `tripOpenItems` מחזירה שורות
שהרכיב מצייר בצורה שהמוקאפ מצייר — נקודה, משפט, שורת פירוט — וכל שורה היא קישור
למסך שבו התיקון קורה.

**הדחיפות נגזרת ולא קבועה לפי סוג.** לינה חסרה היא מידע שימושי חודשיים מראש
ודחופה שבועיים מראש, ולכן היא נצבעת בצבע האזהרה בתוך שלושה שבועות מהיציאה — המספר
שהעתק של שלב ההזמנות כבר משתמש בו לרכבות.

**מה לא מחושב בכוונה:** ״איזו רגל לא הוזמנה״. לאפליקציה אין מושג של טיסה
מתוכננת-אך-לא-מוזמנת, והמצאה של אחת מהמסלול הייתה מסמנת טיול שמישהו נוסע בו
באוטו. במקומה שורה אחת כשאין שום טיסה ושום רכבת.

**כשאין כלום פתוח** מרונדר ״הכול מסודר״ ולא כלום. במסך הזה החלופה למצוב היא
בדיוק הטור הריק שהמשימה באה לתקן.

### שני שינויים תומכים

1. **`TwoPane` — החלונית אופציונלית,** ובלעדיה הוא מתקפל לטור אחד **ממורכז באזור
   התוכן.** צמוד לקצה ההתחלה, המידה של 660px הייתה יושבת על הרַיל עם 520px לבנים
   לצידה ב-1920 — אותו חלל א-סימטרי, רמה אחת פנימה.
2. **הרכבת מצב ״לפני״ יושבת ברכיב פיצ׳ר ולא בעמוד.** ההרמס לא יכול לרנדר את
   העמוד (העמוד קורא מהדאטהבייס), ולכן הרכבה שנשארת בעמוד היא הרכבה שאף סצנה לא
   בודקת — וזאת בדיוק הפריסה שאף אחד לא שם לב אליה.

### מה נמדד

| סצנה | 1920 | 375 |
| --- | --- | --- |
| `today-before` (62 יום, יש עלויות) | טור 660, חלונית 372, בלי גלישה | בלי גלישה, אפס אלמנטים חורגים |
| `today-before-bare` (טיול חדש) | טור יחיד 660 ממורכז: 506→1166 באזור תוכן 214→1458 | בלי גלישה |
| `app-frame` (בטיול) | ללא רגרסיה: רַיל 248 בקצה, טור 660 | — |

51 סצנות `/preview` מחזירות 200 ברינדור שרת. `type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד

- **המסך האמיתי עם נתוני אמת.** הפיצול לפי שלב נבדק דרך `TodayBefore` בהרמס, לא
  דרך `/trips/[id]/today` מול דאטהבייס.
- **התחזית בחלונית.** נבדקה רק בענף ה״אין תחזית״ (רחוק מ-16 יום). `WeatherPanel`
  יורד לרשת ואין לו סצנה במצב ״לפני״.
- **״מה עוד לא הוזמן״ מול לו״ז אמיתי.** הלינה נבדקת לפי `booking.city`, שהוא
  טקסט חופשי — עיר שנכתבה בהזמנה אחרת מאיך שהיא בערי המסלול תיראה כלא מוזמנת.

### T2 — ימים ✅

**מה היה שבור.** המסך רינדר את כל ארבעה-עשר הימים ברצף, עם אינדקס ימים דביק לצידם
ב-`lg`. **אינדקס לתוך רשימה הוא מה שצריך כשהרשימה עצמה היא הבעיה.** הרצועה
האופקית — מודל האינטראקציה שהעיצוב מצייר — הייתה קיימת בקוד במסך ״היום״ ורק בו.

- [x] **`DayStrip` חולץ מ-`DayPager`** ומשמש עכשיו את שני המסכים. זה היה החלק
      היחיד ממודל ״יום אחד בכל פעם״ שכבר היה בנוי, והוא היה בנוי במסך אחד בדיוק.
- [x] **המסך נפתח על יום אחד:** רצועה, ואז כותרת `יום 3 · טוקיו` עם נקודת צבע
      ותאריך — בדיוק הצורה של המוקאפ.
- [x] **החלונית נושאת את מה שיום בודד לא יכול לענות עליו:** לוח החודש, הערים
      במסלול, והימים שעוד ריקים. שלושתם בוררי יום — לחיצה על תא בלוח או על שורת
      עיר מחליפה את היום שעל המסך.
- [x] **הבקרות שמדברות על הלו״ז כולו עברו לחלונית** — ״כמה ימים בכל עיר״ ו״בנייה
      מחדש״. הן היו שני הדברים הראשונים במסך שכל נושאו הוא הלוח.
- [x] **שורת פריט וכרטיס הזמנה** — היו בנויים (`DayTimeline` בגרסת `full`).
      נמדד: אריח בקצה ההתחלה, שעה ככותרת-על, צ׳ברון בקצה הסיום.

### הבורר ״ציר שעות / רשימה״ נמחק

שלוש נימוקים, ולא אחד: העיצוב מצייר **מצגת אחת למסך** (חוק 04), הגרסה ״רשימה״
הייתה המקום האחרון באפליקציה עם **אייקון מחיקה יושב במנוחה בשורה** (חוק 05 אוסר
זאת במפורש), ו-`DayTimeline` בגרסת `full` עושה עכשיו בדיוק את מה שהרשימה עשתה.
המחיקה עצמה שרדה — היא בדיאלוג שהצ׳ברון פותח.

### שני באגים שנמצאו במדידה

1. **יום ריק בלי לינה שבר את רצף העיר.** `cityOfDay` נבנה מהפריטים ואז מהלינה,
   ויום שאין לו אף אחד מהם נשמט — כך שריצה של חמישה ימים בטוקיו נחתכה לשתיים
   והחלונית דיווחה **3 לילות במקום 4.** עכשיו יום כזה נושא את העיר של היום הקודם
   קדימה: **לא מתקרנפים באמצע הטיול.** היום היחיד שזה טועה בו הוא יום המעבר, ובו
   כמעט תמיד יש את הרכבת או הטיסה, שנושאות את שם העיר החדשה בעצמן.
2. **הלילות נספרו לפי מספר הימים שנרשמו** ולא לפי הראשון עד האחרון. לילה הוא מעבר
   בין שני ימים, ולכן תחנה מיום 6 עד יום 9 היא שלושה לילות — בין אם יום 8 היה ריק
   ובין אם לא.

`weekdayAfterDayNumber` נהפך לציבורי בתוך המודול: הכותרת שמה את היום ליד ״יום 3״
ולא בתוך תווית אחת, ובלעדיו היא הדפיסה ״יום 6 · קיוטו״ מעל ״יום ג׳, 29.09״.

### מה נמדד

| רוחב | רצועה | לוח החודש | טור ראשי | חלונית | גלישה |
| --- | --- | --- | --- | --- | --- |
| 1920 | 14 גלולות, ה-26 נבחרת | שני לוחות (ספט׳ + אוק׳) | 660 | 372 | אין |
| 768 | כן | מוסתר | 705 (ממלא) | בזרימה | אין |
| 375 | כן, נגללת | מוסתר | 343 | בזרימה | אפס אלמנטים חורגים |

**אריתמטיקת הלוח נבדקה על מעבר חודש:** 1.9.2026 נופל על ג׳ (2 תאים ריקים לפניו),
1.10.2026 על ה׳ (4). המסלול אחרי התיקון: טוקיו 4 לילות 24.9–28.9, קיוטו 3,
אוסקה 2, נארה 1 — עשרה לילות בערים ושלושה לילות מעבר, סך 13 = 14 ימים פחות אחד.
בחירת יום נבדקה בלחיצה: שורת ״קיוטו״ → יום 6, תא 12 בלוח → יום 12 עם באנר
״היום הזה פנוי״. `app-frame` ו-`day-pager` בלי רגרסיה.

53 סצנות `/preview` מחזירות 200. `type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד

- **המסך האמיתי מול דאטהבייס.** נבדק דרך `Itinerary` בהרמס עם פיקסצ׳ר של 14 ימים.
- **״בנייה מחדש״.** הכפתור עבר מקום; הקריאה ל-`/api/ai/itinerary` לא נבדקה מההרמס
  (אין סשן).
- **`EditEntryDialog` מהצ׳ברון.** נשאר כשהיה, לא נפתח בבדיקה הזאת.

### T3 — מה עושים? ✅

- [x] **רשת קטגוריות בשלוש עמודות, עם אריח מגוון לכל קטגוריה.** הרשת הייתה
      `2/3/4/6` — וב-xl זה הכניס שש אריחים לשורה אחת בגובה 110px מעל מסך ריק,
      כלומר סרגל כלים ולא הצעד הראשון של הטאב. שלוש עמודות בכל רוחב.
- [x] **הטון צובע את הריבוע של האייקון, לא את האריח.** האריחים היו פעם מלאים
      בפסטל מקצה לקצה, וזה בוטל בצדק — שש בלוקים רוויים במסך הפתיחה, והצבע לא
      אמר כלום. התשובה של העיצוב היא האמצע: כרטיס לבן עם ריבוע 44px מגוון בתוכו,
      אותו טיפול שכל גליף דומיין באפליקציה מקבל כבר.
- [x] **״נבחרו לטיול״ כשורות עם וי ירוק** — כרטיס אחד, שורה לפריט.
- [x] **הסדר: רשת → הכרטיס המואר → מה שנבחר.** נמדד בטלפון: בדיוק שלושת אלה,
      בזה הסדר.

### שלוש הסרות, כל אחת עם נימוק

1. **ה-X האדום בכל שורה של ״נבחרו לטיול״ ירד.** הווי הירוק **הוא** הבקרה: ריחוף
   הופך אותו ל-X, כלומר ״נבחר — לחצו לביטול״. זה מה שזה באמת — טוגל של בוליאני
   אחד, לא מחיקה — וזה מציית לחוק 05 במקום להפר אותו. (אותו אייקון ירד מרשימת
   הלו״ז ב-T2.)
2. **קישור Google Maps בכל שורה ירד.** המקומות ברשימה הזאת הם הסיכות במפה שלידה
   ובטאב ״מפה״. קישור חיצוני בכל שורה של רשימה מקוצרת הוא בדיוק מה שהעיצוב מקלף.
3. **הקיבוץ לפי עיר ירד**, והעיר עברה לשורת המשנה. בחלונית של 372px אין עמודה
   שנייה לתת לגריד של ערים, והעיר היא עובדה אחת על מקום — לא הדרך אליו.

### שני שינויי מקום

- **״גילוי יעדים״ הוא עכשיו האלמנט המואר של המסך** (`AuraPanel`). חוק 03 מבקש
  אלמנט מואר אחד למסך, ובעיצוב זה ההצעה של ה-AI. הרשת מעליו לבנה על אפור, וכרטיסי
  הערים שהוא מייצר לבנים על אפור. רק הבקשה זוהרת.
- **הטופס הידני עבר לתחתית.** הוא ישב מתחת לחיפוש כדי שלטיול בלי ערים תהיה דרך
  קדימה — אבל הדרך קדימה לטיול כזה היא הגילוי שמעל, לא הקלדה ידנית של מקום לתוך
  עיר שעוד לא קיימת. פתח מילוט שייך לתחתית.
- **חלונית המפה מוסתרת מתחת ל-xl.** זה הרוחב שבו החלונית עצמה מופיעה; מתחתיו
  תוכן החלונית נופל לזרימה, ומפה שם הייתה יושבת בין ההצעה של ה-AI לבין מה שנבחר —
  בזמן שטאב ״מפה״ נמצא בהקשה אחת ועושה את אותה עבודה במסך מלא.

### `ExploreScreen` — הרכבה שיצאה מהעמוד

אותו טעם כמו `TodayBefore`: ההרמס לא יכול לרנדר את העמוד, ולכן הרכבה שנשארת בו
היא הרכבה שאף סצנה לא בודקת. המפה מועברת כ-`ReactNode` — היא גאוקודדת, והעמוד
מחזיק את הגבול הזה.

### מה נמדד

| רוחב | רשת | סדר | טור | חלונית |
| --- | --- | --- | --- | --- |
| 1920 | 3 × 213px, שש טונים שונים | רשת → מואר → (חלונית) | 660 | 372 |
| 1280 | 3 עמודות | כנ״ל, המפה מופיעה | 557 | 372 |
| 768 | 3 × 228px | רשת → מואר → נבחרו | 705 | בזרימה |
| 375 | 3 × 108px | רשת → מואר → נבחרו | 343 | בזרימה |

שש הקטגוריות מקבלות שישה טונים שונים (rose, amber, lilac, mint, sky, peach).
ביטול בחירה נבדק בריחוף: הווי מתחלף ל-X. אפס אלמנטים חורגים בכל הרוחבים.
55 סצנות `/preview` מחזירות 200. `type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד ומה נשאר פתוח

- **שדה חיפוש חופשי מעל הרשת.** במוקאפ יש ״חפשו מקום, שכונה או אטרקציה״ מעל
  הרשת. בקוד החיפוש חי **בתוך** קטגוריה (״חיפוש בתוך מסעדות״), כי Overpass דורש
  פילטר קטגוריה. חיפוש חוצה-קטגוריות הוא פיצ׳ר חדש, לא תיקון פריסה — לא נבנה,
  וזה הפער הגלוי היחיד מול המוקאפ במסך הזה.
- **שתי פאנלים מוארים באותו מסך, תיאורטית.** הפאנל של ״הצעות באזור״ בתוך תצוגת
  קטגוריה נשאר מואר, ומתחתיו ״גילוי יעדים״ מואר גם. הם מסך שלם זה מזה בגלילה,
  אבל זו הפרה אפשרית של חוק 03 ששווה מבט.
- **הקריאות ל-AI ול-Overpass.** אין סשן בהרמס.

### T4 — מפה ✅

המשימה הזאת הייתה **מדידה, לא ציור מחדש** — המפה במסך מלא, הצ׳יפים הצפים והגיליון
כבר היו בנויים. מה שהתבקש: לוודא שהמפה ממלאת את הגובה בתוך המסגרת החדשה של T0.
היא לא מילאה, ובדרך נמצאה רגרסיה שלא הייתה במסך הזה בלבד.

### רגרסיה מ-T0: 16px של כלום מעל כל מסך

עוטף ה-`nav` ב-`AppShell` נשא `pt-4`, ורכיב ניווט חופשי לא לרנדר כלום בזרימה —
הסרגל בטלפון `fixed` ושורת הגלולות `hidden` מחוץ ל-md–lg. כלומר בכל רוחב חוץ
מאחד, העוטף היה **16px של חלל מת מעל כל מסך.** נמדד במסך המפה ב-375: הכותרת
נגמרה ב-56 והמפה התחילה ב-**72**, כלומר רצועה אפורה מתחת לסרגל האפליקציה במסך
שכל עניינו לגעת בו. הריפוד עבר לשורת הגלולות עצמה, שהיא הדבר היחיד שם שאי פעם
נמצא בזרימה. אחרי התיקון: המפה, וגם פס האור בכל שאר הטאבים, מתחילים ב-56 בדיוק.

### גובה המפה — שלושה מספרים, שניים מהם היו שגויים

| רוחב | נגרע | למה | מה היה |
| --- | --- | --- | --- |
| ‎< md | 3.5rem | סרגל האפליקציה, וזה הכול | היה נכון |
| md–lg | 7.375rem | + שורת הגלולות (2.875rem) וה-1rem שמעליה | **חרג מהחלון ב-62px** — בדיוק אותה שורה |
| ‎≥ lg | 4.75rem | סרגל + 1.25rem הריפוד של טור התוכן | היה 14rem, כלומר **נגמר 148px לפני התחתית** |

ה-14rem כוילו מול המסגרת ש-T0 החליפה. חלונית התחנות קיבלה את אותו 4.75rem
ב-`sticky` וב-`max-h`, כך שהקצה העליון שלה ושל המפה מיושרים ושתיהן נגמרות
בתחתית החלון. גם ה-skeleton של הטעינה עודכן.

### מה נמדד

| רוחב | מפה | חלונית | חריגה |
| --- | --- | --- | --- |
| 375 | 56→812, רוחב מלא, נוגעת בשלושת הקצוות | גיליון תחתון | 0 |
| 768 | 118→900 (מתחת לגלולות), רוחב מלא | גיליון תחתון | 0 |
| 1280 | 76→900 | 76→900, 372px | 0 |
| 1920 | 76→1000, 784px | 76→1000, 372px | 0 |

`spaceBelowMap` הוא 0 בכל רוחב שיש בו חלונית. 56 סצנות `/preview` מחזירות 200.
`type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד

- **ה-20px בין הכותרת למפה מ-lg.** זה `pt-5` של טור התוכן, והמפה שם היא כרטיס
  מעוגל עם מסגרת וצל — כרטיס שנוגע בסרגל בפינה מעוגלת נראה שבור. השארתי בכוונה;
  זו לא ״רצועה סביב המפה״ אלא המרווח של הכרטיס.
- **2.875rem כגובה שורת הגלולות** הוא מספר מדוד ולא נגזר. הוא נובע מסקאלת הטיפוגרפיה
  (`text-sm` בתוך `py-1.5` ועוד `p-1`), ואם הסקאלה תזוז הוא יזוז איתה.
- **אריחי Leaflet על מכשיר אמיתי.** נטענו בהרמס; לא נבדק ברשת אטית.

### T5 — עוד ✅

הקריטריון: **שבע שורות ברשימה, וכרטיס נפרד בתחתית.** שתי שורות היו חסרות לגמרי,
ואחת הייתה צריכה לצאת מהרשימה.

- [x] **מדריכי הערים.** המדריכים עצמם היו קיימים ב-`/trips/[id]/city/[city]`
      והיו נגישים **רק** מכרטיס בתוך פאנל הגילוי בטאב אחר. כלומר לטיול עם ארבע
      ערים היו ארבעה מדריכים ואף רשימה שלהם: אחרי שגוללים מעל פאנל הגילוי, הדרך
      חזרה הייתה לזכור את ה-URL. נוסף `/more/guides` עם `CityGuideList`.
- [x] **מי בא איתנו.** רשימת החברים הייתה קבורה תחת כותרת שחלקה עם הקישור
      הפומבי. פוצל ל-`/more/members`, ו-`/more/share` נשאר עם הקישור בלבד. הפיצול
      הוא לא קוסמטי: אלה שניים משלושת מכניזמי הגישה בטבלה של `ARCHITECTURE.md`,
      והם **שונים זה מזה בכוונה** — חשבון מזומן רואה את הטיול לא מצונזר, וקישור
      לא נחוש מראה עותק מצונזר. כותרת אחת מעל שניהם היא מה שגרם להם להיקרא
      כפיצ׳ר אחד.
- [x] **״פרטי הטיול״ יצא מהרשימה לכרטיס השני,** ומתחתיו שורת המחיקה בצבע הסכנה.
      המחיקה הייתה בתחתית `/more/trip`, מתחת לסיכום ההוצאות — נכון ברוח, ושני
      מסכים עמוק בפועל. עכשיו היא השורה האחרונה של הכרטיס האחרון, עם דיאלוג
      שמונה את כל מה שנמחק איתה. חוק 05 מבקש בדיוק את זה: פעולה הרסנית לא יושבת
      במנוחה בין שבע שורות שלוחצים עליהן כל יום.
- [x] **מצב חי בשורת המשנה** נשאר, וגדל: שמות הערים בשורת המדריכים, ומספר
      המטיילים בשורת ״מי בא איתנו״. מצב ההזמנות רוכב על שורת ״פרטי הטיול״ — הורדה
      שלה מהרשימה לא הייתה מותרת לאבד את האיתות הזה.

### `MoreMenu` — עוד הרכבה שיצאה מהעמוד

אותו טעם כמו `TodayBefore` ו-`ExploreScreen`. מה ש-T5 צריך להוכיח הוא **ספירה של
שורות וכרטיס**, וזה בדיוק מה שההרמס לא יכול לבדוק כשההרכבה יושבת בעמוד שקורא
מהדאטהבייס.

### באג RTL שנמצא במדידה: `truncate` חותך את **תחילת** המחרוזת

שם עיר לא-שביר ב-`truncate` בתוך `direction: rtl` נחתך בהתחלה, והשלוש-נקודות
יוצאות מהמסך — נמדד ב-375, השם רונדר כמילה שמתחילה באמצע עצמה. עבר ל-
`line-clamp-2 wrap-anywhere`, שזו כבר הקונבנציה של `TripAuraBand` ו-
`SectionHeading` לטקסט שהמשתמש כתב. **`block` לא יכול לשבת יחד עם `line-clamp`:**
הוא דורס את ה-`display:-webkit-box` שהקלאמפ צריך, והקלאמפ פשוט לא חל — נמדד שלוש
שורות במקום שתיים עד שהוסר. תוקן גם ב-`SelectedList` (T3), שהיה באותה תקלה.

### מה נמדד

| רוחב | שורות | כרטיסים | רוחב כרטיס | גלישה |
| --- | --- | --- | --- | --- |
| 1920 | 7 | 2 | 672 באזור תוכן של 1244, בקצה ההתחלה | אין |
| 768 | 7 | 2 | 672 | אין |
| 375 | 7 | 2 | 343 | אין, אפס אלמנטים חורגים |

שורת המחיקה נמדדה: טקסט ב-`danger-ink`, אריח ב-`danger-tint`, רדיוס 12px כמו שבע
השורות מעליה, ריפוד 14/16 זהה, ובלי מסגרת משלה. 58 סצנות `/preview` מחזירות 200.
`build` מייצר את שלושת המסלולים החדשים.

### מה לא נמדד

- **שלושת העמודים החדשים/המפוצלים** (`/more/guides`, `/more/members`,
  `/more/share`) — מאחורי התחברות. נבדקו רק דרך `CityGuideList` בהרמס ודרך
  ה-build.
- **הדיאלוג של המחיקה** נפתח בהרמס אבל `deleteTrip` לא נקרא (אין סשן).
- **״איך זה עובד״ נשאר ברשימה** למרות שהוא לא במוקאפ. הסרתו הייתה מאבדת את
  המדריך המלא, והמסמך לא ביקש.

### T6 — בית ✅

- [x] **הרַיל קיים גם כאן.** ל-`/profile` לא היה רַיל בכלל, כלומר **המסך שכולם
      מתחילים בו היה המסך היחיד שלא נראה כמו האפליקציה** — והמעבר ממנו לתוך טיול
      הזיז את כל המסגרת 248px הצידה. נוסף `HomeRail`.
- [x] **הרַיל נושא את האור של הטיול הקרוב.** זו הקריאה של העיצוב והיא הנכונה:
      המסך הזה עוסק בעיקר בטיול הזה, והרַיל הוא החלק מהמסגרת שימשיך להראות את
      אותו צבע אחרי שעברת אליו. גם הצבע לא קופץ, לא רק הגיאומטריה.
- [x] **גיבור-פס ורשימת טיולים** — היו בנויים.

### שני פריטים בַרַיל, וזה כנות ולא דלות

העיצוב מצייר שלושה — ״הטיולים שלי״, ״טיול חדש״ ו״הפרופיל שלי״ — אבל **״טיול
חדש״ הוא דיאלוג ולא מסלול**, ולכן הוא יושב בראש הרַיל, במשבצת שבַרַיל של טיול
מחזיקה את המחליף. ו**אין מסך חשבון** שיהיה השלישי: היציאה מהחשבון בסרגל
העליון, שם היא נגישה גם בטלפון. הפריט השני הוא הטיול הקרוב עצמו — היעד הסביר
ביותר מהמסך הזה, וזה מה שגורם לרַיל להיקרא כניווט ולא כתווית עם שורה אחת.

### שני דברים שנשברו ותוקנו באותו קומיט

1. **MYTRIP הופיע פעמיים.** `AuraHero` מרנדר שורת זהות עם הלוגו והאווטאר, וכך
   גם `SideNav`. לרגע היו על אותו מסך שני לוגואים ושני אווטארים במרחק מטר. שורת
   הזהות של הגיבור מוסתרת מ-`lg` — מתחת ל-lg אין רַיל, ואז היא המקום היחיד שבו
   האפליקציה אומרת את שמה.
2. **הכפתור בַרַיל היה לבן ליד פריט לבן.** לבן שמור לפריט הנבחר היחיד; שני
   בלוקים לבנים בראש הרַיל ואף אחד לא אומר מי המצב. הכפתור עבר לזכוכית, אותו
   משטח שהמחליף משתמש בו במשבצת שמתחתיו.

### הסקלטון של הטעינה תוקן גם הוא

`profile/loading.tsx` נשאר טור בודד של `max-w-6xl` בלי רַיל, כלומר **המסגרת זזה
248px הצידה ברגע שהעמוד האמיתי מגיע** — בדיוק הבאג שהקובץ הזה קיים כדי למנוע,
וההערה בראשו אומרת את זה. עודכן למסגרת החדשה, עם בלוק כהה במקום הרַיל (שימר
כהה שם היה נחשה גרועה יותר מעמודה כהה).

### מה נמדד

| מסך | רַיל | אזור התוכן | לוגואים |
| --- | --- | --- | --- |
| `home-frame` ב-1920 | 1672→1920, 248 | 214→1458 | 1 |
| `app-frame` ב-1920 | 1672→1920, 248 | 214→1458 | 1 |
| `home-frame-empty` ב-1920 | 1672→1920, 248 | — | 1 |
| `home-frame` ב-375 | מוסתר | — | 1 (של הגיבור) |

**שתי השורות הראשונות זהות — זה כל הקריטריון של T6.** אפס אלמנטים חורגים ב-375.
60 סצנות `/preview` מחזירות 200. `type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד

- **`/profile` האמיתי מול דאטהבייס.** נבדק דרך שתי סצנות הרמס.
- **החלונית שהמוקאפ מצייר לבית** — ״מה מתקרב״ ו״עדיין פתוח״ לצד רשימת הטיולים.
  היא לא ברשימת השינויים של T6, והתוכן שלה כבר קיים כרכיבים (`UpNext`,
  `OpenItems`) — כלומר זו הוספה של פריסה, לא של פיצ׳ר. נשארה פתוחה.
- **דיאלוג ״טיול חדש״ מהרַיל.** נפתח, אבל `createTrip` לא נקרא (אין סשן).

### T7 — שאר המסכים ✅

אין למסכים האלה מוקאפ, ולכן הבדיקה היא **מול שבעת הכללים** והקריטריון הוא ״אף מסך
לא נפתח אחרת משאר האפליקציה״. הסבר, כלל אחרי כלל:

| כלל | תוצאה |
| --- | --- |
| 01 — האור הוא הזהות היחידה; אין תמונות | ✅ אין `<Image>` ואין `next/image` בכל הקוד; `getPlaceImage` נותר רק בשתי הערות |
| 02 — כחול = ״לחץ כאן״, מצב נצבע בדיו | ✅ הפריט הנבחר בַרַיל, ברצועת הימים ובלוח החודש — כולם דיו |
| 03 — אלמנט מואר אחד למסך | ⚠️ **הופר בטאב ״מה עושים?״** — תוקן, ראו למטה |
| 04 — יום הוא רשימה | ✅ T2 |
| 05 — פעולות הרסניות לא במנוחה | ⚠️ **שתי הפרות ממשיות** — אחת תוקנה, אחת נשארה מודעת |
| 06 — אייקוני קו, אף פעם אימוג׳י | ✅ סריקה על כל `src` לא מצאה אף אימוג׳י |
| 07 — הדסקטופ מראה מה שהטלפון מסתיר | ✅ T1–T4 |

### הפער הגדול: מסך מדריך העיר נפתח בלי רַיל

`/trips/[id]/city/[city]` יושב **מחוץ** לקבוצת `(tabs)`, ולכן לא קיבל את הרַיל
שהלייאאוט מספק. כלומר המעבר מ״עוד → מדריכי הערים״ לתוך מדריך הזיז את כל המסגרת
248px הצידה — **אותו פגם בדיוק ש-T6 תיקן בבית, רמה אחת למטה.** נוסף לו
`TripSideNav` עם אור הטיול, ובלי טאב נבחר: המסך הזה נמצא מתחת לכל החמישה, והרַיל
אומר את זה בכך שאף אחד מהם לא דולק. גם הסקלטון של הטעינה עודכן, שאותה קפיצה לא
תחזור דרכו. הקישור חזרה מצביע עכשיו על אינדקס המדריכים שנוצר ב-T5.

### חוק 03: שני פאנלים מוארים בטאב אחד

הפאנל של ״הצעות באזור״ ב-`place-search` היה `AuraPanel` שההערה שלו קראה לו ״האלמנט
המואר היחיד במסך הגילוי״ — והוא לא היה: פאנל הגילוי שמתחתיו באותו טאב מואר גם, וזה
נמצא שתי רמות בתוך זרימה (פותחים קטגוריה, מקלידים אזור), כך ששניהם יכולים להיות על
המסך יחד. הוא הורד למשטח שקט עם ניצוץ ואריח בגוון הפעולה — זה כל מה שהאור נשא שם.
**נמדד בטאב ״מה עושים?״ ב-1920: שלושה משטחים מוארים, מתוכם שניים כרום (רַיל ופס)
ואחד תוכן.**

### חוק 05: שתי הפרות, אחת תוקנה

**תוקן: הסרת אדם מטיול וביטול הזמנה קרו בהקשה אחת, בלי שום אישור.** הראשונה שוללת
ממישהו גישה, השנייה מבטלת טוקן שאולי כבר יושב לו בתיבה. שתיהן עברו לדיאלוג
שאומר מה בדיוק נגמר — ״הם יאבדו את הגישה״, ״הקישור שנשלח יפסיק לעבוד״ — ולא
״האם אתם בטוחים?״, שזו הגרסה שאנשים לומדים לסגור בלי לקרוא.

**נשאר מודע: אייקוני ה-X בשורות ההזמנות והציוד.** העיצוב מבקש החלקה ולחיצה ארוכה
במקומם, וזה **פרימיטיב אינטראקציה חדש** ולא תיקון פריסה — הסתרה מאחורי ריחוף
לבד הייתה משאירה מסכי מגע בלי דרך למחוק בכלל. בהזמנות יש כבר דיאלוג אישור, ובציוד
הפריט הוא מילה שמקלידים מחדש בשלוש שניות. שווה משימה משלו.

### מה נמדד

- **אימוג׳י:** סריקה על טווחי ה-Unicode של אימוג׳י בכל `src` — אפס.
- **פתיחת מסך:** כל שמונה המסכים תחת ״עוד״ נפתחים ב-`MoreBackLink` ואז כותרת
  `level="page"`. ב-`phrases` וב-`chat` הכותרת יושבת **בתוך** הרכיב, ליד הפעולות
  ששייכות לה — זה מכוון ומתועד, ולא חוסר.
- **דיאלוג האישור** נבדק בלחיצה בהרמס: נפתח עם הכותרת הנכונה ועם ״ביטול״/״הסרה״.
- 60 סצנות `/preview` מחזירות 200. `type-check`, `lint` ו-`build` עוברים.

### מה לא נמדד

- **מסך העיר האמיתי.** הוא מאחורי התחברות ו-`CityGuide` יורד ל-AI בטעינה, אז אין
  לו סצנה. המסגרת שלו זהה מבחינה מבנית ל-`app-frame` (אותו `AppShell`, אותו
  `TripSideNav`) והבילד מטיפס אותה — אבל היא לא רונדרה.
- **הפאנל השקט של ״הצעות באזור״.** הוא נמצא שתי רמות בתוך זרימה שדורשת חיפוש
  Overpass, ואי אפשר להגיע אליו מההרמס.

## 🧷 פאזה M — שש הפערים שנשארו אחרי T7

T0–T7 הביאו את המסגרת ואת שמונה המסכים למוקאפ. השוואה חוזרת מול שלושת מסמכי
העיצוב — לא מול הסיכומים שלי — מצאה שישה פערים שנשארו. כאן הם נסגרים, כולל
ברמת הפרודקט: קומפוננטות חדשות, מיגרציה, ולוגיקה שלא הייתה.

### מה נסגר

- [x] **חלוניות ההקשר** שהמוקאפ מצייר בטאב ״מה עושים?״ — תחנות היום, תחזית,
      ״דורש תשומת לב״, ״מה קרוב״ והוצאות.
- [x] **מדריך העיר** נפתח באור של הטיול, והסקציות שלו מאחורי גלולות במקום גלילה
      אחת ארוכה.
- [x] **ארכיון טיולים** — `trip_status` קיבל ערך `archived` (מיגרציה 0020),
      `ArchiveTripButton` לשני הכיוונים, ו-`/profile` מפריד פעילים מארכיון.
- [x] **חיפוש חופשי ב-Explore** — לפני זה אפשר היה להגיע למקום רק דרך קטגוריה,
      והמוקאפ מצייר שדה חופשי מעל הרשת.
- [x] **מחיקה בהחלקה ולחיצה ארוכה** — חוק 05, הפער היחיד שדרש פרימיטיב חדש.
- [x] **`/preview` גדל ל-64 סצנות**, כולל סצנה לפרימיטיב עצמו.

### `SwipeAction` — למה זה יצא פרימיטיב ולא מחיקה בשני מקומות

העיצוב אומר את זה בלי לרכך: ״פעולות הרסניות לא יושבות במנוחה... לא כאייקון בשורה
שגוללים לידה״, והתשובה שלו היא החלקה ולחיצה ארוכה. **התשובה הזאת שלמה רק לאצבע.**
עכבר לא מחליק ומקלדת לא לוחצת לחיצה ארוכה, ולכן החלוקה היא:

- **הפרימיטיב** אחראי על האצבע: גרירה של השורה לכיוון תחילת השורה מעל 72px, או
  החזקה של 550ms בלי תזוזה. מתחת לשורה נחשף פאנל אדום — `aria-hidden`, לא כפתור.
- **הקורא** אחראי על השאר: כפתור אמיתי בתוך השורה, שקוף במנוחה ונחשף ב-
  `group-hover/swipe` או ב-`focus-visible` שלו. רשימת ה-`className` הזאת מיוצאת
  כ-`REVEALED_ACTION`, כדי שלא תיכתב פעם אחת נכון ופעם אחת לא.

**שלושת הבאגים שנמצאו במדידה, לא בקריאה:**

1. **הגרסה הראשונה חשפה לכיוון ההפוך.** היא הזיזה את השורה ב-`translateX` עם
   סימן קבוע — נכון ב-LTR, ובמסמך RTL היא מזיזה את השורה **מעל** הפאנל ולא ממנו.
   התיקון: `inset-inline-end` במקום `transform` (כיוון לוגי, אפס סימנים לנחש),
   וכיוון הגרירה נקרא מהאלמנט עצמו ב-`pointerdown`.
2. **חשיפה בריחוף שמחליקה את השורה הסתירה את הצ׳קבוקס.** זה נמדד ברשימת הציוד:
   השורה מחליקה לכיוון תחילת השורה, העוטף חותך, והצ׳קבוקס — הפעולה הראשית של
   הרשימה כולה — נעלם ברגע שהמצביע נגע בשורה. **גרירה מותר לה לחתוך את תחילת
   השורה, כי זה מה שהאצבע ביקשה. ריחוף לא.** מכאן החלוקה שלמעלה.
3. **`h-full` לא עבר דרך העוטף.** בגריד ההזמנות העוטף נמתח לגובה השורה והכרטיס
   שבתוכו מדד 100% מ-div בגובה אוטומטי — 218px מול 196px, ושני כרטיסים באותה
   שורה הפסיקו להיות באותו גובה.

בנוסף: `hasPointerCapture` כתנאי לכל `pointermove` הוחלף ב-ref. אם ה-capture
נכשל — וב-pointer סינתטי הוא נכשל — כל תנועה נזרקת והשורה פשוט מסרבת לזוז.
ה-capture נשאר, כשיפור ולא כתנאי.

### מה נמדד

- **התנהגות המחווה**, מול הרכיב האמיתי: השורה עוקבת אחרי האצבע (50px גרירה →
  50px היסט), נעצרת ב-96px, **מתעלמת מגרירה לכיוון ההפוך** (0px), מתחייבת
  בשחרור מעל 72px, מתחייבת בהחזקה של 700ms, סופרת החזקה עם רעידה של 4px כהחזקה,
  ומבטלת אותה בגרירה של 20px. חוזרת ל-0 ב-150ms.
- **במנוחה אין אדום.** נמדד ב-375, ב-768 וב-1280 גם על `gear` וגם על
  `booking-list`: אטימות הפאנל 0, אטימות הכפתור 0, ואפס גלילה אופקית.
- **מיקוד מקלדת** חושף את הכפתור (אטימות 1) ומשאיר את השורה במקומה; צילום מסך
  מראה שורה אחת עם X ממוסגר וכל השאר נקיות.
- **גובה כרטיסי ההזמנה** אחרי התיקון: עוטף = תוכן = כרטיס בכל שלושת הרוחבים.
- `type-check`, `lint` ו-`build` עוברים; כל סצנות `/preview` מחזירות 200.

### מה לא נמדד

- **אצבע אמיתית.** ההרמס לא מייצר `pointerType: "touch"` אמיתי, ו-React מתעלם
  מ-PointerEvent סינתטי. המחווה נמדדה בהפעלה ישירה של ה-handlers של הרכיב —
  אותה לוגיקה, בלי שכבת האירועים של React. גלילה אנכית עם אצבע על שורה
  (`touch-pan-y`) **לא נבדקה בכלל** ומחייבת מכשיר.
- **`prefers-reduced-motion`.** ההחלקה חוזרת ב-150ms; לא נכתב לה גידור.
- **מחיקת פריט ציוד היא חד-כיוונית.** בהזמנות יש דיאלוג אישור, ובציוד אין — לחיצה
  ארוכה מוחקת מיד. זו החלטה: הפריט הוא מילה שמקלידים מחדש בשדה שיושב מעל הרשימה.
  אין Undo, וזה נרשם כאן ולא הוסתר.
